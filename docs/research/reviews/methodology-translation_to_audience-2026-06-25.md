# Methodology review — translation_to_audience.md (r1, 2026-06-25)

**Reviewer mode**: fresh-session peer review. Reviewer is independent of the drafting session. Fresh session — no exposure to the drafting context; doc-only knowledge.
**Target**: [`docs/research/methodology/translation_to_audience.md`](../methodology/translation_to_audience.md) (r1, 2026-06-25)
**Review date**: 2026-06-25
**Standards applied**: [CONVENTIONS](../CONVENTIONS.md) §1, §1.2, §2.1, §2.2, §3, §4; [plan](../methodology/_plan_results_analysis_layer.md) r5 §6.6 spec + §3 stage-map dependency + §3.5 + §3.6 + §3.7 + §3.8 + §3.9 + §3.10 (hard predictive gate — binding) + §3.11 + §3.12 (commentary layer — patient-track-only, binding) + §4 producer/reviewer split (translation row) + §5 output-structure tree (the binding tree that settles output-path ambiguity) + §9 layer anti-patterns + §10.6 + §10.7 deferred questions; [limitations doc](../methodology/research_line_limitations.md) r3 §3 + §5 (translation row binding) + §8 downstream-citation-count; [synthesis-structure map](../methodology/synthesis_structure_map.md) r3 §3 / §4 / §5; [Stage A guide](../methodology/actionability_translation.md) r2 LOCKED (most common upstream source); [Stage S₂ guide](../methodology/external_contextualisation.md) r2 LOCKED; [Stage S₁ guide](../methodology/internal_synthesis.md) r2 LOCKED; [Stage I guide](../methodology/verdict_to_inference.md) r2 LOCKED; [Stage D guide](../methodology/descriptive_precondition_audit.md) r2 LOCKED; [RESEARCH-REPORT](../RESEARCH-REPORT.md) §5.2 PPV-with-base-rate precedent; [hypothesis_lock_process.md](../methodology/hypothesis_lock_process.md); [personal_hypotheses.md](../personal_hypotheses.md) line 32; Daza 2018 / Shamseer-CENT 2015 / Tate-SCRIBE 2016 / Natesan 2023 / WWC 2022 SCED.

---

## 1. Overall verdict

**REVISION RECOMMENDED (mild).** This is guide #6 of six, the final guide in the layer's §11 step 6 arc. The draft is structurally faithful to plan §6.6, implements all eleven declared content sections (§5.1–§5.11, the eleventh present only on the patient-audience track-file when the source is a Stage A `construct-*.md` with §5.9 filled), and the seven inventions beyond §6.6 are — with two operational concerns and two cross-reference observations — methodologically sound and operationally implementable. **The two load-bearing reviewer-concern checks both pass.** The §3.12 commentary patient-audience-ONLY hard separation is preserved at every touchpoint (§1, §3, §4.1 FORBIDDEN + §4.2 ELIGIBLE + §4.5 summary table, §5 outline with §5.11 absent from research-audience track, §5.11 patient-track-only spec with six-element discipline, §7.3 anti-pattern + §7.4 commentary-predictive-wording anti-pattern, §9.6 gate-items 12-17). The §3.10 hard predictive gate propagates without backdoor: Stage T inherits source's tier (§2 closing paragraph), §7.1 forbids tier-upgrading wording, §7.4 forbids commentary-translated-to-predictive, and §9.6 gate-item 19 enforces. The output-path ambiguity resolution (parallel `research-audience/<source-name>.md` + `patient-audience/<source-name>.md` folders) is the **correct** interpretation of locked-plan §5 (the binding tree shows these folders verbatim at lines 647-654). The layperson-test gate is operationally implementable as either-fired-or-explicitly-pending-marked, with the §7.8 anti-pattern as the indefinite-deferral block.

Six findings drive the REVISION RECOMMENDED verdict rather than ACCEPT. Two are required (R1, R2); four are recommended (A1–A4). **None blocks §11 step 6 closure** (the six-guide arc completes on lock); all six are one-revision-cycle fixes that can be absorbed before §11 step 7 (skill build) begins.

- **R1.** **`plain_language_dictionary.md` does not yet exist on disk.** The target cites it at §2 input #5 ("the live dictionary maintained by Stage T") + §4.3 ("dictionary maintains entries in both Dutch and English for every term") + §5.10 ("Dictionary update record") + §6.3 (source-dictionary conflict rule) + §7.10 (methodology-jargon-without-dictionary-entry anti-pattern) + §9.5 (dictionary update protocol) + §9.6 (gate items 3 + 25) as if it were a living artefact already at `docs/research/methodology/plain_language_dictionary.md`. Verified absent (Glob returned no match). Locked plan §4 producer/reviewer split correctly classifies it as "Maintained by `/research-interpret` translate stage; one term added per technical term encountered" — so the dictionary's growth is **expected** to begin at first Stage T artefact draft, not before. But the draft assumes the file already exists as a loadable input (§2 input #5: "Stage T loads the current dictionary"); a first-Stage-T-session would find no file to load and the skill's §9.6 gate-item 3 ("§5.3 plain-language translation uses methodology vocabulary without dictionary entry or inline definition") would fire on every term. The fix is one of: (a) one-sentence addition at §2 input #5 + §9.1 Load phase + §9.5 dictionary protocol naming the first-Stage-T-session's bootstrapping responsibility (create the file with frontmatter + empty term list before appending terms); OR (b) a sibling-task added to §11 step 7 brief that creates the empty dictionary scaffold during skill build; OR (c) a stage-T-first-run special-case at §9.5. Without one of these, §11 step 7 skill dry-run on the first Stage T target will hit a file-missing error on §9.1 Load. Operationally important for the very next step.

- **R2.** **§3 source-stage map-change-needed halt routing produces only the §5.7 `open_inputs` entry, but §5.7's enumeration of refusal-to-proceed paths does not include the §6.6 map-change-needed case as a distinct refusal-path.** The §5.7 refusal-paths #1-#5 are: source missing/unlocked; source Stage A but §5.9 not filled; dictionary missing terms; layperson-test pool not identified; visual summary not produced. The §6.6 map-change-needed halt at §6.6 says "Produce only the §5.7 `open_inputs` entry naming the proposed source-or-map change" — this would be a sixth distinct refusal-path that §5.7 should enumerate for symmetry with the other five, OR §6.6 should explicitly route the entry under refusal-path #1's variant (which §9.2 Gate already does name as "Source-stage map-change-needed surfaces (per §6.6 halt-criteria) → halt; produce only §5.7 entry per refusal-path #1 variant + route-out instructions"). The mismatch between §5.7 (five paths) and §9.2 (refusal-path #1 + a variant) and §6.6 (separate halt criteria producing a §5.7 entry) creates a small inconsistency: §9.6 gate enforcement on map-change-needed cases is not crisp because §5.7 does not name the case. One-sentence fix at §5.7 (sixth refusal-path: "Source-stage map-change-needed per §6.6 → halt; entry: proposed source-or-map change → Stage T blocked → route to separate producer-mode source/map revision per §6.6 + §3.7 → fallback 'none'") closes the loop with §6.6 + §9.2 + §9.6.

Recommended:

- **A1.** Length discipline. **1644 lines lands 44 over the 1600 upper bound (2.75% over).** The agent's self-report names this within the brief's 44-line over-allowance, but the reviewer assesses §5.5 (L-ID rendering, with both worked examples at full breadth) and §5.11 (commentary translation, with both K-stress-fatigue-monitoring + K-bout-recovery-signal worked examples) as the most compressible surfaces. The §5.5 patient-track worked example renders all seven L-IDs as grouped Dutch prose; this is excellent operational guidance for the first Stage T dry-run, but a fresh Stage T drafter on a second source would benefit more from a compact rule + diff-from-first-example pattern than from two full worked examples per construct. Realistic compression: 30-50 lines (~2-3%) without losing operational content. Not blocking; the §6.6 brief explicitly granted the 44-line over-allowance, and the worked examples carry the highest pedagogical value for the dry-run-at-§11-step-9 future drafter.

- **A2.** **§5.10 cross-references include "locked plan (§3.5; §3.7; §3.8; §3.9; §3.10; §3.11; §3.12; §4; §6.6)" but omit §3.6 (map conflict-resolution at Stage T) and §5 (output-structure tree).** §3.6 is cited at §6.6 conflict rule + §10 cross-references, but the §5.10 section-template line for cross-references should include §3.6 for completeness (§6.6 Stage T halt-criteria is the §3.6-at-Stage-T operationalisation; a fresh drafter scanning §5.10 alone would miss the §3.6 binding). Similarly §5 of the plan (the output-structure tree) is the binding source for the output-path resolution at §3 of the target; §5.10 should cite it explicitly. One-sentence fix: add "§3.6; §5" to the §5.10 plan citation list.

- **A3.** **§9.6 refuse-to-lock gate is the largest gate in the layer (23 items per the agent's self-report; literal count confirms 23 distinct bullet-items).** Spot-checking five-to-seven items for distinctness, implementability, and collective completeness:
  - **Item 1 (source-claim verbatim from source)**: text-comparison check; trivially implementable; distinct from item 2 (audience-specificity). Sound.
  - **Item 7 (quality-measure translation missing when source is Stage A tier-2+)**: presence-check conditional on source-stage + tier; implementable as conditional skill check; distinct from item 8 (bare-percentage without base-rate context). Sound.
  - **Item 9 (layperson-test status block missing on patient-audience track)**: presence-check on §5.7 sub-block; implementable; distinct from item 10 (indefinite deferral — semantic check on the sub-block's content). Sound.
  - **Item 11 (visual missing on patient-audience track without §5.7 visual-pending entry)**: two-part check (visual presence + fallback log presence); implementable as paired presence-checks; distinct from items 9-10 (layperson-test) + item 25 (jargon-without-rendering). Sound.
  - **Item 12 (commentary section present on research-audience track-file)**: presence-check on §5.11 in the wrong track-file; trivially implementable as path-based check; this is the load-bearing §3.12 hard-separation enforcement at lock time. Sound and load-bearing.
  - **Item 14 (commentary section's subject-attribution missing on any sentence)**: per-sentence text-pattern detection on §5.11 against the §3.12 subject-attribution markers ("ik merk op" / "in mijn ervaring" / "I notice" / etc.); implementable as regex + per-sentence iteration; distinct from items 12-13 (commentary track + forbidden wording) + item 16 (layperson-test gate not fired). Sound but harder to fully automate than the presence-checks.
  - **Item 16 (commentary's layperson-test gate not fired per §5.7 layperson-test status block)**: cross-section check (§5.11 presence implies §5.7 layperson-test status block fired or pending); implementable as conditional presence-check. Sound.

  Collectively the 23 items are complete enough to enforce both the §3.12 commentary discipline (items 12-16 + 12-related: research-track-forbidden, forbidden-wording, subject-attribution, attached-to citation, layperson-test gate) and the §3.10 hard predictive gate (items 7, 8, 17, 19 enforce tier-2+ quality measure presence + bare-percentage prohibition + advice-form prohibition + tier-upgrading prohibition) and the L-ID + audience-specificity + base-language + skip-research-internal-explicitness baseline. **No gate item gap found.** A4 below is a structural readability observation, not a gap.

- **A4.** §11 lock log entry is dense — at ~50+ lines of unbroken prose it matches the same A3-class observation flagged in guide #4 r1 and guide #5 r1 reviews. Two-or-three short paragraphs (one for inventions; one for §6.6 ambiguity interpretations; one for the §11 step 7 skill dependency note + the worked-example anchor + the layer-closure note) would scan better without losing content. The "Closes §11 step 6" note at the end is the layer-closure flag; surfacing it more prominently would help a future drafter who is reading the lock log to confirm the layer is at its closure-point.

## 2. Four-input bar findings (CONVENTIONS §2.2)

This MD is producer-mode infrastructure (per §3 of the target, per plan §4 row "Guide MDs (6×)") rather than a methodology MD locking a substantive analytical choice; the four-input bar applies in lighter form. Two of the four inputs apply directly; two apply indirectly.

| Input | Status | Reasoning |
|---|---|---|
| 1. Best-practices standards | PASS | The two-track audience translation discipline traces to the project's plain-language-with-base-rate precedent (RESEARCH-REPORT §5.2 "wrong 24 times out of 25") + N-of-1 reporting standards (CENT 2015 item 22 generalisability for the L1 patient-track plain-language rendering; SCRIBE 2016 for the L4 layperson-test discipline as the analyst-is-subject mitigation reach at the audience level; Natesan 2023 as defensibility bar; Daza 2018 inherited via L-ID propagation; WWC 2022 SCED inherited via S₂'s contextualisation source). The §1 alternatives-considered paragraph cites two rejected paths (single general-audience track; merging Stage T into Stage A) with four-point rejection reasoning consistent with §3.12 separation, layperson-test scoping, and commensurability-across-source-stages. |
| 2. Established literature | PASS | The §2 inputs list cites all relevant anchors with one-sentence operational role: limitations doc r3 §5 binding for translation row; Stage A guide §5.7 quality measures + §5.9 commentary; RESEARCH-REPORT §5.2 PPV-with-base-rate precedent; literature methodology anchors at §10 (Daza / CENT / SCRIBE / Natesan / WWC). The body uses the anchors at §5.5 (L-ID rendering), §5.6 (quality-measure translation), §5.11 (commentary translation), §6.4 (layperson-test gate). Consistent with guides #2-#5 r1 strength on this dimension. |
| 3. Tradeoff vision | PASS | The §1 alternatives-considered paragraph satisfies CONVENTIONS §2.2 item 3 explicitly. Specific judgment-calls visible in the body: (a) two tracks as load-bearing (vs single track — sound; §3.12 separation, layperson-test scoping); (b) §3.12 commentary patient-track-ONLY (vs commentary in both tracks — sound; §3.12 hard separation enforced); (c) layperson-test as binding gate fired-or-explicitly-pending (vs indefinite deferral — sound; §7.8 anti-pattern); (d) base-language deferred to per-artefact with heuristic (vs guide-level default — sound; honors plan §10.6 deferral); (e) layperson-test recruitment pool deferred to per-artefact (vs global pool policy — sound; honors plan §10.7 deferral). The five judgment-calls are all coherent with the locked plan; the §6.6 ambiguity interpretations (output-path, base-language heuristic, recruitment-pool per-artefact) are the three the agent's self-report names, all defensible. |
| 4. Research limitations + objectives | PASS | The §5 worked examples draw from the two active constructs in the map's r3 (K-stress-fatigue-monitoring tier-1; K-bout-recovery-signal tier-2) and a Stage S₂ topic source (topic-stress-fatigue-pacing) + a Stage I source (HA-C4) per the §3 four-source anchor list. §5.5 L-ID worked examples implement the limitations doc r3 §5 row for translation faithfully (per-track rendering: research-audience keeps L-IDs by ID; patient-audience renders in plain-language without naming L-IDs; both render the same applicable-limitations set). The §5.11 commentary worked examples for both K-constructs match the upstream Stage A guide r2 §5.9 worked examples in tier + subject-attribution + permitted-wording discipline. Designed-for-this-corpus is unambiguous. |

Overall: 4 PASS. The four-input compliance is strong. The trajectory across the layer's six guides — guide #1 r1 (2 PASS, 2 PARTIAL) → guides #2 / #3 / #4 / #5 (each 4 PASS) → guide #6 (4 PASS) — shows the drafting-discipline propagation has fully landed and is sustaining at the layer's closure-guide.

## 3. Faithfulness to §6.6 spec

§6.6 has nine listed elements (purpose, inputs, output, 11-section outline, checklist sketch, conflict rules, anti-patterns, interview seeds, agent-instruction outline). Mapping:

| §6.6 element | Implementation | Faithfulness |
|---|---|---|
| Purpose | §1 (purpose block-quote + skill-precondition + what Stage T does NOT do) | **Faithful with extension.** The eight-bullet "What Stage T does NOT do" list (re-test; predict-beyond-source-tier; invent caveats; carry commentary in research-audience track; advise; translate unlocked source; multi-source-per-session; defer layperson-test indefinitely) operationalises §6.6's purpose into anti-pattern preview. The §3.12 hard separation at bullet #4 and the layperson-test bullet #8 are the §3.12-binding-since-§6.6-spec additions; both sound. The §1 alternatives-considered paragraph satisfies CONVENTIONS §2.2 item 3 explicitly. |
| Inputs | §2 (twelve numbered inputs) | **Faithful with extension.** §6.6 names four inputs (source artefact; limitations doc; plain-language dictionary; Stage A tier). §2 expands to twelve by adding source's stage (#2); synthesis-structure map row (#3); audience-definition specifics (#6); layperson-test recruitment context (#7); §3.12 commentary section (#9); methodology MDs (#10); RESEARCH-REPORT §5.2 (#11); CONVENTIONS (#12). All additions sound and binding. The closing "what Stage T does NOT load" paragraph is the right negative-space discipline. R1 above is on input #5's dictionary-existence assumption. |
| Output | §3 (path + flat naming + mode + L-ID discipline + skip-research-internal + worked-example anchors + hard rules) | **Faithful with extension.** The output-path resolution (parallel `research-audience/<source-name>.md` + `patient-audience/<source-name>.md`) is correct — verified against locked-plan §5 output-structure tree (lines 647-654 show `translation/research-audience/` and `translation/patient-audience/` as parallel folders with by-source-name files explicitly). The agent's interpretation of "by-source-name" as flat naming inside each audience-track subfolder matches the plan tree verbatim. The skip-research-internal option is correctly carried from plan §3 dependency rule + §3.8 stopping-criteria row for translation artefacts. The two hard rules at §3 (no L-ID omitted; no new L-ID cited) correctly translate the limitations doc r3 §5 row for translation into binding form. |
| Section outline | §5 (eleven sub-sections, §5.1–§5.11) | **Faithful with extension.** §6.6 names eleven sections (1-11); §5 implements all eleven with §5.11 present only on the patient-audience track-file conditional on source being Stage A with §5.9 filled. The conditional-presence discipline for §5.11 is the load-bearing §3.12-binding (research-audience track-file MUST OMIT §5.11; patient-audience track-file's §5.11 absence is allowed only when source is non-Stage-A or Stage A with §5.9 skipped). The §5.7 layperson-test status block as a sub-section of §5.7 `open_inputs` (vs a separate §-section) is a structural compression decision that preserves the §6.6 spec's "Patient-relevance test (patient-audience track only)" content. Spot-checking the section-by-section against §6.6 spec: §5.1 (source-citation) → §6.6 element 1; §5.2 (audience-definition) → element 2; §5.3 (plain-language translation) → element 4 (compressed: the §6.6 "Narrative arc" element 3 is folded into §5.3 + §5.4 + §5.9; this is a minor structural compression but content-faithful); §5.4 (caveats) → element 6 (honest-uncertainty); §5.5 (L-ID rendering) → element 6 (honest-uncertainty per L-IDs); §5.6 (quality-measure translation) → element 9; §5.7 (`open_inputs` + layperson-test) → elements 7+8; §5.8 (visual summary) → element 5; §5.9 (follow-up communication) → element 10; §5.10 (cross-references) → not in §6.6 spec but matches guides #1-#5 §5/§4 chain-traceability discipline; §5.11 (commentary translation) → element 11. The §6.6 "Narrative arc" element 3 compression into §5.3+§5.4+§5.9 is the one structural deviation; it preserves content but loses the explicit narrative-arc-section framing. A future drafter might benefit from a §5.3 opening note that §5.3+§5.4+§5.9 collectively realise the §6.6 narrative-arc element. |
| Checklist sketch | Folded into §5 sub-sections + §9.6 refuse-to-lock gate | **Faithful.** The §6.6 checklist's seven bullets (audience explicitly defined; dictionary updated; visual present/specified; honest uncertainty matches source tier; patient-relevance test passed; source claim not exceeded/reduced; both tracks or skip-research-internal) are implemented across §5 + §9.6: audience-defined (§5.2 + §9.6 item 2); dictionary updated (§5.10 + §9.5 + §9.6 item 25); visual present/specified (§5.8 + §9.6 item 11); honest-uncertainty (§5.3+§5.4+§5.5 + §9.6 items 17 + 19 + 20); patient-relevance (§5.7 + §9.6 items 9-10 + 16); claim-not-exceeded (§7.1 + §9.6 items 17-21); both-tracks-or-skip (§3 + §9.6 item 22 + §7.12 anti-pattern). All seven land. |
| Conflict rules | §6 (6 rules) | **Faithful with extension.** §6.6 names 4 conflict rules (translation requires stronger claim; CANNOT-SAY rendering; source-dictionary conflict; layperson-test fails). §6 implements all 4 (§6.1, §6.2, §6.3, §6.4) and adds §6.5 (commentary translation conflict with source §5.9 — Stage T does not add commentary content beyond source) and §6.6 (source-stage map-change-needed — Invention 6 per agent's self-report). Both additions sound; §6.5 is the load-bearing addition for the §3.12 source-binding-ceiling discipline at Stage T; §6.6 is the §3.6-at-Stage-T operationalisation parallel to guide #3 §6.1 + guide #4 §6.1 + guide #5 §6.5. |
| Anti-patterns | §7 (15 anti-patterns) | **Faithful with extension.** §6.6 names 7 anti-patterns (tier-upgrading in wording; faux-balanced wording; stripping uncertainty for accessibility; treating dictionary as optional; producing only research-audience track; patronising tone; quoting source unchanged). §7 implements all 7 (§7.1, §7.2, §7.7, §7.10, §7.12, §7.13, §7.11 — order rearranged but content-faithful) and adds 8 more (§7.3 research-track-carrying-commentary; §7.4 commentary-translated-to-predictive-wording; §7.5 translation-framed-as-advice; §7.6 inventing-new-caveats-post-hoc; §7.8 indefinite-layperson-test-deferral; §7.9 audience-definition-generic; §7.14 cross-language-drift; §7.15 re-routing-source-to-different-stage). All 8 additions distinct and sound — §7.3 + §7.4 operationalise the §3.12 hard separations at anti-pattern level; §7.6 operationalises the no-new-caveats discipline (parallel to Stage A §7.6); §7.8 operationalises the §3.12 implementability check at anti-pattern level; §7.9 operationalises the §4.4 specificity binding; §7.14 + §7.15 operationalise the base-language + source-routing disciplines at anti-pattern level. |
| Interview seeds | §8 (3 required + 1 optional) | **Faithful with extension.** §6.6 names 5 seeds (specific audience; most-important-sentence; honest-uncertainty-everyday-vocabulary; PAIS-clinic-conversation; technical-term replacement). §8 compresses to 3 required (§8.1 audience-selection; §8.2 plain-language rendering; §8.3 layperson-test scheduling) + §8.4 optional (commentary-rendering for Stage A sources with §5.9 filled). The compression is structural: §6.6 seeds 1 + 4 → §8.1; seeds 2 + 5 → §8.2; seed 3 → §8.2 closing; §8.3 + §8.4 are new (layperson-test scheduling per §3.12 implementability check + commentary-rendering per §3.12 patient-track-only operationalisation). All §6.6 seed content preserved + two structurally important additions. |
| Agent-instruction outline | §9 (8 phases) | **Faithful with extension.** §6.6 names 5 bullets (Load / Refuse / Refuse-lock-on-dictionary / Walk / Produce / Recommend). §9 expands to 8 phases (Load / Gate / Extract / Interview / Produce / Refuse-to-lock-gate / Review-handoff / Acceptance + drift-trigger registration). The Refuse-to-lock gate (§9.6) enumerates 23 structural checks (agent self-reports twenty-three; literal count confirms 23). Extension correct because the relevant plan rules (§3.7 drift; §3.8 completion; §3.12 implementability check; §4 producer/reviewer split row; §11 step 7 skill dependency) post-date §6.6's sketch. R1 above is on the §9.1 Load phase's dictionary-bootstrapping responsibility. |

No §6.6 element is missing or substantively altered. Spec faithfulness is high — matches guides #2-#5 r1 strength on the §6.6 spec discipline. The two §6.6 spec ambiguities the agent's self-report flags (output-path tree-vs-dispatcher; plain-language base-language per-artefact heuristic; layperson-test recruitment-pool per-artefact judgment) — actually three — are all resolved cleanly and consistent with locked-plan §5 + §10.6 + §10.7 respectively.

## 4. The seven inventions beyond §6.6

### Invention 1 — §4.5 two-track summary table

**Sound.** The §6.6 spec carries the per-track distinction in the section-outline prose but does not name a summary table; §4.5's table extends to four columns (track / audience persona / methodology vocabulary / §3.12 commentary / layperson-test). The columns make the §3.12 hard separation visible at the tier-mapping surface (currently distributed across §4.1 FORBIDDEN + §4.2 ELIGIBLE + §5.11 binding). The closing "discipline cascade" paragraph (research-audience preserves; patient-audience renders; neither patronises nor misleads) is the right closing framing. Parallel structurally to guide #5's §4.4 tier-mapping summary table. Sound and operationally implementable.

### Invention 2 — §5 ten-section outline consolidation

**Sound.** Note: the agent's self-report calls this "ten-section" but the literal count is eleven (§5.1 through §5.11). The actual reduction is from §6.6's eleven-element outline to a structurally-uniform eleven-section outline with §5.11 conditional-on-source-stage-and-§5.9-presence. The structural uniformity is the value-add: every Stage T track-file has §5.1–§5.10; only patient-audience track-files where source is Stage A with §5.9 filled add §5.11. The §6.6 narrative-arc element 3 is folded into §5.3+§5.4+§5.9 — this is content-preserving but loses the explicit narrative-arc-section framing. Per §3 Faithfulness assessment above. Sound; A2 below recommends a minor §5.10 cross-reference addition.

### Invention 3 — §5.7 layperson-test status block

**Sound — this is the load-bearing operationalisation of the §3.12 implementability check at Stage T.** The two valid lock states (Tested entry with date + pool + observed-interpretation + revision-trigger; Layperson-test-pending entry with §10.7 per-artefact pool-identification recorded as `open_inputs` entry #4) cover the operationally distinct lock cases. The hard rule (indefinite deferral without the pending mark is forbidden per §7.8 + §9.6) closes the loop with the §7.8 anti-pattern + the §9.6 gate-item 10 enforcement. **Critical for reviewer concern 5 (layperson-test gate operationalisation):** the gate is fully operationally implementable as either-fired-or-explicitly-pending; the Layperson-test-pending status is a valid lock state, not a block on lock; the §7.8 anti-pattern + §9.6 gate-item 10 enforce the no-indefinite-deferral discipline at lock time. Sound.

### Invention 4 — §5.10 dictionary-update record

**Sound.** §6.6 spec mentions "Plain-language dictionary updated with every technical term used in the source artefact" as a checklist item; §5.10's dictionary-update record turns this into a per-translation audit-trail (term + dictionary path + rationale, recorded at §5.10 + propagated via §9.5 protocol). This makes the dictionary-growth-per-Stage-T-session traceable — without it, the dictionary grows opaquely and a fresh drafter cannot reconstruct which term was added by which translation. Operationally implementable as an append-only sub-section in §5.10. Sound. R1 above is on the dictionary's first-Stage-T-session bootstrapping prerequisite, not on §5.10 itself.

### Invention 5 — §5.11 commentary translation six-element discipline

**Sound — this is the load-bearing §3.12-at-Stage-T operationalisation.** The six elements (attached-to citation preserved; subject-attribution every sentence; permitted-wording-only; forbidden-wording-bound; cannot promote tier; layperson-test gate fires specifically) translate the upstream Stage A guide §5.9 four-required-disciplines + five hard-separations into the patient-track-translation domain. The Dutch + English permitted/forbidden wording lists in §5.11 + §4.2 correctly carry the §3.12 binding across languages (key for the Dutch patient-audience persona that §4.3 defaults to). The §5.11 two worked examples (K-stress-fatigue-monitoring tier-1; K-bout-recovery-signal tier-2) match the upstream Stage A guide r2 §5.9 worked examples in tier + subject-attribution + permitted-wording-only — content-faithful translations rather than re-invented commentary. The layperson-test-fail worked example at the end of §5.11 ("Ah, so I should rest when my Garmin stress is mid-range?" → read as advice → §6.4 revision routing) is the right operational closing for the §3.12 implementability check at the per-paragraph level. Sound and load-bearing.

### Invention 6 — §6.6 source-stage map-change-needed halt rule

**Sound.** The plan §3.6 conflict-resolution rule names the discipline at the layer level; the two concrete halt-criteria at §6.6 (source artefact's stage cell in map is wrong; source's L-ID block contradicts map's L-ID notes column) are the operationally implementable cases at Stage T. Stage T's map-change-needed triggers are rare (most map-revision triggers fire at S₁/S₂/A levels), so the two-criterion set is reasonable rather than four-or-more parallel to guide #5 §6.5's tier-level halt-criteria. The route-out instructions (stop drafting mid-session; do NOT save partial artefact; do NOT edit map or source in-session; produce only §5.7 `open_inputs` entry; hand off to user; resume after separate producer-mode session) match plan §3.6's "halt-and-route" discipline verbatim and are parallel to guides #3/#4/#5's halt-and-route framings. R2 above is on the §5.7 enumeration missing this case as a sixth refusal-path.

### Invention 7 — §9.6 twenty-three-item refuse-to-lock gate

**Sound — this is the largest gate in the layer and per A3 above spot-checking five-to-seven items confirms distinctness, implementability, and collective completeness.** The 23 checks span: source-binding (items 1, 4 — source-claim verbatim + caveats invented forbidden); audience-discipline (item 2 — specificity binding); dictionary-binding (items 3, 25 — methodology vocabulary without dictionary entry); L-ID discipline (items 5, 6 — omitted source-cited L-ID + added L-ID source did not cite); quality-measure discipline (items 7, 8 — tier-2+ presence + bare-percentage prohibition); layperson-test discipline (items 9, 10, 16 — block-missing + indefinite-deferral + commentary-gate-not-fired); visual discipline (item 11 — visual + visual-pending fallback); commentary discipline (items 12, 13, 14, 15 — research-track-present + forbidden-wording + subject-attribution missing + attached-to citation missing); language discipline (items 17, 18, 19 — advice + tier-upgrading + faux-balanced); jargon discipline (item 25 — patient-track methodology-jargon-without-rendering); language-consistency (item 22 — cross-language mixing without warrant); skip-research-internal explicitness (item 24); patronising-tone (item 26); and the catch-all anti-pattern check (item 27). The 23 items collectively enforce the §3.10 hard predictive gate (items 8 + 19) + §3.12 commentary discipline (items 12-16) + L-ID binding (items 5-6) + audience-specificity (item 2) + dictionary-binding (items 3 + 25) + visual + layperson-test + base-language + skip + advice-prohibition + tier-ceiling at lock time. **No gate-item gap found.** R2 above is on the missing source-stage-map-change-needed refusal-path; that's a §5.7 enumeration issue, not a §9.6 gate gap.

## 5. Per-section findings

### §1 Purpose

Sound. The block-quote framing matches plan §6.6's opening verbatim ("A locked upstream artefact is research output written for the research line itself..."). The eight-bullet "What Stage T does NOT do" list is operationally important boundary statement; the §3.12 hard separation at bullet #4 and the layperson-test bullet #8 are the §3.12-binding additions. The "Alternatives considered" paragraph satisfies CONVENTIONS §2.2 item 3 explicitly. The skill-precondition note matches guides #1-#5 framing. The closing "this guide is #6 of six — the last" framing surfaces the layer-closure status correctly.

### §2 Inputs

Sound (modulo R1 dictionary-existence assumption). The twelve numbered inputs cover §6.6's four plus the layer-level binding additions. Input #4 (limitations doc + §5 translation row) correctly carries the per-track L-ID rendering binding. Input #5 (plain-language dictionary) is correct in spirit but R1 above identifies the first-Stage-T-session bootstrapping gap. Input #7 (layperson-test recruitment context with the §10.7 deferred-question per-artefact resolution) is the right structural place for the deferred-question handling. Input #9 (§3.12 commentary section) correctly bounds to Stage A sources with §5.9 filled. The closing "what Stage T does NOT load" paragraph is the right negative-space discipline — including the load-bearing "Stage T does not re-evaluate the §3.10 hard predictive gate; it inherits the source's tier reading" framing.

### §3 Output

Sound (per Invention 2 + Output assessment in §3 Faithfulness above). The output-path resolution is verified correct against locked-plan §5 lines 647-654. The flat-naming-inside-each-audience-track-subfolder convention matches the plan tree verbatim. The skip-research-internal option is correctly carried; the "Both audience tracks produced (or skip-research-internal recorded)" stopping criterion matches plan §3.8 row verbatim. The two hard rules at §3 (no L-ID omitted; no new L-ID cited) are correctly framed and enforced at §9.6 gates 5-6. The worked-example anchors align with the synthesis-structure map r3 §3/§4/§5 rows.

### §4 The two audience tracks

Sound. §4.1 (research-audience track) + §4.2 (patient-audience track) carry the §3.12 commentary status binding faithfully at each track (FORBIDDEN at research; ELIGIBLE at patient when source is Stage A with §5.9 filled). The forbidden-wording lists per track correctly enforce the §3.10 gate at the wording level (per §7.1 + §7.4 anti-patterns). The §4.3 base-language sub-section correctly defers the global default per locked-plan §10.6, with a non-binding heuristic (patient-audience defaults Dutch; research-audience defaults English) and per-artefact deviation allowed. The §4.4 audience-specificity binding correctly carries the §6.6 spec's "specific subgroup (not 'general public')" requirement + the §7.9 anti-pattern enforcement. The §4.5 summary table is Invention 1.

**Critical for reviewer concern 4 (§3.12 patient-audience-ONLY at all touchpoints)**: §4.1 FORBIDDEN + §4.2 ELIGIBLE + §4.5 table correctly carry the hard separation at the track-mapping surface. Verified.

### §5 Section outline

Sound (modulo R2 refusal-path miss, A1 length, A2 cross-reference).

**§5.1 (source artefact citation)**: sound; mechanical header copy from source; the "Source §3.12 commentary status (Stage A sources only)" line is the right structural flag for the §5.11 conditional-presence routing.

**§5.2 (audience definition)**: sound; the per-track persona + prior-knowledge + likely-first-read-questions + base-language template is the right operational shape. The hard rule on generic-audience prohibition (§4.4 specificity binding + §7.9 + §9.6 gate item 2) is correctly carried.

**§5.3 (plain-language translation of source's core claim)**: sound; the per-track wording discipline (research-audience: preserved methodology vocabulary; patient-audience: plain-language replacement) is the right reduction of §6.6's narrative-arc + plain-language-body content. The worked example for K-bout-recovery-signal tier-2 correctly carries the PPV-with-base-rate plain-Dutch rendering ("N van de M gevallen" without PPV label; "~2 keer per jaar" base rate context preserved).

**§5.4 (translation of source's caveats)**: sound; the per-track rendering rules + the hard rule on patient-track NOT-DO refusals rendering ("wat ik er NIET mee kan" / "what I cannot use this for") correctly translate the upstream Stage A guide §5.4 four-NOT-DO-categories into the patient-track audience domain. The worked example for K-stress-fatigue-monitoring correctly renders four categories (predictive / causal / group-level / advice) with methodology vocabulary absent + honest-uncertainty preserved.

**§5.5 (translation of source's L-ID citations)**: sound; the per-track rendering rules + the hard rule (omit no source-cited L-ID in either track; add no L-ID source did not cite) correctly translate the limitations doc r3 §5 row for translation. The worked example for K-stress-fatigue-monitoring patient-audience renders all seven L-IDs in plain Dutch grouped into two audience-driven conceptual concerns ("wat de grenzen zijn"; "hoe streng het onderzoek is") with L1+L2+L3 in the first group + L4+L6+L7 in the second + L5 omitted as NA. The grouping is audience-driven rather than one-to-one L-ID-mapping; this is the right operational call (one paragraph per L-ID would be tedious in plain language; conceptual grouping reads naturally).

**§5.6 (quality-measure translation per §3.10)**: sound; the per-track binding (research-audience: technical measure names + plain-language gloss; patient-audience: plain-language frame only with base-rate context) correctly translates the §6.6 spec's quality-measure-translation block. The conditional handling for source-is-Stage-I/S₁/S₂ (section omitted), source-is-Stage-A-tier-1 (section records "exempt"), source-is-Stage-A-tier-2+ (required content per track) covers the operational cases. The worked example for K-bout-recovery-signal tier-2 patient-audience correctly preserves PPV as "N van de M gevallen" + base rate as "~2 keer per jaar" + the §3.10 tier-2-not-tier-3 distinction without forecast wording.

**§5.7 (`open_inputs` block + layperson-test status)**: sound (modulo R2). The five refusal-to-proceed paths cover the operationally distinct refusal cases at Stage T (source missing/unlocked; source Stage A but §5.9 not filled; dictionary missing terms; layperson-test pool not identified; visual summary not produced). The crucial distinction: refusal-path #1 produces only `open_inputs` entry (translation artefact not drafted); refusal-paths #2-#5 produce fallback rendering with limitation recorded. Parallel to guide #5 §5.6 + guide #4 §5.6 refusal-path structures. The layperson-test status sub-block correctly operationalises the §3.12 implementability check at Stage T (Invention 3). R2 above is the sixth-refusal-path observation for §6.6 map-change-needed cases.

**§5.8 (visual summary)**: sound; per-track binding (research-audience: image strongly preferred but optional; patient-audience: mandatory and primary with visual-pending fallback) correctly carries the §6.6 spec's visual-summary binding. The visual specification template + layperson-caption template are operationally implementable. The layperson-caption-is-layperson-test-gated framing is the right §3.12 + §6.6 binding at the visual level.

**§5.9 (follow-up communication)**: sound; per-track rendering rules correctly carry the §3.11 binding at the Stage T per-track level. The "what is still unknown" + "what could I observe myself" + "what could I ask a clinician" three-paragraph patient-audience structure is the right operational shape. The worked example for K-bout-recovery-signal correctly renders three categories + honest framing without false hope / counsel-of-despair (per §6.6 spec).

**§5.10 (cross-references)**: sound (modulo A2 plan citation omitting §3.6 + §5). The dictionary-update record sub-section (Invention 4) is the right structural place for the per-translation audit trail.

**§5.11 (subject-narrative commentary translation, patient-audience track ONLY)**: sound — this is the load-bearing §3.12-at-Stage-T operationalisation (Invention 5). The conditional-presence routing (absent on research-audience track; absent on patient-audience track when source is non-Stage-A or Stage A with §5.9 skipped; present on patient-audience track when source is Stage A with §5.9 filled) correctly carries the §3.12 hard separation. The six-element discipline + the two worked examples + the layperson-test-fail worked example at the end are all operationally clear.

**Critical for reviewer concern 7 (L-ID propagation audience-appropriate)**: §5.5 correctly carries the per-track rendering rules — research-audience keeps L-IDs by ID; patient-audience renders in plain-language without naming L-IDs. The worked examples for K-stress-fatigue-monitoring both tracks demonstrate the per-track rendering on the same applicable-limitations set. Verified.

### §6 Conflict rules

Sound. §6.1 (translation requires stronger claim than source allows), §6.2 (source has CANNOT-SAY positioning), §6.3 (source and dictionary conflict on meaning), §6.4 (layperson-test fails) are direct cite-and-implement of the §6.6 spec's four conflict rules. §6.5 (commentary translation conflict with source §5.9 — source as binding ceiling) is the load-bearing addition for the §3.12 source-binding-ceiling discipline at Stage T (Stage T does not add commentary content beyond source; routing is via §3.7 drift back to source §5.9 for revision). §6.6 (source-stage map-change-needed = §3.6-at-Stage-T operationalisation) is Invention 6.

§6.4 correctly carries the layperson-test-fail → revision routing per §6.6 spec; the closing paragraph ("Commentary translation §5.11 is the highest-risk surface for layperson-test failure per §3.12 implementability check") is the right §3.12 + §6.6 binding flag.

### §7 Anti-patterns

Sound. The 15 anti-patterns + the §6.6 spec's 7 are correctly mapped (per §3 Faithfulness above). The 8 additions (§7.3 research-track-carrying-commentary; §7.4 commentary-translated-to-predictive; §7.5 translation-framed-as-advice; §7.6 inventing-new-caveats-post-hoc; §7.8 indefinite-layperson-test-deferral; §7.9 audience-definition-generic; §7.14 cross-language-drift; §7.15 re-routing-source-to-different-stage) are distinct and sound.

**Reviewer concern 6 (§3.10 hard predictive gate propagation at §7)**: §7.1 (tier-upgrading in wording) + §7.4 (commentary translated to predictive wording) operationalise §3.10 at the wording level at Stage T. The §7.1 paragraph explicitly carries "Stage A backdoor-predictive-claims anti-pattern (guide #5 §7.5) propagates here verbatim — Stage T MUST NOT render wording that exceeds the source's tier." This is the §3.10-via-Stage-A-§7.5 backdoor closure at Stage T. **Verified.**

**Reviewer concern 4 (§3.12 patient-audience-ONLY at §7)**: §7.3 (research-audience track carrying §3.12 commentary) + §7.4 (commentary translated to predictive wording) are the load-bearing §3.12-binding anti-patterns at Stage T. §7.3's "Why this anti-pattern is load-bearing" framing correctly identifies the epistemic-category-blur risk. **Verified.**

### §8 Interview-prompt seeds

Sound. The 3 required seeds + 1 optional are the right reduction of §6.6's 5 seeds + §3.12 commentary-shape addition. §8.3 (layperson-test scheduling) correctly carries the §10.7 per-artefact resolution + the §7.8 anti-pattern reminder. §8.4 (optional commentary-rendering for Stage A sources with §5.9 filled) parallels guide #5 §8.4 confirmation-seed pattern but carries §3.12-specific discipline (eligibility check; permitted-wording-only; forbidden-wording flagging; layperson-test scheduling specifically for commentary block).

The §9.4 closing paragraph ("Skill MUST NOT autonomously fill §5.3 plain-language replacements, §5.7 layperson-test pool, §5.11 commentary rendering") correctly carries the user-articulation-required discipline for judgment-call fields.

### §9 Agent-instruction outline

Sound (per Invention 7). The eight phases are concrete. The §9.6 refuse-to-lock gate's 23 structural checks are the right protection layer (per A3 spot-check above). The §9.7 review handoff correctly routes to `/research-review` plus layperson-test for patient-audience track (matches plan §4 row for translation artefacts: "Skill-driven / Fresh-session `/research-review` plus layperson-test where patient-audience track exists"). The §9.8 acceptance + drift-trigger registration enumerates 4 re-examination triggers (per §3.7 + commentary-carrying patient-tracks add a fifth trigger: layperson-test re-test reveals previously-undetected forbidden-wording).

The "Drift-trigger registration is manual-pending-skill" + "limitations doc §8 downstream-citation-count is manual-pending-skill" framings at the close of §9.8 correctly carry the manual-until-skill-lands discipline that the §11 step 7 skill-build is the binding implementation event.

R1 above is on the §9.1 Load phase's dictionary-bootstrapping responsibility.

### §10 Cross-references

Comprehensive (modulo A2 minor §3.6 + §5 omission from plan citation line). All cited paths verified to exist:
- `_plan_results_analysis_layer.md` §3.5/§3.7/§3.8/§3.9/§3.10/§3.11/§3.12/§4/§6.6 — exist (verified).
- Guides #1-#5 LOCKED r2 — all five verified to exist at the named paths.
- `research_line_limitations.md` §3/§5/§8 — exists (verified).
- `synthesis_structure_map.md` §3/§4/§5 — exists (verified).
- `plain_language_dictionary.md` — **does NOT exist** (per R1 above).
- `RESEARCH-REPORT.md` §5.2 — verified to exist.
- `personal_hypotheses.md` §32 — verified.
- `hypothesis_lock_process.md` — verified.
- `bout_level_recovery_dynamics.md` / `citalopram_phase_stratification.md` / `symptom_mention_asymmetry.md` — all verified.

### §11 Lock log

Per A4 above: ~50+ lines of unbroken prose. Content is correct (seven named inventions; three §6.6 ambiguity interpretations resolved; skill-precondition note; closes §11 step 6 note). The "guide #6 of six — the LAST guide in §11 step 6 (D → I → S₁ → S₂ → A → T arc complete on r1-draft)" framing surfaces the layer-closure status correctly. The "On lock + review absorption, the six-guide arc is complete and the layer is ready for §11 step 7 (skill build)" closing line is the right §11-step-6-to-§11-step-7 handoff flag.

## 6. Cross-cutting concerns

### §3.12 patient-audience-ONLY commentary boundary (load-bearing review check)

**Strongly enforced; verified at all touchpoints.** The boundary is operationalised at six enforcement layers:

1. **Layer-binding statement**: §1 (sixth bullet "What Stage T does NOT do: NOT carry §3.12 commentary in research-audience track") + §2 input #9 ("Research-track omits per §3.12 hard separation + §4.2 + §5.11 + §7.3 + §9.6") correctly name §3.12 patient-track-ONLY as binding.

2. **Track mapping (§4)**: §4.1 research-audience track carries "**FORBIDDEN** per locked-plan §3.12 hard separation"; §4.2 patient-audience track carries "**ELIGIBLE when source is Stage A with §5.9 filled**"; §4.5 summary table carries both verbatim. Three-place enforcement at the track-mapping surface.

3. **Section outline (§5)**: §5 opening paragraph carries "Patient-audience track adds §5.11 when the source is Stage A with §5.9 commentary filled; research-audience track ends at §5.10 with no §5.11"; §5.11 itself carries "This section is present only on the patient-audience track-file AND only when the source is Stage A `construct-*.md` with §5.9 commentary filled. Research-audience track-files: §5.11 is absent". Two-place enforcement at the section-outline surface.

4. **Anti-patterns (§7)**: §7.3 (research-audience track carrying §3.12 commentary) operationalises the boundary as anti-pattern; §7.4 (commentary translated to predictive wording) operationalises the §3.10-via-§3.12-backdoor closure at Stage T. Two-place enforcement at the anti-pattern surface.

5. **Refuse-to-lock gate (§9.6)**: gate item 12 ("§5.11 commentary section present on research-audience track-file") refuses to lock if commentary appears in the wrong track; gate items 13-16 enforce the §3.12 internal discipline within the patient-track. Five-item enforcement at lock time.

6. **Cross-references (§10)**: bold-emphasised pointer "§3.12 (commentary layer — patient-track ONLY; §5.11 + §6.4 + §6.5 + §7.3 + §7.4 operationalise)" explicitly anchors the patient-track-ONLY discipline at the cross-reference surface.

No §5 section permits commentary in research-audience track. The §5.11 conditional-presence routing is exhaustive (absent on research-track always; absent on patient-track when source is non-Stage-A or Stage A with §5.9 skipped; present on patient-track when source is Stage A with §5.9 filled). **The §3.12 patient-audience-ONLY hard separation holds at every touchpoint. Verified per reviewer concern 4.**

### §3.10 hard predictive gate propagation (second load-bearing review check)

**Strongly enforced; no backdoors found.** Stage T inherits source's tier reading and does not re-evaluate the §3.10 gate; the enforcement at Stage T is on **wording** that would exceed the source's tier:

1. **Layer-binding statement**: §1 (second bullet "NOT predict beyond what the source's tier permits") + §2 closing paragraph ("Stage T does not re-evaluate the §3.10 hard predictive gate; it inherits the source's tier reading") correctly name §3.10 as binding-via-source.

2. **Anti-patterns (§7)**: §7.1 (tier-upgrading in wording) explicitly carries "Stage A backdoor-predictive-claims anti-pattern (guide #5 §7.5) propagates here verbatim — Stage T MUST NOT render wording that exceeds the source's tier." §7.4 (commentary translated to predictive wording) operationalises the §3.12-via-§3.10-binding at the commentary level. §7.5 (translation framed as advice) inherits the Stage A §7.2 advice-prohibition. Three-place enforcement at the anti-pattern surface.

3. **Conflict rules (§6)**: §6.1 (translation requires stronger claim than source allows) refuses the stronger rendering and routes via §3.7 drift to source-stage revision. §6.2 (source has CANNOT-SAY positioning) refuses faux-balanced softening per §7.7. Two-place enforcement at the conflict-rule surface.

4. **Refuse-to-lock gate (§9.6)**: gate items 17 (advice-form wording) + 19 (tier-upgrading wording) + 20 (faux-balanced wording softening clear source call) enforce the §3.10-via-Stage-T-wording discipline at lock time. Three-item enforcement at lock time.

**Verified per reviewer concern 6.** The §3.10 hard predictive gate propagates without backdoor; the §3.12 commentary cannot become predictive in translation per §7.4 + §9.6 gate-item 13.

### Layperson-test gate operationalisation

**Operationally implementable as either-fired-or-explicitly-pending-marked.** Per reviewer concern 5: §5.7 layperson-test status block names two valid lock states (Tested entry with five required content items; Layperson-test-pending entry with §10.7 per-artefact pool-identification recorded as `open_inputs` entry #4). Layperson-test-pending is a valid lock state, not a block on lock; the §7.8 anti-pattern (indefinite layperson-test deferral) + §9.6 gate-item 10 (patient-audience track locks with indefinite deferral) enforce the no-indefinite-deferral discipline at lock time. The gate is fully operationally implementable. **Verified.**

### Output-path ambiguity resolution

**Correctly resolved per locked-plan §5 output-structure tree.** Per reviewer concern 3: the locked-plan §5 tree (lines 647-654) explicitly shows `analyses/translation/research-audience/` and `analyses/translation/patient-audience/` as parallel folders with by-source-name flat files. The agent's interpretation (`research-audience/<source-name>.md` + `patient-audience/<source-name>.md`) is the binding §5-tree interpretation; the alternative dispatcher-brief form (`<source-stage>/<source-artefact-name>.md`) is not in the §5 tree and would conflict with it. **Verified.**

### Layer-closure check

**Clean.** Per concern 18: §10 cross-references list all five prior guides LOCKED r2 explicitly; the layer's D → I → S₁ → S₂ → A → T flow is named at §1 explicitly ("Per `_plan_results_analysis_layer.md` §3 stage-map: D → I → S₁ → S₂ → A → T"); the layer's outputs are now fully specified across the six guides; the §11 lock log carries the "guide #6 of six — the LAST guide in §11 step 6 (D → I → S₁ → S₂ → A → T arc complete on r1-draft)" closure flag + the "ready for §11 step 7 (skill build)" handoff flag. The layer-closure is clean.

## 7. Required actions for revision

1. **R1: Address `plain_language_dictionary.md` first-Stage-T-session bootstrapping.** Choose one of: (a) one-sentence addition at §2 input #5 + §9.1 Load phase + §9.5 dictionary protocol naming the first-Stage-T-session's bootstrapping responsibility (create file with frontmatter + empty term list before appending terms); OR (b) a sibling-task added to §11 step 7 brief that creates the empty dictionary scaffold during skill build; OR (c) a stage-T-first-run special-case at §9.5. The plan-level option (b) is structurally cleanest but requires a sibling edit to the locked plan §11 step 7; the guide-level option (a) is the minimum-change-cost fix.

2. **R2: Add sixth refusal-path to §5.7 for source-stage map-change-needed.** One-sentence addition: "**6. Source-stage map-change-needed per §6.6** → halt; entry: proposed source-or-map change → Stage T blocked → "route to separate producer-mode source/map revision per §6.6 + §3.7" → fallback 'none' (artefact not drafted)." Closes the §5.7 ↔ §6.6 ↔ §9.2 loop.

## 8. Recommended actions

1. **A1: Length discipline.** Consider compressing §5.5 patient-audience worked example (one full prose example for K-stress-fatigue-monitoring + compact diff for K-bout-recovery-signal) and §5.11 commentary worked example (one full Dutch example for tier-1 + compact diff for tier-2). Realistic compression: 30-50 lines (~2-3%) without losing operational content. Not blocking.

2. **A2: §5.10 cross-references plan citation completeness.** Add §3.6 + §5 to the plan citation list: "locked plan (§3.5; §3.6; §3.7; §3.8; §3.9; §3.10; §3.11; §3.12; §4; §5; §6.6)". One-sentence fix.

3. **A3: §9.6 gate scannability.** No change needed — the 23 items are well-scoped and distinct per spot-check above. Optional: a one-line per-item-category header would aid scannability (e.g., "Items 1-2: source + audience binding"; "Items 3 + 25: dictionary binding"; "Items 5-6: L-ID binding"; "Items 7-8: quality-measure binding"; etc.).

4. **A4: §11 lock log scannability.** Two-or-three short paragraphs (one for inventions; one for §6.6 ambiguity interpretations; one for §11 step 7 skill dependency note + worked-example anchor + layer-closure note) would scan better. Same A3-class observation flagged in guides #4 r1 and #5 r1 reviews; remains optional refinement.

## 9. Summary verdict

**REVISION RECOMMENDED (mild).** Two required fixes (R1: dictionary bootstrapping; R2: §5.7 sixth refusal-path); four recommended refinements (A1-A4). None blocks §11 step 6 closure. The layer-closure is clean — guide #6 closes the D → I → S₁ → S₂ → A → T arc, all five prior guides are cross-referenced as LOCKED r2, and the layer's outputs are fully specified across the six guides. The §3.12 patient-audience-ONLY commentary boundary holds at six enforcement layers; the layperson-test gate is operationally implementable as either-fired-or-explicitly-pending; the output-path ambiguity is correctly resolved per locked-plan §5 binding tree. The §11 step 7 skill build can proceed after R1 + R2 absorption; the dry-run at §11 step 9 will succeed if R1 is addressed before first Stage T artefact draft.

---

*Review report drafted by Claude under fresh-session-reviewer discipline per CONVENTIONS §1.2. Target was read in full; no edits made to target per reviewer-mode rule.*
