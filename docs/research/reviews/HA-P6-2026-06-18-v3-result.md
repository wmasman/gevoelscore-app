# Review: HA-P6 — Post-crash window distinctive autonomic-recovery shape v3 result (HA-P6-v3-result)

**Target**: [`../analyses/hypotheses/HA-P6/result.md`](../analyses/hypotheses/HA-P6/result.md)
**Target commit**: `bbcb478` (2026-06-17, reader's notes addendum) on top of script-emitted result at collision-commit `19d33e4` + audit-trail empty commit `a980b1c`. Working tree clean for the target.
**Adjacent artefacts in scope**: [`hypothesis.md`](../analyses/hypotheses/HA-P6/hypothesis.md) (v3 LOCKED `97b74df`), [`script.py`](../analyses/hypotheses/HA-P6/script.py), [`dry-run-report.md`](../analyses/hypotheses/HA-P6/dry-run-report.md). Four §9 propagation commits: `9103163` (register row), `94a4bb3` ([crash_episode_descriptive.md §8](../methodology/crash_episode_descriptive.md)), `eb42888` ([time_resolution.md §2.3](../methodology/time_resolution.md)), `a1395e1` ([HA-P7 cross-test note](../analyses/hypotheses/HA-P7/result.md)).
**Reviewer mode**: Claude (independent peer reviewer per CONVENTIONS §1.2) — *Fresh session — no exposure to the v3 test-execution drafting context; doc-only knowledge; second-iteration validation that v3's test-executed result + reader's notes addendum + §9 propagation commits faithfully execute the v3 LOCKED spec at commit `97b74df` and absorb the spec's §9 first-branch propagation discipline.*
**Review date**: 2026-06-18

---

## 1. What the data shows

The v3 result is a **Layer-1 descriptive characterisation** of the per-channel post-crash median trajectory across 7 channels on the LC era (n=29 crash_v2 episodes; per-phase split unmedicated 18 / buildup 3 / consolidation 6 / afbouw 2 / post_afbouw 0). The result.md emits — per the spec §5 findings shape — (i) per-channel × phase × baseline-arm × detrend-arm trajectory medians + IQR + per-day block-bootstrap CIs at the per-channel `E[L]`; (ii) the §4.8.1 four-verdict E[L] table for both Series A pooled-LC and Series B unmedicated-stratum; (iii) the §4.8.2 recovery-completion-day estimate per channel × phase; (iv) the §4.8.3 algorithmic shape classification; (v) the §3.7 detrend sensitivity per cell; (vi) the §4.8.4 secondary Spearman correlations on the locked headline cell; (vii) the §9 first/second-branch propagation evaluation.

Headline empirical readings, as numerically reported (verdicts unchanged from script output, restated for the reader of this report):

- **§9 first branch fires**: 4 / 7 channels statistically-distinguishable from Arm-A matched-deep-trough control on ≥2 of 5 primary-window days (paired stationary-bootstrap CI on median difference excludes 0). Strict ≥3 of 5 days: 3 / 7. Distinguishable channels: `stress_mean_sleep`, `all_day_stress_avg`, `bb_lowest`, `stress_low_motion_min_count_S60_Mlow`.
- **§9 second branch does NOT fire**: 12 / 33 (channel × phase) cells show "Arm-A match" (CI on diff includes 0 on every day); 36% is below the implicit-majority 50% threshold for "RTM dominates."
- **v3 closure firings**: closure #1 (Series B rebind) fired for `stress_mean_sleep` (A FAIL E[L]\*=19.65 → B PASS-real E[L]\*=4.83 → bind B; E[L]_used=5); closure #2 (PASS-fallback-no-cutoff override E[L]=14) fired for `bb_lowest` and `resting_hr`; closure #3 (cap at 21) fired for `all_day_stress_avg` (A E[L]\*=26.60, round=27>21 → CAP-BINDING at 21); closure #4 (paired-by-episode stationary bootstrap) used on the §9 first-branch headline cell.
- **§4.8.4 secondary correlations with CI excluding 0** (on the locked headline cell, B=10000 at per-channel E[L]): `all_day_stress_avg` rate↔duration ρ=+0.285 [+0.021, +0.585] (n=22); `all_day_stress_avg` completeness↔next-interval ρ=−0.338 [−0.493, −0.168] (n=18, **inverse direction**); `stress_low_motion_min_count_S60_Mlow` rate↔duration ρ=+0.389 [+0.013, +0.709] (n=22).

The interpretive overlay added in the reader's notes addendum (2026-06-17, reviewer-mode-with-authorization per CONVENTIONS §1.2) is clearly separated from the script-emitted result: Note 1 names the algorithmic-`slow-grind-incomplete` vs the actual relapse-and-recover trajectory for `gevoelscore`; Note 2 names the inverse direction of `all_day_stress_avg` completeness↔next-interval and proposes three candidate mechanism readings without picking one; Notes 3+4 distinguish within-episode-tail (rate↔duration) recovery debt from across-episode (completeness↔next-interval) recovery debt and summarise the corrected reading.

The four §9 propagation commits land downstream artefacts: per-channel timing into [`crash_episode_descriptive.md §8`](../methodology/crash_episode_descriptive.md) and [`time_resolution.md §2.3`](../methodology/time_resolution.md), HA-P7 cross-test interpretive overlay on [`HA-P7/result.md`](../analyses/hypotheses/HA-P7/result.md), and a register-row update at [`personal_hypotheses.md`](../personal_hypotheses.md).

---

## 2. What fired and why

### Layer 1 — Universal reporting (inherits from SCRIBE 2016, STROBE 2007)

**[L1.3 — analysis method named with parameters, minor — §9 evaluation surface omits four spec bullets]**
The result.md §9 *observation-shape propagations — evaluation on the locked headline cell* section (lines 171–177) explicitly fires/non-fires only three things: first branch (≥3 of 7 channels), second branch (Arm-A matches majority), and the §4.8.4 secondary-correlation CI-excludes-0 set. The v3 spec §9 enumerates **nine** pre-spec'd downstream-implication bullets (lines 462–497 of hypothesis.md): distinct-recovery-shape (1); RTM-dominates (2); consolidation-only-detrend-failure (3); per-channel-timing-differences (4); Arm-A-vs-Arm-B-divergence (5); t0-sensitivity-divergence (6); rate↔duration-OR (7); completeness↔next-interval-OR (8); spec-sanity-halt (9). Bullets 7+8 ARE addressed (the secondary-correlation set is exactly those two). Bullets 3, 4, 5, 6, 9 are not explicitly fired/non-fired in the §9 evaluation section. The supporting tables ARE present (§3.7 detrend table at result.md lines 109–145 → bullet 3; per-channel recovery days in headline table → bullet 4; Arm-A vs Arm-B z-trajectory and diff tables → bullet 5; t0-sensitivity table at lines 161–169 → bullet 6; dry-run-report.md `PASS` → bullet 9), and bullet 4 in particular IS clearly propagated (commit `eb42888` → time_resolution.md §2.3). But the result.md's §9 section itself does not surface the full set of fire/non-fire verdicts. **Minor** — the audit trail is intact (downstream propagations fire correctly per the supporting tables), but a complete §9 evaluation in result.md would make the spec→result coverage self-contained.

**[L1.3 — analysis method named with parameters, minor — headline-cell label disagrees with spec single-cell lock identifier]**
The v3 spec §9 single-cell lock for bullets 7+8 (and the first-branch trigger evaluation) is named **`pooled-LC × Arm-A × no-detrend × episode-end-t0 × primary-window`** (hypothesis.md line 464, 466). The v2 closure (b) baseline-source disambiguation (hypothesis.md §4.8.4 closure b) explicitly addressed this naming: *"the 'pooled-LC × Arm-A × ...' single-cell-lock identifier names the §1.1 trajectory-comparison cell; the §4.8.4 completeness Spearman uses the per-episode array from that cell, with denominators from Arm-B μ_ch"*. The result.md labels the headline cell as **`pooled LC × Arm-B lagged baseline × ...`** at line 7 (the §4.8.3 classifier-output table) and at line 62 (the §4.8.4 row header). This is a documented latitude: the Arm-B label more honestly reflects which baseline the z-scored numerator + completeness denominator come from; the Arm-A label is the spec's lock identifier. The result.md picks "data-source naming" over "spec-lock-identifier naming" without flagging the choice. **Minor** — a one-sentence note in the result.md §4.8.4 row header acknowledging the spec uses "Arm-A" as the lock identifier (matching v2 closure b's disambiguation) would close the labeling gap without changing the data.

### Layer 2 — Observational n=1 (inherits from Daza 2018, Personal Science)

No fires. The v3 spec's pooled-LC stationarity contamination concern (the v2 audit's L2.2 substantive fire) was absorbed via the Series B unmedicated-stratum sensitivity arm + the Series-B-rebind binding rule (closure #1), and the result.md surfaces both Series A and Series B E[L]\* magnitudes per channel + the binding choice + the v3 caveat #1 in §8. `stress_mean_sleep` is the textbook case where closure #1 fires (A E[L]\*=19.65 → B E[L]\*=4.83 PASS-real → bind B; E[L]_used=5 not 21), exactly the disentanglement the closure was designed for. The v2 L2.2 fire is visibly closed.

### Layer 3 — Time-series specific (inherits from Natesan Batley 2023, WWC 2022, CENT 2015)

**[L3.5 — trend vs level separation, minor — §3.7 detrend sensitivity does not survive on the pooled-LC headline for most channels]**
The result.md §3.7 detrend sensitivity table (lines 109–145) reports per (channel × phase) "no-detrend category" vs "detrended category" + "survives?". Pooled-row survives = 0 of 7 channels; per-phase survives = 2 of 28 cells (consolidation for `stress_mean_sleep` and `gevoelscore`). The §9 bullet 3 *"recovery shape fails §3.7 detrend on consolidation only"* propagation is the spec's pre-specced read for the case where consolidation-fails-but-unmedicated-and-afbouw-survive. The observed pattern is **almost-everywhere-fails-detrend** (24/28 per-phase cells categorically shift under detrend; only 2 cells survive). This is more pervasive than the spec's bullet 3 anticipated — the LC recovery trajectory (~10/year crashes → ~2/year per [registry.md](../analyses/hypotheses/registry.md)) is leaking through most cells, not just consolidation. The result.md §8 caveat 8 acknowledges CONVENTIONS §3.7 binding but does not synthesise the empirical "almost-everywhere-fails-detrend" pattern as itself a finding. **Minor** — the data is reported honestly (every cell named); the synthesis is one sentence away. The §9 first-branch fire on Arm-A (matched-deep-trough control, which itself absorbs trend by construction at the matched-day pair level) is independent of this concern: Arm-A's paired-diff trigger does not depend on detrending because the matched-control trajectory is on a same-phase non-crash day with similar pre-trajectory.

### Layer 4 — Project-specific audit hooks (inherits from CONVENTIONS §3, §4)

No fires. The v3-NEW machinery clears every Layer-4 hook:

- §3.1 (personal baseline) — Arm-B z-scores against [t0−90, t0−30] LC-era same-phase lagged baseline per §4.5 step 6.
- §3.2 (`_lagged_lcera` for PEM hypotheses) — §4.5 implements the lagged-LC-era baseline directly; v3 channel set in §4.1 is the project-canonical autonomic-load + recovery set, no `_lagged` vs non-lagged confusion.
- §3.3 (one column per definitional pair) — `stress_mean_sleep` vs `all_day_stress_avg` are different time-windows (sleep vs all-day); `bb_lowest` vs `bb_overnight_gain` are level vs delta; `stress_low_motion_min_count_S60_Mlow` is the count primitive. No near-identical-pair members co-present.
- §3.4 (crash-drop sensitivity) — §8 caveat 9 (the "inapplicable-to-primary by construction" dispatch) is verbatim the canonical disposition for trajectory-on-crash-episodes designs; inapplicable-to-secondary because the §4.8.4 correlations are per-episode summaries.
- §3.6 (named counts) — n=29 LC-era crash_v2 episodes named with scheme + predicate + source in §3; per-channel undefined-completeness counts surfaced per-row in §4.8.4 table.
- §4.2 (caveat-class vs a-priori-class) — §8 caveats 1–11 are honest acknowledgements; the v3-NEW caveats #1 (pooled-LC contamination + Series B sensitivity), #3 (cap-binding), #7 (granularity mismatch) are textbook caveat-class framings.
- §4.3 (prior-driven vs exploratory) — explicitly Layer-1 descriptive with NO SUPPORTED bar (line 5 of result.md); prior-driven via spec §2 motivation set (lived experience + Wiggers H5 + Aitken + sibling project context).

### Side observations

- **Side** — Spec → script faithfulness audit of the v3-NEW machinery is clean. `assign_verdict()` ([`script.py` lines 448–461](../analyses/hypotheses/HA-P6/script.py)) maps the estimator's `note` field through two regexes (`_NOTE_NO_CUTOFF_RX` matches `no clear acf cutoff|all lags within max_lag are significant|all lags`; `_NOTE_DEGENERATE_RX` matches `closed-form formula degenerate|zero variance|n=\d+ < 30|n<30`) which cover the spec's named note-strings exactly. `compute_el_verdicts()` (lines 464–523) gates the Series-B rebind on the spec's two-conjunct rule (Series-A FAIL on upper end AND Series-B PASS-real) and caps via `min(int(round(el)), E_BLOCK_OVERRIDE_CAP)` per closure #3. The dry-run-report.md verdict table matches the result.md headline E[L] table row-for-row.

- **Side** — The %% literal bugs in the original script-emitted result.md (mentioned in the handoff as fixed during the test session at lines 19 + 45) are cosmetic-only; the headline numerical content (E[L]\* values, verdicts, binding choices, cap-binding flags) is identical between dry-run-report.md and result.md. No numerical reporting affected.

- **Side** — The collision-commit `19d33e4` (HA-P6 v3 files landed inside a HA-C4-labeled commit because a concurrent session committed first) was repaired by audit-trail empty commit `a980b1c`. This is an acceptable repair under the project's audit-trail discipline — the test-executed event is grep-discoverable via the empty commit's message and the register-row pointer at commit `9103163` cites both commits — but the lock-process MD discipline strictly would prefer a single isolated test-executed commit. Non-fire; flagged for awareness only. Practical fix (rebase-fixup) would be more destructive than the repair.

- **Side** — Reader's Notes Note 4's framing *"Within-episode-tail recovery debt: empirically supported"* is one step more specific than the spec §9 bullet 7's downstream-propagation language *"recovery shape carries information about same-episode dynamics"*. The reader's notes acknowledges (Note 4 line 268) that the signals hold under the v3 spec's deliberately wide CIs (E[L]=21 cap-binding for `all_day_stress_avg`), and explicitly names that all three CI-excludes-0 readings survive the bootstrap's honest pessimism. The "empirically supported" framing is reasonable interpretive overlay for the within-episode-tail bullet but sits at the boundary of what CONVENTIONS §1.2 reviewer-mode-with-authorization should claim on a Layer-1 descriptive output. Non-fire because the framing is clearly flagged as interpretive overlay (the addendum header on line 218 says *"NOT a post-hoc spec modification. NOT a pre-registered finding"*), but worth noting that the reader's notes is reaching just-past the spec's bullet-7 language. The HA-P7 cross-test note propagation (commit `a1395e1`) absorbs this synthesis as *"recovery-debt mechanism reading partly empirically settled"* — language consistent with the reader's notes framing.

- **Side** — The §4.8.4 row reports `bb_overnight_gain` correlations as `--` because n=3 (rate vs duration) and n=2 (completeness vs next-interval) are below the per-channel reportable threshold. This is consistent with the channel's 2024-09-18 coverage gap (only 5 post-cut episodes contribute) but means the §9-locked-cell secondary correlation is silent on this channel. Non-fire — the spec's secondary-correlation framing tolerates per-channel n constraints — but result.md does not surface the n-too-small-to-report flag as a v3 caveat-class disclosure.

---

## 3. What does not fire (selective)

Non-trivial passes worth surfacing — these are the highest-yield checks per the handoff brief:

- **Reader's Notes Note 2 sign-correction — strong pass on Layer 3 interpretive discipline (the handoff's highest-yield check).** The §9 bullet 8 spec predicate (*"finds OR on recovery-completeness ↔ next-crash-interval → recovery debt is empirically supported"*) is literally fired by the script-emitted output: CI on `all_day_stress_avg` completeness↔next-interval excludes 0 (ρ=−0.338, CI=[−0.493, −0.168], n=18). The reader's notes Note 2 correctly identifies the **inverse direction** — naive recovery debt predicts positive (more complete recovery → longer next-interval); the observed sign is negative. The correction *"the §9 bullet 8 propagation should NOT be read as supporting the recovery-debt mechanism for this channel"* is defensible: the bullet was drafted with implicit positive direction in mind, and the spec's "OR" predicate (CI excludes 0) is mechanism-direction-agnostic. The three candidate readings (exuberance / push-through; tight feedback loop; measurement artefact) are honestly framed as candidates without picking one ("the data alone cannot pick between these"). This is good Layer-3 discipline — signal-yes, direction-inverse-from-mechanism, mechanism-ambiguous; no overreach on either the supported or refuted side. Closes the highest-yield Layer-3 risk the handoff identified.

- **L3.1 (autocorrelation addressed) — strongest pass in the project.** The v3 four-verdict E[L] policy + Series A/B + cap + paired-by-episode stationary bootstrap is the most thorough block-length disposition the project's reviews have seen. Per Natesan Batley 2023's 83.8%-ignore-autocorrelation baseline, this is the opposite tail of the distribution. The result.md surfaces every v3-NEW machinery decision per channel: verdict (A and B), E[L]\* (A and B), binding choice, cap-binding flag, rationale per row. The dry-run-report.md PASS confirms the four-verdict logic operates as spec'd (`stress_mean_sleep` A FAIL → B PASS-real → bind B; `all_day_stress_avg` A FAIL → B not PASS-real → bind A + CAP; `bb_lowest` and `resting_hr` A PASS-fallback-no-cutoff → E[L]=14; `gevoelscore` A PASS-fallback-degenerate → E[L]=7 default). All eight v2 audit closures are visibly absorbed in the running result.

- **L3.4 (block length documented) — strong pass via Series A + Series B + cap-binding flag.** Per [`permutation_null_block_length.md`](../methodology/permutation_null_block_length.md) override clause conditions (i)–(iii): (i) the rule + range `[3.5, 10.5]` factor-of-2 framing is named in spec + script; (ii) the override is pre-registered at v3 lock + the override RULE is deterministic; (iii) the ACF readout lands in the test session's dry-run-report.md + result.md. The split-compliance reading the v2 audit flagged (rule in hypothesis.md, magnitude in result.md) is closed by the v3 result emitting the magnitudes + binding choices alongside the rule in one table. The cap-binding flag for `all_day_stress_avg` (E[L]\*=26.60 → E[L]_used=21) is surfaced clearly in the headline E[L] table and in §8 v3 caveat #3.

- **L2.2 (stationarity assumption) — strong pass via Series B sensitivity arm.** The v2 L2.2 substantive fire (pooled-LC E[L]\* under interpretation (ii) inherits intervention-induced level-shift contamination) is concretely closed by the closure #1 Series B sensitivity arm + the FAIL-rebind binding rule. `stress_mean_sleep` is the empirical demonstration: pooled-LC A FAIL (E[L]\*=19.65, dose-modulated channel with documented level shifts at CPAP + Citalopram transitions) vs unmedicated-stratum B PASS-real (E[L]\*=4.83); the rebind binds the override to E[L]_used=5, not 21. The honest "cleaner ACF anchor" reading the v2 audit recommended is operationalised.

- **§4.8.1 closure (b) per-episode completeness threading — strong pass on the per-channel ε rule.** The v2 closure (b) baseline-source disambiguation (per-episode `completeness_per_episode` threaded from the locked cell; ε = 0.5 × σ_ch per channel; per-channel undefined-completeness exclusion) is correctly surfaced in result.md §4.8.4: each channel row reports n + n undefined separately. The disambiguation that completeness binds to Arm-B μ_ch regardless of the cell's named §1.1 baseline-arm is surfaced in §8 v2 caveat (b) verbatim.

- **§9 propagation faithfulness — propagation commits track the spec's first-branch sub-bullets.** Commit `94a4bb3` (crash_episode_descriptive.md §8) propagates spec §9 first-branch sub-bullets 1 + 2 (update MD with empirical per-channel timing estimates + emit per-channel timing table). Commit `a1395e1` (HA-P7 cross-test note) propagates first-branch sub-bullet 3 (inform HA-P7 14d-window assumption) + the secondary-correlation bullet 7 (rate↔duration → candidate covariate). Commit `eb42888` (time_resolution.md §2.3) propagates the per-channel-timing-differences bullet (median 2.5d across the 4 distinguishable channels; the 2-day gap between resting_hr t+1 and stress channels t+3 in the Wiggers H5 direction is named as sibling reading, not tested prediction). Commit `9103163` (register row) absorbs the headline status. All four commits explicitly acknowledge interpretive-overlay-vs-spec-modification discipline; the HA-P7 cross-test note in particular is careful to preserve the HA-P7 LOCKED NOT-SUPPORTED verdict + caveats unchanged.

- **L3.3 (multi-comparison discipline) — strong pass via single-cell lock.** The §9-head single-cell lock (`pooled-LC × Arm-A × no-detrend × episode-end-t0 × primary-window`) for bullets 7+8 + first-branch trigger evaluation is honored: §4.8.4 correlations on cells outside the lock are reported in `result.csv` under `secondary_cell_signal` (per spec §9 head; not visible in the result.md text, would need to confirm against the CSV — accepted on faith per the spec's machinery). The "≥3 of 7 channels" + "≥2 of 5 days" pre-spec'd thresholds replace ad-hoc multi-comparison correction with a pre-registered operational gate.

- **§4.8.1 four-verdict mapping to estimator's `note` field — strong pass on note-string coverage.** The two regexes in `script.py` (`_NOTE_NO_CUTOFF_RX`, `_NOTE_DEGENERATE_RX`) cover the estimator's known note-string set (no clear ACF cutoff / all lags significant; closed-form degenerate; zero variance; n<30). The dry-run-report.md row for `bb_lowest` confirms the regex match: Series A note `"No clear ACF cutoff (all lags within max_lag are significant..."` → verdict `PASS-fallback-no-cutoff` → E[L]=14. The mapping is exhaustive given the estimator's documented note vocabulary.

---

## 4. What would strengthen this finding

Per [`hypothesis_lock_process.md §3.6`](../methodology/hypothesis_lock_process.md#36-re-audit-step-stage-4-of-the-arc) acceptability criteria, each item is classified as **(i) compressible** (one-line spec-precision refinement → no v4 trigger), **(ii) substantive** (v4 spec revision required → HA-P6-v4 queued), or **(iii) side observation** (flagged-for-awareness, non-fire).

1. **(i) compressible** — **Complete the result.md §9 evaluation surface.** Add fire/non-fire one-liners for §9 bullets 3 (consolidation-only-detrend-failure), 4 (per-channel timing differences), 5 (Arm-A-vs-Arm-B divergence), 6 (t0-sensitivity divergence). The supporting data is already in result.md tables; one new sub-bullet per bullet under the §9 *evaluation on the locked headline cell* heading would close the gap. Expected effect: result.md becomes a self-contained spec→result coverage map without needing to follow the propagation commits to reconstruct what fired. The current state is auditable via the propagation commits (`eb42888` is the bullet-4 audit trail) but not self-contained in the result.

2. **(i) compressible** — **Add a §4.8.4 row-header parenthetical naming the spec's lock identifier.** The result.md §4.8.4 row header at line 62 reads "(locked headline cell -- pooled-LC × Arm-B × no-detrend × episode-end × primary)". Add: "(spec single-cell-lock identifier per §9 head + v2 closure (b) disambiguation: `pooled-LC × Arm-A × ...`; the Arm-B label here reflects the data source for the completeness denominator μ_ch per v2 closure (b))." Expected effect: closes the L1.3 minor labeling disagreement without changing data; preserves both the spec-lock-identifier naming and the data-source naming in one place.

3. **(i) compressible** — **Synthesise the empirical "almost-everywhere-fails-detrend" pattern in §8.** The §3.7 detrend sensitivity table shows 2/28 per-phase cells survive detrending. The §9 bullet 3 was drafted anticipating consolidation-only-failure; the empirical pattern is more pervasive. Add a §8 caveat sentence: *"The §3.7 detrend sensitivity shows 25/28 per-phase cells categorically shift under detrend (vs the spec §9 bullet 3 consolidation-only-failure anticipation); the LC recovery trajectory leaks through nearly all cells, not just consolidation. The Arm-A paired-diff §9 first-branch trigger is independent of this concern by construction (matched-control trajectory is on a same-phase non-crash day with similar pre-trajectory, so trend is absorbed at the matched-day pair level)."* Expected effect: surfaces the empirical-vs-anticipated pattern as a finding-in-its-own-right; sharpens the load-bearing-Arm-A vs LC-trajectory-leaks-Arm-B-z-scores reading.

4. **(iii) side observation** — **Acknowledge Reader's Notes Note 4 framing boundary in result.md narrative.** The "Within-episode-tail recovery debt: empirically supported" framing in Note 4 reaches just-past the spec §9 bullet 7's "carries information about same-episode dynamics" language. If the framing is meant to stand as a reviewer-mode-with-authorization interpretive overlay (which it is, per the addendum header), a one-line acknowledgement that the framing is "more specific than the spec §9 bullet 7's downstream-propagation language" would close the boundary. Expected effect: closes a Layer-3 marginal-interpretive-overreach side concern; the reader's notes remains clearly distinct from a §1 claim.

5. **(iii) side observation** — **Surface bb_overnight_gain too-small-n disclosure in §4.8.4 caveat.** The §4.8.4 row for `bb_overnight_gain` reports `--` for both correlations (n=3, n=2). Add a one-line caveat noting that the §9-locked-cell secondary correlation is silent for this channel due to the 2024-09-18 coverage gap. Expected effect: makes the per-channel n constraints explicit at the §4.8.4 reading point; closes a minor disclosure gap.

No (ii) **substantive** items. The v3 result is a clean execution of the v3 LOCKED spec; nothing here triggers HA-P6-v4. The v3 spec's §4.8.4 day-level-E[L]-on-per-episode-summary granularity mismatch (closure #7) is already explicitly **deferred** to a methodology-MD-update + a v4 absorption per spec §8 v3 caveat #7; this review does not contest that deferral.

---

## 5. Verdict

**PASS** — the v3 result faithfully executes the v3 LOCKED spec at commit `97b74df`, absorbs all eight v2 audit closures correctly (Series B FAIL-rebind fires for `stress_mean_sleep`; PASS-fallback-no-cutoff E[L]=14 fires for `bb_lowest` and `resting_hr`; cap at 21 binds for `all_day_stress_avg`; paired-by-episode stationary bootstrap on the §9 first-branch headline; per-episode completeness threading + ε=0.5×σ_ch rule + Arm-B μ_ch disambiguation), and faithfully propagates the spec's §9 first-branch sub-bullets via four well-disciplined interpretive-overlay-or-pure-observation commits. The highest-yield Layer-3 check (Reader's Notes Note 2 sign-correction on `all_day_stress_avg` completeness↔next-interval) is honestly handled — signal-yes, direction-inverse-from-naive-recovery-debt, mechanism-ambiguous with three candidate readings, no overreach. Five minor / side observations queued as (i) compressible (§9 evaluation surface completeness; §4.8.4 spec-lock-identifier acknowledgement; almost-everywhere-fails-detrend synthesis) and (iii) flagged-for-awareness (Note-4 framing boundary; bb_overnight_gain n-too-small disclosure); none trigger HA-P6-v4 spec revision.

---

## Methodology

This review walks the 4-layer checklist defined in [reviews/README.md](README.md), anchored in:

- SCRIBE 2016 (Tate et al., [PMC4873717](https://pmc.ncbi.nlm.nih.gov/articles/PMC4873717/))
- CENT 2015 ([shamseer_2015_cent_consort_nof1.pdf](../literature/methodology/shamseer_2015_cent_consort_nof1.pdf))
- STROBE 2007 ([vonelm_2007_strobe_observational_checklist.pdf](../literature/methodology/vonelm_2007_strobe_observational_checklist.pdf))
- Daza 2018 ([daza_2018_self_tracked_n_of_1_counterfactual.pdf](../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf))
- WWC 2022 SCED standards ([wwc_2022_standards_handbook_v5_0.pdf](../literature/methodology/wwc_2022_standards_handbook_v5_0.pdf))
- Natesan Batley 2023 ([natesan_2023_nof1_evidence_reporting_systematic_review.pdf](../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf))

Project-specific audit hooks from [../CONVENTIONS.md](../CONVENTIONS.md) §3 and §4, including the [hypothesis_lock_process MD](../methodology/hypothesis_lock_process.md)'s lock-blocking gates (power-calc dispatch, single-cell headline lock, register-row pointer, re-audit / compression decision per §3.6), the [permutation_null_block_length.md](../methodology/permutation_null_block_length.md) override clause (conditions (i)–(iii) for per-hypothesis E[L] override), and the prior fresh-session audit at [HA-P6-2026-06-17-v2.md](HA-P6-2026-06-17-v2.md) (v2 PASS-with-caveats; 8 named closures absorbed into v3).

HA-P6-v3 is Layer-1 DESCRIPTIVE characterisation per CONVENTIONS §2.1, not an inferential SUPPORTED / NOT-SUPPORTED test. The §5 spec section enumerates result.md output shape (reported regardless of data); §9 enumerates pre-specified downstream propagations per observation shape. This review is a **second-iteration validation** per CONVENTIONS §1.2 + [`feedback_pre_reg_writer_role.md`](C:/Users/Gebruiker/.claude/projects/c--Users-Gebruiker-Documents-gevoelscore-app/memory/feedback_pre_reg_writer_role.md) — the v3 result + reader's notes addendum + four §9 propagation commits were drafted in a session that this reviewer had no exposure to. Audit applied at the v3-NEW-machinery execution faithfulness level (Series A/B, four-verdict, FAIL-rebind, cap, paired stationary diff), at the spec → script → dry-run → result coverage level (all eight v2 audit closures absorbed), and at the §9 propagation-commit interpretive-overlay-discipline level (reader's notes Note 2 sign-correction; HA-P7 cross-test note's "partly empirically settled" framing). No concerns about a falsification bar that does not exist by design.
