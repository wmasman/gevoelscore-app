# Review: HA-C4 v1 — Stress fails to drop during rest after overexertion, 3-channel confirmatory triad (HA-C4)

**Target**: [`analyses/hypotheses/HA-C4/hypothesis.md`](../analyses/hypotheses/HA-C4/hypothesis.md)
**Target commit**: `b76b1e0` (2026-06-17 13:02 +02:00) — clean working tree at review time. Same-commit drafted-and-handed-off (lineage: `b76b1e0` v1 DRAFTED, no prior HA-C4 revision in git).
**Reviewer mode**: Claude (independent peer reviewer per CONVENTIONS §1.2). *Fresh session — no exposure to the drafting context or to HA-C4b's per-episode z-scores; doc-only knowledge.*
**Review date**: 2026-06-17

## 1. What the data shows

The target is a **v1 pre-registration** (not a result), drafted under reviewer-mode-with-authorization in a shared-context session with HA-C4b v3's test execution. v1 invokes the [`hypothesis_lock_process.md` §3.2 Stage-1 clause](../methodology/hypothesis_lock_process.md#32-drafting-step-step-1-of-the-arc) allowing shared-context drafting and pre-stages the fresh-session [§3.4 audit](../methodology/hypothesis_lock_process.md#34-audit-step-step-2-of-the-arc) (this report) as the integrity check on §4 / §5 operational choices. The headline cell is byte-inherited from the [design MD §C4 worked-example anchor](../methodology/wiggers_test_design_on_chained_regime.md#c4--we-class-3-channel-confirmatory-triad):

- **Unmedicated phase only** (2022-04-04 → 2024-04-08; §4.4 citing [`citalopram_phase_stratification`](../methodology/citalopram_phase_stratification.md) §5.B dose-modulation on stress channels at β = +0.43-0.57/mg).
- **3-channel triad** on heavy-T vs non-heavy-T (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T): Ch1 `stress_post_peak_time_to_rest_min` on T (NaN coded as 1080-min cap per §4.5); Ch2 `stress_high_duration_min` on T; Ch3 `awake_stress_avg` on T+1 with chain-T+1 exclusion (§4.7).
- **Statistical method**: per-channel one-sided Mann-Whitney U + Cliff's delta, block-permutation null at E[L]=7 with `RANDOM_SEED = 20260617`, applied independently per era × phase cell (§4.8 + §4.9).
- **Verdict rule**: pass-2-of-3 channel confirmations (each requiring p < 0.05 AND Cliff's delta ≥ +0.20 in BOTH train AND validate eras) = SUPPORTED; 1-of-3 = PARTIAL; 0-of-3 = REJECTED (§5.3). Holm step-down across the 3 channels per era is reported as a secondary multiplicity-fragility flag, NOT verdict-driving (§5.5).
- **Inconclusive bar**: per-channel × per-era cell with < 30 days in either arm is INCONCLUSIVE (§5.4).

The drafting session has executed HA-C4b v1 → v2 → v3 (NOT-SUPPORTED on the motion-filter crash-precursor framing) and knows the identity of 10 unmedicated heavy-T-and-crash days (the HA-C4b crash-day pool intersected with heavy-T), HA-C4b v3's NOT-SUPPORTED verdict, and HA11's SUPPORTED-on-train (+22.8pp) verdict. The drafter has NOT seen HA-C4's per-day channel values on individual days.

## 2. What fired and why

### Layer 1 — Universal reporting (inherits from SCRIBE 2016, STROBE 2007)

**[L1.4 — operationalised measures and reference distributions defined with their source-file path, CONVENTIONS §2.1 + `hypothesis_lock_process.md` §5 sanity-check row "§7 ranges anchored to the EXACT column being measured"] — substantive.** v1 §7 claims unmedicated-phase distributional anchors for each channel ("Channel 1 expected ~60-180 min", "Channel 2 expected ~20-60 min per day", "Channel 3 expected ~30-50") and cites [`lc_phase_descriptive.md`](../methodology/lc_phase_descriptive.md) §1176-§1230 as the per-phase reference. Three problems compound:

1. **The cited line range only covers Channel 1's two metrics + a sensitivity arm**. Lines 1176-1192 are `stress_post_peak_drop_avg`; 1193-1209 are `stress_post_peak_time_to_rest_min`; 1210-1226 are `stress_recovery_pct_within_2h`. Channel 2 (`stress_high_duration_min`) lives at lines 1159-1174 — outside the cited range. Channel 3 (`awake_stress_avg`) lives at lines 523-538 — far outside the cited range. The citation undershoots for Ch2 and Ch3.
2. **The cited MD partitions by `pre_corona | corona_infection | lc`, not by citalopram phase**. The "lc" column is LC-aggregate (n=1310 for Ch1; n=1501 for Ch2; n=1502 for Ch3) and includes ~638 consolidation days at 30mg steady-state. Per the [`citalopram_phase_stratification` §2 table](../methodology/citalopram_phase_stratification.md), `all_day_stress_avg` has β = +0.57/mg (sibling channel to Ch3 `awake_stress_avg`) — a ~17-point shift at 30mg. The LC-aggregate medians will not equal the unmedicated-only medians by that magnitude on the stress-level channels. v1 §7 ranges are therefore not the EXACT-column EXACT-phase anchors the [`hypothesis_lock_process.md` §5 sanity-check row](../methodology/hypothesis_lock_process.md#5-sanity-check-questions-before-lock) requires.
3. **The Channel 1 anchor phrasing "expected ~60-180 min based on the dictionary's coverage stats" is malformed**. Coverage (86.7%) does not give a median. The actual LC-aggregate p50 is 69.5 min — at the bottom of the asserted [60, 180] range. The Channel 3 anchor "expected ~30-50 ... per [DATA_DICTIONARY §7B] `awake_stress_avg` has ~98.6% coverage" similarly cites a coverage stat as if it informed a distributional range.

This is precisely the HA-C4b v1 failure mode that the lock-process MD §5 sanity-check row was added to catch (anchor range derived from a definitional cousin's distribution; actual column median tripping the §7 gate at dry-run; forcing v2 redraft post-lock). Repeating it here is a foreseeable risk and lock-blocking per the §5 row.

Magnitude: substantive. Closure: replace the §7 ranges with `per_day_master.csv` medians + IQR computed on the unmedicated-only filter (`lc_phase == "lc" AND citalopram_phase == "unmedicated"`) for each of the four channels actually under test (Ch1 primary + drop_avg companion; Ch2; Ch3), with the script-line + filter visible.

### Layer 2 — Observational n=1 (inherits from Daza 2018, Personal Science)

**[L2.5 — held-out structure framing under shared-context drafting, memory `project_garmin_research_bias_boundary` + [`hypothesis_lock_process.md` §3.2](../methodology/hypothesis_lock_process.md#32-drafting-step-step-1-of-the-arc) clause + HA-C4b v3 audit precedent] — substantive.** v1's Authorship block (§Authorship) documents the data-exposure boundary honestly: the drafter knows the identity of 10 unmedicated heavy-T-and-crash days and their HA-C4b sister-metric z-scores; the drafter has NOT seen the HA-C4-specific Ch1/Ch2/Ch3 values on individual days. The Authorship pre-stages the eight specific operational choices the auditor should test for data-driven bias (per the handoff brief). Walking each:

- **Heavy-T eligibility = T only (vs HA-C4b's T-or-T-1 union)**: defensible on its own terms (more conservative; the t+1 reactivity channel uses T+1 separately) AND the design MD §C4 specifies "Day-classification on T" for Ch1/Ch2 and "T+1 for Ch3" — design-MD-inherited.
- **3-channel definitions + pass-2-of-3 verdict rule**: byte-inherited from [`wiggers_test_design_on_chained_regime.md` §C4](../methodology/wiggers_test_design_on_chained_regime.md#c4--we-class-3-channel-confirmatory-triad). Pre-existing design lock.
- **NaN-as-1080-min encoding for Channel 1 (§4.5)**: defensible as a waking-window upper bound; the actual LC-aggregate max is 842 min so 1080 > max guarantees NaN ranks above all observed. Mann-Whitney is rank-based; the specific 1080 value only matters as "higher than any non-NaN value", and the descriptive NaN-fraction companion (§4.11) is the alternative read. Closeable without §3.2 escalation.
- **Chain-T+1 exclusion for Channel 3 (§4.7)**: design-MD-named for Ch3. Defensible.
- **Cliff's delta ≥ +0.20 threshold (§4.8 step 4)**: standard small-to-medium effect; matches HA-C4b's choice; not tuned by HA-C4 drafter.
- **Empirical p < 0.05**: standard; not tightened against multi-channel multiplicity at the channel level (Holm at §5.5 is the secondary report). Design-MD-pattern.
- **Holm step-down inclusion (§5.5)**: secondary report only; does not modify the §5.3 verdict bar. Defensible inclusion as a fragility flag.
- **±50% per-phase median tolerance (vs HA-C4b's ±20%)**: justified in §7 ("the recovery-dynamic channels have larger natural variation than the cumulative-count primitive"), AND the HA-C4b v1 anchor failure showed ±20% was too tight against an anchor weakness. The relaxation is the right direction; whether ±50% is the right magnitude depends on the variability of the EXACT-column EXACT-phase descriptive — which the L1.4 fire above prevents the auditor from verifying directly. Coupled concern: tightening the §7 anchor (L1.4 closure) may make a tighter tolerance (e.g. ±30%) defensible without dry-run risk.
- **Per-channel ≥ 30 inconclusive bar (vs HA-C4b's ≥ 10)**: defensible — HA-C4b's n=10 bar was forced by the rare-event regime (29 crash episodes in corpus, ~10 in unmedicated). HA-C4's comparison pool is heavy-T days (estimated ~200 in unmedicated per the design MD) vs non-heavy-T days. The bar increase reflects sample availability, not gate tuning.

The Authorship block also pre-states the §3.4 audit should specifically verify "none of the §4 / §5 operational choices below were biased by knowledge of the 10 crash-day identities." Going down that list: the choices either (a) are design-MD-inherited (Ch1-3 definitions, T-only eligibility for Ch1/Ch2, T+1 chain exclusion for Ch3, verdict rule), or (b) are independently defensible on their own terms (NaN-1080, delta ≥ 0.20, p < 0.05, Holm secondary). The two choices the drafter actually had to make under exposure — **±50% tolerance and ≥ 30 inconclusive bar** — are both justified by reasons orthogonal to the 10-day identity (HA-C4b v1 anchor lesson; sample-pool size). The Authorship's pre-stated boundary holds.

The structural analog to the HA-C4b v3 audit's L2.5 substantive fire IS present but materially weaker. HA-C4b v3's fire was about choosing-among-three-options-to-fix-a-known-asymmetry under exposure to which option restored a specific known-identity high-z episode — the mechanical link to a known data point was direct. HA-C4 v1's exposure is to crash-day IDENTITIES on heavy-T but NOT to Ch1-3 channel values on those days; the mechanical link from "knowing which days are crashes" to "biasing a tolerance threshold or a sample-size bar" is several steps removed and requires the drafter to model "would my v1 choice differ if these 10 days were different?" — which the L2.5 lens cannot verify directly.

Magnitude: substantive but closeable by user acceptance of the documented boundary as priced-in per the §3.2 clause, OR by a §3.2 fresh-session redraft of just the ±50% tolerance + ≥ 30 inconclusive bar choices (the two genuinely-drafter-chosen items) with the 10-day identity held out. The latter is the strictest-discipline path and would close the L2.5 concern cleanly; the former requires the user to accept the documented boundary.

### Layer 3 — Time-series specific (inherits from Natesan Batley 2023, WWC 2022, CENT 2015)

**[L3.1 — heavy-T LABEL sequence autocorrelation vs channel-value autocorrelation, [`permutation_null_block_length.md`](../methodology/permutation_null_block_length.md) + design MD §Cross-cutting hygiene] — minor.** §4.9 permutes the binary `is_heavy_T[d]` LABEL sequence in blocks (geometric-distributed, E[L]=7) while keeping channel values fixed. The project-canonical E[L]=7 is derived for daily physiological signals' autocorrelation (per [`permutation_null_block_length.md` §4](../methodology/permutation_null_block_length.md) — "empirical ACF observations for daily physiological signals typically decay within a week"). The heavy-T LABEL sequence has its OWN autocorrelation, driven by the sustained-push regime — heavy days cluster in pushes (the design MD's "chained regime" framing). The spec uses the same E[L]=7 for the label sequence and includes the E[L]\* data-driven companion (§4.9 + design MD's "data-driven `E[L]*` companion + factor-of-2 flag rule") as the empirical check.

Magnitude: minor. The E[L]\* companion is the discipline-preserving anchor here. Closure: a one-line acknowledgment in §4.9 that the E[L]=7 anchor is derived for channel-value autocorrelation and the label sequence may have a different effective E[L], with the data-driven E[L]\* on the label sequence as the empirical check. The factor-of-2 flag (§4.9 last paragraph) then activates as expected.

**L3.3 — multi-comparison discipline across 3 channels × 2 eras — does NOT fire.** v1 §5.0 pre-specifies the 3-channel triad with pass-2-of-3 verdict rule as the project-canonical multi-channel discipline (per the design MD §C4 lock). The rule is a structured aggregation that operates as a single-decision-cell at the verdict level: 4 verdict outcomes (3-of-3 / 2-of-3 / 1-of-3 / 0-of-3) plus a 0.5-counter for single-era confirmation, all pre-spec'd in §5.3. Holm step-down across the 3 channels per era is the secondary correction (§5.5), reported as a fragility flag and explicitly NOT verdict-driving. This satisfies [`hypothesis_lock_process.md` §3.8 gate 2](../methodology/hypothesis_lock_process.md#38-lock-step-stage-5-of-the-arc) ("single-cell headline lock OR explicit multi-comparison correction"). Clean.

**L3.4 (permutation null block length documented) — does NOT fire.** §4.9 cites [`permutation_null_block_length.md`](../methodology/permutation_null_block_length.md) verbatim; the geometric-distributed mean E[L]=7 stationary-bootstrap pattern is the canonical project default; B = 10,000 resamples; one-sided empirical p-value formula given. Clean.

### Layer 4 — Project-specific audit hooks (inherits from CONVENTIONS §3, §4)

**[L4.4 — crash-drop sensitivity row absent on a Layer-4 comparison touching heavy-T days, CONVENTIONS §3.4] — substantive.** §3.4 audit hook fires on Layer 4+ correlations / CCF / regressions touching PEM-pacing variables; the hook's spirit applies to any Mann-Whitney group comparison where one arm (heavy-T) intentionally pools known-crash days with non-crash heavy-T days. Per the Authorship block, the drafter knows that 10 of the unmedicated heavy-T days are crash episodes. Within the heavy-T pool the crash subset may dominate the "stuck sympathetic" tail (the C4 mechanism IS expected to be strongest on crash days, which is the §9.1 "protective-rather-than-predictive" reading's whole point). The §3.4 hook's question — "is the effect crash-driven or robust across the broader heavy-T pool?" — is exactly the question the spec leaves unanswered.

The spec includes §4.7 (chain-T+1 exclusion for Ch3) and §4.11 sensitivity companions for Ch1 (`stress_post_peak_drop_avg`, `stress_recovery_pct_within_2h`, NaN-fraction contrast) but does NOT include any sensitivity arm dropping `is_crash == True` from the heavy-T pool. The omission is more pointed because the drafter knows the 10 specific crash days; with knowledge of WHICH heavy-T days are crashes, choosing NOT to add the sensitivity row is harder to defend than under no exposure.

Magnitude: substantive. Closeable by one of:

- **(a)** Add a §4.11 sensitivity arm: re-run the per-channel Mann-Whitney with `is_crash == True` dropped from BOTH arms; report Δ Cliff's delta. If |Δ| > 0.10 on any channel, surface as a finding per §3.4. (Mirrors the HA-P7 §3.4 pattern.)
- **(b)** Explicit §3.4 dispatch in §8: a one-sentence rationale ("§3.4 inapplicable to primary: the heavy-T arm intentionally pools crash and non-crash heavy-T days because the C4 mechanism is expected to be strongest on crash days; the §9.1 'protective-rather-than-predictive' reading depends on this pooling; the descriptive companion in §4.11 reports per-channel crash-vs-non-crash distribution within the heavy-T arm"). This dispatch must be paired with the descriptive companion actually being added to §4.11.

Option (a) is the cleaner discipline-rebuilding move; option (b) preserves the spec architecture but requires the descriptive companion be added to §4.11. Either closes the L4.4 fire.

**[L4.5 — spike-detecting metric for sympathetic arousal, CONVENTIONS §3.5] — minor.** Channel 3's `awake_stress_avg` on T+1 is a daily mean. §3.5 prefers spike / peak / count metrics over averages for sympathetic-arousal proxies: *"a 5-minute spike of intense stress in a 24-hour day is dilution-invisible to a daily mean — and the user's lived-experience report is that exactly that shape (intense moment in an otherwise calm day) can trigger a crash."* For "next-day reactivity" the spec is testing a sustained-elevation reading (which an average captures well) rather than a spike reading. The Wiggers source quote v1 §2 cites for Ch3 — *"The day after you've done too much you can see stress spikes much faster, despite resting"* (PDF lines 1141-1143) — uses the word "spikes" explicitly, suggesting the spike reading IS the source-intended pattern.

Magnitude: minor. The spec's choice of mean is defensible (Garmin stress on a "the next day feels different" framing), but the spike-reading is closer to the verbatim Wiggers wording. Closure: add a sensitivity companion to §4.11 on `stress_high_duration_min` on T+1 (or `awake_stress_max` on T+1), report alongside the primary. If both readings agree, the average-vs-spike question is closed; if they diverge, the divergence is informative.

**[L4.7 — a-priori-class framing in §2 sister-test context and §9 outcome-branch labels, CONVENTIONS §4.2] — minor.** v1 §2 frames HA-C4 SUPPORTED as completing a "protective-rather-than-predictive" reading with HA-C4b NOT-SUPPORTED + HA11 SUPPORTED-on-train. §9.1 then attaches mechanistic interpretive scripts to each pre-spec'd 2-of-3 channel configuration ("the system catches up overnight" for Ch1+Ch2-not-Ch3; "the t+1 signal is the strongest" for Ch2+Ch3-not-Ch1; "a milder C4 expression" for Ch1+Ch3-not-Ch2). These are interpretive readings PRE-SPEC'D before any test data is observed; per CONVENTIONS §4.2 the cut rule applies — "presupposes X matters before any test has run; lifts X from confounder-acknowledgment to analytical basis".

Magnitude: minor. The §5.3 verdict bar itself is unaffected (the channel labels at the bar are descriptive: "Ch1+Ch2 confirmed, Ch3 not"); the interpretive scripts live below the bar at §9. But the result.md author inherits the interpretive scripts as a pre-staged reading, which subtly narrows the interpretation space. Closure: replace the mechanistic interpretive labels in §9.1 with verdict-invariant descriptive labels — "Ch1+Ch2 confirmed, Ch3 not — same-day phenomena present without next-day reactivity"; "Ch2+Ch3 confirmed, Ch1 not — walls + reactivity present, decay-time-to-rest in range". The result.md author then interprets against what the data actually shows.

**L4.2 (`_lagged_lcera` discipline) — does NOT fire.** `exertion_class_lagged_lcera` is the v3.2 lagged-baseline LC-era-restricted variant correctly chosen for a PEM-pacing hypothesis (per CONVENTIONS §3.2 — "default to `_lagged_lcera` for PEM-pacing tests gated on `lc_phase == 'lc'`"). Spec respects the audit hook.

**L4.3 (one column per definitional pair) — does NOT fire.** The four channels are functionally distinct: Ch1 (time-to-rest) ≠ Ch1-secondary `stress_post_peak_drop_avg` (mean drop magnitude) ≠ Ch2 `stress_high_duration_min` (count above threshold) ≠ Ch3 `awake_stress_avg` on T+1 (mean on next day). DATA_DICTIONARY §C4 doesn't flag near-identical pairs among these four. Clean.

**L4.6 (named counts: scheme + unit + source file) — does NOT fire.** §4.1 names heavy-T as `exertion_class_lagged_lcera ∈ {heavy, very_heavy}` with the methodology MD pointer; §6 names the April-2024 cluster exclusion with the methodology citation; the Authorship block names the 10 crash-day identities with explicit dates + the HA-C4b pool source. Clean.

**L4.8 (prior-driven hypothesis framed as confirmatory) — does NOT fire.** §2 explicitly invokes the prior structure: Wiggers verbatim quotes with PDF line refs (lines 1112-1119, 1140-1143, 1223-1231, 1306-1314, batch-2-verified 2026-06-12); lived-experience anchor via [`garmin_pacing_practice.md`](../methodology/garmin_pacing_practice.md); four prior-SUPPORTED autonomic-deviation precursors as the prior-strength evidence. Framing is confirmatory ("tests whether the pattern that the participant identifies experientially is reproducible at the daily-aggregate descriptive level"). Walk-forward gate (§4.10) preserved as the magnitude-calibration discipline. Clean per CONVENTIONS §4.3.

### Side observations

- **Side** — the cited [`lc_phase_descriptive.md`](../methodology/lc_phase_descriptive.md) §1176-§1230 line range covers only Channel 1's two metrics (`stress_post_peak_drop_avg` 1176-1192 + `stress_post_peak_time_to_rest_min` 1193-1209) plus a Ch1-sensitivity companion (`stress_recovery_pct_within_2h` 1210-1226). Channel 2 (`stress_high_duration_min` at lines 1159-1174) and Channel 3 (`awake_stress_avg` at lines 523-538) are outside the cited range. The §7 citation undershoots for Ch2/Ch3. Closed jointly with the L1.4 §7 rebuild.
- **Side** — §7 Channel 1 anchor reads *"Median (non-NaN): expected ~60-180 min based on the dictionary's coverage stats."* Coverage stats don't give median; the actual LC-aggregate p50 is 69.5 min (per lc_phase_descriptive.md line 1205). The anchor reasoning is malformed independent of the L1.4 phase-specificity concern.
- **Side** — §7 Channel 3 anchor cites coverage ("~98.6% coverage") as if it informed the range "~30-50". Actual LC-aggregate p50 for `awake_stress_avg` is 43 (per lc_phase_descriptive.md line 535), p25=37, p75=48. The anchor reasoning is similarly malformed.
- **Side** — §4.9 `RANDOM_SEED = 20260617` is distinct from HA-C4b v3's `20260615` per the spec; defensible. Worth confirming at test-time that the seed is passed through to all stochastic steps (block-permutation null + E[L]\* companion + any bootstrap variant in §4.11 sensitivity arms).
- **Side** — §4.4 *"v1 primary scope: unmedicated phase only (LC era start 2022-04-04 → 2024-04-08)"*. The §4.3 day-validity rule writes the gate as `T < 2024-04-09` (i.e. `2022-04-04 ≤ T ≤ 2024-04-08`). The `≤ 2024-04-08` boundary matches [`citalopram_phase_stratification` §3 unmedicated window](../methodology/citalopram_phase_stratification.md#3-the-four-phase-citalopram-traject-stratification) ("LC start → 2024-04-08"). Internally consistent.
- **Side** — the Authorship block says *"era anchors from HA-C4b v3 §8.x (cut at lock per option B; preserved in git history at commits `7595a8b` / `a7bf32a`)."* The git-history fallback for cut content is a reasonable discipline-preserving choice but is invisible from the v1 spec face; a future result.md author resolving the era anchors via this route should be cued by a §8 forward-pointer.
- **Side** — §4.2 Channel 2 NaN handling: *"No special NaN encoding ... days with NaN are excluded"*. §4.6 Channel 2 line repeats this. The two §s agree; redundant but not contradictory.

## 3. What does not fire (selective)

- **§3.8 gate 1 (power-calc dispatch) passes with strong evidence**. §8 carries the Daza 2018 within-subject design dispatch verbatim ("power calculation is inapplicable per Daza 2018 within-subject design — the n-of-1 corpus does not have separate treatment and control arms in the classical sense. The block-permutation null at E[L]=7 (§4.9) is the within-subject inferential machinery; the §5.1 (a) discrimination + (b) effect-size gates determine per-channel confirmation rather than a power-thresholded p-value."). The Authorship block §3.8 pre-statement (gate 1) names §8 as the dispatch location. Lock-blocking gate 1 met.

- **§3.8 gate 2 (multi-comparison discipline) passes**. §5.0 pre-specifies the 3-channel triad with pass-2-of-3 verdict rule as the project-canonical multi-channel discipline; §5.5 adds Holm step-down across channels per era as the within-test secondary correction. The combination is design-MD-locked at [`wiggers_test_design_on_chained_regime.md` §C4](../methodology/wiggers_test_design_on_chained_regime.md#c4--we-class-3-channel-confirmatory-triad). Single-decision-cell at the verdict level. Lock-blocking gate 2 met.

- **Citalopram-phase scope (§4.4 unmedicated-only) passes**. The dose-modulation citation is precise (β = +0.43-0.57/mg on stress-mean-sleep / all-day-stress-avg per [`citalopram_dose_response §5.6`](../methodology/citalopram_dose_response_stress_mean_sleep.md)). For Ch3 specifically `awake_stress_avg` is in the same family as the v3-CONFIRMED `all_day_stress_avg` — a ~17-point shift at 30mg would directly contaminate the heavy-T-vs-non-heavy-T comparison if pooled across phases. Restricting to unmedicated avoids the contamination cleanly. Sample-size loss is acknowledged in §8 ("unmedicated = pre-citalopram corpus, not 'no medication overall'") and the sensitivity arms on later phases (§4.4 paragraph 2) provide the descriptive companion read. Per-channel ≥ 30 inconclusive bar (§5.4) is the dry-run safety net.

- **Chained-regime adjustment for Ch3 (§4.7) passes**. The exclusion of heavy-T days where T+1 is also heavy is the cleanest way to handle the t+1-confound (the comparison is "the day AFTER overdoing it" — both days being heavy collapses the contrast). The sensitivity arm WITHOUT the exclusion (§4.7 last paragraph) provides the alternative read. Defensible inheritance of the design MD §C4 chained-regime pattern.

- **L3.4 + L3.1 inferential machinery passes**. §4.9 cites [`permutation_null_block_length.md`](../methodology/permutation_null_block_length.md) verbatim; stationary bootstrap with geometric-distributed mean E[L]=7; B = 10,000 resamples; one-sided empirical p-value formula `(1 + #{U_null >= U_observed}) / (B + 1)` (correct small-sample p-value with the +1 correction); E[L]\* data-driven companion + factor-of-2 flag pre-specified. The L3.1 minor concern above is about the L3.1 conceptual layer (label vs value autocorrelation), not the L3.4 mechanical layer.

- **§3.8 gate 4 (re-audit completed clean OR compression decision) — this audit IS the canonical §3.4 audit per the v1 Authorship block §3.8 pre-statement (gate 4). The verdict below dispatches the gate-4 status.

## 4. What would strengthen this finding

1. **Replace §7 anchors with `per_day_master.csv` medians + IQR on the unmedicated-only filter for the EXACT four channels under test**. Compute on `lc_phase == "lc" AND citalopram_phase == "unmedicated"` for: `stress_post_peak_time_to_rest_min` (non-NaN median; NaN fraction separately); `stress_post_peak_drop_avg`; `stress_high_duration_min`; `awake_stress_avg`. Cite the script-line + filter in §7 so the numbers are reproducible from a single script run. Inherits from CONVENTIONS §2.1 (operationalised measures defined with their source-file path) + [`hypothesis_lock_process.md` §5 sanity-check row](../methodology/hypothesis_lock_process.md#5-sanity-check-questions-before-lock) ("§7 ranges anchored to the EXACT column being measured"). Expected effect: closes the L1.4 substantive fire as a mechanical r2 fix; prevents the foreseeable dry-run §7 sanity-gate fail that would otherwise force a v2 redraft post-lock (the exact HA-C4b v1 lesson). If the unmedicated-only median is materially different from the LC-aggregate (likely for `awake_stress_avg` given the ~17-point dose-modulation), the ±50% tolerance can be reconsidered against the tightened anchor — possibly tightening to ±30% becomes defensible, partly closing the L2.5 concern on the tolerance choice.

2. **Add an explicit crash-drop sensitivity arm to §4.11 OR a one-sentence §3.4 dispatch in §8 paired with a descriptive companion**. Option (a): re-run the per-channel Mann-Whitney with `is_crash == True` dropped from BOTH arms; report Δ Cliff's delta in the result.md table; |Δ| > 0.10 on any channel surfaces as a finding per CONVENTIONS §3.4. Option (b): §8 dispatch — "§3.4 inapplicable to primary: the heavy-T arm intentionally pools crash and non-crash heavy-T days because the C4 mechanism is expected to be strongest on crash days (the §9.1 'protective-rather-than-predictive' reading depends on this pooling); descriptive companion in §4.11 reports per-channel crash-vs-non-crash distribution within the heavy-T arm." Inherits from CONVENTIONS §3.4 + the HA-C4b v3 audit's L4.4 closure pattern. Expected effect: closes the L4.4 substantive fire; under the §3.2 shared-context drafting, the explicit engagement is what insulates the spec from "drafter knew the 10 crash IDs and silently chose not to test sensitivity" — either option discharges that concern.

3. **Reframe the §9.1 outcome-branch labels to be verdict-invariant descriptive labels**. Replace the mechanistic interpretive scripts ("the system catches up overnight", "the t+1 signal is the strongest", "a milder C4 expression") with verdict-invariant descriptive scripts — "Ch1+Ch2 confirmed, Ch3 not — same-day phenomena present without next-day reactivity"; "Ch2+Ch3 confirmed, Ch1 not — walls + reactivity present, decay-time-to-rest in range"; "Ch1+Ch3 confirmed, Ch2 not — decay failure + reactivity present, sustained-high count in range". The result.md author then interprets against what the data actually shows. Inherits from CONVENTIONS §4.2 (caveat-class framing kept, a-priori-class framing cut). Expected effect: closes the L4.7 minor fire; removes interpretive flexibility risk surfaced when the data informs which 2-of-3 configuration fires.

4. **Add a one-line acknowledgment to §4.9 that E[L]=7 is derived for channel-value autocorrelation, with the data-driven E[L]\* on the heavy-T LABEL sequence as the empirical check**. The current §4.9 names E[L]\* but doesn't separate the channel-value-autocorrelation derivation (which gives the 7-day anchor) from the label-sequence-autocorrelation (which the permutation actually consumes). Inherits from [`permutation_null_block_length.md`](../methodology/permutation_null_block_length.md) §3 ("ACF varies by metric") + design MD §Cross-cutting hygiene item 4. Expected effect: closes the L3.1 minor flag; pre-emptively documents the foreseeable rolling-sum-predictor pattern surfaced in [`hypothesis_lock_process.md` §4.6](../methodology/hypothesis_lock_process.md#46-result-time--rolling-sum-predictor-structural-autocorrelation-factor-of-2-flag) (although heavy-T is not a rolling sum, the same factor-of-2 flag mechanics apply if the label autocorrelation exceeds 7).

5. **Add a Channel 3 spike-metric sensitivity companion to §4.11**. Run a parallel Mann-Whitney on `stress_high_duration_min` on T+1 (or `awake_stress_max` on T+1) alongside the primary `awake_stress_avg` on T+1. Report both in the result.md. Inherits from CONVENTIONS §3.5 (spike-detecting metrics over daily averages for sympathetic-arousal proxies) + the verbatim Wiggers wording ("stress spikes much faster" on T+1, PDF lines 1141-1143). Expected effect: closes the L4.5 minor flag; if both readings agree, the average-vs-spike question is closed; if they diverge, the divergence is informative for the §9 interpretation.

6. **Address the L2.5 substantive concern explicitly at lock-time** by either (a) the user accepting the documented data-exposure boundary as priced-in per the [`hypothesis_lock_process.md` §3.2 clause](../methodology/hypothesis_lock_process.md#32-drafting-step-step-1-of-the-arc), with the lock-commit explicitly naming this acceptance, OR (b) escalating to a §3.2 fresh-session redraft of the two genuinely-drafter-chosen items (±50% tolerance + ≥ 30 inconclusive bar) with the 10-day identity held out. The recommendation 1 closure (tighten §7 anchor → possibly tighten tolerance to ±30%) partly absorbs this if the tolerance is reconsidered against the rebuilt anchor. The strictest-discipline path is (b); the pragmatic path is (a). Either is a lock-time decision, not an r2 mechanical fix.

## 5. Verdict

**REVISION RECOMMENDED** — Layer 1 substantive fire (L1.4 §7 anchor specificity): the §7 unmedicated-phase distributional anchors cite coverage stats (which don't speak to median) + a [`lc_phase_descriptive.md`](../methodology/lc_phase_descriptive.md) line range that only covers Channel 1 and partitions by `pre_corona | corona_infection | lc` (not by citalopram phase); the citation undershoots for Ch2 and Ch3 and conflates LC-aggregate with unmedicated-only across a corpus where the v3-CONFIRMED `all_day_stress_avg` β = +0.57/mg implies a ~17-point shift on the sibling Ch3 channel between phases. This is the exact HA-C4b v1 failure mode that the [`hypothesis_lock_process.md` §5 sanity-check row](../methodology/hypothesis_lock_process.md#5-sanity-check-questions-before-lock) was added to catch and would foreseeably trip the §7 sanity gate at dry-run, forcing v2. Closeable mechanically by Section 4 item 1. Layer 4 substantive fire (L4.4 crash-drop) and Layer 2 substantive concern (L2.5 shared-context drafting) both closeable by Section 4 items 2 and 6 (option a or b) without §3.2 fresh-session escalation. All four §3.8 lock-blocking gates are either met in the spec wording (gates 1, 2, 4) or correctly pre-staged as lock-commit responsibilities (gate 3). The fires are mechanical r2 closures; no v2-redraft required.

### Verification of the handoff's eight binding questions for v1

1. **§3.2-clause shared-context drafting boundary (L2.5 analog)** — the data-exposure boundary IS documented honestly in the Authorship block. Walking the eight pre-stated operational choices: T-only eligibility, 3-channel definitions, chain-T+1 exclusion, Cliff's delta ≥ 0.20, p < 0.05, Holm secondary, NaN-as-1080 — all are either design-MD-inherited or independently defensible on their own terms (not data-driven by knowledge of 10-day identities). The two genuinely drafter-chosen items — **±50% tolerance** and **≥ 30 inconclusive bar** — are both justified by reasons orthogonal to the 10-day identity (HA-C4b v1 anchor lesson; sample-pool size). The L2.5 fire is substantive but materially weaker than HA-C4b v3's (which had a direct mechanical link to a known-identity episode); closeable by user acceptance of the documented boundary OR a §3.2 fresh-session redraft of just the two drafter-chosen items.

2. **Triad pre-specified verdict rule as multi-comparison discipline (§3.8 gate 2)** — passes. The 3-channel triad with pass-2-of-3 + Holm secondary is design-MD-locked + project-canonical. Single-decision-cell at the verdict level.

3. **Citalopram-phase scope (§4.4 unmedicated-only)** — defensible. The §5.B dose-modulation citation is precise; Ch3 `awake_stress_avg` is in the v3-CONFIRMED family (β = +0.57/mg on the sibling `all_day_stress_avg`); restricting to unmedicated avoids the contamination cleanly. Sample-size loss is acknowledged + sensitivity arms on later phases are reported descriptively.

4. **Chained-regime adjustment for Ch3 (§4.7)** — defensible. The "heavy-T followed by heavy-T+1" exclusion is the cleanest way to handle the t+1-confound; sensitivity arm WITHOUT exclusion provides the alternative read. The audit's specific question — "should the exclusion go further (e.g. also exclude T+1 = crash days)?" — is closeable by the L4.4 crash-drop sensitivity arm (Section 4 item 2 option a), which would implicitly cover that case.

5. **Channel 1 NaN-as-positive 1080-min encoding (§4.5)** — defensible alongside the per-arm NaN-fraction descriptive contrast (§4.11). The 1080 value is above the LC-aggregate max (842); Mann-Whitney is rank-based so the specific value only matters as "higher than any non-NaN observation". The encoding-fragility sensitivity arm (§9.4) is the right discipline against post-hoc rationalisation.

6. **Mann-Whitney + Cliff's delta + block-permutation null (§4.8 + §4.9)** — correctly specified. The block-permutation permutes the LABEL sequence (not the channel values), preserving channel autocorrelation. E[L]=7 is the project-canonical default; E[L]\* data-driven companion + factor-of-2 flag included. The L3.1 minor flag above (channel-value vs label-sequence autocorrelation derivation) is closeable by Section 4 item 4 wording.

7. **Sister-test cross-reference framing (§2 + §9)** — partially fires as L4.7 minor (a-priori-class framing in §9.1 outcome-branch labels). Closeable by Section 4 item 3 wording (verdict-invariant descriptive labels).

8. **§7 anchor calibration (the HA-C4b v1 lesson)** — fires as L1.4 substantive. The cited [`lc_phase_descriptive.md`](../methodology/lc_phase_descriptive.md) §1176-§1230 line range covers only Channel 1's two metrics + a Ch1 sensitivity companion; Ch2 (lines 1159-1174) and Ch3 (lines 523-538) are outside the cited range; AND the MD partitions by `pre_corona | corona_infection | lc`, not by citalopram phase, so unmedicated-only anchors cannot be derived from the cited source. Closeable mechanically by Section 4 item 1.

### §3.8 lock-blocking gates summary

| gate | requirement | status in v1 wording |
|---|---|---|
| **1. Power-calc dispatch** | one-line "inapplicable per Daza 2018" OR explicit calc | **met** — §8 carries the Daza 2018 within-subject design dispatch verbatim with the block-permutation null at E[L]=7 named as the within-subject inferential machinery |
| **2. Single-cell headline lock OR multi-comparison correction** | hard rule pre-registered | **met** — §5.0 pre-specifies the 3-channel triad with pass-2-of-3 verdict rule + Holm step-down across channels per era; design-MD-locked |
| **3. Register-row pointer** | wiggers_testable_hypotheses.md C4 row line 92 update OR non-supersession confirmation | **lock-commit responsibility** — v1 Authorship block §3.8 pre-statement says "C4 row line 92 will gain a forward pointer to this folder at lock-commit time. Currently the register points at the design MD; the lock-commit elevates that pointer to the formal HA-C4 pre-reg." The pointer is currently the design MD (`wiggers_test_design_on_chained_regime.md §C4`), so the lock-commit message must add the HA-C4-folder pointer. Path is correct; the audit cannot preempt the lock-commit |
| **4. Re-audit completed clean OR compression decision** | this audit | **met with REVISION RECOMMENDED** — this audit serves as v1's canonical §3.4 audit per the v1 Authorship block §3.8 pre-statement (gate 4). The verdict is REVISION RECOMMENDED on the §7 anchor weakness (L1.4 substantive) + crash-drop omission (L4.4 substantive) + shared-context drafting (L2.5 substantive); all closeable by mechanical r2 (Section 4 items 1, 2, 6) without §3.2 fresh-session escalation. Per [`hypothesis_lock_process.md` §3.6](../methodology/hypothesis_lock_process.md#36-re-audit-step-stage-4-of-the-arc), the r2 fixes here ARE mechanical (citation rebuilds + sensitivity-arm addition + wording-tightening; no architectural change), so §3.6 compression of the canonical re-audit may be acceptable IF the user judges Section 4 items 1, 2, 6 as discharged at r2 acceptance. The strictest-discipline path retains the canonical §3.6 re-audit in a fresh session |

Gates 1, 2, 4 are met at the spec wording / audit level. Gate 3 is a lock-commit-message responsibility (audit cannot preempt). Section 4 items 1 and 2 are the analytically critical pre-lock recommendations; items 3-5 are wording / discipline-record polish; item 6 is a lock-time user decision on L2.5 disposition.

---

## Methodology

This review walks the 4-layer checklist defined in
[reviews/README.md](README.md), anchored in:

- SCRIBE 2016 (Tate et al., [PMC4873717](https://pmc.ncbi.nlm.nih.gov/articles/PMC4873717/))
- CENT 2015 ([shamseer_2015_cent_consort_nof1.pdf](../literature/methodology/shamseer_2015_cent_consort_nof1.pdf))
- STROBE 2007 ([vonelm_2007_strobe_observational_checklist.pdf](../literature/methodology/vonelm_2007_strobe_observational_checklist.pdf))
- Daza 2018 ([daza_2018_self_tracked_n_of_1_counterfactual.pdf](../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf))
- WWC 2022 SCED standards ([wwc_2022_standards_handbook_v5_0.pdf](../literature/methodology/wwc_2022_standards_handbook_v5_0.pdf))
- Natesan Batley 2023 ([natesan_2023_nof1_evidence_reporting_systematic_review.pdf](../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf))

Project-specific audit hooks from
[../CONVENTIONS.md](../CONVENTIONS.md) §3 and §4.
