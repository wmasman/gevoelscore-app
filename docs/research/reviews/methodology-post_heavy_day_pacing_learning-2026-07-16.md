# Methodology review: Post-heavy-day pacing learning (methodology/post_heavy_day_pacing_learning.md)

**Target**: [../methodology/post_heavy_day_pacing_learning.md](../methodology/post_heavy_day_pacing_learning.md)
**Target commit**: `8d9a118` (2026-07-16 initial draft as producer-mode methodology MD sub-arc of parent Q24 MD). Working tree clean at review time; target file present at commit unchanged.
**Reviewer mode**: Claude (independent methodology peer reviewer per [CONVENTIONS §1.2](../CONVENTIONS.md#12-reviewer-mode-claude-reads--writes-a-reasoned-report--recommendations); producer-mode MD under §2.2 four-input bar). Fresh session — no exposure to the drafting context; doc-only knowledge.
**Review date**: 2026-07-16

---

## 1. What the MD specifies

An **operand definition** for two trajectory-based sub-arcs that inherit the parent Q24 MD ([`post_heavy_day_compensatory_rest.md`](../methodology/post_heavy_day_compensatory_rest.md) LOCKED r1, commit `58b7723`) machinery verbatim and layer stratification axes on top:

- **§3 — Q24 sub-part 3 phase-stratified pacing**: read per-outcome AUC / slope / peak-location / RTBT trajectory-summary panels for the compensatory-success pool per parent MD §7 across four locked `recovery_phase` buckets (`lc_pre_ergo` n=19 / `pacing_pre_citalopram_learning` n=12 / `pacing_habit_established` n=125 / `citalopram_modulated` n=158; total 314 gap=0 heavy-episode-ends). Each phase reads against its own phase-stratified matched-ordinary comparator; headline contrast is late-phase-pair vs early-phase-pair AUC magnitude on activity outcomes. Direction pre-commit (§3.4): learned-pacing = late-phase AUC magnitude EXCEEDS early-phase AUC magnitude (compensatory drop strengthens over LC recovery). Sensitivity axis: calendar year (5 buckets).
- **§4 — Dose-response pacing**: read AUC + slope contrasts between the parent MD §9.1 intensity-stratified triggers (combined n=125 / very_heavy_only n=52 / heavy_only n=73 at +3d strict-clean). Direction pre-commit (§4.4): `|AUC_very_heavy| > |AUC_heavy|` on compensatory-success activity outcomes; RTBT longer for very_heavy. Literature anchor: Van Campen 2020 severity-scaling of physiological deficit → within-participant behavioural-compensation-scaling transfer.
- **Discipline pre-commits**: (a) §3.5 rescue vs non-rescue subset framing for the 36 raw-only-significant Wave 1 drift-entangled cells (rescue = monotone AUC-magnitude strengthening across phases in pre-committed direction; non-rescue = flat or reversed); (b) §3.6 100% temporal alignment of `citalopram_modulated` phase with post-citalopram-onset era pre-committed as inherited caveat with three named-but-not-executed disambiguation designs (within-citalopram post-hoc split, pre-citalopram-only companion, citalopram-native effect subtraction); (c) §4.6 descriptive-only-at-Stage-D on dose-response (no between-strata inferential test pre-committed); (d) §6.3 intensity × phase cross-tab probed and documented (roughly balanced across phases: heavy fraction 42-56%).
- **Reporting discipline**: §3.6 pre-commits that sub-part 3 findings are always named "phase-stratified" (never "learned-pacing") in Stage D descriptive audit output; interpretation as learned-pacing vs citalopram-modulation deferred to Stage S1 (internal synthesis) at the earliest.
- **Inheritance scope** (§2): stratum, unit, comparator, windows, overlap, pool split, nine trajectory summary stats, `linear_detrend_on_pre` per §7.11, B=10,000 block-bootstrap per §7.10, zero-vs-NaN policy per §11 — all inherited verbatim from parent Q24 MD LOCKED r1 by pointer table. Explicit no-re-derivation gate.

Downstream: Stage D descriptive audit consumes the operand only after r2 lock (mirror of parent MD's compression discipline). Stage H per-HA pre-regs on either arc are downstream sessions gated on Stage D verdicts.

**Data-availability verified independently** (probe against `$GEVOELSCORE_DATA_PATH/unified/per_day_master.csv`, LC-era stratum n=1524, 2026-07-16): per-phase heavy-episode-end counts 19/12/125/158→314 reproduce §3.1 byte-identically; year distribution 44/87/81/66/36→314 reproduces §3.1 sensitivity table; intensity × phase cross-tab reproduces §6.3 byte-identically; the 100% citalopram temporal-alignment claim (§3.6: n=158 citalopram_modulated episode-ends = n=158 post-2024-04-09 episode-ends in that phase; zero pre-onset in that phase; zero post-onset in the three pre-cital phases) verifies.

---

## 2. What fired and why

### Spine — §2.2 four-input bar (inherits from CONVENTIONS §2.2)

#### I1 — Best-practices standards

- **I1.1 — Change-point / segmentation state-of-art not named on the phase axis [minor].** The §3 phase axis is inherited verbatim from [`lc_recovery_phase_axis.md`](../methodology/lc_recovery_phase_axis.md) r2 (lived-experience + intervention-evidence M1/M2 warrant classes), which is the correct precedent for a hybrid subject-anchored / event-anchored segmentation. But the change-point-detection state-of-art (PELT [Killick et al. 2012], Bayesian online [Adams & MacKay 2007], `ruptures` library) is not named-and-rejected anywhere in the target MD for the "should we validate the four boundaries algorithmically?" question. Silence, not rejection-with-reason — the I1.2 failure mode. Given the axis is inherited from a locked sister MD that itself makes the M1/M2 warrant call, this is a minor fire, not substantive: a one-line pointer at §3.1 forwarding to `lc_recovery_phase_axis.md` for the boundary-validation-declined-with-reason rationale would close.
- **I1.2 — Within-subject dose-response state-of-art not named [minor].** §4.3-§4.5 lock a between-strata AUC / slope descriptive contrast on very_heavy vs heavy triggers, with the Van Campen 2020 severity-scaling anchor materially cited (§4.5). The corresponding state-of-art for within-subject dose-response on continuous outcomes — random-slope mixed-effects models (Newey-West HAC SE for autocorrelation), or the SCED dose-response family in WWC 2022 Handbook §V — is not named. The MD explicitly rejects a between-strata inferential test at Stage D (§4.6), which partly absolves the fire, but the state-of-art for the inferential form the arc might take at Stage H is silent. A one-line "at Stage H this becomes a mixed-effects or SCED dose-response fit; not pre-committed here" would close.
- **I1.3 — Envelope-drift decomposition standard partially named [pass, partial].** §3.6 explicitly names five confounders that the four-phase axis conflates (learned-pacing, citalopram-modulation, envelope drift, seasonality, aging) and correctly points to `linear_detrend_on_pre` per parent §7.11 as the smooth-component removal machinery. The MD is admirably honest that the detrend cannot separate learned-pacing from citalopram-modulation because they are 100% temporally aligned. What is not named is any state-of-art for confounder identification under 100% temporal alignment at n=1 — there isn't one that survives the alignment structurally, which is exactly why §3.6 names three disambiguation designs and defers them. Naming this explicitly ("no state-of-art design identifies learned-pacing from citalopram-modulation at n=1 under 100% temporal alignment; the three §3.6 disambiguation designs each attack a different degree of freedom without resolving") would sharpen the framing.

#### I2 — Established literature

- **I2.1 — Van Campen 2020 severity-scaling anchor is material [pass, non-trivial].** §4.5 cites Van Campen 2020 with an explicit sentence on what the paper's evidence contributes (day-2 CPET severity-scaling of physiological deficit → within-participant behavioural-compensation-scaling transfer). Ghali 2020 + Moore 2023 corroborating anchors also materially cited. The **transfer caveat** ("between-participant severity axis → within-participant intensity stratum; literature-anchored direction, not literature-proven at n=1") is exactly the §2.2 discipline of naming what the citation does not do. Best-in-class handling.
- **I2.2 — Learned-pacing / pacing-envelope literature not cited for §3.4 direction pre-commit [substantive].** The learned-pacing direction pre-commit in §3.4 (later-phase AUC magnitude exceeds earlier-phase AUC magnitude on activity outcomes = "compensatory drop strengthens over time") is the load-bearing directional claim of the phase-stratified arc. This claim has substantial published grounding in the ME/CFS + Long COVID pacing literature: **Goudsmit et al. 2012** on the envelope-theory / pacing framework (activity within the sustainable envelope prevents PEM); **Nijs et al. 2013** on activity self-regulation adaptation over time in CFS; **Sanal-Hayes et al. 2023** on pacing effectiveness in Long COVID. The [`lc_recovery_phase_axis.md`](../methodology/lc_recovery_phase_axis.md) sister MD already notes these are "candidate anchors not yet fetched into `docs/research/literature/`", but the direction pre-commit in §3.4 is where the literature would materially anchor the reading — without at least a placeholder citation-status note ("direction pre-commit is derived from lived experience + envelope-theory prior; specific pacing-adaptation literature anchors deferred per `lc_recovery_phase_axis.md` citation-status paragraph"), the direction reads as first-principles reasoning where established literature actually exists. Substantive because it affects §4.3 in [CONVENTIONS](../CONVENTIONS.md#43-prior-driven-hypotheses-are-confirmatory-not-exploratory) prior-driven framing: with the pacing-adaptation literature anchor named, the phase-stratified arc is confirmatory of a literature-anchored prior; without it, the direction reads as post-hoc-motivated.
- **I2.3 — Radin 2024 nightly-HR-persistence not carried into §4 dose-response direction [minor].** Parent MD §6.2 explicitly cites Radin 2024 as anchor for the `sleep_hr_avg_spo2` per-channel decay screen. Sub-part 4 in §4.5 discusses Van Campen behavioural-compensation scaling but is silent on the analogous autonomic-severity-scaling reading (Radin 2024 documents Long COVID RHR trajectories that diverge from controls for up to a year — a natural dose-response prior at the physiological channel). Not load-bearing since the §4 outcome family is activity-only (per §4.2), but a one-line acknowledgement that the dose-response reading on autonomic channels would carry additional literature grounding via Radin 2024 would flag the extensibility.
- **I2.4 — pem_recovery_trajectory_review literature-anchor cross-ref is material [pass].** §4.5 cross-refs the local synthesis literature review at [`literature/reviews/pem_recovery_trajectory_review.md`](../literature/reviews/pem_recovery_trajectory_review.md) §5 with the correct quote ("a minor dip should recover faster than a major crash, and recovery length is a severity signal") from that review's deliverable 3. The review itself materially anchors Van Campen 2020 + Ghali 2020 + Moore 2023 with DOIs.

#### I3 — Tradeoff vision

- **I3.1 — Phase-axis-vs-year-axis tradeoff explicit [pass].** §3.1 primary axis = `recovery_phase` (four buckets) + sensitivity axis = calendar year (5 buckets) with rationale explicit ("year axis reveals within-phase variation that the four-bucket schema collapses, especially `citalopram_modulated` spanning 2024Q2 through 2026Q2, a ~2-year window across which compensatory behaviour may have continued to evolve independently of citalopram onset"). Alternative names + declines with reason.
- **I3.2 — Two-arc split rationale explicit [pass].** Authorship block third paragraph ("Sister MD split rationale") + §1.2 + §1.3 name why sub-part 3 + dose-response are co-located (both trajectory-based magnitude outcomes on the parent MD machinery) and why sub-part 5 + streak-→-crash live in the sister [`heavy_day_crash_risk_prediction.md`](../methodology/heavy_day_crash_risk_prediction.md) (predictive-categorical outcomes, structurally different machinery). Alternatives named + rejected with reason.
- **I3.3 — Late-vs-early phase-pair contrast tradeoff explicit [pass, partial].** §3.3 pre-commits the primary contrast as `late-pair AUC vs early-pair AUC` where late = (`pacing_habit_established` + `citalopram_modulated`, n=283) and early = (`lc_pre_ergo` + `pacing_pre_citalopram_learning`, n=31). The sample-size case ("early n=31 combined vs late n=283") is stated explicitly. What is less-explicitly framed is why *pairing* rather than a monotone-across-4-phases test — a 4-phase monotone contrast (Jonckheere-Terpstra trend test at n=314 is well-powered on the composite; individual phase-level pooling loses information) is not named. This is a minor fire on I3.4 grounds: the pairing choice is one of multiple choices, alternatives are not named + not rejected. A one-line "monotone-across-4-phases contrast (Jonckheere-Terpstra or similar) is a natural alternative; deferred because §3.6 100% citalopram alignment makes the 4-phase monotone reading conflate learned-pacing with citalopram in the same reading, whereas late-vs-early pairing at least isolates the pre-cital / post-cital split as a single degree of freedom" would close.
- **I3.4 — Dose-response descriptive-only vision explicit [pass, non-trivial].** §4.6 gives three explicit rationales for descriptive-only-at-Stage-D on dose-response (sample-size floor at very_heavy_only +5d n=19; multiplicity add of one contrast on top of parent MD's 15-cell trajectory verdict matrix without literature-anchored a-priori-direction; composite-artefact risk in scaling-reversed reading). Alternatives named, rationale tied to the specific corpus constraints. Best-in-class §2.2 tradeoff-vision discipline on this arc.
- **I3.5 — 3-disambiguation-design menu explicit but not scoped [substantive].** §3.6 names three disambiguation designs (within-citalopram post-hoc phase re-split, pre-citalopram-only companion, citalopram-native effect subtraction) with a sentence each on what the design would attack — good. But the tradeoff vision on *which* of the three to prioritise for a downstream arc, or *whether* any of them structurally identifies learned-pacing at n=1 under 100% temporal alignment, is silent. This is load-bearing because sub-part 3 findings that fire in the late-phase-strengthening direction cannot be interpreted as learned-pacing without one of these disambiguation designs running — the target correctly names this as a caveat (§3.6, §6.1) and defers the interpretation to Stage S1 at the earliest, but the reader is left without a call on which design has the highest disambiguation return on the corpus's remaining degrees of freedom. See item 6 of Section 4 for a specific fix.

#### I4 — Research limitations + objectives

- **I4.1 — n=1 acknowledged where it bites [pass].** §3.3 "Sample-size warning up front" explicitly flags the early-phase pool floor (n=19 + n=12 = 31 combined) with per-window strict-clean sample floors estimated in §6.4 (~3-5 at +5d for `lc_pre_ergo`; ~2 at +5d for `pacing_pre_citalopram_learning`, flagged narrative-only). §4.6 anchors sample-size floor at very_heavy_only +5d n=19. §3.6 explicitly acknowledges that disambiguating learned-pacing from citalopram-modulation at n=1 under 100% temporal alignment requires additional design.
- **I4.2 — Observational design acknowledged [pass].** §3.6 explicitly names the 100% temporal confound as the load-bearing observational-design limit; §6.2 pre-commits envelope-drift as the load-bearing confound for trajectory reads (extends parent MD §10 item 8); §6.3 pre-committed intensity × phase cross-tab surfaces the structural check for whether intensity clustering by phase creates a phantom dose-response signal (it does not — cross-tab is roughly balanced).
- **I4.3 — Descriptive-vs-inferential objective stated [pass].** §1.2 disavows pre-reg framing ("NOT a pre-registration ... no falsifier or Stage H inferential test is locked here"); §3.4 + §4.4 direction pre-commits explicitly labeled as reading axis per parent §7.7 discipline with opposite-direction findings named as sign-inversions; §4.6 pre-commits descriptive-only-at-Stage-D on dose-response.
- **I4.4 — Corpus-specific constraints acknowledged [pass, partial].** LC recovery trajectory (§3.6 confounder enumeration includes it explicitly), citalopram onset date (§3.6 100% alignment), envelope drift (§6.2). What is not named as a corpus-specific constraint: the participant's **tactical Garmin use pre-2026-Q2** (per `project_garmin_research_bias_boundary` — tactical pacing via real-time signals + body signals + ergo principles across the entire LC-era) as an unmodeled independent confounder for the phase-stratified arc. If tactical-Garmin-pacing improved over the LC-era in parallel with the "learned-pacing" the target hopes to detect, then a late-phase strengthening finding conflates learned-pacing-in-the-behavioural-sense with better-tactical-Garmin-use, not only with citalopram. Substantive-partial: the confound is real and load-bearing for the phase-stratified interpretation, but is only implicitly covered by the umbrella "learned-pacing" that §3.6 defers to Stage S1. See Section 4 item 3.

### Layer 1 — Discipline gates (inherits from CONVENTIONS §2.1, §4.1-§4.3)

- **L1.1 — §2.1 descriptive-before-inference [pass, non-trivial].** §1.2 explicitly disavows Stage H pre-reg framing and gates any Stage H authorship on Stage D descriptive audit landing first; §4.6 explicitly disavows a between-strata inferential test at Stage D on dose-response. This is textbook §2.1 discipline. Additionally the compression-and-lock discipline in §8 gates any Stage D output on r2 lock.
- **L1.2 — §4.1 no interpretive marks [pass, non-trivial and load-bearing].** The §3.6 pre-commit that sub-part 3 findings are always named "phase-stratified" (never "learned-pacing") in Stage D output — and that the interpretation as learned-pacing vs citalopram-modulation is deferred to Stage S1 at the earliest — is precisely the caveat-class framing §4.1 exists to enforce. The MD refuses to bake "learned-pacing" as a measurement-procedure name into a descriptive operand definition, even though the sub-part is *called* "phase-stratified pacing" at the arc level. §6.1 reinforces this reporting discipline. Best-in-class discipline on the most naming-load-bearing choice in the MD.
- **L1.3 — §4.2 caveats vs a-priori [pass].** §3.6 caveat-class framing on citalopram entanglement is textbook §4.2: "acknowledged as an uncorrected confounder ... does NOT attribute a late-phase-strengthening finding to learned-pacing alone. The pre-committed reading is that a late-phase-strengthening finding is consistent with learned-pacing but also consistent with citalopram-modulation and cannot be distinguished from it on the four-phase axis alone." Neither revisionist (does not remove the observation) nor a-priori (does not build methodology on citalopram-modulation being the answer). §3.5 rescue vs non-rescue framing is similarly caveat-class.
- **L1.4 — §4.3 prior-driven framing [pass, partial].** The Q24 sub-part 3 question has strong prior sources (envelope theory, HA-C4c precedent, lived experience — the participant learned pacing per the phase-axis definitions) so confirmatory framing is justified per §4.3. But as flagged in I2.2 above, the confirmatory framing would be sharpened by explicit citation of the envelope-theory / pacing-adaptation literature that materially anchors the direction pre-commit; without it, the confirmatory framing rests on lived-experience-only prior at first-principles level.

### Layer 2 — Observational n=1 (inherits from Daza 2018)

- **L2.1 — Within-subject counterfactual framing stated [pass].** The matched-ordinary comparator is inherited verbatim from parent MD §4.1 set-membership matching; §3.3 correctly extends the discipline to phase-stratified matched-ordinary (each phase's trajectory reads against its own phase-stratified matched-ordinary comparator pool drawn only from same-phase days).
- **L2.2 — Stationarity assumption acknowledged where it bites [pass, load-bearing].** §6.2 elaborates the envelope-drift confound and points at parent MD §10 item 8 for the escalation-to-caveat rule; §3.6 acknowledges the underlying non-stationarity of the four-phase segmentation with explicit disambiguation-designs deferred. This addresses the L2.2 stationarity concern head-on and correctly ties it to the parent MD's era-stratified-comparator sensitivity arm rule.
- **L2.3 — Calendar-time vs subject-time framing clear [pass].** LC-era stratum locked; `recovery_phase` bucket boundaries traceable to `lc_recovery_phase_axis.md` §2; calendar-year sensitivity axis provides the calendar-time reference (§3.1). Both axes are correctly framed as *subject-time-anchored* (LC recovery phases + calendar year within LC).
- **L2.4 — Data provenance traceable [pass].** Every count in §3.1 + §6.3 + §7 is probed against `per_day_master.csv` 2026-07-16 with column names explicit + values reproducible (verified independently: all 6 counts probed at review time reproduce byte-identically). Extractor pointer for `recovery_phase` is `pipeline/03_consolidate/build_unified_dataset.py` (§3.1). Inheritance table (§2) traces each machinery element to a specific parent MD section.

### Layer 3 — Time-series specific (inherits from Natesan Batley 2023, WWC 2022, CENT 2015)

- **L3.1 — Autocorrelation [pass, inherited].** §2 inheritance table + §5 explicit "Bootstrap (B=10,000, block-length-1 for strict overlap, block length ≥ w for inclusive)" pointer correctly inherits parent MD §7.10 machinery. Additionally §5 pins that "per-phase per-outcome bootstrap runs use each phase's own episode-end pool as the resample space" — the right adaptation of parent MD block-length-1 discipline for the phase-stratified arc. No fire.
- **L3.2 — Trajectory-detrend sensitivity per CONVENTIONS §3.7 [pass, non-trivial].** §5 inheritance + §6.2 explicit "raw and detrended per-phase trajectory-summary panels reported side-by-side" pre-commit correctly extends the parent MD §7.11 discipline to the phase-stratified arc. §3.5 rescue vs non-rescue framing is the sharpest handling of the detrend-fragility question the reviewer has seen in the Q24 MD family: the rescue framing correctly recognises that per-phase detrend cannot remove the pacing-learning drift (§3.5: "if the within-phase detrended read collapses in every phase but the cross-phase AUC-magnitude trend is monotone, the reading is that the drift being detrended out at the corpus level IS the pacing-learning signal (which the per-phase detrend then removes twice-over)"). This is exactly the drift-entanglement discipline the Stage D descriptive audit §9.1 caveat 9 pre-committed as the Wave 1 → sub-part 3 disambiguation link.
- **L3.3 — Multiplicity [pass, inherited].** §5 inheritance table correctly points at parent MD §7.10 (descriptive-only at Stage D, no multiplicity correction; Stage H uses parent MD single-cell headline + Holm step-down). No fire.
- **L3.4 — Level vs trend [pass, non-trivial].** §3.5 explicitly acknowledges the corpus-general level-vs-change disagreement framing from Stage D audit §10 item 8 (r4 pickup flag) and pre-commits that sub-part 3 phase-stratification "does not resolve level-vs-change disagreements on its own, but it does provide a phase-conditional read of each channel's level+change trajectory that is downstream-interpretable at Stage S1". Reports per-phase pre-window mean levels alongside AUC magnitudes rather than silently compressing. Best-in-class handling of a subtle CONVENTIONS §3.7-adjacent concern.
- **L3.5 — State-of-art for the specific time-series question [minor].** As noted in I1.1, algorithmic change-point-detection is not named for the phase-axis validation question; as noted in I1.2, mixed-effects / SCED dose-response is not named for the §4 within-subject dose-response question. Layer-1-descriptive framing partially absolves both. Naming both (adopted / rejected with reason) would close.

### Layer 4 — Project-specific audit hooks (inherits from CONVENTIONS §3)

- **L4.1 — §3.1 personal baseline [pass].** Heavy-day trigger inherited from parent MD §2.2 uses `_lagged_lcera` (personal-baseline anchored); phase axis is subject-anchored per `lc_recovery_phase_axis.md`.
- **L4.2 — §3.2 lagged-lcera variant [pass].** Trigger operand is `exertion_class_lagged_lcera` per parent §2.2 inheritance.
- **L4.3 — §3.3 one column per definitional pair [pass].** §3.2 outcome table + parent MD §6.1 handling explicitly names `effective_exertion_min` primary + companion siblings.
- **L4.4 — §3.4 crash-drop sensitivity [not applicable, redirected].** Handled at parent MD §3.5 pool-split level (compensatory-success vs compensatory-failure); the target correctly inherits without re-deriving.
- **L4.5 — §3.5 spike-detecting metrics [not applicable].** §3 + §4 outcome family is activity-only per §3.2 + §4.2; spike-detecting metrics are the autonomic-family handling in the parent MD.
- **L4.6 — §3.6 named counts [pass, non-trivial].** Every count in §3.1 + §3.3 + §4.1 + §6.3 + §7 names labelling scheme + unit + source file + predicate + probe date. Independent verification against `per_day_master.csv` 2026-07-16 reproduces all 6 counts probed byte-identically.
- **L4.7 — §3.7 trajectory-detrend sensitivity [pass, inherited].** See L3.2 above.
- **L4.8 — §3.10 operationalisation faithful to data [pass, non-trivial].** The 100% citalopram temporal-alignment finding in §3.6 is a mechanical partition-identity check: "n=158 in that phase × 158 post-onset = 158, cross-tab confirmed" — corpus identity asserted with the day-count it holds on. §7.1 heavy-episode-end counts per phase are corpus counts (not spot-checks). §6.3 intensity × phase cross-tab is a partition identity on `(recovery_phase, exertion_class_lagged_lcera)` with total 314 asserted. Best-in-class §3.10 discipline — every count in the MD passes the "single-day spot-check standing in for a corpus identity is a fire" bar.

### Side observations

- **Side** — §3.1 primary axis table gives the four `recovery_phase` bucket window bounds. The `pacing_habit_established` end date is stated as `2024-04-08` (i.e. the day before citalopram_modulated starts at 2024-04-09). The `lc_recovery_phase_axis.md` §2 canonical helper `if d < date(2024, 4, 9): return "pacing_habit_established"` implies the bucket boundary is `[start, 2024-04-09)` half-open — same behaviour, minor phrasing inconsistency. Not a fire.
- **Side** — §3.1 sensitivity-axis year table lists 2022 as "partial Apr-Dec" (44 episode-ends) and 2026 as "partial Jan-Jun" (36 episode-ends). Consistent with LC-era bounds 2022-04-04 → 2026-06-05. No fire.
- **Side** — §4.1 intensity-stratification sample table gives the "Very-heavy only" strict-clean sample as **52** at +3d. Parent MD §9.1 lists this same figure as **52** for the "very_heavy_only + combined-clean at +3d" cell — matches. The target correctly inherits parent §9.1's cross-stratum scan discipline ("intensity-stratified triggers use combined heavy as the scan set for the strict-clean overlap filter"). No fire.
- **Side** — §4.5 Van Campen 2020 citation gives DOI + venue but not PMID; the parent MD's own literature review [`literature/reviews/pem_recovery_trajectory_review.md`](../literature/reviews/pem_recovery_trajectory_review.md) §9 gives PMID 32629923 for Van Campen 2020. Trivial addition-worth-considering.
- **Side** — §6.4 sample-size table gives per-phase strict-clean floor estimates at +5d ("~3-5" for `lc_pre_ergo`; "~2 (narrative-only)" for `pacing_pre_citalopram_learning`) as "order-of-magnitude only ... exact per-phase strict-clean sample floors must be computed at Stage D". Correct discipline — the estimates are honest, and the exact numbers depend on the pool-split × phase intersection that Stage D will produce. No fire.

---

## 3. What does not fire (selective)

Non-trivial passes worth naming:

- **L1.2 (§4.1 no interpretive marks) passes with the "phase-stratified" naming discipline as the sharpest example seen in the Q24 MD family.** §3.6 pre-commits that findings are always named "phase-stratified" (never "learned-pacing") in Stage D output; §6.1 reinforces the reporting discipline. This refuses to bake an imputed mechanism (learned-pacing) into a measurement-procedure name for a construct where the mechanism is confounded by a documented pharmacological intervention. The pattern is a paradigm example of §4.1 discipline that other methodology MDs could adopt when a construct name matches a confounded interpretation.
- **L3.2 (§3.7 trajectory-detrend sensitivity) passes with the §3.5 rescue vs non-rescue framing as a sharper handling than the parent MD provides.** The parent MD §7.11 sets up the raw + detrended side-by-side discipline; the target extends it into an explicit rescue framework: monotone AUC-magnitude strengthening across phases in the pre-committed direction is a candidate for reclassification from nuisance-drift to compensatory-behaviour-improvement-drift, precisely because the corpus-level detrend cannot separate the two. This is the Wave 1 → sub-part 3 disambiguation link Stage D audit §9.1 caveat 9 pre-committed, correctly operationalised at design time.
- **I3.4 (dose-response descriptive-only tradeoff) passes non-trivially.** §4.6 three-part rationale (sample-size floor + multiplicity add + composite-artefact risk) is exactly the §2.2 four-input tradeoff-vision discipline cleared with room to spare. The composite-artefact-risk reading in particular ("if between-strata contrast shows scaling reversed, the primary reading is composite-class-label misspecification, which is a definition-audit finding, not a physiological-scaling test") is a subtle L4 audit-hook-adjacent insight — the same discipline the parent MD §6.1 applies to definitional-pair composite outcomes.
- **L4.8 (§3.10 operationalisation faithful to data) passes with the 100% citalopram temporal-alignment finding as a mechanical corpus-identity check.** §3.6 asserts "n=158 in that phase × 158 post-onset = 158, cross-tab confirmed" — this is exactly the CONVENTIONS §3.10 discipline: an identity asserted on the whole corpus with the day-count it holds on, not a single-day spot-check. Verified independently at review time. Best-in-class handling of a load-bearing partition identity.
- **§8 compression-and-lock discipline mirrors parent MD §12 verbatim.** Explicit no-re-derivation gate ("any reviewer finding that this MD re-derived a piece of parent MD machinery ... is a re-derivation-violation and forces pre-lock removal of the re-derivation") is a producer-mode-discipline pattern that guards against inheritance drift; other sub-arc methodology MDs could adopt it.

---

## 4. What would strengthen this MD

Concrete revisions, each named + rationale + expected effect:

1. **Add a citation-status paragraph naming the envelope-theory / pacing-adaptation literature anchor for the §3.4 direction pre-commit.** Cite Goudsmit et al. 2012 (envelope theory) + Nijs et al. 2013 (activity self-regulation in CFS) + Sanal-Hayes et al. 2023 (pacing in Long COVID) as the literature anchor for the "compensatory drop strengthens over time" direction, or forward-cite the citation-status paragraph in [`lc_recovery_phase_axis.md`](../methodology/lc_recovery_phase_axis.md) which flags these anchors as "not yet fetched into `docs/research/literature/`". **Inherits**: §2.2 I2 material-citation discipline + §4.3 prior-driven framing (a literature-anchored prior converts the phase-stratified arc from lived-experience-only prior to confirmatory-of-established-literature). **Expected effect**: closes I2.2 substantive fire; sharpens the L1.4 partial pass into a full pass.

2. **Add a §3.6 disambiguation-design tradeoff paragraph.** For the three disambiguation designs named (within-citalopram post-hoc split, pre-citalopram-only companion, citalopram-native effect subtraction), add one paragraph on (a) which is highest-priority for a downstream arc and why, (b) which structurally CAN identify learned-pacing at n=1 vs which only shifts the confound to a different degree of freedom, (c) sample-size + feasibility per design. Design 1 (within-citalopram sub-split) uses the same n=158 pool, does not require additional data, and directly disambiguates citalopram-onset-step from within-citalopram-era learned-pacing continuation — likely the highest-return design. Design 2 (pre-citalopram-only companion) is n=156 across 3 phases and 2 years, workable at Stage D descriptive-with-CI. Design 3 (citalopram-native subtraction) depends on the sister `citalopram_phase_stratification.md` operand at compatible granularity — highest-methodology-cost. **Inherits**: §2.2 I3 tradeoff-vision discipline. **Expected effect**: closes I3.5 substantive fire; converts the current "three named but not scoped" into a "three named + reasoned priority for the downstream arc".

3. **Add tactical-Garmin-use as a §3.6 or §6.1 corpus-specific confounder.** Per project memory `project_garmin_research_bias_boundary`, the participant used Garmin tactically (real-time signals + body signals + ergo principles) across the entire LC-era pre-2026. If tactical-Garmin-pacing improved over the LC-era in parallel with any behavioural learned-pacing, the phase-stratified arc conflates the two. Add one sentence explicitly acknowledging this as a fifth confound bundled into the four-phase axis (alongside disease natural history, citalopram, deconditioning, seasonality), and note that no design at n=1 separates tactical-Garmin-improvement from behavioural-learned-pacing on the same corpus. **Inherits**: §2.2 I4 corpus-constraints discipline + project memory. **Expected effect**: closes I4.4 substantive-partial fire; makes the §6.1 reporting discipline ("phase-stratified" never "learned-pacing") load-bearing on a *fifth* named confound rather than a four-confound bundle.

4. **Add a §3.3 monotone-across-4-phases contrast pointer.** In the primary contrast subsection, add one sentence naming Jonckheere-Terpstra (or comparable monotone-trend contrast on the composite of four ordered phases) as an alternative to the late-vs-early pairing, and reject with reason: the 4-phase monotone reading conflates learned-pacing with citalopram in the same reading, whereas late-vs-early pairing at least isolates the pre-cital / post-cital split as a single degree of freedom that the disambiguation designs (§3.6) can attack directly. **Inherits**: §2.2 I3 tradeoff-vision discipline. **Expected effect**: closes I3.3 partial fire; makes the pairing choice explicit rather than implicit.

5. **Add a one-line pointer to `lc_recovery_phase_axis.md` at §3.1 explaining that boundary validation (change-point detection) was declined-with-reason at the axis-lock level.** The `lc_recovery_phase_axis.md` r2 lock at 2026-06-19 makes M1 lived-experience + M2 intervention-evidence the boundary warrant class rather than data-driven change-point detection; the target does not need to re-derive this call, only to point at where it lives. **Inherits**: §2.2 I1 best-practices-standards discipline (algorithmic change-point-detection state-of-art named at the sister MD, adopted-or-rejected explicitly). **Expected effect**: closes I1.1 minor fire.

6. **Add a §4 pointer to WWC 2022 SCED §V (or mixed-effects state-of-art) as the Stage-H-form the dose-response arc would take.** One sentence naming that the descriptive-only-at-Stage-D framing at §4.6 rejects the inferential form at Stage D, and that at Stage H the state-of-art form would be a mixed-effects fit or WWC 2022 SCED dose-response family — not pre-committed here, but named as the pathway for downstream authorship. **Inherits**: §2.2 I1 best-practices-standards discipline. **Expected effect**: closes I1.2 minor fire; signals the arc's inferential-form is a deliberate deferral, not methodological silence.

7. **Add a §4.5 one-line acknowledgement of Radin 2024 nightly-HR-persistence as the autonomic-family analog of Van Campen behavioural severity-scaling.** Even though §4 outcome family is activity-only per §4.2, the extensibility of the arc to autonomic outcomes at Stage D companion reads (per §4.2's "Sleep + autonomic + subjective outcome families may be added as descriptive companion reads at Stage D per orchestrator disposition") would carry additional literature grounding via Radin 2024. **Inherits**: §2.2 I2 material-citation discipline + parent MD §6.2 anchor discipline. **Expected effect**: closes I2.3 minor fire.

8. **Consider adding a rescue-subset counting metric to §3.5.** The current §3.5 rescue framework is descriptive-language-only ("monotone (or near-monotone) strengthening"). At Stage D, a concrete metric — e.g. Spearman's rho of AUC magnitude on the 4-phase ordinal axis, or a monotonicity-score defined on the ordered phase sequence — would make the rescue vs non-rescue call reproducible across analysts. Not a design change, but a discipline-tightening extension. **Inherits**: parent MD §7.10 descriptive-with-CI-only discipline; SCED single-subject monotonicity conventions. **Expected effect**: converts §3.5 from a qualitative discipline into a mechanically-verifiable one.

---

## 5. Verdict

**DEFENSIBLE with revision** — the MD is a strong inheritance-only extension of the parent Q24 MD LOCKED r1, with exemplary §3.6 caveat-class handling of the 100% citalopram temporal alignment (the most load-bearing finding surfaced during drafting) and best-in-class §4.1 discipline in the "phase-stratified" (never "learned-pacing") reporting pre-commit; the two substantive fires — I2.2 (envelope-theory / pacing-adaptation literature not cited for the §3.4 direction pre-commit) and I3.5 (three disambiguation designs named but not scoped for downstream priority) — are compression-absorbable as mechanical additions per the parent MD's §12 compression discipline; the tactical-Garmin-use fifth-confound addition (I4.4) is a load-bearing caveat-addition that similarly slots into §3.6 without architectural change; the architecture stands as drafted and the operand is well-defined enough that Stage D descriptive audit can execute against r2 without design ambiguity.

---

## Methodology

This methodology review walks CONVENTIONS §2.2 four-input bar plus the applicable items from the 4-layer checklist defined in [../reviews/README.md](README.md), anchored in:

- SCRIBE 2016 (Tate et al., [PMC4873717](https://pmc.ncbi.nlm.nih.gov/articles/PMC4873717/))
- CENT 2015 ([shamseer_2015_cent_consort_nof1.pdf](../literature/methodology/shamseer_2015_cent_consort_nof1.pdf))
- STROBE 2007 ([vonelm_2007_strobe_observational_checklist.pdf](../literature/methodology/vonelm_2007_strobe_observational_checklist.pdf))
- Daza 2018 ([daza_2018_self_tracked_n_of_1_counterfactual.pdf](../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf))
- WWC 2022 SCED standards ([wwc_2022_standards_handbook_v5_0.pdf](../literature/methodology/wwc_2022_standards_handbook_v5_0.pdf))
- Natesan Batley 2023 ([natesan_2023_nof1_evidence_reporting_systematic_review.pdf](../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf))

Plus the state-of-art literature specific to this methodology question (named in Section 2's I1 / I2 cells with material citation): Van Campen et al. 2020 *Healthcare* (severity-scaling of day-2 CPET physiological deficit — literature anchor for §4 dose-response direction pre-commit); Moore et al. 2023 *Medicina* (recovery-time range 1-64 d in ME/CFS — severity signal per pem_recovery_trajectory_review §5); Ghali et al. 2020 *J Transl Med* (epidemiological PEM severity-scaling); Radin et al. 2024 *npj Digit Med* (autonomic-family severity anchor for extensibility per §4.7 in Section 4); envelope-theory + pacing-adaptation literature named as citation-status-deferred at [`lc_recovery_phase_axis.md`](../methodology/lc_recovery_phase_axis.md) (Goudsmit 2012; Nijs 2013; Sanal-Hayes 2023 — recommended anchors for the §3.4 direction pre-commit per Section 4 item 1); change-point-detection state-of-art (Killick et al. 2012 PELT; Adams & MacKay 2007 Bayesian online) named as declined-at-axis-lock-level per Section 4 item 5; WWC 2022 SCED Handbook §V + mixed-effects dose-response family named as the Stage-H-form for §4 per Section 4 item 6.

Structural comparison drawn against sister methodology MDs [`post_heavy_day_compensatory_rest.md`](../methodology/post_heavy_day_compensatory_rest.md) LOCKED r1 (parent Q24 MD; the target inherits stratum + unit + comparator + windows + overlap + pool split + trajectory summary stats + detrend + bootstrap + multiplicity + zero-vs-NaN + 8 pre-committed caveats verbatim), the parent MD review [`methodology-post_heavy_day_compensatory_rest-2026-07-15.md`](methodology-post_heavy_day_compensatory_rest-2026-07-15.md) (structural precedent for the review layer-checks + verdict framing on this MD family), [`lc_recovery_phase_axis.md`](../methodology/lc_recovery_phase_axis.md) LOCKED r2 (canonical source for the four-phase axis inherited at §3.1), [`citalopram_phase_stratification.md`](../methodology/citalopram_phase_stratification.md) (referenced at §3.6 disambiguation design 3), [`intervention_effects_descriptive.md`](../methodology/intervention_effects_descriptive.md) (referenced at §3.6 disambiguation design 3 + §6.1 detrend precedent), [`heavy_day_crash_risk_prediction.md`](../methodology/heavy_day_crash_risk_prediction.md) (sister MD covering sub-part 5 + streak-→-crash predictive-categorical outcomes; cross-arc consistency verified at §1.2), and Stage D descriptive audit [`Q24-post-heavy-trajectory/descriptive_audit.md`](../analyses/descriptive/Q24-post-heavy-trajectory/descriptive_audit.md) LOCKED r4 (§9.1 caveat 9 drift-entanglement reframe supplied the drafting trigger for §3.5 rescue vs non-rescue framing; §10 item 8 level-vs-change disagreement supplied the corpus-general framing at §3.5).

Project-specific audit hooks from [../CONVENTIONS.md](../CONVENTIONS.md) §2.2 (four-input bar), §2.1 / §3 / §4 (discipline gates + audit hooks that apply to methodology choices). CONVENTIONS §3.6 named-counts + §3.7 trajectory-detrend sensitivity + §3.10 operationalisation-faithful-to-data + §4.1 no-interpretive-marks + §4.2 caveats-vs-a-priori are the load-bearing L1 + L3 + L4 hooks for this specific methodology question. Independent data-availability verification at review time (probe against `$GEVOELSCORE_DATA_PATH/unified/per_day_master.csv` LC-era stratum n=1524, 2026-07-16): all 6 counts in §3.1 + §6.3 + §7 reproduce byte-identically, including the 100% citalopram temporal-alignment identity (§3.6: 158/158 citalopram_modulated episode-ends are post-2024-04-09; zero pre-onset in that phase; zero post-onset in any of the three pre-cital phases).
