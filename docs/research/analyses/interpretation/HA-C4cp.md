# HA-C4cp — interpretation (Stage I)

**Status**: **LOCKED r1 2026-07-14** by user acceptance ("yes lets finish what we were doing now" 2026-07-14 driving Bundle H+ event 11 — HA-C4cp downstream cascade Stage I → Stage S₁ per pre-reg §9.2 PARTIAL branch DEFERRED artefacts). Drafted 2026-07-14 in reviewer-mode-with-authorization per [CONVENTIONS §1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode). Stage D upstream [`descriptive_audit.md`](../descriptive/HA-C4cp/descriptive_audit.md) LOCKED r1 2026-07-14 (Bundle H+ event 9). Sister-artefact upstream [HA-C4c Stage I](HA-C4c.md) LOCKED r1 2026-07-08. Fresh-session `/research-review` per plan §4 NOT dispatched pre-lock — user-authorized deviation matching HA-C4c precedent (rationale: Stage D LOCKED r1 provides upstream-verification-chain; §3-§4 substantive content derived deterministically from `result.md` LOCKED r1 machinery; §4.6 lived-experience prior inherited from HA-C4c §4.6 USER-ARTICULATED framing at the same operand-family layer per user's "envelope-crossing" framing which the SD-anchored operand family directly operationalises; §9.6 walk clean on all substantive checks). Retained as retrospective drift-trigger per plan §3.7.

## Authorship

Drafted 2026-07-14 by Claude (Opus 4.7, 1M context) in reviewer-mode-with-authorization per [CONVENTIONS §1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode). Authorising user: Willem ("yes lets finish what we were doing now" 2026-07-14). Fresh-session `/research-review` NOT dispatched pre-lock per user-authorized deviation matching HA-C4c precedent.

---

## 1. Target HA + verdict

- **HA ID**: HA-C4cp (revision r2)
- **Pre-reg lock date**: 2026-07-09 (r2 LOCKED at [`hypothesis.md`](../hypotheses/HA-C4cp/hypothesis.md); Bundle H+ event 7 commit `2d22881`; §3.6-compression on fresh-session /research-review at [`reviews/HA-C4cp-2026-07-09.md`](../../reviews/HA-C4cp-2026-07-09.md) verdict PASS-with-caveats)
- **Result lock date**: 2026-07-14 (test execution + `result.md` LOCKED r1; Bundle H+ event 10 commit `cd71ac6`; worktree HEAD at run `e7da406`; pipeline commit `521e9fe`)
- **Stage D audit lock date**: LOCKED r1 2026-07-14 (Bundle H+ event 9 commit `e7da406`)
- **Headline verdict from `result.md`** (verbatim §1 + §2, no relabeling): **PARTIAL** — direction = +, (a) discrimination PASS at block-permutation p = 0.0090, (b) effect-size FAIL at Cliff's δ = +0.079 vs pre-committed threshold ≥ +0.20 (95% CI [+0.024, +0.132] by paired-bootstrap B=2000). Per-bar table: direction PASS (δ = +0.079 > 0); bar (a) PASS (p = 0.0090 < 0.05); bar (b) FAIL (δ = +0.079 < +0.20). **Same failure-mode configuration as sister-HA HA-C4c** (which was p PASS at 0.0001 + effect-size FAIL at δ = +0.1523 CI [+0.064, +0.178] fully bracketed below +0.20); HA-C4cp reproduces the pattern at slightly smaller magnitude on the personal-baseline SD-anchored operand family. This is the **CONCORDANT-BELOW-THRESHOLD joint-cluster shape** anticipated at pre-reg §9.2 as the modal outcome (operand-family compression argument: personal-baseline z-scoring against a rolling reference typically compresses cross-day effect sizes relative to fixed-threshold operands per CONVENTIONS §3.1); joint-cluster reading fires at Stage S₁ downstream, NOT here.
- **Stage D verdict-trust call** (verbatim from [`descriptive_audit.md`](../descriptive/HA-C4cp/descriptive_audit.md) LOCKED r1 §1): all sanity gates PASS, all walk-forward gates PASS on primary + Z=1 + z_max sensitivity cells, cross-op-independence descriptively corroborated (Cohen's κ = 0.592 pre-gate; 35.1% cap-unreachable rate); parent operand determinism reproduction PASS (byte-identical to HA-C4c result.md 0.6444 on frozen 1274-day HA-C4c-vintage stratum). No `NOT BACKSTOPPED` load-bearing assumption. **TRUSTED**.
- **Operationalisation summary** (one sentence, paraphrasing pre-reg §1 + §4): substantive Wiggers C4 retest at bout resolution on the personal-baseline SD-anchored operand `bout_n_did_not_return_2sd_day` (per-day count of bouts whose `tail_length` exceeds `subject_lagged_median(d) + 2 × subject_lagged_mad(d)`, where the reference is computed over the `[d-90, d-30]` LC-era lagged bout-level pool per parent MD §3.2.2 LOCKED r3 with median + `1.4826 × MAD` robust statistics + ≥ 30-bout validity bar + candidate-day exclusion via window edge + `did_not_return_flag` bouts INCLUDED in reference pool + April 2024 cluster excluded) tested as heavy-T-vs-non-heavy-T contrast (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T inherited from HA-C4c §4.3 verbatim) on the same cross-phase-pooled stratum HA-C4c uses (inherited from HA-C4c §4.2 verbatim), evaluated via Mann-Whitney U + Cliff's δ + block-permutation null at E[L]=7 (B=10,000; seed `20260709`) against the same pre-committed two-bar SUPPORTED gate HA-C4c uses (discrimination + effect-size ≥ +0.20; verbatim per user Q "recommended selection" 2026-07-09 keeping the sister-pattern discipline over operand-family-compression argument).
- **Synthesis-structure map cluster**: [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) (same cluster as HA-C4c; HA-C4cp is the second substantive member added at result.md LOCK 2026-07-14). Cluster transitions from **single-member (HA-C4c only) → 2-member (HA-C4c + HA-C4cp)** on drift-triggered Stage S₁ re-consumption per pre-reg §9.2 branch. Cascade-receiver from C-bout-framework (HA11-bout-redo; unchanged from HA-C4c's cascade context). Cross-op-independence gap per guide r3 §4.2.1 condition 2 **CLOSED at strict guide r3 §4.2 two-independent-HAs bar** via operand-family-level independence per OI-025 protocol §5.4 four-condition argument (fixed-absolute-threshold vs personal-baseline-rolling-reference differ in reference frame, construct alignment, distributional-drift sensitivity, and low-n failure modes). **Stage I here does NOT consume the sister-HA verdict as co-equal** per [`verdict_to_inference.md`](../../methodology/verdict_to_inference.md) §5.5 + §6.3 (cluster-level coherence is Stage S₁'s job, not Stage I's); Stage I preserves the CONCORDANT-BELOW-THRESHOLD framing as the pre-reg-anticipated joint-cluster shape without licensing the joint reading here.

## 2. What the data shows

Descriptive paraphrase, no claim language per CONVENTIONS §4.1.

**Pool sizes (primary cell, cross-phase pooled + reference-window valid)**: n_heavy = 465 days; n_non_heavy = 809 days; primary-cell non-NaN total = 1274 (matches HA-C4c primary stratum byte-identically after §4.4 gate 4 reference-window validity trim removes 15 non-heavy days at the sub-phase-4b left edge). Heavy-T mean `bout_n_did_not_return_2sd_day` = 0.538 bouts/day (median 0.00); non-heavy-T mean = 0.441 (median 0.00). Per-arm named counts per pre-reg §4.11: n_flagged_bouts in heavy-T pool = 607 (sum across days).

**Per-arm statistics**: Mann-Whitney U (heavy first) = 202970; one-sided normal-approx descriptive p = 0.0093. **Cliff's δ = +0.079** (heavy vs non-heavy) with 95% CI [+0.024, +0.132] (paired-bootstrap B=2000). **Block-permutation p = 0.0090** (B=10,000; E[L]=7; seed `20260709`); block-perm null δ median = +0.000 with 95% CI [−0.062, +0.065]. The observed δ = +0.079 sits **outside** the block-permutation null 95% CI upper bound (+0.065), yielding p = 0.0090; the point estimate sits in the [+0.04, +0.13] band (fully bracketed by CI [+0.024, +0.132]) — magnitude-below-threshold in the strict pre-committed sense, ~2× smaller than HA-C4c's landed δ.

**Per-bar table** (`result.md` §2):
- Direction (heavy-T > non-heavy-T; Cliff's δ > 0): **PASS** at δ = +0.079.
- Bar (a) — discrimination (block-perm p < 0.05): **PASS** at p = 0.0090.
- Bar (b) — effect-size (Cliff's δ ≥ +0.20): **FAIL** at δ = +0.079.

**Companion descriptives** (per-day distribution; `result.md` §4): per-day mean = 0.4765 (in §10.4 HALT window [0.01, 0.60] PASS; OUTSIDE narrower informational anchor [0.05, 0.30]); median = 0.00 (in [0, 1] informational anchor INSIDE); σ = 0.6655; p25-p75 = [0.00, 1.00]; range [0.00, 3.00]. All four §10.4 dry-run sanity gates PASS at run-time (per Bundle H+ event 9 Stage D descriptive audit + Bundle H+ event 10 dry-run report). **E[L]\* data-driven companion** on `bout_n_did_not_return_2sd_day[d]` over cross-phase-pooled stratum = **7.00 days** exactly (cutoff_lag = None; factor-of-2 deviation flag NOT FIRED per CONVENTIONS §3.6). Block-length choice exactly calibrated.

**Reference-window audit trace** (per §4.11 mandatory reporting; parent MD §3.2.2 audit-trace discipline): `subject_lagged_median_day` heavy-T mean = 46.18 min (median 47.00, p25-p75 [38.00, 53.00]); non-heavy-T mean = 46.73 min (median 46.00, p25-p75 [39.00, 54.00]). `subject_lagged_mad_day` heavy-T mean = 59.27 min (median 58.56, p25-p75 [48.18, 69.68]); non-heavy-T mean = 60.18 min (median 57.82, p25-p75 [49.67, 69.68]). Heavy-T and non-heavy-T reference-window distributions are essentially indistinguishable in central-tendency + scale — the personal baseline itself is stable across heavy-T vs non-heavy-T contrast; the signal in the primary operand is NOT reference-window-drift-mediated. Reference-window shortfall days on primary stratum = 15 (routed to NaN via §4.4 gate 4; all at the sub-phase-4b left edge). MAD systematically higher than median across the stratum (median-MAD 57.82 min vs median-median 46.50 min) — signature of the 180-cap-censored `tail_length` distribution robustly handled per parent MD §3.2.2.

**Sensitivity arms** (`result.md` §5; descriptive per pre-reg §4.10 + §5.0 single-cell headline lock; cannot promote to SUPPORTED):

- **Z=1 sensitivity** on `bout_n_did_not_return_1sd_day` (n_heavy=465, n_non_heavy=809): δ = +0.106; p = 0.0009; bar (a) PASS + bar (b) FAIL; verdict **PARTIAL** consistent with primary. Z=1 flag rate ~2× primary (heavy mean 1.026 vs primary 0.538); the operand-family stringency gradient is smooth and the direction-consistency at Z=1 confirms the signal is not Z=2-threshold-specific.
- **z_max continuous sensitivity** on `bout_return_time_z_max_day` (n_heavy=463, n_non_heavy=806): δ = +0.137; p = 0.0001; bar (a) PASS + bar (b) FAIL; verdict **PARTIAL** consistent with primary. **Strongest signal across the sensitivity family** — the continuous outcome yields the largest δ (still below +0.20 threshold; effect-size FAIL preserved). Heavy-T max-z mean = 1.831 SD-equivalents; non-heavy-T = 1.520 SD-equivalents — the participant's typical worst-bout-of-the-day is ~1.7 SDs above their personal reference, higher on heavy-T days.
- **Reference-window shorter-lag** on `[d-60, d-30]` instead of `[d-90, d-30]` (n_heavy=463, n_non_heavy=806): δ = +0.037; p = 0.1327; bar (a) FAIL + bar (b) FAIL; verdict **REJECTED**. **Reference-window fragility flag fired** — the primary's signal is materially reduced (δ +0.079 → +0.037) and loses statistical distinguishability (p 0.0090 → 0.1327) when the reference window is halved from 60 days to 30 days. Reading per pre-reg §8 caveat 6 (LOAD-BEARING reference-window construction dependency): the `[d-90, d-30]` window is a load-bearing pre-commit; the shorter-lag arm does NOT invalidate the primary but signals that the primary's operational calibration is sensitive to window-length choice.
- **Unmedicated-only stratum** (n_heavy=183, n_non_heavy=323): δ = +0.004; p = 0.4678; both bars FAIL; verdict **REJECTED**. **Stratum-fragility flag fired at higher amplitude than HA-C4c** — HA-C4c's unmedicated-only sensitivity produced δ = +0.059 vs primary +0.120 (a 50% attenuation, REJECTED); HA-C4cp's unmedicated-only produces δ = +0.004 vs primary +0.079 (a 95% attenuation, essentially zero-signal, REJECTED). The signal on the personal-baseline operand vanishes almost entirely in the unmedicated sub-arm. Substantive observation, NOT interpreted at Stage I beyond noting cross-phase pooling is more load-bearing on the SD-anchored operand than on HA-C4c's fixed-absolute-threshold operand.
- **Motion-clean-only** (motion_confound_flag = False; n_heavy=465, n_non_heavy=809): heavy mean = 0.000, non-heavy mean = 0.000; δ = +0.000; p = 1.0000; both bars FAIL; verdict **REJECTED**. **Motion-fragility flag fired** — as at HA-C4c, the 99.3% motion-confound corpus property collapses this arm to a zero-signal degenerate variant (only 32/4317 motion-clean bouts corpus-wide). Reading per pre-reg §8 caveat 4 verbatim: corpus-property reaffirmation, NOT primary fragility; the operand's SD-anchored family inherits the same motion-tagged-bout-pool constraint HA-C4c's fixed-threshold operand carries.
- **Transient-excluded** (transient_flag = False; n_heavy=465, n_non_heavy=809): δ = +0.082; p = 0.0061; bar (a) PASS + bar (b) FAIL; verdict **PARTIAL** consistent with primary. Transient exclusion does NOT materially attenuate the signal (as at HA-C4c); no transient-fragility flag.
- **Baseline-invalid-excluded** (n_heavy=465, n_non_heavy=809): δ = +0.079; p = 0.0092; verdict **PARTIAL** consistent with primary. Essentially identical to primary (44/4317 = 1.0% baseline-invalid; low-impact per pre-reg §4.10 anticipation).
- **Reference-pool `did_not_return`-excluded** (per pre-reg §4.10; new for HA-C4cp per parent MD §3.2.2 alternatives-considered): δ = +0.091; p = 0.0024; verdict **PARTIAL** consistent with primary. Excluding 180-cap bouts from the reference pool deflates `subject_lagged_median` and shifts Z-thresholds down, resulting in a HIGHER flag rate (as parent MD §3.2.2 rationale predicted); the primary signal's direction is preserved and slightly stronger (δ +0.079 → +0.091). No reference-censoring fragility flag.

**Crash-drop sensitivity** (CONVENTIONS §3.4 + pre-reg §4.10; `result.md` §5): primary → crash-dropped: n_heavy 465 → 441; n_non_heavy 809 → 745; Cliff's δ +0.079 → +0.096; block-perm p 0.0090 → 0.0016; verdict PARTIAL unchanged. **|Δ Cliff's δ| = 0.017** (threshold 0.10 per CONVENTIONS §3.4; 0.20 sign-flip threshold per pre-reg §5.2): **NOT FIRED (CLEAN)**. **|Δ pp on discrimination| = 1.70 pp** (threshold 5.0 pp per HA11-bout-redo analogue): NOT FIRED. Route-to-REJECTED (|Δ δ| > 0.20 AND sign-flip): NO. The heavy-T signal is robust across the broader pool with crash-days dropped; **not crash-driven** — matches HA-C4c's crash-drop-clean pattern.

**Approach A dose-adjusted sensitivity arm** (`result.md` §5; pre-reg §4.9 inheritance-by-analogue; descriptive companion under underpowered-NULL frame): three sub-arms report — primary template β = +0.056/mg (δ = +0.081, p = 0.0206, PARTIAL); CI lower bracket β = +0.145/mg (δ = +0.057, p = 0.0910, REJECTED); CI upper bracket β = −0.034/mg (δ = +0.043, p = 0.1506, REJECTED). The primary template is consistent with primary at PARTIAL; CI-bracket sub-arms DIVERGE (REJECTED) — β-precision-fragility surfaced at the CI-bracket layer, exactly the outcome pre-reg §8 caveat 3 amplification anticipated (SD-anchored operand family is FURTHER removed from the `bout_n_fast_recovery_day` analogue than HA-C4c's fixed-absolute operand; inheritance-fragility is expected to be amplified). Descriptive-only per pre-reg §4.9 underpowered-NULL frame; NOT part of the primary verdict.

**Holm step-down** (`result.md` §6; 7-of-7 sens arms all valid): at α=0.05 across {primary, z1_count, z_max_continuous, reference_window_shorter_lag, unmedicated_only, motion_clean_only, transient_excluded}: primary Holm-adjusted p = 0.0360 (rejected YES); transient_excluded 0.0305 (YES); z1_count 0.0054 (YES); z_max_continuous 0.0007 (YES); reference_window_shorter_lag 0.3981 (NO); unmedicated_only 0.9355 (NO); motion_clean_only 1.0000 (NO). **4-of-7 arms survive Holm at α=0.05.** The primary + Z=1 + z_max + transient_excluded cluster survives corrected multiplicity; the primary at adj_p = 0.036 sits just under the α = 0.05 bar with narrow margin.

**Family-membership caveat load-bearing note per pre-reg §5.3 + §8 caveat 6** (surfaced explicitly in result.md §6): the primary + Z=1 + z_max_continuous three arms share the parent MD §3.2.2 reference-window construction — same `[d-90, d-30]` window, same reference pool, same `subject_lagged_median` + `subject_lagged_mad` scaling. The Holm-3-of-3 "survival" of primary + z1 + z_max is NOT independent confirmation; it is **consistent-reference-window internal-consistency**. The `reference_window_shorter_lag` arm is the first genuinely reference-window-independent diagnostic (different window definition), and it REJECTED at δ = +0.037. The `reference_pool_dnr_excluded` arm (not in Holm family) is the second reference-window-independent diagnostic, and it PARTIAL at δ = +0.091.

**Cross-op concordance descriptive companion** (`result.md` §4; per pre-reg §4.10 sister-test cross-reference; descriptive per Pass 1 discipline): 2×2 concordance table between HA-C4c parent operand (`bout_n_did_not_return ≥ 1`) and HA-C4cp primary operand (`bout_n_did_not_return_2sd_day ≥ 1`) on n=1274 primary stratum: 427 (33.5%) both fire; 61 (4.8%) HA-C4cp-only; 259 (20.3%) HA-C4c-only; 527 (41.4%) neither. **Cohen's κ = 0.507** (moderate agreement per Landis-Koch 1977: 0.41-0.60 moderate; 0.61-0.80 substantial). **Cap-unreachable days**: 452 of 1274 (35.5%) have Z=2 threshold > 180-min `tail_length` cap by construction — the SD-anchored operand cannot fire on those days regardless of bout activity. This is the direct descriptive expression of the cross-op-independence property parent MD §3.2.2 + OI-025 protocol §5.4 four-condition argument argues for at operand-family level.

**Sister-test cross-reference table** (`result.md` §7; descriptive only, no cross-test pass conclusion per CONVENTIONS §4.4): HA-C4c r2 PARTIAL (cross-phase-pooled bout-level fixed-absolute-threshold; δ=+0.1523, p=0.0091, n=1274 = 465 heavy + 809 non-heavy); HA-C4c-stringency-companion NON-TRIGGER (Pass 1 f2(T) monotone descent 0.5863 → 0.1020 across T∈{30,60,120,180}; OI-025 CLOSED-DESCRIPTIVE-ONLY); HA-C4 v2 REJECTED (daily-aggregate); HA-C4b v3 NOT-SUPPORTED; HA11 v1 SUPPORTED-on-train; HA11-bout-redo PARTIAL (framework-validity); HA-C4cp (this test) PARTIAL (personal-baseline SD-anchored; δ=+0.079, p=0.0090, n=1274 = 465 heavy + 809 non-heavy).

**Stage D verdict-trust call**: TRUSTED (per LOCKED r1 descriptive audit; all sanity gates PASS + walk-forward PASS + parent operand determinism byte-identical to HA-C4c anchor).

## 3. What the verdict licenses

The verdict licenses **the within-operationalisation partial-positive substantive claim on the personal-baseline SD-anchored operand family with the per-bar pattern preserved (bar (a) PASS + bar (b) FAIL) and the below-threshold effect-size treated as part of the claim shape**:

> The bout-level personal-baseline SD-anchored operand `bout_n_did_not_return_2sd_day` (per-day count of bouts whose `tail_length` exceeds the participant's own rolling `[d-90, d-30]` LC-era lagged reference median + 2 × `1.4826 × MAD` per parent MD §3.2.2 — the personal-baseline Z=2 tail count of "return-time atypical for me") evaluated under the locked cross-phase-pooled substantive operationalisation (heavy-T-vs-non-heavy-T contrast on `citalopram_phase(d) ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}` × recovery-phase 4b + 5 stratum; n_heavy = 465, n_non_heavy = 809) **produces a direction-correct + statistically-distinguishable + effect-size-below-threshold contrast on this corpus's substantive primary cell — Cliff's δ = +0.079 (95% CI [+0.024, +0.132]) fully bracketed below the pre-committed +0.20 discipline threshold, with block-permutation p = 0.0090 below α = 0.05 (E[L] = 7, B = 10000, seed `20260709`)** on the same cross-phase-pooled stratum HA-C4c uses.

**The licensed claim is bounded by the operationalisation** per guide §4.3 rule 1: PARTIAL of this specific SD-anchored operand on this specific cross-phase-pooled stratum under this specific heavy-T-vs-non-heavy-T contrast. Per CONVENTIONS §4.3 confirmatory framing: the test set out to confirm whether the personal-baseline SD-anchored operand's per-day Z=2 tail count is systematically higher on heavy-T days than on non-heavy-T days at cross-phase-pooled resolution, with two pre-committed bars (discrimination + effect-size at the SAME +0.20 threshold HA-C4c uses per user Q "sister-pattern discipline wins over operand-family compression"). The result is a within-operationalisation PARTIAL that clears one bar cleanly and fails the other in an informative-and-preserved way. Per guide §5.3 mapping rule for PARTIAL: the unmet condition — bar (b) effect-size — is part of the licensed claim shape and is preserved in this §3 sentence; the licensed claim is **direction-of-signal-at-magnitude**, and the below-threshold effect-size is part of the signal's shape at this corpus's operationalisation, NOT noise to bracket away.

**SAME failure-mode configuration as sister-HA HA-C4c** (worth naming explicitly): both HAs land at direction PASS + (a) discrimination PASS + (b) effect-size FAIL. HA-C4c's landed δ = +0.1523 (CI [+0.064, +0.178]); HA-C4cp's landed δ = +0.079 (CI [+0.024, +0.132]) — ~2× smaller magnitude on the personal-baseline operand family, as pre-reg §7 + §8 caveat 2 anticipated via the operand-family-compression argument. Both effect-size CIs are fully bracketed below the +0.20 discipline threshold. Both bar (a) discriminations clear cleanly (HA-C4c at p = 0.0001; HA-C4cp at p = 0.0090). **The parallel failure-mode configuration is the CONCORDANT-BELOW-THRESHOLD joint-cluster shape per pre-reg §9.2 anticipated modal outcome** — Stage I here notes for traceability; the joint-cluster reading itself fires at Stage S₁ per `internal_synthesis.md` §5 verdict table on drift-triggered re-consumption of [`cluster-C-bout-substance.md`](../synthesis/cluster-C-bout-substance.md).

**Per pre-reg §9.2 PARTIAL branch (carried verbatim as part of the licensed claim shape)**: the licensed claim carries the (b)-failing PARTIAL configuration explicitly — a **weak-effect-but-real positive pattern on the personal-baseline SD-anchored operand family**. The signal exists in the predicted direction, is statistically real at cross-phase-pooled scale (Holm-4-of-7 arms survive multiplicity correction with the primary itself at adj_p = 0.036), and is smaller than the +0.20 discipline threshold — those three facts together are the shape of the licensed claim.

**Cross-op-independence gap CLOSURE reading** (per pre-reg §9.2 + Stage A r4 update): the two HAs now populate the C-bout-substance cluster at **operand-family-level cross-op-independence** per OI-025 protocol §5.4 four-condition argument. The gap named at Stage A construct-bout-recovery-signal §5.2 is now CLOSED at the strict guide r3 §4.2 two-independent-HAs bar — no longer via §4.2.1 gap-named + future-closeable interim reading. The strict-magnitude-bar is NOT cleared at either operationalisation (both PARTIAL at δ < +0.20); this is the corpus-effect-size ceiling reading per pre-reg §9.2 substantive interpretive note — honest sample-limitation, NOT a Wiggers-C4 disproof.

**Descriptive corroboration of cross-op-independence at operand-family level**: the 2×2 concordance table (κ = 0.507 moderate) + 35.5% cap-unreachable-day rate + reference-window-shorter-lag REJECTED at δ = +0.037 (fragility flag) all provide direct descriptive corroboration that the two operands capture materially different aspects of the return-time distribution. **The joint-cluster claim shape licensed here is: two operand-family-independent operationalisations of the same substantive Wiggers-C4 question both produce direction-correct + statistically-distinguishable + effect-size-below-threshold contrasts on this corpus** — a claim shape structurally different from either "both operationalisations reject Wiggers-C4" or "both operationalisations confirm Wiggers-C4".

## 4. What the verdict does NOT license

Four predictable overclaim shapes addressed explicitly per guide §4.4:

1. **PARTIAL does NOT license Cliff's δ = +0.079 as a "small-but-real substantive personal-baseline recovery signal" without the below-threshold pattern preserved.** Per guide §5.3 anti-overclaim: below-threshold effect sizes are part of the licensed-claim shape at PARTIAL-with-(b)-failing. The point estimate δ = +0.079 with 95% CI [+0.024, +0.132] sits ENTIRELY below +0.20 — not straddling, not marginally missing. Reading this as "essentially SUPPORTED at small effect size" would collapse the discipline distinction between "SUPPORTED at δ ≥ +0.20" and "PARTIAL at direction-correct-but-below-threshold." Stage I preserves the fully-bracketed-below shape as part of the claim, not a footnote.

2. **PARTIAL does NOT license collapse to weak SUPPORTED** per guide §7.6 anti-pattern. The p = 0.0090 clears bar (a) cleanly; that is informative about whether the small effect exists at cross-phase-pooled scale (it does), NOT about whether the effect is large enough to meet the discipline's substantive-relevance bar (it isn't). The locked verdict rule is two-bar gated with PARTIAL preserved at exactly-one-met + direction-correct; the rule is what Stage I cites.

3. **PARTIAL does NOT license "Wiggers C4 is REJECTED on personal-baseline reference frame" NOR "Wiggers C4 is CONFIRMED on personal-baseline reference frame"** per guide §7.1 anti-pattern applied at PARTIAL-with-below-threshold-effect-size. Reading toward CONFIRMED would smuggle the p PASS into a magnitude claim the effect-size FAIL disconfirms; reading toward REJECTED would smuggle the effect-size FAIL into a signal-non-existence claim the direction PASS + p PASS jointly disconfirm. Both smuggling moves erase the licensed shape. Per pre-reg §8 caveat 1 verbatim: A REJECTED verdict on HA-C4cp with HA-C4c at PARTIAL reads as "the signal is anchored to the fixed-absolute-threshold reference frame only"; a SUPPORTED verdict reads as "the signal generalises across both operand families". The actual PARTIAL-on-both-with-CONCORDANT-BELOW-THRESHOLD outcome reads as neither pole — it is the anticipated modal shape per pre-reg §9.2.

4. **PARTIAL on cross-phase-pooled stratum does NOT license per-phase claims.** The unmedicated-only sensitivity arm REJECTED at δ = +0.004 with p = 0.4678 (n_heavy=183 / n_non_heavy=323) is a **stratum-fragility finding** per pre-reg §9.5 with HIGHER amplitude than HA-C4c's parallel arm (HA-C4c unmedicated δ = +0.059 vs primary +0.120 = 50% attenuation; HA-C4cp unmedicated δ = +0.004 vs primary +0.079 = 95% attenuation, essentially zero-signal). Per pre-reg §8 caveat 8: the personal-baseline operand's cross-phase pooling is MORE load-bearing than HA-C4c's fixed-absolute-threshold operand's; medicated phases contribute disproportionately to the SD-anchored signal. The claim is bounded to cross-phase-pooled reach; per-phase reach is out of scope at Stage I and the stratum-fragility becomes Stage S₁ / S₂ contextualisation territory (what does it mean about the underlying autonomic mechanism that the medicated phases contribute *more* of the SD-anchored contrast than the fixed-threshold contrast? — surfaced but NOT interpreted at Stage I).

**Sensitivity-arm overclaim explicit address** per guide §5.5 (selective-reading anti-pattern applied to sensitivity arms): the motion-clean-only REJECTED via near-zero signal (δ = +0.000; p = 1.0000) is a **corpus-property reaffirmation** per pre-reg §8 caveat 4, NOT primary fragility. The transient-excluded + baseline-invalid-excluded PARTIAL consistent with primary is robustness observation, NOT licensing extension. The Approach A dose-adjusted sub-arms (primary template PARTIAL; CI-brackets REJECTED — DIVERGENT) surface the β-precision-fragility explicitly pre-registered per pre-reg §8 caveat 3 underpowered-NULL framing; the divergence is descriptive companion property, NOT a load-bearing claim about dose modulation of the SD-anchored operand. **The Holm-4-of-7 arms surviving multiplicity correction is NOT independent confirmation** per pre-reg §5.3 family-membership caveat (LOAD-BEARING): the primary + Z=1 + z_max_continuous three arms share the parent MD §3.2.2 reference-window construction; their Holm co-survival is consistent-reference-window internal-consistency, not three-independent-tests confirmation. The reference_window_shorter_lag REJECTED at δ = +0.037 (Holm not rejected) is the first genuinely reference-window-independent diagnostic and it flags **reference-window-fragility** — the primary's `[d-90, d-30]` window is a load-bearing pre-commit whose signal materially attenuates under `[d-60, d-30]`. **Stage I preserves this reference-window-fragility as caveat-narrowing on the §3 licensed claim**: the claim is bounded to the `[d-90, d-30]` reference-window construction and does NOT project to shorter-lag reference windows on this corpus.

## 4.5 Caveats narrowing the claim (with L-ID citation block)

### 5a. Pre-reg + audit caveats (carry-forward from upstream)

All 8 pre-reg §8 caveats + all Stage D observations carry forward as caveat narrowing (verbatim references; full text at pre-reg §8 and descriptive_audit.md):

1. **Personal-baseline reference-window carries a subject-relative interpretation** (pre-reg §8.1) — the SD-anchored operand tests "how much longer than the subject's own recent typical return-time is this bout", not "how long is the bout in absolute minutes"; the §3 licensed claim is scoped to the personal-baseline reference frame.
2. **HA-C4c PARTIAL calibration discount** (pre-reg §8.2) — HA-C4c's landed δ = +0.1523 missed +0.20 by −0.05; operand-family compression predicts comparable-or-lower δ on HA-C4cp; the observed δ = +0.079 is IN the anticipated modal-outcome band; PARTIAL is the anticipated modal outcome, not a surprise-shortfall.
3. **Approach A inheritance-by-analogue caveat with AMPLIFICATION for HA-C4cp** (pre-reg §8.3) — `bout_n_fast_recovery_day` β = −0.056/mg [CI −0.145, +0.034] p=0.223 template is NULL/weakly-consistent; sign-flip is fiat directional prior; analogue substitution uncertainty is AMPLIFIED for HA-C4cp because SD-anchored operand family is FURTHER removed from the analogue's operand family than HA-C4c's fixed-absolute operand. Approach A CI-bracket divergent-REJECTED observation (§2 above) empirically confirms the pre-reg amplification anticipation.
4. **Motion-confound corpus property** (pre-reg §8.4) — 99.3% motion-confound at bout level per HA11-bout-redo result §4; the SD-anchored family inherits this; motion-clean-only sensitivity arm REJECTED via near-zero signal is corpus-property reaffirmation per §4 above.
5. **Transient-fragility inheritance** (pre-reg §8.5) — anticipated per HA11-bout-redo pattern; empirically NOT fired at HA-C4cp (transient-excluded δ = +0.082 vs primary +0.079, consistent).
6. **Reference-window construction dependency (LOAD-BEARING per pre-reg §5.3 family-membership caveat)** (pre-reg §8.6) — Z=1 + z_max + primary share parent MD §3.2.2 reference-window construction; Holm co-survival is consistent-reference-window internal-consistency; the reference_window_shorter_lag REJECTED at δ = +0.037 is the first reference-window-independent diagnostic and IS empirically live as a reference-window-fragility flag. **§3 licensed claim is bounded to the `[d-90, d-30]` reference-window construction.**
7. **Cross-op independence at operand-family level, NOT raw-substrate level** (pre-reg §8.7) — HA-C4c + HA-C4cp both derive from the same Firstbeat-per-minute stress signal via the same bout-detection pipeline; independence binds at operand-family level (fixed-absolute-threshold vs personal-baseline-rolling); shared-substrate correlated failure modes would affect both. The κ = 0.507 moderate + 35.5% cap-unreachable rate directly corroborate the operand-family-level independence at descriptive layer.
8. **Reference-window left-edge trim** (pre-reg §8.8) — 15 non-heavy-T days at sub-phase-4b left edge route to NaN via §4.4 gate 4 reference-window shortfall (per Bundle H+ event 10 dry-run report). Trim does NOT block walk-forward gate (465 + 809 = 1274 well above ≥30 per arm). Descriptive note; no primary-verdict impact.

From Stage D `descriptive_audit.md` §1 + §5 + §7 (LOCKED r1 2026-07-14): all sanity gates PASS; parent operand determinism reproduction PASS byte-identical to HA-C4c result.md target on frozen 1274-day HA-C4c-vintage stratum. Five open inputs / data-quality observations all CAVEAT-CLASS per CONVENTIONS §4.2 (surfaced descriptively; not interpreted at Stage I).

### 5b. L-ID citation block (binding per limitations doc §5 row for `interpretation.md`)

Per the synthesis-structure map's §3 [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) row L-IDs column: cluster L-IDs = **L1, L2, L3, L4, L7** (L5 NA — no v24 primary signals; L6 NA — gevoelscore not in primary cell). Same L-ID set as HA-C4c per single-cluster row; each cited with one-sentence project-specific application per guide §4.5 hard rule.

> *Cites L1 (single-subject reach)*: this verdict is about one subject's `bout_n_did_not_return_2sd_day` operand producing a direction-correct + statistically-distinguishable + effect-size-below-threshold contrast between heavy-T and non-heavy-T days at the cross-phase-pooled substantive layer on this single corpus; per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) N-of-1 inference reach, PARTIAL here does not project the +0.079 effect-size point estimate to a group-level substantive claim about Wiggers C4 at bout resolution on personal-baseline reference frame. **Cross-operationalisation reading DOES apply at the cluster level** (2-member cluster now populated with HA-C4c + HA-C4cp per Stage A r4 §5.2 cross-op-independence gap CLOSURE); joint-cluster reading fires at Stage S₁ per `internal_synthesis.md` §5, NOT here at Stage I. Cross-test cross-reference to HA-C4 v2 REJECTED (daily-aggregate) is a resolution-of-scale pair (Stage S₁ / S₂ territory), NOT a Stage I licensing extension.

> *Cites L2 (era confounds)*: HA-C4cp runs on the LC era cross-phase-pooled stratum (same as HA-C4c: `citalopram_phase ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}` × recovery-phase 4b + 5); the PARTIAL substantive verdict does NOT project to pre-LC era or to the unmedicated-only sub-stratum where the sensitivity arm REJECTED at δ = +0.004 p = 0.4678 — a stratum-fragility finding at HIGHER amplitude than HA-C4c's (unmedicated 95% attenuation for HA-C4cp vs 50% for HA-C4c). Per pre-reg §8 caveat 8: the SD-anchored operand's cross-phase pooling is more load-bearing than HA-C4c's fixed-absolute-threshold operand's. **The medicated phases contribute disproportionately to the SD-anchored signal** — a substantive stratum-fragility observation Stage S₁ / S₂ contextualise.

> *Cites L3 (device generations)*: `bout_n_did_not_return_2sd_day` is derived from `bout_n_did_not_return` per-bout `tail_length` values via `compute_sd_anchored_features()` at Bundle H+ event 8 (`521e9fe`) on the same per-minute Garmin stress signal HA-C4c uses on FR245 Elevate-V3 device generation. The PARTIAL verdict is constrained to within-device-generation reach. The Firstbeat-input amplification caveat (pre-reg §8 caveat 7 operational-vs-mechanistic; inherited from HA-C4c) compounds with L3: the reference-window construction is device × extraction-threshold interaction that a different device generation might show at a different rate. Cross-device-generation projection requires explicit calibration evidence per limitations doc §3 L3.

> *Cites L4 (analyst-is-subject)*: HA-C4cp is prior-driven by the sister-pattern discipline (parallel to HA-C3 → HA-C3p precedent per guide r3 §4.2.1 canonical model) and the CONVENTIONS §3.1 personal-baseline discipline; per CONVENTIONS §4.3 the test is confirmatory of the Wiggers C4 substantive claim at bout resolution on the personal-baseline reference frame, not exploratory. The L4 mitigation reach per limitations doc r3 §3 L4 caveat: fresh-session `/research-review` peer-check at the pre-reg lock event was dispatched (Bundle H+ event 7 at [`reviews/HA-C4cp-2026-07-09.md`](../../reviews/HA-C4cp-2026-07-09.md) verdict PASS-with-caveats, three substantive absorbs at r2 §3.6 compression); on `result.md` NOT dispatched pre-lock per plan §4 producer/reviewer split table row for `result.md` (test-execution output; deterministic reproduction of pre-committed §5 verdict machinery). §4.6 lived-experience-prior inheritance from HA-C4c §4.6 USER-ARTICULATED framing: the user's "envelope-crossing not training-load-heavy" reframing at HA-C4c specifically pointed toward CONVENTIONS §3.1 personal-baseline discipline as the more natural operationalisation of the felt pattern; **HA-C4cp is the operational realisation of that user-articulated reframing** — the personal-baseline SD-anchored operand family IS the direct machinery for the envelope-crossing question. This is a rare case where the L4 pattern flips: instead of divergence between dispatcher-default and subject-articulated prior, the SUBJECT's articulated prior at HA-C4c drove the SD-anchored operand family's structural pre-commit. See §4.6 below for the full traceability.

> *L5 (presence-conditioned data layer) NA*: HA-C4cp uses no v24 primary signals; `bout_n_did_not_return_2sd_day` is derived from per-minute Garmin stress trace via the SD-anchored derivative operand family (parent MD §3.2.2; non-v24); `exertion_class_lagged_lcera` is Garmin training-load-derived (non-v24); `crash_v1` labels used only in §4.10 crash-drop sensitivity arm are HA11 v1's primary outcome from Garmin-derived Firstbeat signals (non-v24). Same L5 NA reason as HA-C4c; the map's §3 row marks L5 NA for the C-bout-substance cluster.

> *L6 (self-reporting) NA*: HA-C4cp does NOT use `gevoelscore` in the primary cell — primary outcome is `bout_n_did_not_return_2sd_day` (Garmin-derived per-day count via the extended bout-extraction pipeline at Bundle H+ event 8); the heavy-T classifier is `exertion_class_lagged_lcera` (Garmin training-load-derived). Same L6 NA reason as HA-C4c; distinct from HA11-bout-redo where L6 applied at the upstream methodology layer via `crash_v1` participant-defined-triage inheritance; at HA-C4cp the crash-drop use of `crash_v1` is a §4.10 sensitivity arm only + it did not fire clean (|Δ δ| = 0.017 < 0.10 threshold).

> *Cites L7 (survivorship)*: the primary-cell effective coverage n_heavy = 465 / n_non_heavy = 809 (total 1274 primary-stratum non-NaN days) represents the gated subset surviving the §4.4 day-validity gate (≥ 600 valid per-minute stress samples + LC era + 21-day device-baseline-lag exclusion + April 2024 cluster exclusion + **reference-window validity gate: `subject_lagged_median` non-NaN** per parent MD §3.2.2 additive gate) + §4.4 exertion-classification non-NaN gate + §4.2 cross-phase-pooled stratum restriction (days `>= 2022-11-17`). The verdict does not project to days outside the gated subset. The 15-day reference-window-shortfall trim documented in dry-run-report.md is at the sub-phase-4b left edge and does not materially affect coverage. Per Bundle H+ event 9 Stage D descriptive audit §5 walk-forward gate check PASS on all 3 evaluated cells (primary + Z=1 + z_max); the coverage is comfortably adequate for the ≥30-per-arm bar per pre-reg §4.7 walk-forward gate.

## 4.6 Lived-experience prior reconciliation

**STATUS: USER-ARTICULATED-INHERITED-VIA-HA-C4c-§4.6 2026-07-08** (the sister-HA's Stage I §4.6 USER-ARTICULATED framing at Phase C cluster-level joint lock session directly motivated the SD-anchored operand family structural pre-commit; HA-C4cp is the operational realisation of that reframing).

**Verbatim user articulation** (inherited from HA-C4c §4.6, Willem 2026-07-08):

> "My felt pattern is that on days that I go out of my energy envelope (so maybe, heavy exertion above baseline?) and especially near the end of the day, I can get 'stuck' longer. So dispatcher's default is good, except I don't know if I would use the word SHOULD here."

**Structured reading applied to HA-C4cp** (per guide §4.6 four-component decomposition):

1. **Directional agreement — inherited from HA-C4c**: YES on the direction. The user's felt prior is that out-of-envelope days DO leave the participant stuck longer. HA-C4cp's landed δ = +0.079 is direction-consistent with this prior.
2. **Scope reframing — from "heavy exertion" to "out of energy envelope" — OPERATIONALLY REALISED AT HA-C4cp**: the user's reframing at HA-C4c specifically pointed toward CONVENTIONS §3.1 personal-baseline discipline as the more natural operationalisation of the felt pattern. **HA-C4cp's operand family IS the operational answer to that reframing** — the SD-anchored operand `bout_n_did_not_return_2sd_day` measures "return-time atypical for me" on a personal-baseline-rolling reference frame, which is the direct operationalisation of "out of my energy envelope". The user-articulated reframing at HA-C4c drove the structural pre-commit that produced HA-C4cp; HA-C4cp is the response to the reframing, not just another test.
3. **Temporal texture — "especially near the end of the day"**: still not captured by the per-day aggregate `bout_n_did_not_return_2sd_day` operand. Any intraday time-of-day sub-analysis would need a per-bout feature extension (bout start-time relative to day-window) at parent MD level; out of scope at HA-C4cp; carried forward as follow-up per §4.8.
4. **Language nuance — drop "SHOULD"**: same as HA-C4c; Stage I preserves this by removing prescriptive/expectation framing from any narrative rendering.

**Implication for §3 licensed claim** (guide §4.6 agreement-does-not-strengthen rule): the directional agreement does NOT strengthen the §3 licensed claim; L4 coupling makes prior-verdict agreement systematically more likely than chance in the subset of tests where the pattern is felt-visible. The reframing point 2 above IS a substantive traceability observation: **HA-C4cp's operand family exists because the user articulated at HA-C4c that the personal-baseline framing was the more natural operationalisation of the felt pattern**; this is a rare case where L4 flips positive-direction (user's articulated prior at earlier test drove the structural pre-commit of a later test), rather than the more common divergence pattern.

**Phase-A + Phase-B + Phase-C + Phase-C-followup precedent traceability block (L4 pattern now verified five times)**:

- HA-C3 v2 Phase A.1 (2026-06-25): null-prior pattern.
- HA-C3p Phase A.2 (2026-06-25): null-prior pattern.
- HA11-bout-redo Phase B (2026-07-07): null-on-pattern + tactical-BB divergence.
- HA-C4c Phase C (2026-07-08): first-time directional agreement WITH SUBSTANTIVE REFRAMING (envelope-crossing not training-load-heavy).
- HA-C4cp Phase C-followup (2026-07-14, THIS artefact): **operational realisation of the HA-C4c reframing**; the SD-anchored operand family IS the direct operationalisation of the "out of my energy envelope" articulation; the L4 traceability at HA-C4cp is inheritance-from-HA-C4c-§4.6 rather than a new session articulation. **First-time L4-pattern-inversion**: instead of the subject's articulated prior diverging from the dispatcher's default (the pattern verified at the first four invocations), the subject's articulated prior at an earlier test **drove the structural pre-commit** that produced this later test's operand family.

**Interpretation of the L4 pattern updated** (five-invocation view): the subject's felt prior lives at the personal-envelope / temporal-profile / actual-felt-quality layer, NOT at the parent-MD's operationalisation layer or the pre-reg's testing-classifier layer. **When the subject articulates a reframing at an earlier test, the reframing CAN drive the structural pre-commit of a later test's operand family** — HA-C4cp is the demonstration of this pattern. The dispatcher-side + subject-side agree on the direction of the substantive question (Wiggers-C4 stuck-sympathetic post-exertion); the disagreement lives at the operationalisation layer (fixed-absolute-threshold vs personal-baseline-rolling reference frame); the disagreement is now RESOLVED at the corpus level by having BOTH operationalisations tested (HA-C4c + HA-C4cp = cross-op-independence gap CLOSED at strict guide r3 §4.2 bar).

**L4 meta-recursion acknowledgment preserved** (per guide §4.6 closing paragraph): the §4.6 reconciliation is authored by the same agent and same subject whose L4 coupling §4.6 is designed to surface. The recursion is bounded by the same four mechanisms HA-C4c §4.6 named (structural separation of §4.6 from §2/§3; no-auto-resolution rule on divergent priors — NA for the inheritance case; agreement-does-not-strengthen rule invoked explicitly above; fresh-session `/research-review` peer-check on the LOCKED artefact per CONVENTIONS §1.2 user-authorized-deviated per HA-C4c precedent).

## 4.7 Closure-path statement

PARTIAL verdict; closure-path is **operational per guide §4.7 for "one-of-two-bars MET + direction-correct" framing** (HA-C4cp's verdict carries "direction-correct + (a) discrimination PASS + (b) effect-size FAIL" per `result.md` §1 + pre-reg §5.2; the unmet condition is bar (b) effect-size ≥ +0.20).

**Closure-path for the bar (b) FAIL — SAME pattern as HA-C4c's bar (b) FAIL**: bar (b) (Cliff's δ ≥ +0.20) failed at δ = +0.079 with 95% CI [+0.024, +0.132] because the substantive heavy-T-vs-non-heavy-T contrast on the personal-baseline SD-anchored operand at this corpus's cross-phase-pooled n is genuinely at a small-effect magnitude — even smaller than HA-C4c's landed δ = +0.1523 (the operand-family compression argument's anticipated outcome).

**Four operational paths for bar (b) FAIL closure** (named per guide §4.7; pre-registration of any upgrade-HA is §4.8 own-research territory):

1. **A larger heavy-T pool would tighten the CI but not change the point estimate.** Same as HA-C4c's closure-path 1. The heavy-T contrast on `bout_n_did_not_return_2sd_day` at this corpus's operationalisation is genuinely small-effect; more days would sharpen the CI around δ ≈ +0.079 but would not shift the point estimate materially. **No closure via corpus accrual at this operand + stratum**.

2. **Operand-refinement pre-reg** per pre-reg §9.5 HA-C4cp-v2 escalation table — v2 could re-tune the reference-window (`[d-60, d-30]` promoted to primary; currently the reference-window-shorter-lag sensitivity arm REJECTED, so this is likely NOT the productive route on this corpus); v2 could promote Z=1 count to primary (currently Z=1 sensitivity is PARTIAL at δ = +0.106, closer to but still below +0.20); v2 could promote z_max continuous to primary (currently strongest signal at δ = +0.137, still below +0.20). Per pre-reg §9.5 escalation table, **HA-C4cp-v2 is NOT triggered by this outcome** — the verdict is direction-consistent PARTIAL, not v2-remediable REJECTED/INCONCLUSIVE. Available if downstream synthesis identifies the below-threshold effect-size as too binding.

3. **Reference-window construction refinement** per pre-reg §8 caveat 6 LOAD-BEARING framing — the reference-window-shorter-lag REJECTED at δ = +0.037 IS a live reference-window-fragility flag; a v2 could investigate whether a different lagged-baseline construction (`[d-120, d-30]`? `[d-90, d-30]` with different validity threshold?) produces a more stable operand. Downstream methodology work; NOT required for the §3 licensed claim to stand.

4. **NO CLOSURE — PARTIAL with bar (b) FAIL is a legitimate completion state for the substantive Wiggers C4 question on the personal-baseline SD-anchored reference frame at bout resolution.** Per pre-reg §9.2 branch verbatim: the signal exists in the predicted direction, is statistically real at cross-phase-pooled scale, and is smaller than the +0.20 discipline threshold — those three facts together are the shape of the licensed claim. **The substantive Wiggers C4 claim at bout resolution on this corpus's personal-baseline-rolling operationalisation is characterised at δ = +0.079 with 95% CI [+0.024, +0.132] — that is the substantive finding, not a stepping stone to a hypothetical SUPPORTED verdict.** Reading paths 1-3 as required closures would misread the pre-reg's own PARTIAL discipline.

**Per pre-reg §9.2 PARTIAL branch (carried verbatim)**: HA-C4cp PARTIAL adds a PARTIAL pointer to the C4 register row (HA-C4c PARTIAL pointer stays; HA-C4 v2 REJECTED-at-daily-aggregate pointer stays); cross-test pass folds in the CONCORDANT-BELOW-THRESHOLD joint-cluster shape; parent MD + pipeline all remain LOCKED; HA-C4cp-v2 NOT triggered per §9.5.

## 4.8 Follow-up suggestions (own-research + external-research tracks)

### Own-research track

- **Reference-window construction refinement descriptive companion**. The reference-window-shorter-lag sensitivity arm REJECTED at δ = +0.037 (Holm not rejected at adj_p = 0.398) IS a live reference-window-fragility flag. A descriptive companion at `analyses/descriptive/operationalisation_support/sd_anchored_reference_window_sensitivity/` could characterise the δ-vs-reference-window-length curve at multiple windows (`[d-30, d-1]`, `[d-60, d-30]`, `[d-90, d-30]`, `[d-120, d-30]`, `[d-180, d-30]`) as descriptive substrate before any pre-reg refinement. Effort: S (same bouts already extracted; add reference-window variants as descriptive columns). Motivation-visible-in-distribution rule applies: any Step 2 pre-reg refinement must be descriptively motivated, not peek-and-adjust on δ curves.

- **Intraday time-of-day extension** per HA-C4c §4.6 temporal-texture observation. The user's articulated "especially near the end of the day" framing is not captured by any current per-day aggregate operand. A per-bout feature extension at parent MD level (bout start-time relative to day-window; sleep-window-crossing flag; end-of-day-vs-morning temporal stratification) could operationalise this. Requires parent MD §3.2 or §4 extension; effort M at parent MD revision time; drift-trigger to HA11-bout-redo per its Stage I §10 drift trigger 3. Cross-references user's HA-C4c §4.6 temporal-texture articulation.

- **Per-channel missingness-rate × era × phase MCAR/MAR diagnostic on the SD-anchored operand family** — sister to OI-023 (for HA-C4c parent operand `bout_n_did_not_return`) and OI-017 (for `bout_n_fast_recovery_day`). A three-operand joint diagnostic run could cover the entire bout-level operand family in one S-effort run. Descriptive infrastructure; tightens L7 reach narrative at future Stage I re-examinations.

- **HA-C4cp-v3 with z_max continuous as primary**. The z_max continuous sensitivity arm showed the strongest signal (δ = +0.137, p = 0.0001, Holm adj_p = 0.0007). If the substantive question could be reframed as "how atypical was your worst bout of the day" rather than "how many atypical bouts did you have", z_max continuous could be a stronger primary operand. Requires pre-reg; new HA folder. NOT triggered by current PARTIAL outcome per §9.5; available if downstream synthesis identifies operand-choice-of-primary as material.

### External-research track

- **Cross-population framework-validity replication of the SD-anchored bout-level recovery operand on PEM-prone populations**. We cannot run this ourselves because **L1 (single-subject reach)** prevents the N=1 corpus from settling whether the personal-baseline SD-anchored operand's PARTIAL verdict at δ = +0.079 generalises beyond this subject. Per Daza 2018 N-of-1 inference reach: cross-subject substantive replication would require a group-level study with the same per-minute Garmin stress instrumentation + same bout-detection rule + same SD-anchored operand construction (median + `1.4826 × MAD` over per-subject `[d-90, d-30]` lagged reference; ≥ 30-bout validity bar; `did_not_return_flag` INCLUDED in reference pool) + same heavy-T contrast operationalisation + same two-bar discipline. The study would carry pre-registration of the same two §5 bars with the within-population effect-size discipline threshold potentially adjusted to the group's distribution per CENT 2015 item 22 generalisability statement.

- **Cross-device-generation comparability study for the SD-anchored operand family** at framework-validity layer. We cannot run this ourselves because **L3 (device generations)** + the reference-window construction is device × extraction-threshold × algorithm interaction. A cross-device design would test whether the personal-baseline reference-window statistics (median, MAD) are device-stable AND whether the derivative Z=2 flag rate is device-stable. Would clarify whether the substantive Wiggers C4 finding at bout resolution on personal-baseline reference frame is device-stable or device-specific.

Both external-research entries respect locked-plan §3.11 N=1-limit scoping discipline (the L-ID-explicit-naming requirement per guide §4.8).

## 4.9 `open_inputs` block

Two slots surfaced (both non-blocking cascade-context):

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | **OI-036 (new; non-blocking cascade-context)**: Reference-window construction fragility diagnostic on the SD-anchored operand family — the reference_window_shorter_lag REJECTED at δ = +0.037 is a live fragility flag surfaced by pre-reg §8 caveat 6; a descriptive characterisation of the δ-vs-reference-window-length curve at multiple windows (`[d-30, d-1]`, `[d-60, d-30]`, `[d-90, d-30]`, `[d-120, d-30]`, `[d-180, d-30]`) would clarify whether the primary's `[d-90, d-30]` is a natural inflection or an arbitrary cut. NOT load-bearing for the §3 licensed claim (which is scoped to `[d-90, d-30]` construction); cascade-context follow-up. | Tightens the reference-window-fragility caveat's reach narrative from "empirically live at one alternative window" to "characterised across the reference-window-length family". Non-blocking. | S effort per the shared descriptive-companion pattern at `analyses/descriptive/operationalisation_support/`; script template from `desc-SMS` exists. | Reference-window-fragility caveat stands per §4.5 caveat 6 empirically-live framing at one alternative window; L7 reach narrative bounded by the caveat. |
| 2 | **OI-023 (carry-forward from HA-C4c Stage I §4.9 + Stage D §4.5; REMAINS OPEN)**: Per-channel missingness-rate × era × phase MCAR/MAR diagnostic on `bout_n_did_not_return_2sd_day` + `bout_n_did_not_return_1sd_day` + `bout_return_time_z_max_day` (three-operand joint sister to OI-017 for `bout_n_fast_recovery_day`); non-load-bearing per Stage D TRUSTED reading; cascade-context sister. | Tightens L7 citation from "gating-discipline-bounded" reading to "fully audit-backed" reading; jointly with OI-017 covers all bout-level operands (5 operands total including HA-C4c parent) in one S-effort run. | S effort; jointly with OI-017 becomes a five-operand run at same S effort; script template from `desc-SMS` exists. | TRUSTED stands with L7 narrowing caveat at Stage I per §4.5 L7 row gating-discipline-bounded reading. |

## 4.10 Cross-references

- HA-C4cp r2 [`hypothesis.md`](../hypotheses/HA-C4cp/hypothesis.md), [`result.md`](../hypotheses/HA-C4cp/result.md), [`test.py`](../hypotheses/HA-C4cp/test.py), [`dry-run-report.md`](../hypotheses/HA-C4cp/dry-run-report.md), `result-data.json` (gitignored).
- Stage D audit [`descriptive/HA-C4cp/descriptive_audit.md`](../descriptive/HA-C4cp/descriptive_audit.md) — LOCKED r1 2026-07-14 (Bundle H+ event 9).
- **Sister-HA Stage I (co-cluster member)**: [`HA-C4c.md`](HA-C4c.md) — LOCKED r1 2026-07-08. Same cluster (C-bout-substance now 2-member with HA-C4cp addition); same failure-mode configuration (both direction PASS + (a) PASS + (b) FAIL); different operand family (fixed-absolute-threshold vs personal-baseline-rolling). Cross-op-independence gap CLOSED at strict guide r3 §4.2 bar via the two-HA populated cluster.
- Synthesis-structure map [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) cluster row — declared 2026-06-23 as single-member; transitions to 2-member on drift-triggered Stage S₁ re-consumption per pre-reg §9.2 branch.
- **Downstream Stage S₁ (2-member cluster reading)**: [`synthesis/cluster-C-bout-substance.md`](../synthesis/cluster-C-bout-substance.md) — LOCKED r1 2026-07-08 with trivial-ORTHOGONAL single-member reading; r1 → r2 re-consumption triggered by HA-C4cp result.md LOCK per Stage A §5.6 row-6 drift-trigger 5 discipline. Joint verdict expected CONCORDANT-BELOW-THRESHOLD per pre-reg §9.2 anticipated modal outcome + `internal_synthesis.md` §5 verdict table.
- Parent methodology MD: [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) — LOCKED r3 2026-07-09 (Bundle H+ event 7); §3.2.2 "Personal-baseline SD-anchored derivative operand family" (the operand family HA-C4cp operationalises); §3.2 parent absolute-threshold rule HA-C4c uses; §4 per-bout feature set + per-day aggregations; §5.1 stationary-bootstrap E[L]=7 inheritance; §6 framework-validity discipline unchanged (HA11-bout-redo remains the framework-validity gate).
- OI-025 protocol at [`hypotheses/HA-C4c-stringency-companion/protocol.md`](../hypotheses/HA-C4c-stringency-companion/protocol.md) LOCKED r1 2026-07-09 §5.3 (source of pre-committed operand family) + §5.4 (cross-op-independence four-condition argument at operand-family level) + §5.5 (closure-path framing).
- Pipeline extension: [`pipeline/02_features/extract_stress_bouts.py`](../../pipeline/02_features/extract_stress_bouts.py) + [`pipeline/03_consolidate/build_unified_dataset.py`](../../pipeline/03_consolidate/build_unified_dataset.py) Bundle H+ event 8 commit `521e9fe`; SD-anchored derivative operand family columns landed in `per_day_master.csv` + `per_bout_master.csv`.
- Fresh-session /research-review reports: [`reviews/HA-C4cp-2026-07-09.md`](../../reviews/HA-C4cp-2026-07-09.md) verdict PASS-with-caveats (three substantive absorbs at r2 §3.6 compression); [`reviews/bout_level_recovery_dynamics-r3-extension-2026-07-09.md`](../../reviews/bout_level_recovery_dynamics-r3-extension-2026-07-09.md) verdict PASS (three non-blocking clarity refinements at r3 §3.6 compression).
- **Stage A construct (upstream + downstream)**: [`analyses/actionability/construct-bout-recovery-signal.md`](../actionability/construct-bout-recovery-signal.md) LOCKED r4 2026-07-14 (Bundle H+ event 10 §5.2 evidence-layer paragraph update + §5.6 row-6 pointer CLOSED-BY-EXECUTION-PARTIAL + §12 lock log new row; cross-op-independence gap CLOSED at strict guide r3 §4.2 bar via the 2-HA populated cluster).
- OI-033 status: [`_open_inputs.md`](../../methodology/_open_inputs.md) OI-033 CLOSED-BY-EXECUTION-PARTIAL per Bundle H+ event 10 (row 55 + Bundle H+ event 10 lock-log row).
- Layer limitations doc: [`research_line_limitations.md`](../../methodology/research_line_limitations.md) LOCKED r3; §3 seven L-IDs; §5 citation requirements row for `interpretation.md`; §8 downstream-citation-count table (Stage I increments per L-ID cited — L1, L2, L3, L4, L7 for this HA; L5 + L6 NA with reasons).
- **Cross-test cross-references** (per pre-reg §4.10 sister-test companion + `result.md` §7 sister-test table): [HA-C4 v2](../hypotheses/HA-C4/) REJECTED (daily-aggregate); [HA-C4b v3](../hypotheses/HA-C4b/) NOT-SUPPORTED; [HA11 v1](../hypotheses/HA11-stress-udip/) SUPPORTED-on-train; [HA11-bout-redo](../hypotheses/HA11-bout-redo/) PARTIAL (framework-validity cascade-source; unchanged); [HA-C4c-stringency-companion](../hypotheses/HA-C4c-stringency-companion/) NON-TRIGGER (Pass 1 f2(T) monotone descent; OI-025 CLOSED-DESCRIPTIVE-ONLY).
- CONVENTIONS [§1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode) reviewer-mode-with-authorization; [§3.1](../../CONVENTIONS.md#31-personal-baseline-not-absolute-thresholds) personal-baseline discipline (load-bearing for the SD-anchored operand family); [§3.2](../../CONVENTIONS.md#32-lagged-baseline-for-sustained-push-hypotheses) `_lagged_lcera` window discipline (load-bearing for the reference-window construction); [§3.6](../../CONVENTIONS.md) named counts; [§4.1](../../CONVENTIONS.md#41-no-interpretive-marks-on-raw-or-descriptive-layers) descriptive-before-inference; [§4.2](../../CONVENTIONS.md#42-caveats-vs-a-priori-claims) caveats vs a-priori; [§4.3](../../CONVENTIONS.md#43-prior-driven-hypotheses-are-confirmatory) confirmatory framing.
- Literature methodology anchors: [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) (N-of-1 inference reach); [Shamseer / CENT 2015](../../literature/methodology/shamseer_2015_cent_consort_nof1.pdf) (CENT items 21 + 22 generalisability statement); [Tate / SCRIBE 2016](../../literature/methodology/tate_2016_scribe_single_case_reporting.pdf) (L4 participant-as-researcher transparency); [Natesan 2023](../../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf) (defensibility bar).

---

## §9.6 refuse-to-lock gate walk

Per guide §9.6 walk (item-by-item):

1. **A §4.5 L-ID is missing from the citation block that the synthesis-structure map's cluster row lists**: NO — every L-ID from the C-bout-substance cluster row (L1, L2, L3, L4, L7; L5 NA; L6 NA) is cited with one-sentence project-specific application.

2. **§4.4 does not address the four predictable overclaim shapes explicitly**: NO — the four-bullet list is present (below-threshold-as-noise; 2-bar-PARTIAL-to-weak-SUPPORTED; PARTIAL-to-CONFIRMED-or-REJECTED-polar-collapse; PARTIAL-on-cross-phase-pooled-to-per-phase-generalisation). Plus explicit sensitivity-arm overclaim address (motion-clean-only + transient-excluded + Approach A + Holm co-survival cannot promote primary; family-membership caveat load-bearing framing preserved).

3. **§4.8 carries an external-research suggestion without explicit N=1-limit scoping**: NO — both external-research entries explicitly name the L-ID that prevents us from running them ourselves (cross-population cites L1; cross-device-generation cites L3). Both respect locked-plan §3.11 binding.

4. **§4.6 records a divergence without recording both readings or without naming the resolution path**: NO — §4.6 status is USER-ARTICULATED-INHERITED-VIA-HA-C4c-§4.6; the inheritance case does not have a divergence; the L4-pattern-inversion (subject's articulated prior at earlier test drove structural pre-commit of later test's operand family) is explicitly named and traced through the Phase-A + Phase-B + Phase-C + Phase-C-followup precedent block.

5. **§3 / §2 contain verdict-overclaim language that violates §7 anti-patterns**: NO — §2 reports per-cell descriptive numbers with no verdict-label use. §3 reports the operationalisation-bound PARTIAL claim with per-bar pattern preserved + qualifier carried verbatim per guide §5.5. §3 does NOT extend to "Wiggers C4 is confirmed on personal-baseline reference frame" (§7.1 anti-pattern applied at PARTIAL-with-below-threshold — covered at §4.4 bullet 3), does NOT extend to "underlying autonomic mechanism is validated" (§7.2), does NOT reframe the hypothesis (§7.3), does NOT smuggle a lived-experience prior (§7.4 — §4.6 traceability is agreement-does-not-strengthen), does NOT invent caveats post-hoc (§7.5 — all caveats from pre-reg §8 + Stage D audit + cascade context), does NOT collapse PARTIAL into weak SUPPORTED (§7.6 — covered at §4.4 bullet 2), does NOT carry §3.12 subject-narrative commentary (§7.7), does NOT compute or cite predictive-quality measures (§7.8 — no PPV, no base rate), does NOT invent new verdict labels (§7.9 — verdict label carried verbatim from `result.md` §1 throughout).

**Zero substantive §9.6 fires**. The artefact is structurally complete on every §9.6 substantive check.

## §10 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-07-14 | **LOCKED r1** by user acceptance ("yes lets finish what we were doing now" 2026-07-14 driving Bundle H+ event 11 HA-C4cp downstream cascade Stage I → Stage S₁ per pre-reg §9.2 PARTIAL branch DEFERRED artefacts) | HA-C4cp Stage I interpretation LOCKED r1 as part of Bundle H+ event 11 wave. Cluster C-bout-substance is now 2-member (HA-C4c + HA-C4cp); joint verdict CONCORDANT-BELOW-THRESHOLD per pre-reg §9.2 anticipated modal outcome fires at Stage S₁ downstream, NOT here at Stage I. **INVERSE from HA-C4c note**: HA-C4c's Stage I noted an INVERSE failure-mode distinction from cascade-source HA11-bout-redo (HA-C4c: p PASS + magnitude FAIL; HA11-bout-redo: magnitude PASS + p FAIL). HA-C4cp's Stage I notes a **SAME failure-mode configuration as sister-HA HA-C4c** (both: direction PASS + (a) PASS + (b) FAIL; HA-C4cp at ~2× smaller magnitude on the personal-baseline operand family per operand-family compression anticipation). **Reference-window fragility** empirically confirmed via reference_window_shorter_lag REJECTED (δ = +0.037; Holm not-rejected); §3 licensed claim scoped to `[d-90, d-30]` reference-window construction. **Cross-phase pooling MORE load-bearing than HA-C4c**: unmedicated-only sensitivity REJECTED at δ = +0.004 (95% attenuation vs HA-C4c's 50%); the SD-anchored signal essentially vanishes in the unmedicated sub-arm. **Cross-op-independence gap CLOSED** at strict guide r3 §4.2 bar via operand-family-level independence; the joint-cluster shape licensed here (Stage I level) is: two operand-family-independent operationalisations both produce direction-correct + statistically-real + effect-size-below-threshold contrasts on this corpus. **Approach A CI-brackets DIVERGENT (REJECTED)** — pre-reg §8 caveat 3 AMPLIFICATION empirically confirmed for HA-C4cp. **L4-pattern-inversion first-time**: subject's articulated prior at HA-C4c §4.6 (envelope-crossing not training-load-heavy) drove the structural pre-commit of HA-C4cp's SD-anchored operand family; HA-C4cp is the operational realisation of the reframing, not just another test. Fresh-session `/research-review` per plan §4 NOT dispatched pre-lock — user-authorized deviation matching HA-C4c precedent (rationale in Status header). Retained as retrospective drift-trigger per plan §3.7. **Drift triggers registered**: (1) HA-C4cp `result.md` re-runs (e.g. operand-refinement per §9.5 escalation table; pipeline `extract_stress_bouts.py` re-LOCK; parent MD §3.2.2 revision); (2) Stage D `descriptive_audit.md` re-examined; (3) cited methodology MD changes lock-version (parent MD `bout_level_recovery_dynamics.md` r3; OI-025 protocol; `research_line_limitations.md` r3; `synthesis_structure_map.md` r3); (4) cascade-source C-bout-framework re-LOCK or HA11-bout-redo re-run changes framework-validity verdict; (5) **[DISCHARGED-AT-BUNDLE-H+-EVENT-11] Stage S₁ cluster-C-bout-substance r1 → r2 re-consumption lands** (this Stage I re-consumed at that time for the joint-cluster reading); (6) ≥6 months elapse since lock; (7) OI-036 reference-window fragility diagnostic lands (would clarify §4.5 caveat 6 reach). |
