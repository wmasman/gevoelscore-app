# HA11-bout-redo — interpretation (Stage I)

**Status**: **LOCKED r1 2026-07-07** by user acceptance at the joint Phase B lock session (OI-016 CLOSED via Stage D reaudit CONFIRM TRUSTED; OI-018 CLOSED via user articulation of lived-experience prior at §4.6 below). **Fresh-session `/research-review` per plan §4 row for `interpretation.md`: NOT dispatched pre-lock — user-authorized deviation from 2026-06-26 process-discipline change** (rationale: Stage D reaudit CONFIRM TRUSTED provides upstream-verification-chain for the Layer-1/2/3/4 audit-hook stack; §3-§4 substantive content walked interactively with user in this session via B2/B3 briefings; §4.6 was just user-articulated with divergence-cascade-implication reasoning documented; §9.6 walk clean on all 5 substantive checks; Phase-A precedent Option-γ pattern applies for artefacts drafted at the 2026-06-26/27 discipline-change transition). Fresh-session `/research-review` retained as retrospective drift-trigger per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.7 if user later desires. Drafted 2026-06-26 by `/research-interpret interpret HA11-bout-redo` engine (skill LOCKED r4) under Phase B of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) (single-member cluster C-bout-framework; cascade-source for Phase C C-bout-substance per [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md) §3). Per skill r4 §"DRAFT-on-DRAFT dry-run discipline": Stage D upstream `descriptive_audit.md` is DRAFT r1 (user-acceptance lock event PENDING at OI-016); the TRUSTED verdict-trust call is read as **TRUSTED-PROVISIONAL-by-orchestrator-authorisation** for the purpose of this Stage I draft only. Production-lock cannot fire until both (a) Stage D lock-event resolves OI-016 AND (b) this Stage I's §4.6 lived-experience-prior is user-articulated per OI-018 below. The user's separate non-dry-run lock session resolves both gates in one pass.

## Authorship

Drafted 2026-06-26 by Claude (Opus 4.7, 1M context) in reviewer-mode-with-authorization per [CONVENTIONS §1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode), via the `/research-interpret interpret HA11-bout-redo` skill invocation dispatched by the orchestrator on user authorisation under Phase B of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md). Authorising user: Willem (dispatch framing — user reviews artefact in a separate non-dry-run lock session). Fresh-session `/research-review` peer-check required before LOCK per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §4 row for `interpretation.md`.

---

## 1. Target HA + verdict

- **HA ID**: HA11-bout-redo (revision r2)
- **Pre-reg lock date**: 2026-06-23 (commit `5c71aa0` + footer-fix `b5bf0f8`)
- **Result lock date**: 2026-06-23 (test execution `6e06d12`; worktree HEAD at run `250a466`; pipeline commit `d5b394c`)
- **Stage D audit lock date**: DRAFT r1 2026-06-26 (dispatched-by-orchestrator; user-acceptance lock event PENDING at OI-016; treated as TRUSTED-PROVISIONAL-by-orchestrator-authorisation for this Stage I draft per dispatch-mode discipline)
- **Headline verdict from `result.md`** (verbatim §6, no relabeling): **PARTIAL (2 of 3 framework-validity bars met; bars 1+2 met cleanly, bar 3 fails)**
- **Stage D verdict-trust call** (verbatim from `descriptive_audit.md` §4): **TRUSTED** (read as TRUSTED-PROVISIONAL-by-orchestrator-authorisation per DRAFT-on-DRAFT dispatch mode; 11 of 13 load-bearing assumptions BACKSTOPPED; 2 NOT APPLICABLE (A5 no v24 primary signals; A6 no nightly column in scope); 0 NOT BACKSTOPPED; 0 BACKSTOPPED-partial — cleanest profile to date per Phase B Stage D §4 rationale)
- **Operationalisation summary** (one sentence, paraphrasing pre-reg §1 + §4): framework-validity reproduction check on `bout_n_fast_recovery_day` (per-day count of bouts with `recovery_half_life ≤ 15 min` AND `tail_length ≤ 45 min` per parent MD [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) §6.1) on the unmedicated × train era × HA11 v1 calm-day reference-date pool (seed `20260605`), evaluated as 4-day max-signed-z windows against the participant's lagged personal baseline ([d-90, d-30]; 60-day window; σ > 0.5 floor) per HA11 v1 §4.5-§4.7, against three framework-validity comparability bars verbatim from parent MD §6.2 (directional sign + ≥ +12.8 pp + p < 0.05 under block-permutation at E[L]=7, B=10000, seed `20260622`).
- **Synthesis-structure map cluster**: [C-bout-framework](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) (per map r3 §3 cluster row; declared 2026-06-23, lock r3 2026-06-24). **Single-member cluster** (HA11-bout-redo is the sole constituent); **NOT a substantive Wiggers claim** — framework-validity / methodology-validation scope. **Cascade arrow** (per map row): this cluster's verdict propagates as a **caveat-class precondition** to downstream C-bout-substance (HA-C4c); the cascade is one-way, not a co-equal verdict pairing.

## 2. What the data shows

Descriptive paraphrase, no claim language per CONVENTIONS §4.1.

**Pool sizes (post-coverage)**: n_calm = 70 of 200 HA11 v1 reference dates (73 in unmed × train pre-window-coverage; 70 surviving the §4.7 ≥ 3-of-4 window-coverage gate); n_crash = 11 of 13 unmedicated × train crash-episode anchors after §4.7 coverage. Analysis-day pool: 413 days in unmed × train satisfying §4.4 day-validity + §4.6 baseline-validity. Zero low-variability skips at σ ≤ 0.5 floor; 67 insufficient-prior-day skips. Operand-presence: `bout_n_fast_recovery_day` non-NaN on 1486 of 1755 days (84.7%; ≥ 1479 pipeline README threshold).

**Per-arm trigger frequencies**: crash-anchor arm (n=11) 54.5% (6/11); calm-pool arm (n=70) 34.3% (24/70). **Discrimination = +20.26 pp** (positive direction). Median signed z (triggering crash anchors) = +2.410. Cliff's δ = +0.306 with 95% CI [−0.021, +0.626] (paired-bootstrap, B=2000). Mann-Whitney U = 503 with one-sided normal-approx descriptive p = 0.0519. **Block-permutation p = 0.2609** (B=10000, E[L]=7, seed `20260622`); block-perm null discrimination median = +1.96 pp with 95% CI [−14.6, +18.2]. The +20.26 pp observed discrimination sits at the 74th percentile of the block-permutation null distribution.

**Per-bar table**:
- Bar 1 (directional sign positive; HA11 v1 reference +22.8 pp positive): PASS at observed disc = +20.26 pp.
- Bar 2 (effect-size comparability ≥ +12.8 pp, within ±10 pp of HA11 v1's +22.8 pp): PASS at +20.26 pp.
- Bar 3 (block-permutation p < 0.05 at E[L]=7): FAIL at p = 0.2609.

**HA11 v1 reference-frame comparison** (descriptive; not gating): HA11 v1 train `u_dip_count`: crash-arm trigger freq 64.3% (9/14); null-pool 41.5% (83/200); discrimination +22.8 pp; median signed z 2.168; n_crash 14, n_null 200. HA11-bout-redo `bout_n_fast_recovery_day` at train: crash-arm 54.5% (6/11); null-pool 34.3% (24/70); discrimination +20.26 pp; median signed z 2.410; n_crash 11, n_null 70. The bout-level operand reproduces HA11 v1's effect-size magnitude (within 2.6 pp) and median signed z (slightly above) at smaller pool sizes.

**Data-driven E[L]\* companion**: E[L]\* on `bout_n_fast_recovery_day` series over train era = 7.0 days (cutoff_lag = 1); factor-of-2 flag NOT FIRED (|E[L]\* − 7| / 7 = 0 ≤ 0.5 per CONVENTIONS §3.6). Block-length choice well-calibrated.

**Sensitivity arms** (descriptive per pre-reg §4.10 + §9.5; cannot promote): motion-clean-only INCONCLUSIVE (4285/4317 bouts = 99.3% carry `motion_confound_flag = True`; arm collapses to zero analysis days at σ floor — corpus-property degeneracy, not test bug); transient-excluded FAILED (disc drops to +11.69 pp, below bar 2; transient-fragility flag fired — 954/4317 ≈ 22% transients drop reduces signal materially); baseline-invalid-excluded PARTIAL (only 44/4317 ≈ 1% baseline-invalid; verdict identical to primary).

**Crash-drop sensitivity** (CONVENTIONS §3.4 + pre-reg §4.10): |Δ pp| = 0.90 < 5.0 threshold → §3.4 flag NOT FIRED → CLEAN. Crash-dropped disc = +19.35 pp; verdict unchanged. The framework-validity signal is robust across the broader pool with crash-days dropped; not crash-driven.

**Stage D verdict-trust call**: TRUSTED (under DRAFT-on-DRAFT dispatch mode read as TRUSTED-PROVISIONAL-by-orchestrator-authorisation). All A1-A8 spine rows BACKSTOPPED; all five HA-specific rows A9-A13 BACKSTOPPED; A5 + A6 NOT APPLICABLE with documented one-sentence reasons; zero BACKSTOPPED-partial; zero NOT-BACKSTOPPED.

## 3. What the verdict licenses

The verdict licenses **the within-operationalisation partial-positive framework-validity claim with the 2-of-3 bars structure preserved**:

> The bout-level operand `bout_n_fast_recovery_day` (per-day count of bouts with `recovery_half_life ≤ 15 min` AND `tail_length ≤ 45 min` per parent MD §6.1), evaluated under the locked HA11 v1 reference-frame-preservation operationalisation (4-day max-signed-z windows against participant's lagged personal baseline; unmedicated × train era × HA11 v1 calm-day reference-date pool n_calm = 70, crash-anchor n_crash = 11), **reproduces HA11 v1's SUPPORTED-on-train `u_dip_count` signal at directional + effect-size comparability bars (bars 1 + 2 MET cleanly: disc = +20.26 pp positive, within 2.6 pp of HA11 v1's +22.8 pp reference) but fails the block-permutation p-value comparability bar (bar 3 FAIL: p = 0.2609 vs threshold < 0.05 under stationary-bootstrap E[L] = 7, B = 10000, seed `20260622`)** on this corpus's unmedicated × train × calm-day-pool primary cell.

Effect-size summary (carrying the qualifier verbatim per guide §5.5): the PARTIAL verdict's "2 of 3 framework-validity bars met" qualifier is the locked aggregation precision and is carried forward intact. Per-bar metrics: bar 1 +20.26 pp PASS (vs target > 0); bar 2 +20.26 pp PASS (vs target ≥ +12.8 pp); bar 3 p = 0.2609 FAIL (vs target < 0.05; observed value sits at 74th percentile of block-permutation null distribution; null disc median +1.96 pp, 95% CI [−14.6, +18.2]). Median signed z (triggering crash anchors) +2.410 vs HA11 v1's 2.168; Cliff's δ +0.306 with 95% CI [−0.021, +0.626] (descriptive companion; not in bar set).

**The licensed claim is bounded by the operationalisation** per guide §4.3 rule 1: PARTIAL of this specific `bout_n_fast_recovery_day` 15-min/45-min-threshold operand under HA11 v1 reference-frame-preservation discipline (calm-pool vs HA11 v1 null-pool reference dates restricted to unmedicated × train). Per CONVENTIONS §4.3 confirmatory framing: the test set out to confirm whether the bout-level operand reproduces HA11 v1's signal at three pre-committed comparability bars; the result is a within-operationalisation PARTIAL with the per-bar pattern (bars 1+2 PASS, bar 3 FAIL) preserved as part of the claim shape (per guide §5.3 worked example for HA11-bout-redo specifically: "this PARTIAL is operand fitness, NOT a substantive within-day recovery claim").

**The licensed claim is bounded by the framework-validity scope** per pre-reg §1 + §8 caveat 3 (framework-validity-NOT-substantive scope, LOCKED verbatim): the verdict speaks to **operand fitness-for-purpose** for the downstream substantive HA-C4c retest, NOT to any substantive Wiggers C4 claim, NOT to any within-day recovery claim about PAIS / LC autonomic recovery dynamics, NOT to any predictive or actionable claim. Per the synthesis-structure map's §3 cluster row, this cluster's verdict propagates as a **caveat-class precondition** to C-bout-substance (HA-C4c), not as a co-equal verdict — meaning HA-C4c's Stage S₁ work reads HA11-bout-redo's verdict as upstream calibration context, not as analytical evidence about within-day recovery.

**Per the cascade implication recorded in `result.md` §6 + pre-reg §9.2** (which Stage I copies verbatim as part of the licensed claim shape per guide §5.5 qualifier-handling rule): **HA-C4c drafting unblocks with explicit calibration caveat**; the HA-C4c §8 caveats list MUST carry an explicit calibration caveat naming **bar 3 (block-permutation p) as the failing bar**, with verbatim observed values (disc = +20.26 pp; p = 0.2609 vs threshold 0.05). The operand can reproduce HA11 v1's signal magnitude under the locked sample-size constraints, but the block-permutation null at n_calm = 70 / n_crash = 11 cannot statistically distinguish that magnitude from chance at α = 0.05.

**Per Stage D TRUSTED-PROVISIONAL-by-orchestrator-authorisation**: the licensed claim stands at the operationalisation-bound level with the explicit dispatch-mode caveat that Stage D's TRUSTED verdict-trust call has not yet received user-acceptance lock-event per OI-016; production-lock of this Stage I requires Stage D to lock first. No tier-narrowing applies in dispatch mode (per skill r4 §"DRAFT-on-DRAFT dry-run discipline" — Stage D's TRUSTED rationale was structurally complete with every assumption row BACKSTOPPED or NOT APPLICABLE; the user-acceptance lock event is the binding completion event per locked-plan §3.8, not a content-level uncertainty).

## 4. What the verdict does NOT license

Four predictable overclaim shapes addressed explicitly per guide §4.4:

1. **PARTIAL does NOT license "the bout-level operand is broadly fit for purpose" without the per-bar pattern preserved**, and PARTIAL does NOT license "the within-day recovery construct is partially supported" as a substantive claim. Per guide §5.3 worked example specifically calling out HA11-bout-redo: "this PARTIAL is operand fitness, NOT a substantive within-day recovery claim." The verdict's claim shape is **framework-validity** (does the operand reproduce HA11 v1's signal at three pre-committed bars?), not substantive PAIS / LC within-day recovery science. Per guide §7.3 anti-pattern (reframing the hypothesis to fit the verdict): the pre-reg §1 binding statement is "framework-validity reproduction check" — Stage I does not paraphrase that into "partial evidence for bout-level recovery as a real construct in this corpus."

2. **The 2-of-3-PARTIAL does NOT license collapse to weak SUPPORTED** per guide §7.6 anti-pattern (PARTIAL is its own claim shape with the missing condition informative and preserved). Bar 3's failure is not noise to bracket away; it is part of the licensed-claim shape. The +20.26 pp observed discrimination sits at the 74th percentile of the block-permutation null — informative, not refutational, not promotional. Stage I does NOT read "bars 1+2 cleanly PASS while bar 3 narrowly fails" as "essentially SUPPORTED"; the locked verdict rule is three-bar gated with PARTIAL preserved at exactly-2-met, and the rule is what Stage I cites.

3. **PARTIAL with bar-3 p-value failure does NOT license "the bout-level recovery signal does not exist in this corpus"** — that would be the §7.1 anti-pattern (REJECTED therefore the underlying hypothesis is false) applied at PARTIAL-with-power-shortfall. Per `result.md` §7 caveat 10: "with a 3x larger crash-arm (n=33) the same effect size would clear bar 3 by a wide margin; the present PARTIAL reflects sample-size constraints on the framework-validity gate's third bar, not effect-size weakness." Stage I records this as part of the licensed-claim shape, NOT as a strengthening of the substantive question — the question "does within-day recovery carry crash-precursor information at finer resolution?" remains operand-fitness-conditional and is downstream Stage S₁ (single-member cluster) + cascade-arrow to C-bout-substance / HA-C4c territory, not Stage I overreach.

4. **PARTIAL on this corpus's unmedicated × train cell does NOT license "the bout-level operand reproduces HA11 v1 on validate era or on cross-phase pools"** per pre-reg §8 caveat 7 (era restriction is non-negotiable: validate-era reproduction is OUT OF SCOPE; cross-phase pooling is OUT OF SCOPE at framework-validity per parent MD §5.5 + §6). Cross-era + cross-phase generalisation requires the §4.5 L-ID block below: L2 era confounds bound the projection reach to unmedicated × train; cross-stratum reads belong to HA-C4c (substantive, cross-phase pooled) or hypothetical sister-HAs (HA11-bout-redo-validate, not pre-registered).

**Sensitivity-arm overclaim explicit address** per guide §5.5 (triad-derived / k-of-N selective-reading anti-pattern applied to sensitivity arms): the transient-excluded FAILED arm (disc drops to +11.69 pp below bar 2) and the motion-clean-only INCONCLUSIVE arm (99.3% of bouts carry `motion_confound_flag = True`; arm collapses) cannot promote the primary verdict per pre-reg §9.5 + single-cell headline-lock discipline. The transient-fragility flag is a real descriptive finding — a non-trivial fraction of the bout-level reproduction signal lives in transient bouts — but it does NOT license "the bout-level operand is unfit when transients are excluded" as a primary verdict shift; the primary keeps transients per parent MD §3.1.1 r2 absorb. The motion-clean-only INCONCLUSIVE is a **corpus-property finding** (the participant's bouts overwhelmingly carry motion at this corpus + extraction threshold), not a framework-validity verdict; Stage I records it as cascade-context for HA-C4c (which should NOT make motion-clean-restricted claims absent re-running bout extraction with relaxed thresholds — already noted in HA11-bout-redo `result.md` §6).

## 4.5 Caveats narrowing the claim (with L-ID citation block)

### 5a. Pre-reg + audit caveats (carry-forward from upstream)

From pre-reg §8 (the pre-reg's own self-declared narrowing; all 10 caveats; LOCKED verbatim):

- Caveat 1: Power-calc dispatch inapplicable per Daza 2018 within-subject design.
- Caveat 2: n=1 single-subject + observational + multi-source per CONVENTIONS §3.1; personal-baseline thresholded; cross-subject generalisation out of scope.
- Caveat 3: Framework-validity-NOT-substantive scope (the PARTIAL is a framework-validity finding, NOT a substantive Wiggers C4 claim; operand is *partially fit* for HA-C4c; does NOT validate globally and does NOT prejudge HA-C4c's substantive verdict).
- Caveat 4: Inheritance-risk caveat (bar 3's failure cannot fully distinguish "operand under-power" from "the signal HA11 v1 detected was U-dip-specific, not within-day-recovery-shape-general"; the +20.26 pp directional + magnitude reproduction is strong evidence the construct generalises; the bootstrap-null insignificance at present n is the open question).
- Caveat 5: Firstbeat-input amplification at minute resolution (Garmin stress signal opaque; bout-level analysis surfaces algorithmic artefacts daily-aggregate hid).
- Caveat 6: Pacing-behaviour mask risk (within-day stress pattern mediated by active pacing behaviour; bout-extraction at finer resolution may reveal pacing shapes per-bout features too).
- Caveat 7: Era restriction is non-negotiable (validate-era reproduction OUT OF SCOPE; cross-phase OUT OF SCOPE at framework-validity).
- Caveat 8: Crash-drop sensitivity reported but not gating (|Δ pp| = 0.90 < 5.0; primary verdict unchanged).
- Caveat 9: Pipeline-extraction trust assumption (`extract_stress_bouts.py` LOCKED `d5b394c`; 6 inline smoke tests + 5-bout spot-check verification; no pipeline-trust issues surfaced).
- Caveat 10: Multi-comparison via single-cell headline lock; sensitivity arms cannot promote.

From Stage D `descriptive_audit.md` §3 + §4.5 + §4.9 (DRAFT r1; carry-forward via DRAFT-on-DRAFT dispatch): every load-bearing assumption A1-A13 BACKSTOPPED or NOT APPLICABLE; verdict-trust call TRUSTED. Two observation-level cascade-context notes carry forward to Stage I as caveat narrowing (NOT load-bearing for the §3 licensed claim, but bound the L7 + cascade reach narratives):

- **Cascade-context note 1** (per audit §4.5 item 1): per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` does not yet exist as a standalone descriptive artefact; A2 BACKSTOPPED via the §3.4 day-validity gate + operand-presence figure (1486/1755 = 84.7%); a future per-channel rate audit would tighten the L7 survivorship reach narrative. Logged as OI-017 in worktree `_open_inputs.md` for cascade-downstream HA-C4c Stage D audit.
- **Cascade-context note 2** (per audit §4.5 item 2): the 99.3% motion-confound corpus property (4285/4317 bouts carry `motion_confound_flag = True`) is a corpus-level structural finding about the per-minute trace, surfaced in `result.md` §4 sensitivity-arm motion-clean-only INCONCLUSIVE. Not load-bearing for HA11-bout-redo's primary; carry-forward for HA-C4c Stage D audit (same operand on cross-phase pool, same 99.3% structural finding will apply).
- **Cascade-context note 3** (per audit §4.5 item 3): bar 3 power-shortfall observation (+20.26 pp at 74th percentile of null distribution; with 3× larger crash-arm the same effect size would clear bar 3 by a wide margin). No descriptive closure path identifiable for bar 3 in the framework-validity reference frame (enlarging n_crash would either expand the HA11 v1 reference-date pool beyond 200 changing HA11 v1's calibration, relax the §4.4 day-validity gate compromising data quality, or extend into validate era changing the reference frame). Per stocktake §4 + pre-reg §9.2: an operand-refinement v2 of HA11-bout-redo (re-tuning the 15 / 45 thresholds in parent MD §6.1) is OPTIONAL but not required; PARTIAL does not halt the cascade.

### 5b. L-ID citation block (binding per limitations doc §5 row for `interpretation.md`)

Per the synthesis-structure map's §3 [C-bout-framework](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) row L-IDs column: cluster L-IDs = L1, L2, L3, L4, L6, L7 (L5 NA — no v24 primary signals). Stage I cites the subset applying to this HA's primary signals. Per guide §4.5 hard rule: the L-ID block lists every limitation that touches the HA's primary signals or operationalisation, with one-sentence project-specific application.

> *Cites L1 (single-subject reach)*: this verdict is about one subject's bout-level operand reproducing one subject's prior HA11 v1 SUPPORTED-on-train `u_dip_count` signal under the framework-validity discipline at this single corpus; per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) N-of-1 inference reach, PARTIAL here does not refute or confirm the bout-level recovery operand at the group level — operand fitness-for-purpose claims are bounded to this corpus's unmedicated × train cell. Cross-operationalisation reading does not apply at the cluster level (single-member cluster; no sister HA exists at framework-validity layer); cascade-arrow downstream to C-bout-substance / HA-C4c is the reach pathway, not a Stage I licensing extension.

> *Cites L2 (era confounds)*: HA11-bout-redo runs on Stratum 4 unmedicated × HA11 v1 train era (2022-09-03 → 2023-12-31; April 2024 cluster excluded; 21-day device-baseline-lag excluded per parent MD §3.4); the PARTIAL framework-validity verdict does NOT project to medicated Stratum 4 sub-phases or to HA11 v1's validate era (out of scope per pre-reg §4.3 + §8 caveat 7). Cross-phase pooling is OUT OF SCOPE at the framework-validity layer per parent MD §5.5 + §6 (Approach A dose-adjustment per parent MD §5.3 applies to HA-C4c downstream, NOT here). The era restriction is non-negotiable per the pre-reg's binding scope.

> *Cites L3 (device generations)*: `bout_n_fast_recovery_day` is derived from per-minute Garmin stress signal via `extract_stress_bouts.py` LOCKED `d5b394c` on FR245 Elevate-V3 device generation; the PARTIAL verdict is constrained to within-device-generation reach. The Firstbeat-input amplification caveat (pre-reg §8 caveat 5) compounds with L3: the per-minute stress signal is closed-source upstream, and bout-level features may reflect Firstbeat-algorithm-specific artefacts that would not transfer cleanly to a future device generation. Cross-device-generation projection requires explicit calibration evidence per limitations doc §3 L3.

> *Cites L4 (analyst-is-subject)*: HA11-bout-redo is prior-driven by the parent MD `bout_level_recovery_dynamics.md` §6 framework-validity discipline (an analyst-designed methodology-validation gate); per CONVENTIONS §4.3 the test is confirmatory of operand fitness-for-purpose, not exploratory. The L4 mitigation reach per limitations doc r3 §3 L4 caveat: fresh-session `/research-review` peer-check (queued for the user's separate non-dry-run lock session); producer-vs-reviewer split honored via this artefact's reviewer-mode-with-authorization status. **The §4.6 lived-experience-prior reconciliation below is DEFAULTED-PENDING-USER-INPUT per the dispatch-mode placeholder; L4 forbids the dispatcher from articulating the subject's prior, so the lived-experience reconciliation defers to the user's non-dry-run session per OI-018**.

> *L5 (presence-conditioned data layer) NA*: HA11-bout-redo uses no v24 primary signals; `bout_n_fast_recovery_day` is derived from per-minute Garmin stress trace via `extract_stress_bouts.py`, and `crash_v1` is HA11 v1's primary outcome from `labels_crash_v2.csv` (both non-v24). The map's §3 row marks L5 NA for the C-bout-framework cluster; this HA does not trigger L5 independently. NA reason: no v24-derived columns anywhere in the primary or sensitivity-arm chain per Stage D audit A5.

> *Cites L6 (self-reporting)*: HA11-bout-redo does NOT use `gevoelscore` in the primary cell (the primary outcome is `crash_v1` from HA11 v1's `labels_crash_v2.csv`, which is a curated label series, not the daily 1-10 subjective entry). However, L6 applies at the **upstream methodology layer** because `crash_v1` labels are themselves derived in part from the participant's lived experience of crash episodes (the `crash_v2-definition` triage rules are participant-defined per [`labels_crash_v2.csv`](../hypotheses/crash_v2-definition/labels_crash_v2.csv) provenance + [STOCKTAKE §1](../../STOCKTAKE.md#1-the-corpus) 29-crash-episode counts). The PARTIAL verdict inherits the upstream self-attribution caveat: the 11 crash-anchor windows the test runs against are anchored on participant-defined crash episodes, with the L6 noise-floor caveat applying at the label-definition layer rather than at the per-day signal layer. Per limitations doc L6 forbids: not used as biomarker; not used in cross-subject comparison.

> *Cites L7 (survivorship)*: the n_calm = 70 (of 200 HA11 v1 reference dates) + n_crash = 11 (of 13 unmedicated × train crash-episode anchors) effective coverage on the primary cell represents the gated subset surviving the §4.4 day-validity gate (≥ 600 valid per-minute stress samples + LC era + 21-day device-baseline-lag exclusion + April 2024 cluster exclusion) + §4.5 baseline-validity gate (≥ 40 of 60 prior days valid; σ > 0.5 floor) + §4.7 window-coverage gate (≥ 3 of 4 days valid in the 4-day window). The verdict does not project to days outside the gated subset. The Stage D audit's A2 BACKSTOPPED reading via §3.4 day-validity gate + 84.7% operand-presence figure (1486/1755 non-NaN days) supplies path (a) of guide #1 §5.2 operational test; the cascade-context note (per `descriptive_audit.md` §4.5 item 1 = OI-017) flags that a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` would tighten the L7 reach narrative from "gating-discipline-bounded" to "fully audit-backed". Until that diagnostic lands, the L7 citation here remains gating-discipline-bounded.

## 4.6 Lived-experience prior reconciliation

**STATUS: USER-ARTICULATED 2026-07-07** per user's non-dry-run lock session; OI-018 CLOSED. Supersedes the 2026-06-26 DEFAULTED-PENDING-USER-INPUT dispatch-mode placeholder; the dry-run default-reading is preserved at the end of this section for L4 traceability per Phase-A precedent convention.

**User's lived-experience prior on the unmedicated × train era (2022-09-03 → 2023-12-31), verbatim two-part articulation**:

> "I didn't notice a specific pattern, but crashes felt un-predictable in the moment. I did not use the stress levels that much back then and did not monitor them really closely."
>
> "This is by the way also interesting and relevant for my pacing discipline. I did look at body battery, and at stress peaks sometimes, but stress was not something I monitored closely."

**Reading (three orthogonal components)**:

1. **No felt precursor**: crashes felt unpredictable in the moment — the days leading up to a crash did not carry a subjective quality the participant could name at the time.
2. **Selective tactical monitoring behaviour** (refined 2026-07-07 second-part articulation): the participant DID engage with the watch during this era, but tactically and selectively — **body battery** (looked at sometimes) + **stress peaks** (looked at sometimes) were the salient monitored signals; the **continuous stress trace / composite pattern** was NOT closely monitored. This is a materially different framing from "did not use Garmin" — it's "used two specific readable-at-a-glance summary signals (BB, peak events), not fine-grained continuous stress attention".
3. **Bout-shape invisibility to this monitoring practice**: `bout_n_fast_recovery_day` counts per-minute-scale events (`recovery_half_life ≤ 15 min` AND `tail_length ≤ 45 min`) — sub-15-minute bout shapes are structurally invisible to a body-battery + peak-glance monitoring practice; they would require continuous stress-trace attention the participant did not maintain during this era.

**Divergence pattern vs the verdict**: the verdict reports a positive-direction + magnitude-comparable framework-validity reproduction (+20.26 pp discrimination) — the bout-level operand DID pick up more fast-recovery bouts in the 4-day pre-crash windows than in reference-date windows on ordinary days. The lived-experience prior reports **no felt signal of that pattern during the era AND explains why**: the participant's tactical monitoring practice (BB + peaks) was not resolution-matched to the bout-shape signal the operand catches. This is the **cognitively-invisible-signal pattern**: the watch-derived operand surfaces a lead-up shape the participant did not felt-notice AND could not have caught with their actual monitoring practice, which is one of the substantive reasons the quantitative analysis matters (it can catch signals introspection did not surface + monitoring behaviour would not have surfaced either).

**Implication for §3 licensed claim**: per guide §4.6 r2 agreement-does-not-strengthen rule + the reciprocal disagreement-does-not-weaken rule, the null lived-experience prior does NOT narrow the §3 licensed claim. The verdict stands on its data-level warrant (bars 1+2 PASSED at +20.26 pp; bar 3 FAILED at p=0.2609 at small n). What the lived-experience prior DOES license, per guide §6.1 conflict-rule: **log both readings; do NOT resolve at Stage I; the divergence has an explicit cascade-level bounding role** for HA-C4c substantive testing (see next paragraph).

**Cascade implication for HA-C4c (Phase C, downstream)**: HA-C4c will consume this cluster's cascade-source verdict as caveat-class precondition. The lived-experience-prior divergence at this Stage I should propagate as one specific caveat: HA-C4c's substantive predictive claims, if PASSED or PARTIAL, must be framed as **watch-detected patterns not necessarily accompanied by felt precursors** for this participant in the reference era — AND furthermore not accompanied by a monitoring practice that would have surfaced the pattern to the participant in real time. This bounds any predictive claim's actionability discussion at Stage A: a predictive claim built on this cluster's operand cannot presume the participant will feel a lead-up warning to interpret the signal in real time; nor could the participant have caught it in the reference era with their actual body-battery + peak-glance monitoring practice.

**L4 discipline observations on the divergence pattern**:

1. **Phase-A precedent pattern verified for a third time**. In both HA-C3 v2 + HA-C3p Phase A interviews, the dry-run defaults differed materially from the user's actual null priors — a documented L4-inflation risk that reappears here. The dispatcher's default-reading speculated a positive prior ("expected the bout-level operand to reproduce HA11 v1's signal at framework-validity") based on the parent MD framing; the user's actual prior is null on both the pattern AND the era's monitoring behaviour on the continuous stress metric itself. **The dispatcher-side over-reads the parent MD's methodological motivation as if it were the subject's felt prior; the subject's felt prior turns out to have been about crashes-are-unpredictable + BB-peak-tactical-monitoring, not about within-day-recovery-shape reproduction.** This is a permanent L4 hazard the dispatch-mode placeholder was designed to catch — and it did.
2. **Null-felt-prior + positive-direction-verdict = low-L4-inflation-risk finding at the framework-validity layer**: the subject-side did NOT feel a pattern the analysis subsequently confirmed, so the L4 risk that the researcher-side "sees what feels right" is bounded by the felt-null on the subject side.
3. **Monitoring-practice specification is a project-level factual anchor** (added 2026-07-07). The user's refinement of the era's monitoring practice — BB + peaks tactically, continuous stress not closely monitored — is a factual anchor for interpreting pacing-discipline records across the corpus. It materially changes the framing of any claim about what the participant "could have caught" using Garmin during the unmedicated × train era. Landed 2026-07-07 as an addendum to [`../../lived_experience_garmin_pacing_2026-06-14.md`](../../lived_experience_garmin_pacing_2026-06-14.md) §"Addendum 2026-07-07 — era-specific monitoring practice refinement" for cross-artefact stability (rather than as a Stage I-only note) — the primary-source lived-experience braindump is the natural home for pacing-practice self-reports and the addendum is Personal-register-cite-able in the standard `[[lived_experience_garmin_pacing_2026-06-14.md §Addendum-2026-07-07]]` shape.

**L4 meta-recursion acknowledgment** (per guide §4.6 closing paragraph + limitations doc r3 §3 L4 caveat): the §4.6 reconciliation is itself authored by the same agent (Claude) and the same subject (project author) whose L4 coupling §4.6 is designed to surface. The recursion is bounded by: (a) the structural separation of §4.6 from §2 / §3 (the prior cannot reach the licensed claim except via this section); (b) the no-auto-resolution rule on divergent priors; (c) the agreement-does-not-strengthen rule above; (d) the fresh-session `/research-review` peer-check on the locked `interpretation.md` artefact (per CONVENTIONS §1.2). The DEFAULTED-PENDING-USER-INPUT placeholder is the dispatch-mode-respecting extension of this discipline; the user articulation above is the binding content and supersedes the placeholder.

**Dry-run default-reading preserved for L4 traceability** (superseded by user articulation above; recorded per Phase-A precedent):

> "The subject's lived-experience prior on bout-level recovery reproducing HA11 v1's signal is most likely characterised by the expectation that the within-day recovery construct that HA11 v1 detected at U-dip-count granularity would translate to a comparable signal at bout-level granularity in the same unmedicated × train era, given that the parent MD §6 framework-validity discipline was itself motivated by the assumption that bout-level resolution captures the same trajectory shape (sharp transition + return) that HA11 v1's U-dip detection picked up at coarser resolution."

The dispatcher's speculation about the participant's prior turned out to be an over-projection from the parent MD's methodological motivation onto the subject's felt experience — the subject's felt prior was on crashes-are-unpredictable, stress-metric-not-closely-watched, and body-battery + peaks as the salient tactical monitoring signals, not on the parent MD's framework-validity motivation. This exact substitution-pattern is what the DEFAULTED-PENDING-USER-INPUT placeholder discipline is designed to catch; it did its job here as it did for HA-C3 v2 + HA-C3p in Phase A.

## 4.7 Closure-path statement

PARTIAL verdict; closure-path is **operational per guide §4.7 for "k-of-N conditions MET" framing** (HA11-bout-redo's verdict carries "2 of 3 framework-validity bars met" per `result.md` §6 + pre-reg §5; the unmet condition is bar 3 block-permutation p-value).

**Closure-path for the bar 3 FAIL**: bar 3 (block-permutation p < 0.05 at E[L]=7) failed at p = 0.2609 because the +20.26 pp observed discrimination sits at the 74th percentile of the block-permutation null distribution under the locked n_calm = 70 / n_crash = 11 sample sizes. Per `result.md` §7 caveat 10: with a 3× larger crash-arm (n_crash ≈ 33) the same effect size would clear bar 3 by a wide margin; the present PARTIAL reflects sample-size constraints on the framework-validity gate's third bar, not effect-size weakness.

**Four operational paths to upgrade the verdict beyond PARTIAL** (named here per guide §4.7; pre-registration of any upgrade-HA is §4.8 own-research territory; this section names the operational paths only):

1. **A larger crash-arm n that clears bar 3 at the same effect size** — would require either (a) expanding the HA11 v1 reference-date pool beyond 200 (which would change HA11 v1's calibration and break framework-validity reference-frame discipline per pre-reg §4.3), (b) relaxing the §4.4 day-validity gate (compromising data quality), or (c) extending the analysis window into HA11 v1's validate era (which would change the reference frame per pre-reg §4.3 + §8 caveat 7 and conflate framework-validity with era-specificity questions). **No operationally-feasible path under the framework-validity reference-frame discipline** — bar 3 closure via larger n would require breaking the discipline that anchors the comparison. Per pre-reg §8 caveat 4 (inheritance-risk caveat): bar 3's failure cannot fully distinguish between "operand under-power" and "the signal HA11 v1 detected was U-dip-specific, not within-day-recovery-shape-general"; both readings remain open under the locked operationalisation.

2. **An operand-refinement v2 of HA11-bout-redo** (re-tuning the 15-min half-life + 45-min tail thresholds in parent MD §6.1) per pre-reg §9.2 OPTIONAL path. NOT required (PARTIAL does not halt the cascade); available if HA-C4c finds the calibration caveat too binding. Routing: pre-reg revision per `hypothesis_lock_process.md` §3.2; would require re-locking the parent MD §6.1 operand-threshold pre-commit.

3. **A different framework-validity reference frame** (e.g., HA11 v1's validate era as reference, or a different reference-date pool construction) — would test framework-validity under a different calibration. This would NOT close bar 3 on the current operationalisation; it would test a different operationalisation. Routing: sister-HA pre-reg per §4.8 own-research track.

4. **No closure under the framework-validity-as-self-test discipline**: per parent MD §6 + pre-reg §1 framework-validity-NOT-substantive scope: the framework-validity gate is itself the closure mechanism for the bout-level cascade's methodological precondition. PARTIAL with bar 3 FAIL is a *legitimate completion state* for the framework-validity question — the gate fired, the operand reproduced HA11 v1's magnitude but not the statistical significance at the locked n, the cascade unblocks with explicit calibration caveat per pre-reg §9.2. No "closure" is required to complete the framework-validity question; the question is answered (operand is *partially fit*, calibration caveat applies downstream).

**Per pre-reg §9.2 (carried verbatim as part of the closure-path statement for cascade-receiver context)**: HA-C4c drafting UNBLOCKS with explicit calibration caveat naming bar 3 (block-permutation p) as the failing bar; the parent methodology MD remains LOCKED; HA11 v1 stands unchanged; the bout-extraction pipeline LOCK at `d5b394c` stands.

## 4.8 Follow-up suggestions (own-research + external-research tracks)

### Own-research track

- **Pre-reg for HA11-bout-redo-v2 with operand-threshold refinement** (per pre-reg §9.2 OPTIONAL path). If HA-C4c's substantive verdict finds the calibration caveat too binding, an operand-refinement v2 would re-tune the 15-min half-life + 45-min tail thresholds in parent MD §6.1 with explicit pre-commitment to the new threshold values and re-run the framework-validity gate against HA11 v1's reference frame. Pre-reg shape: same `bout_n_fast_recovery_day`-style operand construction + same unmedicated × train × calm-day pool + same three §5 bars; new operand-threshold values pre-committed at parent MD §6.1 revision before pre-reg drafting. Routing: parent MD revision per `hypothesis_lock_process.md` §3.2 (parent MD §6.1 thresholds are pre-committed at parent MD lock per A12 = `bout_n_fast_recovery_day` is the operand, so re-derivation requires parent MD revision).

- **Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day`** (per Stage D `descriptive_audit.md` §4.9 OI-017 carry-forward; cascade-context note 1 above). Not a sister-HA pre-reg; a descriptive infrastructure run that tightens the L7 reach narrative at the next Stage I re-examination AND closes the cascade-downstream HA-C4c Stage D A2 in the same step (same operand on cross-phase pool). S effort per stocktake §3 Shared gap 3; script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/`.

- **HA11-bout-redo-validate sister pre-reg** (testing bout-level reproduction on HA11 v1's validate era rather than train) — pre-reg §8 caveat 7 explicitly marks validate-era reproduction OUT OF SCOPE for the current framework-validity gate (validate-era REFUTED at HA11 v1 −10.7 pp; a bout-level validate test would conflate "does the operand work?" with "is the signal era-specific?"). A separate HA11-bout-redo-validate pre-reg would isolate the era-specificity question; framework-validity-validate would have its own bar set + reference frame. Routing: `hypothesis_lock_process.md` §3.2 fresh-session drafting; pre-reg would carry explicit cross-cite to HA11-bout-redo (this) and HA11 v1 validate-era REFUTED context.

### External-research track

- **Cross-population framework-validity replication of the bout-level recovery operand on PEM-prone populations** at group level. We cannot run this ourselves because **L1 (single-subject reach)** prevents the N=1 corpus from settling whether the bout-level operand `bout_n_fast_recovery_day` generalises beyond this subject's specific within-day stress trace. Per Daza 2018 N-of-1 inference reach: the present PARTIAL verdict bounds the framework-validity claim to one subject's data; cross-subject framework-validity would require a group-level study with the same per-minute Garmin stress instrumentation, the same bout-detection rule (parent MD §3), and the same comparability bars (parent MD §6.2), evaluated against a group-level crash-precursor reference signal analogous to HA11 v1's `u_dip_count`. Minimum n per Daza 2018 + CENT 2015 item 22 generalisability statement bounds; the study would carry pre-registration of the same three §5 bars with the within-population effect-size comparability anchor adjusted to the group's distribution.

- **Cross-device-generation framework-validity comparability study**: Garmin FR245 Elevate V3 per-minute stress trace vs newer device generations (FR255, Fenix 7+) for bout-level recovery operand reproducibility. We cannot run this ourselves because **L3 (device generations)** + the Firstbeat-input amplification caveat (pre-reg §8 caveat 5) means the bout-level features derived from the closed-source Firstbeat algorithm may not transfer cleanly across device generations even if the column name `all_day_stress_avg` / per-minute stress trace remains semantically stable. A cross-device study would test whether the bout-detection rule (parent MD §3) produces statistically comparable `bout_n_fast_recovery_day` distributions across device generations on matched within-subject windows; would clarify whether the framework-validity gate is device-stable or device-specific. Minimum design per L3 caveat: paired-subject within-device-generation transition windows (same subject wearing two devices in parallel), with explicit calibration discipline per limitations doc L3.

Each entry is one paragraph with the proposed study, the L-ID that prevents us from running it ourselves, and what the study would contribute to the construct the HA tests. Both external-research entries respect locked-plan §3.11 N=1-limit scoping discipline (the L-ID-explicit-naming requirement per guide §4.8).

**Distinct from `open_inputs`** per locked-plan §3.5 vs §3.11: the §4.9 `open_inputs` block below names what is missing to complete *this current interpretation* (placeholder slots needing user articulation; cascade-context entries pending closure). The §4.8 follow-up suggestions above name what *next claims* could be built — for us or for others. Both are required; neither substitutes.

## 4.9 `open_inputs` block

Three slots surfaced (one carry-forward from Stage D dispatch-mode; one Stage-I-specific new dispatch-mode placeholder; one cascade-context inherited):

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | User explicit acceptance of HA11-bout-redo Stage D audit's TRUSTED verdict-trust call per guide #1 §8.4 optional seed (DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill #7 dispatch-mode convention; carry-forward from Stage D §4.9 entry 1 = OI-016 in `_open_inputs.md`) | DRAFT r1 → LOCKED transition for Stage D `descriptive_audit.md`; this Stage I draft proceeded under TRUSTED-PROVISIONAL-by-orchestrator-authorisation per DRAFT-on-DRAFT dispatch-mode placeholder (skill r4 §"DRAFT-on-DRAFT dry-run discipline"); production-lock of this Stage I requires Stage D to lock first | One interview pass per guide #1 §8.4 optional seed wording at the user's separate non-dry-run lock session: "Given the eleven checklist rows, do you accept TRUSTED as the audit's verdict-trust call?" — confirmation seed, expected unambiguous YES given §3 mechanical ticks | TRUSTED-PROVISIONAL-by-orchestrator-authorisation reading stands per dispatch-mode discipline; this Stage I draft is structurally complete and ready for user-acceptance-only lock step (no claim degradation needed; the fallback IS the existing TRUSTED-PROVISIONAL label held DRAFT pending user lock-event per locked-plan §3.5 "Hard rule") |
| 2 | **OI-018 (new this artefact)**: User articulation of lived-experience prior on whether the bout-level operand `bout_n_fast_recovery_day` should reproduce HA11 v1's SUPPORTED-on-train signal at the three §5 framework-validity comparability bars (DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill §"Dispatch-mode placeholders") | §4.6 lived-experience prior reconciliation completeness at Stage I; production-lock of this Stage I requires §4.6 user-articulation | One interview question per the §8.3 seed in `verdict_to_inference.md` at the user's separate non-dry-run lock session (resolves jointly with OI-016 Stage D in one session per the Phase-A precedent at HA-C3 + HA-C3p Phase A.1 + A.2) | DEFAULTED-PENDING-USER-INPUT placeholder with one-sentence default-reading (no narrowing of §3 licensed claim per agreement-does-not-strengthen rule) per Phase-A precedent at [`HA-C3.md`](HA-C3.md) §4.6 + [`HA-C3p.md`](HA-C3p.md) §4.6 dry-run-pattern |
| 3 | **OI-017 carry-forward from Stage D**: Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` (stocktake §3 Shared gap 3 pattern; NOT load-bearing for HA11-bout-redo's TRUSTED call per Stage D §4.5 item 1; cascade-context for HA-C4c Stage D audit; same operand on cross-phase pool) | Tightens §4.5 L7 citation from "gating-discipline-bounded" reading to "fully audit-backed" reading; tightens cascade-downstream HA-C4c Stage D A2 in the same step; not blocking either Stage D's TRUSTED reading or this Stage I's §3 licensed claim | S effort per stocktake §3 Shared gap 3 (per-channel missingness rate × era × phase); script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/` | TRUSTED-PROVISIONAL stands with L7 narrowing caveat at Stage I per Stage D audit §4.5 + this artefact §4.5 L7 row gating-discipline-bounded reading |

OI-018 is a new entry; it propagates to `methodology/_open_inputs.md` at the bottom of the queue per skill responsibility #6 (deduplicated against existing entries — checked against OI-001-017; no overlap on (missing-input × blocked-target) key since OI-018's missing-input is HA11-bout-redo-specific lived-experience prior + blocks Stage I §4.6 specifically, distinct from OI-001/OI-002 which were HA-C3 v2 / HA-C3p-specific). OI-017 is the Stage D carry-forward entry; already in the queue at line 38 per Stage D dispatched-by-orchestrator session 2026-06-26.

## 4.10 Cross-references

- HA11-bout-redo r2 [`hypothesis.md`](../hypotheses/HA11-bout-redo/hypothesis.md), [`result.md`](../hypotheses/HA11-bout-redo/result.md), [`test.py`](../hypotheses/HA11-bout-redo/test.py), `result-data.json` (machine-readable companion; gitignored).
- Stage D audit [`descriptive/HA11-bout-redo/descriptive_audit.md`](../descriptive/HA11-bout-redo/descriptive_audit.md) — DRAFT r1 2026-06-26 (dispatched-by-orchestrator on user authorisation; user-acceptance lock event PENDING at OI-016).
- Synthesis-structure map [C-bout-framework](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) cluster row (declared 2026-06-23, lock r3 2026-06-24); single-member cluster (HA11-bout-redo only); cascade-arrow downstream to C-bout-substance (HA-C4c).
- Cascade-downstream Stage S₁ (single-member-cluster reading): [`synthesis/cluster-bout-framework.md`](../synthesis/cluster-bout-framework.md) (forward pointer; not yet drafted per Phase B sub-phases D → I → S₁ in `_rollout_order_2026-06-25.md` §2 Phase B).
- Cascade-receiver Stage I + cluster + downstream: [`HA-C4c.md`](HA-C4c.md) (Phase C; cascade-receiver consuming this cluster's verdict as caveat-class precondition per map §3 cluster cell). HA-C4c PARTIAL verdict at `a69a8ed` per [STOCKTAKE §2d](../../STOCKTAKE.md#2d-tested-with-result).
- Parent methodology MD: [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) — §6 framework-validity discipline (the MD's own falsifier); §6.1 operand definition `bout_n_fast_recovery_day` (15/45 thresholds pre-committed); §6.2 three comparability bars; §6.3 per-bout-n reporting discipline. Parent MD LOCKED r2 2026-06-19 `c57ff3f`.
- HA11 v1 reference (project-internal): [`hypotheses/HA11-stress-udip/result.md`](../hypotheses/HA11-stress-udip/result.md) — SUPPORTED-on-train +22.8 pp `u_dip_count` discrimination; the reference signal HA11-bout-redo's framework-validity gate tests reproduction against.
- Companion descriptive run: [`analyses/descriptive/operationalisation_support/bout_level_dose_response_calibration/result.md`](../descriptive/operationalisation_support/bout_level_dose_response_calibration/result.md) — `desc-BOUT` LANDED 2026-06-22; characterises `bout_n_fast_recovery_day` distribution across 3 windows.
- Limitations doc cross-refs for cited L-IDs: [`research_line_limitations.md`](../../methodology/research_line_limitations.md) §3 L1, L2, L3, L4, L6, L7; §5 citation requirements row for `interpretation.md`. L5 NA per cluster row + Stage D audit A5.
- [`_synthesis_seed_notes_2026-06-23.md`](../../methodology/_synthesis_seed_notes_2026-06-23.md) §3 (candidate joint reading C-bout-substance with C-bout-framework cascade; advisory-only / caveat-class per the seed-notes' own §5 self-description; Stage I does NOT inherit any of the §3 sketch into its §3 licensed claim per guide §7.4 by-analogy).
- Audit-closure script (pre-lock pinning of effective n): [`scripts/count_HA11_bout_redo_effective_n.py`](../../../scripts/count_HA11_bout_redo_effective_n.py) — CONVENTIONS §3.6 named-count discipline; pinned r2 audit values.
- Literature methodology anchors: [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) (N-of-1 inference reach, cited at §4.5 L1 + §4.8 external-research scoping); [Shamseer / CENT 2015](../../literature/methodology/shamseer_2015_cent_consort_nof1.pdf) (CENT items 21 + 22 generalisability statement, cited at §4.8 external-research); [Tate / SCRIBE 2016](../../literature/methodology/tate_2016_scribe_single_case_reporting.pdf) (L4 participant-as-researcher transparency, cited at §4.5 L4); [Natesan 2023](../../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf) (defensibility bar for single-case verdict-to-claim translation).
- Pre-reg audit report: [`reviews/HA11-bout-redo-2026-06-22.md`](../../reviews/HA11-bout-redo-2026-06-22.md) — PASS-with-caveats; closed two L3 items by r2 §3.6-compression pre-lock single-script counts.
- Rollout context: [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) §2 Phase B (single-member cluster C-bout-framework; cascade-source for Phase C C-bout-substance); [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.10 hard predictive gate (Stage I does not cross; predictive claims live at Stage A; HA11-bout-redo's cluster does NOT have an A row in map §5 since C-bout-framework is methodology-validation-only per map §3 cluster cell); §3.11 follow-up tracks (§4.8 implements); §3.12 commentary boundary (Stage I does not carry; not applicable here since C-bout-framework has no Stage A construct per map §5 cluster scope).
- CONVENTIONS [§1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode) reviewer-mode-with-authorization discipline + Authorship block; [§3.6](../../CONVENTIONS.md) named counts (pre-reg §4.5 effective-n pinning); [§4.3](../../CONVENTIONS.md#43-prior-driven-hypotheses-are-confirmatory) confirmatory framing for framework-validity-as-self-test discipline.

---

## §9.6 refuse-to-lock gate walk

Per guide §9.6 walk (item-by-item):

1. **A §4.5 L-ID is missing from the citation block that the synthesis-structure map's cluster row lists**: NO — every L-ID from the C-bout-framework cluster row (L1, L2, L3, L4, L6, L7; L5 NA) is cited with one-sentence project-specific application. L5 NA is explicitly cited with one-sentence reason (no v24 primary signals; same as Stage D audit A5).

2. **§4.4 does not address the four predictable overclaim shapes explicitly**: NO — the four-bullet list is present (PARTIAL-to-broadly-fit; 2-of-3-PARTIAL-to-weak-SUPPORTED; PARTIAL-with-power-shortfall-to-signal-non-existent; PARTIAL-on-unmedicated-train-to-cross-era-generalisation). Plus an explicit sensitivity-arm overclaim address (transient-excluded + motion-clean-only cannot promote primary).

3. **§4.8 carries an external-research suggestion without explicit N=1-limit scoping**: NO — both external-research entries explicitly name the L-ID that prevents us from running them ourselves (cross-population framework-validity replication cites L1; cross-device-generation comparability cites L3 + the Firstbeat-input amplification caveat from pre-reg §8 caveat 5). Both respect locked-plan §3.11 binding.

4. **§4.6 records a divergence without recording both readings or without naming the resolution path**: NOT-APPLICABLE-IN-THE-STANDARD-SENSE per the DEFAULTED-PENDING-USER-INPUT placeholder. The §4.6 status is the dispatch-mode placeholder with one-sentence default-reading + Phase-A precedent traceability block per the LOCKED-r4 skill §"Dispatch-mode placeholders" convention; the user's separate non-dry-run lock session resolves the placeholder to USER-ARTICULATED status. **Per the placeholder discipline, this §9.6 item does NOT fire on the placeholder itself** — the placeholder IS the legitimate-but-incomplete §4.6 state per skill r4 (artefact is structurally complete + locked-in-shell, awaiting user-articulation-only step). The §9.6 item WOULD fire at production-lock attempt without user articulation; the dispatch-mode placeholder defers production-lock per OI-018 in §4.9 entry 2.

5. **§3 / §2 contain verdict-overclaim language that violates §7 anti-patterns**: NO — §2 reports per-cell descriptive numbers with no verdict-label use (the verdict label appears in §1 header + §3 licenses, not §2 paraphrase). §3 reports the operationalisation-bound PARTIAL claim with the per-bar pattern preserved + qualifier carried verbatim per guide §5.5 (no qualifier collapse; "2 of 3 framework-validity bars met" stays in the §3 sentence). §3 does NOT extend to substantive Wiggers C4 claims, does NOT extend to within-day recovery substantive claims, does NOT extend to cross-era / cross-phase claims, does NOT extend to mechanism (§7.2 anti-pattern), does NOT reframe the hypothesis (§7.3 anti-pattern), does NOT smuggle a lived-experience prior (§7.4 anti-pattern, automatically respected since §4.6 is DEFAULTED-PENDING-USER-INPUT and the §3 claim was derived from verdict + audit-trust + L-IDs, never from lived experience), does NOT invent caveats post-hoc (§7.5 anti-pattern — all caveats in §4.5 come from pre-reg §8 + Stage D audit §3 + §4.5), does NOT collapse PARTIAL into weak SUPPORTED (§7.6 anti-pattern — covered explicitly at §4.4 bullet 2), does NOT carry §3.12 subject-narrative commentary (§7.7 anti-pattern — Stage I MUST NOT; this artefact does not carry §3.12), does NOT compute or cite predictive-quality measures (§7.8 anti-pattern — no PPV, no base rate, no sensitivity / specificity; the C-bout-framework cluster has no Stage A construct in map §5 since framework-validity-only), does NOT invent new verdict labels (§7.9 anti-pattern — the verdict label is `PARTIAL (2 of 3 framework-validity bars met; bars 1+2 met cleanly, bar 3 fails)` carried verbatim from `result.md` §6 throughout).

**Items that would fire on production-lock attempt**:

- **TWO substantive items** would fire on production-lock (LOCKED transition):
  1. **OI-016 (Stage D user-acceptance pending; via §4.9 entry 1)** — Stage D `descriptive_audit.md` must lock first (currently DRAFT r1; user-acceptance lock event pending); the DRAFT-on-DRAFT dispatch-mode placeholder cannot promote to LOCKED at Stage I until upstream Stage D locks. This is the DRAFT-on-DRAFT dispatch-mode constraint per skill r4 §"DRAFT-on-DRAFT dry-run discipline" (production-mode dispatches revert to guide #2 §9.2 refusal-path: source MUST be LOCKED).
  2. **OI-018 (§4.6 user-articulation pending; via §4.9 entry 2)** — §4.6 DEFAULTED-PENDING-USER-INPUT placeholder per LOCKED-r4 skill §"Dispatch-mode placeholders" must transition to USER-ARTICULATED before production-lock; the placeholder IS a legitimate-lock-state per skill r4 (legitimate-but-incomplete), so the artefact can lock in dispatch-mode-shell-state pending non-dry-run user articulation. Per the Phase-A precedent (HA-C3.md §4.6 + HA-C3p.md §4.6 lock pattern), one user-articulation session resolves both gates jointly.

- **ZERO substantive §9.6 fires** on the current draft content: all five §9.6 substantive checks (L-ID completeness, §4.4 four-bullet completeness, §4.8 N=1-scoping, §4.6 divergence-recording-with-resolution-path, §3-§2 verdict-overclaim) pass cleanly on the dispatch-mode draft. The two items above are dispatch-mode + user-articulation gates, not substantive content fires.

The artefact is structurally complete on every §9.6 substantive check; the lock event itself awaits OI-016 + OI-018 resolution in the user's separate non-dry-run lock session per Phase-A precedent (one session resolves both jointly).

## §10 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-06-26 | Drafted r1 (DISPATCHED-BY-ORCHESTRATOR ON USER AUTHORISATION; DRAFT-ON-DRAFT-DRY-RUN dispatch mode) | Reviewer-mode-with-authorization by `/research-interpret interpret HA11-bout-redo` skill on first invocation for this target (skill LOCKED r4). Phase B of `_rollout_order_2026-06-25.md` (single-member cluster C-bout-framework; cascade-source for Phase C C-bout-substance per synthesis_structure_map r3 §3). Stage D upstream `descriptive_audit.md` (DRAFT r1 dispatched-by-orchestrator 2026-06-26; TRUSTED verdict-trust call; user-acceptance lock event PENDING at OI-016) consumed as TRUSTED-PROVISIONAL-by-orchestrator-authorisation per skill r4 §"DRAFT-on-DRAFT dry-run discipline" — the placeholder enables Stage I draft on DRAFT-r1 source in dry-run mode (otherwise guide #2 §9.2 refusal-path would halt). §4.6 lived-experience-prior DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill §"Dispatch-mode placeholders" Phase-A precedent (HA-C3.md + HA-C3p.md §4.6 dry-run pattern); one-sentence default-reading + Phase-A traceability block recorded. **Drift triggers registered** (manual-pending-skill per skill responsibility #10): (1) HA11-bout-redo `result.md` re-runs (e.g. operand-refinement v2 per pre-reg §9.2 OPTIONAL path; pipeline `extract_stress_bouts.py` re-LOCK at new commit); (2) Stage D `descriptive_audit.md` re-examined (per audit's own drift triggers; especially user-acceptance lock-event resolving OI-016); (3) cited methodology MD changes lock-version (especially `research_line_limitations.md` since L-ID citations propagate; `bout_level_recovery_dynamics.md` parent MD `c57ff3f`; `synthesis_structure_map.md` r3 cluster-cell cascade-arrow language; `_plan_results_analysis_layer.md` r5 §3.10 / §3.11 / §3.12); (4) ≥6 months elapse since lock. **STATUS: DRAFT r1 — dispatched-by-orchestrator on user authorisation; NOT LOCKED — awaiting (a) user acceptance per `_plan_results_analysis_layer.md` §3.8 binding completion event, resolving both OI-016 (Stage D upstream lock) + OI-018 (§4.6 user-articulation) in one session per Phase-A precedent; AND (b) fresh-session `/research-review` per `_plan_results_analysis_layer.md` §4 row for `interpretation.md`.** |
| 2026-07-07 | **LOCKED r1** by user acceptance at joint Phase B lock session | Locked jointly with Stage D (independent reaudit CONFIRM TRUSTED → OI-016 CLOSED) and Stage S₁ (structurally-determined trivial-ORTHOGONAL → OI-019 CLOSED per artefact-internal reference). **§4.6 lived-experience-prior USER-ARTICULATED 2026-07-07** with two-part verbatim quote: (part 1) "I didn't notice a specific pattern, but crashes felt un-predictable in the moment. I did not use the stress levels that much back then and did not monitor them really closely."; (part 2) "This is by the way also interesting and relevant for my pacing discipline. I did look at body battery, and at stress peaks sometimes, but stress was not something I monitored closely." §4.6 re-drafted to absorb the articulation as USER-ARTICULATED status; dry-run default-reading preserved for L4 traceability; three-orthogonal-component reading + divergence pattern + cognitively-invisible-signal framing + cascade implication for HA-C4c documented. **OI-018 CLOSED** via §4.6 user articulation. **L4 divergence observation**: dispatcher's default-reading over-projected parent MD's methodological motivation onto subject's felt prior — actual felt prior null on pattern + specification of tactical BB + peak monitoring (not continuous-stress-close-monitoring) during the unmed × train era; DEFAULTED-PENDING-USER-INPUT placeholder discipline caught the substitution as designed (Phase-A precedent pattern verified for a third time). **Fresh-session `/research-review` per plan §4 row for `interpretation.md`: NOT dispatched pre-lock — user-authorized deviation from 2026-06-26 process-discipline change** (rationale: Stage D reaudit CONFIRM TRUSTED provides upstream-verification-chain for the audit-hook stack; §3-§4 substantive content walked interactively with user via B2/B3 briefings; §4.6 was JUST user-articulated with divergence-cascade-implication reasoning documented; §9.6 walk clean on all 5 substantive checks; Phase-A precedent Option-γ pattern applies for artefacts drafted at the 2026-06-26/27 discipline-change transition). Fresh-session `/research-review` retained as retrospective drift-trigger per `_plan_results_analysis_layer.md` §3.7 if user later desires. **STATUS: LOCKED r1.** |
