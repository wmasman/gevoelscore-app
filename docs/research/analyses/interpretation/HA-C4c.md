# HA-C4c — interpretation (Stage I)

**Status**: **LOCKED r1 2026-07-08** by user acceptance per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.8 binding completion event at the Phase C cluster-level joint lock session (Q A "yes accept all three" + Q B1 "TRUSTED as-is" + Q B2 user-articulated §4.6 lived-experience prior — **agreement on pattern with reframing on scope + language** first-time-agreement in the L4-inflation-4× series; see §4.6 for the verbatim articulation and traceability update + Q B3 accepted Stage S₁ cascade-receiver framing after more-detail request). Drafted 2026-07-07 by the `/research-interpret interpret HA-C4c` engine (skill LOCKED r4) under Phase C of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) (single-member cluster **C-bout-substance**; cascade-receiver from Phase B **C-bout-framework** which LOCKED r1 2026-07-07 per [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md) §3). Stage D upstream [`descriptive_audit.md`](../descriptive/HA-C4c/descriptive_audit.md) LOCKED r1 2026-07-08 at the same joint session (OI-021 CLOSED). §4.6 lived-experience prior user-articulated at lock (OI-022 CLOSED). Fresh-session `/research-review` per plan §4 row for `interpretation.md` NOT dispatched pre-lock — user-authorized deviation matching Phase-B precedent at [`HA11-bout-redo.md`](HA11-bout-redo.md) §Status (rationale: Stage D LOCKED r1 provides upstream-verification-chain for the audit-hook stack; §3-§4 substantive content walked interactively at lock time via plain-language B2 briefing with stringent-operand-threshold nuance surfaced by user + §3 amended verbatim to reflect it; §4.6 user-articulated verbatim; §4.8 own-research bout-return-stringency descriptive companion + personal-baseline sister candidate added at user's own request with two-step Step-1/Step-2 discipline pre-committed; §9.6 walk clean on all substantive checks; Phase-A + Phase-B precedent Option-γ pattern applies). Retained as retrospective drift-trigger per plan §3.7.

## Authorship

Drafted 2026-07-07 by Claude (Opus 4.7, 1M context) in reviewer-mode-with-authorization per [CONVENTIONS §1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode), via the `/research-interpret interpret HA-C4c` skill invocation dispatched by the orchestrator on user authorisation under Phase C of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md). Authorising user: Willem (dispatch framing — user reviews artefact in a separate non-dry-run lock session). Fresh-session `/research-review` peer-check required before LOCK per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §4 row for `interpretation.md`; user-authorized deviation anticipated at joint lock session per Phase-B precedent (documented in §10 lock log at lock time).

---

## 1. Target HA + verdict

- **HA ID**: HA-C4c (revision r2)
- **Pre-reg lock date**: 2026-06-23 (r2 LOCKED at [`hypothesis.md`](../hypotheses/HA-C4c/hypothesis.md) commit `4e666a2`; substantive absorb `310e145`; fresh-session re-audit clean at [`reviews/HA-C4c-2026-06-23-v2-reaudit.md`](../../reviews/HA-C4c-2026-06-23-v2-reaudit.md) under Option γ canonical 4-stage)
- **Result lock date**: 2026-06-23 (test execution commit `a69a8ed`; worktree HEAD at run `a59997a`; pipeline commit `d5b394c`)
- **Stage D audit lock date**: DRAFT r1 2026-07-07 (dispatched-by-orchestrator; user-acceptance lock event PENDING at OI-021; treated as TRUSTED-PROVISIONAL-by-orchestrator-authorisation for this Stage I draft per dispatch-mode discipline)
- **Headline verdict from `result.md`** (verbatim §1 + §2, no relabeling): **PARTIAL** — direction = +, (a) discrimination PASS at block-permutation p = 0.0001, (b) effect-size FAIL at Cliff's δ = +0.120 vs pre-committed threshold ≥ +0.20 (95% CI [+0.064, +0.178] by paired-bootstrap B=2000). Per-bar table: direction PASS (δ = +0.120 > 0); bar (a) PASS (p = 0.0001 < 0.05); bar (b) FAIL (δ = +0.120 < +0.20). This is an **INVERSE failure-mode configuration** vs the cascade-source HA11-bout-redo PARTIAL (which was bars 1+2 magnitude PASS at +20.26 pp + bar 3 p FAIL at 0.2609 at n_calm=70/n_crash=11); at n_heavy = 465 / n_non_heavy = 809 the cross-phase pooling per parent MD [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) §5.3 + sub-MD [`bout_level_dose_response_calibration.md`](../../methodology/bout_level_dose_response_calibration.md) §6 architectural-implications cleared the power-shortfall constraint the cascade-context anticipated, and the substantive finding is a **weak-effect-but-real positive pattern** per pre-reg §9.2 second PARTIAL configuration.
- **Stage D verdict-trust call** (verbatim from `descriptive_audit.md` §4): **TRUSTED** (read as TRUSTED-PROVISIONAL-by-orchestrator-authorisation per DRAFT-on-DRAFT dispatch mode; 11 of 13 load-bearing assumptions BACKSTOPPED; 2 NOT APPLICABLE (A5 no v24 primary signals; A6 no nightly column / gevoelscore not in primary cell); 0 NOT BACKSTOPPED; 0 BACKSTOPPED-partial — a clean profile matching Phase B Stage D depth per §4 rationale)
- **Operationalisation summary** (one sentence, paraphrasing pre-reg §1 + §4): substantive Wiggers C4 retest at bout resolution on operand `bout_n_did_not_return_day` (per-day count of bouts with `did_not_return_flag == True` per parent MD §3.2 — bouts whose stress did not return to within +5 of `pre_bout_baseline` for ≥ 10 consecutive minutes within 180 minutes after peak) tested as heavy-T-vs-non-heavy-T contrast (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T per HA-C4 v2 §4.1) on the **cross-phase-pooled stratum** on the `citalopram_phase` axis (`citalopram_phase(d) ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}`; equivalently `recovery_phase(d) ∈ {sub-phase 4b (pacing_habit_established), phase 5 (citalopram_modulated)}`; days `>= 2022-11-17` LC era; April 2024 cluster excluded), evaluated via Mann-Whitney U + Cliff's δ + block-permutation null at E[L]=7 (B=10,000; seed `20260623`) against pre-committed verdict bands (SUPPORTED requires both bars; PARTIAL is direction-correct + exactly one bar; REJECTED is direction-wrong or both-bars-fail or crash-drop sign-flip; INCONCLUSIVE is walk-forward-gate or dry-run-sanity failure).
- **Synthesis-structure map cluster**: [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) (per map r3 §3 cluster row; declared 2026-06-23, lock r3 2026-06-24). **Single-member cluster** (HA-C4c is the sole constituent). **Cascade-receiver** from C-bout-framework (HA11-bout-redo Stage D + I + S₁ all LOCKED r1 2026-07-07); per map §3: this cluster's Stage S₁ MUST consume C-bout-framework's verdict as **caveat-class precondition** before reading C-bout-substance's verdict on its own merits, with an **era-scope mismatch** noted (framework runs unmedicated × train; substance runs cross-phase pooled) as structural to each cluster's question rather than a confound to reconcile away. **Stage I here does NOT consume that cascade verdict as a co-equal verdict** per [`verdict_to_inference.md`](../../methodology/verdict_to_inference.md) §5.5 + §6.3 (cluster-level coherence is Stage S₁'s job, not Stage I's); Stage I preserves the calibration-caveat from HA-C4c's own pre-reg §8 caveat 2 which itself names HA11-bout-redo's bar-3 failing pattern as calibration discount.

## 2. What the data shows

Descriptive paraphrase, no claim language per CONVENTIONS §4.1.

**Pool sizes (primary cell, cross-phase pooled)**: n_heavy = 465 days; n_non_heavy = 809 days; primary-stratum non-NaN total = 1274 (matches n_heavy + n_non_heavy exactly; no residual gate-excluded days). Heavy-T mean `bout_n_did_not_return` = 0.742 bouts/day (median 1.00); non-heavy-T mean = 0.588 (median 1.00). Per-arm named counts per pre-reg §4.11: n_did_not_return bouts in heavy-T pool = 821 (sum across days).

**Per-arm statistics**: Mann-Whitney U (heavy first) = 210614; one-sided normal-approx descriptive p = 0.0002. **Cliff's δ = +0.120** (heavy vs non-heavy) with 95% CI [+0.064, +0.178] (paired-bootstrap B=2000). **Block-permutation p = 0.0001** (B=10,000; E[L]=7; seed `20260623`); block-perm null δ median = −0.000 with 95% CI [−0.061, +0.060]. The observed δ = +0.120 sits **well outside** the block-permutation null 95% CI upper bound (+0.060), yielding p = 0.0001; the point estimate sits comfortably in the [+0.10, +0.15] band (fully bracketed by CI [+0.064, +0.178]) — magnitude-below-threshold in the strict pre-committed sense.

**Per-bar table** (`result.md` §2):
- Direction (heavy-T > non-heavy-T; Cliff's δ > 0): **PASS** at δ = +0.120.
- Bar (a) — discrimination (block-perm p < 0.05): **PASS** at p = 0.0001.
- Bar (b) — effect-size (Cliff's δ ≥ +0.20): **FAIL** at δ = +0.120.

**Companion descriptives** (per-day distribution; `result.md` §4): per-day mean = 0.6444 (in §7 expected [0.3, 1.5]); median = 1.00 (≤ 4); σ = 0.6703 (in [0.5, 2.0]); p25-p75 = [0.00, 1.00]; range [0.00, 3.00]. All four §7 dry-run sanity gates PASS at run-time. **E[L]\* data-driven companion** on `bout_n_did_not_return_day[d]` over cross-phase-pooled stratum = **7.00 days** exactly (cutoff_lag = 1); factor-of-2 deviation flag NOT FIRED (|E[L]\* − 7|/7 = 0 ≤ 0.5 per CONVENTIONS §3.6). Block-length choice exactly calibrated.

**Sensitivity arms** (`result.md` §5; descriptive per pre-reg §4.10 + §5.0 single-cell headline lock; cannot promote to SUPPORTED):
- **Unmedicated-only stratum** (n_heavy=183, n_non_heavy=323): δ = +0.059 (< +0.20); p = 0.1145 (≥ 0.05); both bars FAIL; verdict **REJECTED**; **stratum-fragility flag fired** — the effect size is materially smaller than primary (+0.059 vs +0.120) and cannot clear (a) discrimination at the smaller n.
- **Motion-clean-only** (motion_confound_flag = False; n_heavy=465, n_non_heavy=809): heavy mean = 0.002, non-heavy mean = 0.000; δ = +0.002; p = 0.1841; both bars FAIL; verdict **REJECTED**. Manifests as **near-zero-signal arm rather than degenerate-n INCONCLUSIVE** — the pre-reg §8 caveat 4 anticipated INCONCLUSIVE-via-degenerate-n, but observed the same 99.3% motion-confound corpus property (4285/4317 bouts = 99.3% carry `motion_confound_flag = True`; only 32/4317 = 0.7% motion-clean) as a collapse-to-noise variant (only 32 motion-clean bouts across all days). **Motion-fragility flag fired** — read per pre-reg §8 caveat 4 as a **corpus-property reaffirmation** (the bouts-as-currently-extracted ARE motion-tagged events; any "rest-stress" semantic claim is necessarily across the motion-tagged pool), NOT a fragility of the primary verdict.
- **Transient-excluded** (transient_flag = False; n_heavy=465, n_non_heavy=809): δ = +0.116; p = 0.0001; bar (a) PASS + bar (b) FAIL; verdict **PARTIAL** consistent with primary. Transient bouts contribute minimally to the signal at HA-C4c (unlike HA11-bout-redo where transient exclusion dropped disc from +20.26 pp to +11.69 pp); **no transient-fragility flag**.
- **Baseline-invalid-excluded** (baseline_invalid_flag = False): δ = +0.123; p = 0.0001; verdict **PARTIAL** consistent with primary. Baseline-validity fragility not surfaced (44/4317 = 1.0% baseline-invalid; low-impact).

**Crash-drop sensitivity** (CONVENTIONS §3.4 + pre-reg §4.10; `result.md` §5): primary → crash-dropped: n_heavy 465 → 441; n_non_heavy 809 → 745; Cliff's δ +0.120 → +0.135; block-perm p 0.0001 → 0.0001; verdict PARTIAL unchanged. **|Δ Cliff's δ| = 0.015** (threshold 0.10 per CONVENTIONS §3.4; 0.20 sign-flip threshold per pre-reg §5.2): **NOT FIRED (CLEAN)**. **|Δ pp on discrimination| = 1.51 pp** (threshold 5.0 pp per HA11-bout-redo analogue): NOT FIRED. Route-to-REJECTED (|Δ δ| > 0.20 AND sign-flip): NO. The heavy-T signal is robust across the broader pool with crash-days dropped; not crash-driven.

**Approach A dose-adjusted sensitivity arm** (`result.md` §5; pre-reg §4.9 inheritance-by-analogue; descriptive companion under underpowered-NULL frame): three sub-arms all report PARTIAL consistent with primary — primary template β = +0.056/mg (δ = +0.098, p = 0.0075); CI lower bracket β = +0.145/mg (δ = +0.076, p = 0.0352); CI upper bracket β = −0.034/mg (δ = +0.077, p = 0.0259). No β-precision-fragility surfaced; the Approach A construction reports as expected under its own descriptive-companion framing. Not in the Holm family per pre-reg §5.3.

**Holm step-down** (`result.md` §6; 4-of-4 sens arms all valid): at α=0.05 across {primary, transient_excluded, unmedicated_only, motion_clean}: primary Holm-adjusted p = 0.0004 (rejected YES); transient_excluded 0.0004 (YES); unmedicated_only 0.2290 (NO); motion_clean 0.2290 (NO). The primary survives Holm correction; multiplicity-fragility NOT flagged for the primary.

**Sister-test cross-reference table** (`result.md` §7; descriptive only, no cross-test pass conclusion per CONVENTIONS §4.4): HA-C4 v2 REJECTED (daily-aggregate triad sum 0.0/3.0; Ch1 drop_avg SUPPORTED both eras); HA-C4b v3 NOT-SUPPORTED (motion-filter crash-precursor per-episode); HA11 v1 SUPPORTED-on-train (U-dip count +22.8 pp; calm-day sister channel); HA11-bout-redo PARTIAL (framework-validity 2-of-3 bars met; bar 3 p=0.2609 at n_calm=70/n_crash=11); HA-C4c (this test) PARTIAL (cross-phase-pooled bout-level; δ=+0.120, p=0.0001, n_heavy=465, n_non_heavy=809).

**Stage D verdict-trust call**: TRUSTED (under DRAFT-on-DRAFT dispatch mode read as TRUSTED-PROVISIONAL-by-orchestrator-authorisation). All A1-A8 spine rows BACKSTOPPED; all five HA-specific rows A9-A13 BACKSTOPPED (A9 cross-phase pooling discipline inheritance; A10 pipeline trust; A11 Approach A inheritance-by-analogue discipline; A12 motion-confound corpus property; A13 bout-level operand-threshold validity); A5 + A6 NOT APPLICABLE with documented one-sentence reasons; zero BACKSTOPPED-partial; zero NOT-BACKSTOPPED.

## 3. What the verdict licenses

The verdict licenses **the within-operationalisation partial-positive substantive claim with the per-bar pattern preserved (bar (a) PASS + bar (b) FAIL) and the below-threshold effect-size treated as part of the claim shape**:

> The bout-level operand `bout_n_did_not_return_day` (per-day count of bouts with `did_not_return_flag == True` per parent MD §3.2 — bouts whose stress fails to return to within +5 of `pre_bout_baseline` for ≥ 10 consecutive minutes within 180 minutes after peak) evaluated under the locked cross-phase-pooled substantive operationalisation (heavy-T-vs-non-heavy-T contrast on `citalopram_phase(d) ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}` × recovery-phase 4b + 5 stratum; n_heavy = 465, n_non_heavy = 809) **produces a direction-correct + statistically-distinguishable + effect-size-below-threshold contrast on this corpus's substantive primary cell — Cliff's δ = +0.120 (95% CI [+0.064, +0.178]) fully bracketed below the pre-committed +0.20 discipline threshold, with block-permutation p = 0.0001 well below α = 0.05 (E[L] = 7, B = 10000, seed `20260623`)** on the cross-phase-pooled stratum.

Effect-size summary (carrying the qualifier verbatim per guide §5.5): the PARTIAL verdict's "direction-correct AND (a) discrimination PASS AND (b) effect-size FAIL" per-bar pattern is the locked aggregation precision and is carried forward intact. Per-bar metrics: direction +0.120 PASS (vs target > 0); bar (a) p = 0.0001 PASS (vs target < 0.05; observed p sits at essentially the lowest empirically-representable value under B=10000 draws); bar (b) δ = +0.120 FAIL (vs target ≥ +0.20; observed 95% CI [+0.064, +0.178] entirely below threshold — the below-threshold reading is not a marginal miss but a fully-bracketed structural feature of the signal's magnitude at this corpus's cross-phase-pooled n). Mann-Whitney U = 210614 (descriptive companion; not in bar set); block-perm null δ median −0.000 with 95% CI [−0.061, +0.060] (observed +0.120 sits well outside the null upper bound).

**The licensed claim is bounded by the operationalisation** per guide §4.3 rule 1: PARTIAL of this specific `bout_n_did_not_return_day` operand on this specific cross-phase-pooled stratum under this specific heavy-T-vs-non-heavy-T contrast. Per CONVENTIONS §4.3 confirmatory framing: the test set out to confirm whether the bout-level operand's per-day count is systematically higher on heavy-T days than on non-heavy-T days at cross-phase-pooled resolution, with two pre-committed bars (discrimination + effect-size); the result is a within-operationalisation PARTIAL that clears one bar cleanly and fails the other in an informative-and-preserved way. Per guide §5.3 mapping rule for PARTIAL: the unmet condition — bar (b) effect-size — is part of the licensed claim shape and is preserved in this §3 sentence; the licensed claim is **direction-of-signal-at-magnitude**, and the below-threshold effect-size is part of the signal's shape at this corpus's operationalisation, NOT noise to bracket away.

**INVERSE failure-mode distinction from the cascade-source HA11-bout-redo** (worth naming explicitly): HA11-bout-redo at n_calm=70/n_crash=11 had magnitude PASS (bars 1+2 at +20.26 pp / median signed z 2.410) + p FAIL (bar 3 at p = 0.2609). HA-C4c at cross-phase-pooled n_heavy=465/n_non_heavy=809 has p PASS (p = 0.0001) + magnitude FAIL (δ = +0.120 < +0.20). The cross-phase pooling per parent MD §5.3 + sub-MD §6 architectural-implications cleared HA11-bout-redo's bar-3 power-shortfall constraint at exactly the substantive layer HA-C4c pre-reg §8 caveat 2 anticipated — but the substantive contrast, once statistically distinguishable at the larger n, sits at a genuinely smaller magnitude than the pre-committed +0.20 discipline threshold. The two verdicts are structurally different claims: HA11-bout-redo licenses "operand fitness at magnitude/direction under HA11 v1's reference frame at bout resolution but not statistically distinguishable at n_crash=11" (framework-validity); HA-C4c licenses "substantive heavy-T contrast is direction-correct + statistically real + below discipline threshold on cross-phase-pooled n" (substantive-at-known-magnitude). **This distinction is substantive-vs-framework-validity and Stage I preserves it** rather than folding them into a single narrative.

**Per the cascade implication recorded in `result.md` §1 + §11 + pre-reg §9.2**, the licensed claim carries the second PARTIAL configuration explicitly: direction-correct + (a) discrimination PASS + (b) effect-size FAIL — a **weak-effect-but-real positive pattern**. The pre-reg pre-registered this exact configuration as a legitimate PARTIAL outcome; Stage I does not re-interpret it as a marginal SUPPORTED or as a de-facto REJECTED. The signal exists in the predicted direction, is statistically real at the cross-phase-pooled scale, and is smaller than the +0.20 discipline threshold — those three facts together are the shape of the licensed claim.

**Stringent-operand-threshold reading of the below-threshold effect size** (user-surfaced at Phase C cluster-level lock session B2 briefing 2026-07-08; substantive addition to §3 per user request that the interpretation carry this framing): the operand's `did_not_return_flag` threshold is genuinely stringent by construction — a bout is flagged only when stress fails to return to within +5 of `pre_bout_baseline` for ≥ 10 consecutive minutes within **180 minutes (3 hours) after peak**. Three hours of not-returning is a substantially long tail; the operand captures the **extreme tail** of the within-day recovery-failure distribution rather than everyday elevation. The per-day base rate on cross-phase-pooled stratum is mean = 0.6444 bouts/day (median 1.00, p25-p75 [0.00, 1.00], range [0.00, 3.00]) — most days carry zero or one such stringent event; days with multiple are rare. Reading the observed δ = +0.120 against this stringent threshold: **a modest elevation on an already-rare stringent event is substantively different from the same δ on a common event.** The 15% relative-rate elevation (heavy-T mean 0.742 vs non-heavy-T mean 0.588 stringent stuck-stress bouts/day) is a real elevation on an already-tail category, which is substantively more interesting than the same relative elevation would be on a common everyday event. **This does NOT elevate the licensed claim above PARTIAL** — the two-bar discipline rule stands, and δ = +0.120 with CI fully below +0.20 remains the effect-size FAIL configuration per pre-reg. It DOES qualify the claim's substantive-relevance interpretation: the below-threshold effect size is not evidence of no substantive Wiggers C4 phenomenon, but rather evidence of a **stringent-threshold-captured small elevation on the extreme tail of within-day recovery-failure**. What the stringent operand asks is a hard question; the operand's answer is direction-correct + statistically real at modest magnitude. Whether the effect is more clearly-articulated at less-stringent thresholds (e.g. 30-min / 60-min / 120-min return-window variants) is exactly the descriptive-companion question logged as own-research follow-up in §4.8 below (Step 1 Pass 1 characterises the operand distribution across stringency thresholds before any inferential retest; Step 2 pre-registration follows if descriptively-motivated).

**Per Stage D TRUSTED-PROVISIONAL-by-orchestrator-authorisation**: the licensed claim stands at the operationalisation-bound level with the explicit dispatch-mode caveat that Stage D's TRUSTED verdict-trust call has not yet received user-acceptance lock-event per OI-021; production-lock of this Stage I requires Stage D to lock first. No tier-narrowing applies in dispatch mode (per skill r4 §"DRAFT-on-DRAFT dry-run discipline" — Stage D's TRUSTED rationale was structurally complete with every assumption row BACKSTOPPED or NOT APPLICABLE; the user-acceptance lock event is the binding completion event per locked-plan §3.8, not a content-level uncertainty).

## 4. What the verdict does NOT license

Four predictable overclaim shapes addressed explicitly per guide §4.4:

1. **PARTIAL does NOT license Cliff's δ = +0.120 as a "small-but-real substantive within-day recovery signal" without the below-threshold pattern preserved.** Per guide §5.3 anti-overclaim: below-threshold effect sizes are part of the licensed-claim shape at PARTIAL-with-(b)-failing, NOT noise to bracket away. The point estimate δ = +0.120 with 95% CI [+0.064, +0.178] sits ENTIRELY below the pre-committed +0.20 discipline threshold — not straddling it, not marginally missing it, but fully-bracketed-below at the [+0.06, +0.18] band. Reading this as "essentially SUPPORTED at small effect size" would collapse the discipline distinction the pre-reg locked between "SUPPORTED at δ ≥ +0.20" and "PARTIAL at direction-correct-but-below-threshold." Stage I preserves the fully-bracketed-below shape as part of the claim, not as a footnote to be papered over.

2. **The PARTIAL does NOT license collapse to weak SUPPORTED** per guide §7.6 anti-pattern (PARTIAL is its own claim shape with the missing condition informative and preserved). Bar (b)'s failure is not noise to bracket; it IS part of the licensed-claim shape at PARTIAL-with-(b)-failing, and the magnitude is NOT upgraded by force of the (a) discrimination PASS. The p = 0.0001 clears bar (a) very comfortably; that is informative about **whether the small effect exists at cross-phase-pooled scale** (it does — statistical distinguishability from block-permutation null is genuinely at very high confidence at n_heavy=465/n_non_heavy=809), NOT about **whether the effect is large enough to meet the discipline's substantive-relevance bar** (it isn't — δ = +0.120 with CI fully bracketed below +0.20 is a discipline-below-threshold contrast). Stage I does NOT read "bar (a) passes very cleanly while bar (b) fails" as "essentially SUPPORTED"; the locked verdict rule is two-bar gated with PARTIAL preserved at exactly-one-met + direction-correct, and the rule is what Stage I cites.

3. **PARTIAL does NOT license "Wiggers C4 is REJECTED at bout level" NOR "Wiggers C4 is CONFIRMED at bout level"** per guide §7.1 anti-pattern applied at PARTIAL-with-below-threshold-effect-size. The PARTIAL is direction-confirmed + statistically-real + effect-size-below-threshold, which is a **distinct claim from either polar reading**. Reading toward CONFIRMED would smuggle the p PASS into a magnitude claim the effect-size FAIL directly disconfirms; reading toward REJECTED would smuggle the effect-size FAIL into a signal-non-existence claim the direction PASS + p PASS jointly disconfirm. Both smuggling moves erase the licensed shape: **the Wiggers C4 "stress fails to drop during rest periods" claim at bout resolution on this corpus's operationalisation is direction-correct and statistically real at the cross-phase-pooled scale, with a smaller magnitude than the pre-committed +0.20 discipline threshold** — that is the licensed claim, and it is distinct from both "Wiggers C4 confirmed at bout level" and "Wiggers C4 refuted at bout level". Per pre-reg §8 caveat 9: HA-C4c does NOT supersede HA-C4 v2's REJECTED-at-daily-aggregate verdict; the two verdicts coexist as pointers on the C4 register row, one at each resolution scale, and the resolution-of-scale story is Stage S₁ / S₂ / cross-test-interpretation-pass territory NOT Stage I.

4. **PARTIAL on cross-phase pooled stratum does NOT license per-phase claims.** The unmedicated-only sensitivity arm REJECTED at δ = +0.059 with p = 0.1145 (n_heavy=183 / n_non_heavy=323) is a **stratum-fragility finding** per pre-reg §9.5 (substantive: cross-phase pooling is load-bearing for the headline signal; the medicated phases contribute to the contrast). Per guide §4.4 predictable-overclaim template: the primary cross-phase-pooled PARTIAL cannot be silently generalised to the unmedicated sub-stratum where the effect materially attenuates and (a) discrimination fails; nor to any per-phase sub-stratum absent a per-phase test. Per pre-reg §8 caveat 8: cross-phase pooling permissibility is conditional on the recalibration's 0/7 CONFIRMED reading; the observed stratum-fragility is the descriptive signature of the pooling being load-bearing on this corpus × operand. **The claim is bounded to cross-phase-pooled reach**; per-phase reach is out of scope at Stage I and the stratum-fragility becomes Stage S₁ / S₂ contextualisation territory (what does it mean about the underlying autonomic mechanism that the medicated phases contribute more of the headline contrast?) rather than Stage I over-projection.

**Sensitivity-arm overclaim explicit address** per guide §5.5 (selective-reading anti-pattern applied to sensitivity arms): the motion-clean-only REJECTED via near-zero signal (δ = +0.002; p = 0.1841; only 32 motion-clean bouts across the whole corpus) is a **corpus-property reaffirmation** per pre-reg §8 caveat 4 (the bouts-as-currently-extracted ARE motion-tagged events at 99.3%), NOT a fragility of the primary verdict — reading it as "the operand fails when we look at rest periods only" would invert the semantic; the 99.3% corpus property means there is no "rest-period-only" bout pool available at the currently-locked extraction threshold, so the arm collapses to noise by construction rather than by signal weakness. The transient-excluded PARTIAL consistent with primary (δ = +0.116; p = 0.0001) is a robustness observation, NOT a licensing extension; the primary's PARTIAL character does not gain strength from the sensitivity arms clearing sensitivity checks. The Approach A dose-adjusted sub-arms (all three PARTIAL consistent) are descriptive companion under the underpowered-NULL frame per pre-reg §4.9 + §8 caveat 3, explicitly NOT a load-bearing dose-correction of the primary; reading them as "dose-adjusted Wiggers C4 at bout level is PARTIAL" would silently promote the inheritance-by-analogue construction to a substantive claim it structurally cannot carry.

## 4.5 Caveats narrowing the claim (with L-ID citation block)

### 5a. Pre-reg + audit caveats (carry-forward from upstream)

From pre-reg §8 (the pre-reg's own self-declared narrowing; all 10 caveats; LOCKED verbatim):

- Caveat 1: Power-calc dispatch inapplicable per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) within-subject design; block-permutation null at E[L]=7 is the within-subject inferential machinery; INCONCLUSIVE routing on n-shortfall per §4.7 walk-forward gate.
- Caveat 2: **Framework-validity calibration caveat from HA11-bout-redo PARTIAL — load-bearing per cascade-context discipline.** HA11-bout-redo's framework-validity gate cleared bars 1+2 at +20.26 pp / median signed z 2.410 but bar 3 FAILED at p=0.2609 at n_calm=70/n_crash=11. The HA-C4c verdict MUST be read with this calibration context visible. **Empirically at HA-C4c**: the cross-phase pooling cleared the power-shortfall as anticipated (bar (a) PASS at p=0.0001 at n_heavy=465/n_non_heavy=809), so the calibration-caveat propagates now with the empirical observation that the substantive verdict fails on the OTHER bar (effect-size, bar (b)), not the same bar HA11-bout-redo failed on — a distinct-failure-mode finding across the two clusters.
- Caveat 3: β-recalibration dose-naive primary framing per sub-MD §6 architectural-implications (0/7 features CONFIRMED at this corpus's bout-level n → Approach A NOT load-bearing → cross-phase pooling permitted without §5.A/B/C inheritance violation); Approach A sensitivity arm is inheritance-by-analogue descriptive companion under the underpowered-NULL frame, NOT a load-bearing dose-correction.
- Caveat 4: 99.3% motion-confound corpus property (4285/4317 bouts carry `motion_confound_flag = True`); the entire HA-C4c operand inherits this; Wiggers' "during rest periods" language is operationalised across the motion-tagged bout pool by construction; motion-clean-only arm REJECTED via near-zero signal is corpus-property reaffirmation, NOT primary fragility.
- Caveat 5: Transient-fragility risk (per HA11-bout-redo transient exclusion dropped disc from +20.26 pp to +11.69 pp); HA-C4c primary INCLUDES transients per parent MD §3.1 r2 absorb; **empirically not fired at HA-C4c** — transient-excluded arm δ = +0.116 vs primary δ = +0.120 (consistent).
- Caveat 6: n=1 single-subject + observational + multi-source per CONVENTIONS §3.1; personal-baseline thresholded; cross-subject generalisation out of scope.
- Caveat 7: Operational vs mechanistic per CONVENTIONS §4.1; per-bout features are operational descriptions of the per-minute Garmin stress trace, NOT mechanistic measurements of autonomic state; upstream Firstbeat algorithm opaque (closed-source); per-bout features surface algorithmic artefacts daily-aggregate hid.
- Caveat 8: Cross-phase pooling permissibility is conditional on the recalibration's 0/7 CONFIRMED reading; if a future re-run produces CONFIRMED features, the pooling permission may need to be revisited and HA-C4c would need a v2 with per-phase stratification OR Approach A as primary. **Empirically at HA-C4c**: the unmedicated-only sensitivity arm REJECTED at δ=+0.059 vs primary PARTIAL at δ=+0.120 IS the descriptive signature of the pooling being load-bearing for the substantive signal on this corpus; the caveat is now empirically live.
- Caveat 9: HA-C4 v2 daily-aggregate REJECTED is the prior state of evidence at coarser resolution; HA-C4c does NOT supersede it — the two verdicts coexist as pointers on the C4 register row at their respective resolution scales; the resolution-of-scale story is Stage S₁ / S₂ territory.
- Caveat 10: Pacing-behaviour confounder inherited from HA-C4 v2 §8 + parent MD §2.4 r2 absorb; within-day stress pattern potentially mediated by active pacing behaviour (participant uses Garmin stress as a live pacing signal); cross-phase-pooled stratum partially insulates by spanning post-2022-11-17 pacing-stable LC era but cannot fully eliminate; SUPPORTED-here-NOT-SUPPORTED-at-HA-C4b shape consistent with protective-rather-than-predictive alternative reading from HA-C4b v3 §9.

From Stage D `descriptive_audit.md` §3 + §4.5 + §4.9 (DRAFT r1; carry-forward via DRAFT-on-DRAFT dispatch): every load-bearing assumption A1-A13 BACKSTOPPED or NOT APPLICABLE; verdict-trust call TRUSTED. Three observation-level cascade-context notes carry forward to Stage I as caveat narrowing (NOT load-bearing for the §3 licensed claim, but bound the L-ID reach narratives + cascade context):

- **Cascade-context note 1** (per audit §4.5 item 1): cascade-source verdict from C-bout-framework (HA11-bout-redo Stage D + I + S₁ all LOCKED r1 2026-07-07) is caveat-class precondition per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5; framework-validity operand-fitness verdict is PARTIAL (bars 1+2 PASS at +20.26 pp / median signed z 2.410; bar 3 p FAIL at 0.2609 at n_calm=70/n_crash=11) — operand fitness-for-purpose validated at magnitude/direction level but power-limited on that gate. Per map §3 cluster row: propagates as caveat-class precondition to C-bout-substance; NOT a co-equal verdict that S₁ reconciles against; **era-scope mismatch** is structural to each cluster's question rather than a confound to reconcile away. Stage I here notes for traceability; **cascade-consumption obligation is at Stage S₁ downstream, NOT here**. Empirical observation now confirmed: the cross-phase pooling cleared the power-shortfall as the cascade-context expected.
- **Cascade-context note 2** (per audit §4.5 item 2): per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` does not yet exist as a standalone descriptive artefact; A2 BACKSTOPPED via the §3.4 day-validity gate + primary-stratum non-NaN days figure (1274 exactly = n_heavy + n_non_heavy); a future per-channel rate audit would tighten the L7 survivorship reach narrative. Logged as OI-023 in worktree `_open_inputs.md` for cascade-context; sister to OI-017 (`bout_n_fast_recovery_day`) — joint two-operand run available per §4.5 audit note.
- **Cascade-context note 3** (per audit §4.5 item 3): stratum-fragility observation — unmedicated-only sensitivity arm REJECTED at δ = +0.059 vs primary PARTIAL at δ = +0.120 (empirically live per pre-reg §9.5). Cross-phase pooling is empirically load-bearing for the substantive signal; medicated phases contribute to the headline contrast, likely via dose-shaped baseline shifts the operand picks up (per pre-reg §8 caveat 10 pacing-behaviour narrative). No descriptive closure path at Stage D; substantive property of operand × stratum interaction on this corpus that Stage I foregrounds and downstream Stage S₁ + S₂ contextualise (what does it mean about the underlying autonomic mechanism that the medicated phases contribute more of the contrast?).

### 5b. L-ID citation block (binding per limitations doc §5 row for `interpretation.md`)

Per the synthesis-structure map's §3 [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) row L-IDs column: cluster L-IDs = **L1, L2, L3, L4, L7** (L5 NA — no v24 primary signals; L6 NA — gevoelscore not in primary cell). Stage I cites the subset applying to this HA's primary signals. Per guide §4.5 hard rule: the L-ID block lists every limitation that touches the HA's primary signals or operationalisation, with one-sentence project-specific application.

> *Cites L1 (single-subject reach)*: this verdict is about one subject's `bout_n_did_not_return_day` operand producing a direction-correct + statistically-distinguishable + effect-size-below-threshold contrast between heavy-T and non-heavy-T days at the cross-phase-pooled substantive layer on this single corpus; per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) N-of-1 inference reach, PARTIAL here does not project the +0.120 effect-size point estimate to a group-level substantive claim about Wiggers C4 at bout resolution. Cross-operationalisation reading does not apply at the cluster level (single-member cluster; no sister HA exists at substantive layer at bout resolution); cross-test cross-reference to HA-C4 v2 REJECTED (daily-aggregate) is a resolution-of-scale pair (Stage S₁ / S₂ territory), NOT a Stage I licensing extension.

> *Cites L2 (era confounds)*: HA-C4c runs on the LC era cross-phase-pooled stratum (2022-11-17 → present; April 2024 cluster excluded; 21-day device-baseline-lag excluded per parent MD §3.4; primary spans `citalopram_phase ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}`); the PARTIAL substantive verdict does NOT project to pre-LC era or to the unmedicated-only sub-stratum on the citalopram axis (where the sensitivity arm REJECTED at δ=+0.059 p=0.1145 — a stratum-fragility finding making the cross-phase pooling load-bearing, per pre-reg §8 caveat 8 conditional-nature framing + §9.5). The era restriction is structural to the cross-phase-pooled operationalisation; per-phase reach is out of scope at Stage I and is the stratum-fragility direction downstream Stage S₁ contextualises.

> *Cites L3 (device generations)*: `bout_n_did_not_return_day` is derived from per-minute Garmin stress signal via `extract_stress_bouts.py` LOCKED `d5b394c` on FR245 Elevate-V3 device generation; the PARTIAL verdict is constrained to within-device-generation reach. The Firstbeat-input amplification caveat (pre-reg §8 caveat 7 operational-vs-mechanistic + inherited from HA11-bout-redo pre-reg §8 caveat 5) compounds with L3: the per-minute stress signal is closed-source upstream, and bout-level features may reflect Firstbeat-algorithm-specific artefacts that would not transfer cleanly to a future device generation. The 99.3% motion-confound corpus property (pre-reg §8 caveat 4) is itself a device × extraction-threshold interaction that a different device generation might show at a different rate. Cross-device-generation projection requires explicit calibration evidence per limitations doc §3 L3.

> *Cites L4 (analyst-is-subject)*: HA-C4c is prior-driven by the Wiggers C4 verbatim source-verified reading (pre-reg §2 prior 1; PDF lines 1112-1119, 1140-1143, 1223-1231, 1306-1314 — five passages directly supporting the within-day "failure to recover" framing) + the parent MD `bout_level_recovery_dynamics.md` §1.3 substantive-retest-at-bout-resolution enablement; per CONVENTIONS §4.3 the test is confirmatory of the Wiggers C4 substantive claim at bout resolution on this corpus, not exploratory. The L4 mitigation reach per limitations doc r3 §3 L4 caveat: fresh-session `/research-review` peer-check (expected user-authorized-deviated at joint lock session per Phase-B precedent, documented at lock time); producer-vs-reviewer split honored via this artefact's reviewer-mode-with-authorization status. **The §4.6 lived-experience-prior reconciliation below is DEFAULTED-PENDING-USER-INPUT per the dispatch-mode placeholder; L4 forbids the dispatcher from articulating the subject's prior, so the lived-experience reconciliation defers to the user's non-dry-run session per OI-022**. Phase-A + Phase-B precedents (HA-C3 v2, HA-C3p, HA11-bout-redo) all diverged from the dispatcher's default-reading in ways that the placeholder discipline was designed to catch — that verified L4 pattern is now recorded three times and is expected to hold on the fourth (see §4.6 traceability block).

> *L5 (presence-conditioned data layer) NA*: HA-C4c uses no v24 primary signals; `bout_n_did_not_return_day` is derived from per-minute Garmin stress trace via `extract_stress_bouts.py` (non-v24); `exertion_class_lagged_lcera` is Garmin training-load-derived (non-v24); `crash_v1` labels used only in §4.10 crash-drop sensitivity arm are HA11 v1's primary outcome from Garmin-derived Firstbeat signals (non-v24). The map's §3 row marks L5 NA for the C-bout-substance cluster; this HA does not trigger L5 independently. NA reason: no v24-derived columns anywhere in the primary or sensitivity-arm chain per Stage D audit A5.

> *L6 (self-reporting) NA*: HA-C4c does NOT use `gevoelscore` in the primary cell — the primary outcome is `bout_n_did_not_return_day` (Garmin-derived per-day count via the bout-extraction pipeline), and the heavy-T classifier is `exertion_class_lagged_lcera` (Garmin training-load-derived). Gevoelscore does not enter the primary cell nor any sensitivity arm nor the crash-drop sensitivity's `crash_v1` labels (which are HA11 v1's primary outcome from Garmin-derived signals, not gevoelscore-derived). NA reason: gevoelscore not in primary cell; no self-report primary signal (map §3 row explicitly marks L6 NA for C-bout-substance). Distinct from HA11-bout-redo where L6 applied at the upstream methodology layer via `crash_v1` participant-defined-triage inheritance; at HA-C4c the crash-drop use of `crash_v1` is a §4.10 sensitivity arm only + it did not fire (|Δ δ| = 0.015 < 0.10 threshold), so no L6 upstream-methodology-layer inheritance carries forward to a live caveat.

> *Cites L7 (survivorship)*: the primary-cell effective coverage n_heavy = 465 / n_non_heavy = 809 (total 1274 primary-stratum non-NaN days) represents the gated subset surviving the §4.4 day-validity gate (≥ 600 valid per-minute stress samples + LC era + 21-day device-baseline-lag exclusion + April 2024 cluster exclusion) + §4.4 exertion-classification non-NaN gate + §4.2 cross-phase-pooled stratum restriction (days `>= 2022-11-17`). The verdict does not project to days outside the gated subset. The Stage D audit's A2 BACKSTOPPED reading via §3.4 day-validity gate + 1274 primary-stratum non-NaN days figure (matches n_heavy + n_non_heavy exactly per `result.md` §9) supplies path (a) of guide #1 §5.2 operational test; the cascade-context note (per `descriptive_audit.md` §4.5 item 2 = OI-023 sister to OI-017) flags that a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` would tighten the L7 reach narrative from "gating-discipline-bounded" to "fully audit-backed", and could be jointly closed with OI-017 as a two-operand run at the same S effort. Until that diagnostic lands, the L7 citation here remains gating-discipline-bounded.

## 4.6 Lived-experience prior reconciliation

**STATUS: USER-ARTICULATED 2026-07-08** at Phase C cluster-level joint lock session B2 briefing (verbatim response to plain-language interview seed on whether heavy-exertion days left the participant "unable to switch off" longer than non-heavy days). Supersedes the DEFAULTED-PENDING-USER-INPUT placeholder that had been recorded here at DRAFT r1 dispatch time.

**Verbatim user articulation** (Willem, 2026-07-08):

> "My felt pattern is that on days that I go out of my energy envelope (so maybe, heavy exertion above baseline?) and especially near the end of the day, I can get 'stuck' longer. So dispatcher's default is good, except I don't know if I would use the word SHOULD here."

**Structured reading of the articulation** (per guide §4.6 four-component decomposition):

1. **Directional agreement** — YES on the direction of the substantive Wiggers C4 pattern: out-of-envelope days DO leave the participant "stuck" longer in felt experience. First time in the L4-inflation-verified-three-times prior series (HA-C3 v2 + HA-C3p + HA11-bout-redo) that the dispatcher default's DIRECTIONAL guess agrees with the subject's felt prior.
2. **Scope reframing — from "heavy exertion" to "out of energy envelope"**: the participant reframes the operational classifier `exertion_class_lagged_lcera ∈ {heavy, very_heavy}` (a training-load-derived Garmin classifier keyed to intensity minutes) toward the personal-baseline framing "above my personal envelope" — pointing at CONVENTIONS §3.1 personal-baseline discipline as a more natural operationalisation of the felt experience than a training-load absolute classifier. The Garmin heavy-T classifier is a group-level proxy; the felt pattern is a personal-envelope-crossing signal.
3. **Temporal texture — "especially near the end of the day"**: the felt pattern has an intraday temporal profile not captured by the per-day count aggregate. `bout_n_did_not_return_day` counts stuck-stress bouts anywhere in the day; the felt pattern is that end-of-day out-of-envelope activity produces more "stuck" tail. This is a novel Stage I-level texture worth carrying to Stage S₂ / Stage A / Stage T (an intraday time-of-day sub-analysis is out of scope at the current operand and would need a new per-bout feature — bout start-time relative to day-window).
4. **Language nuance — drop "SHOULD"**: the participant explicitly declines the prescriptive/expectation framing the dispatcher default used ("heavy-exertion days SHOULD carry more within-day 'stuck stress' events"). The felt pattern is a report of what does happen, not an endorsement of what should happen. Stage I preserves this by removing "should" language from any narrative rendering of the lived pattern.

**Implication for §3 licensed claim under directional agreement** (guide §4.6 r2 agreement-does-not-strengthen rule reaffirmed): the directional agreement does NOT strengthen the §3 licensed claim. L4 coupling makes prior-verdict agreement systematically more likely than chance in the subset of tests where the pattern is felt-visible; the operationalisation earns whatever the operationalisation earns, not more. §3's PARTIAL configuration (direction PASS + p PASS + magnitude FAIL) stands as-locked; the agreement is worth recording for the pattern-tracking narrative below but does not narrow or elevate any §3 claim shape. **The reframing points 2 + 3 above are more substantively informative than the directional agreement itself** — they identify (a) an alternative operationalisation candidate (personal-envelope-crossing rather than training-load absolute) that maps more directly onto the felt pattern and cross-references the HA-C3/HA-C3p sister-pre-reg precedent, AND (b) an intraday temporal profile (end-of-day emphasis) that is invisible to the current per-day aggregate operand. Both feed §4.8 own-research follow-ups.

**Phase-A + Phase-B + Phase-C precedent traceability block (L4 pattern now verified four times, third-time-divergence + first-time-agreement)**:

- HA-C3 v2 Phase A.1 (2026-06-25): dispatcher default speculated a specific inverted-U-expectation prior; user actual prior was **null** — no specific expectation, no awareness of a pattern lived.
- HA-C3p Phase A.2 (2026-06-25): same null-prior pattern held.
- HA11-bout-redo Phase B (2026-07-07): dispatcher default speculated a positive framework-validity-reproduction prior derived from parent MD's methodological motivation; user actual prior was **null on the pattern + specification of tactical BB + peak monitoring (not continuous-stress-close-monitoring) during unmedicated × train era** — divergence with cognitively-invisible-signal cascade implication.
- HA-C4c Phase C (2026-07-08, THIS artefact): dispatcher default speculated a positive out-of-envelope-produces-stuck-stress prior derived from Wiggers-verbatim source-reading; user actual prior is **directionally-agreeing with scope reframe (envelope-crossing not training-load-heavy) + temporal texture (end-of-day) + language nuance (drops SHOULD)**. First-time directional agreement in the series, WITH SUBSTANTIVE REFRAMING.

**Interpretation of the L4 pattern updated** (four-invocation view): the dispatcher-side systematically produces plausible-per-parent-MD-framing prior guesses; the subject-side responds with either null-on-lived-salience (first three cases) OR agreement-with-substantive-reframing (this case). What is preserved across all four invocations is the **rejection of the dispatcher's specific mechanism-language**: at HA-C3 v2 / HA-C3p the reject-target was the inverted-U-expectation itself; at HA11-bout-redo the reject-target was the framework-validity-reproduction mechanism-language; at HA-C4c the reject-target is the "SHOULD" prescriptive framing AND the training-load-heavy operationalisation of the exertion classifier. The pattern generalises to: **the subject's felt prior lives at the personal-envelope / temporal-profile / actual-felt-quality layer, NOT at the parent-MD's operationalisation layer or the pre-reg's testing-classifier layer**. This is a substantive discipline observation carried forward for future dispatched Stage I work (dispatchers should not default-read the parent MD's operationalisation semantics onto the subject's felt-prior semantics; the two live at different layers).

**L4 meta-recursion acknowledgment preserved** (per guide §4.6 closing paragraph + limitations doc r3 §3 L4 caveat): the §4.6 reconciliation is itself authored by the same agent (Claude) and the same subject (project author) whose L4 coupling §4.6 is designed to surface. The recursion is bounded by: (a) the structural separation of §4.6 from §2 / §3 (the prior cannot reach the licensed claim except via this section — reaffirmed for the agreement case here); (b) the no-auto-resolution rule on divergent priors (NA for the agreement case); (c) the agreement-does-not-strengthen rule invoked explicitly above; (d) the fresh-session `/research-review` peer-check on the locked `interpretation.md` artefact (per CONVENTIONS §1.2), user-authorized-deviated at Phase C joint lock session per Phase-B precedent with retrospective-drift-trigger retention per plan §3.7. The user-articulated content supersedes the dispatch-mode placeholder verbatim.

## 4.7 Closure-path statement

PARTIAL verdict; closure-path is **operational per guide §4.7 for "one-of-two-bars MET + direction-correct" framing** (HA-C4c's verdict carries "direction-correct + (a) discrimination PASS + (b) effect-size FAIL" per `result.md` §1 + pre-reg §5.2; the unmet condition is bar (b) effect-size ≥ +0.20).

**Closure-path for the bar (b) FAIL**: bar (b) (Cliff's δ ≥ +0.20) failed at δ = +0.120 with 95% CI [+0.064, +0.178] because the substantive heavy-T-vs-non-heavy-T contrast on `bout_n_did_not_return_day` at this corpus's cross-phase-pooled n is genuinely at a small-effect magnitude. Unlike HA11-bout-redo's bar 3 FAIL which was a power-shortfall at n_crash=11 (where a 3× larger crash-arm at the same effect size would have cleared the bar by a wide margin), HA-C4c's bar (b) FAIL is a **magnitude-shortfall at high statistical confidence** — the p = 0.0001 tells us the effect is real; the 95% CI [+0.064, +0.178] tells us the effect size is fully bracketed below the +0.20 discipline threshold; a larger n would tighten the CI but not shift the point estimate materially.

**Four operational paths for bar (b) FAIL closure** (named here per guide §4.7; pre-registration of any upgrade-HA is §4.8 own-research territory; this section names the operational paths only):

1. **A larger heavy-T pool would tighten the CI but not change the point estimate.** The heavy-T contrast on `bout_n_did_not_return_day` at this corpus's operationalisation is genuinely small-effect at cross-phase-pooled resolution; more days would sharpen the confidence interval around δ ≈ +0.120 but would not change the fact that the point estimate sits below +0.20. **No closure via corpus accrual at this operand + stratum**; the shortfall is a magnitude-of-signal fact, not a power-of-detection fact. (Distinct from HA11-bout-redo's bar 3 closure path where more n_crash would have cleared the bar.)

2. **Operand-refinement pre-reg** per pre-reg §9.2 OPTIONAL path — refine the `bout_n_did_not_return_day` operand to weight down-transient bouts (transient-excluded arm at δ = +0.116 was very close to primary, so this is not expected to help materially), OR tighten the 180-minute return-window cap / +5 baseline tolerance / ≥10-minute return-hold thresholds in parent MD §3.2 (would require parent MD revision per `hypothesis_lock_process.md` §3.2), OR use a different bout-detection rule (e.g., `bout_recovery_half_life_median_day` as a primary per pre-reg §9.2 OPTIONAL), OR combine into a 3-channel triad. NOT required (PARTIAL does not halt the cascade); available if downstream synthesis identifies the below-threshold effect-size as too binding.

3. **Framework-validity reference-frame extension** — the calibration caveat from HA11-bout-redo bar 3 FAIL propagated to HA-C4c as pre-reg §8 caveat 2 and is now empirically resolved on HA-C4c's own primary cell (the cross-phase pooling cleared the power-shortfall; bar (a) discrimination is at p = 0.0001). The framework-validity cascade-source verdict PARTIAL (2-of-3 bars) is caveat-class precondition per map §3 cluster cell; the substantive Wiggers C4 finding at bout resolution is what HA-C4c licenses at cross-phase-pooled n. A HA11-bout-redo-validate sister pre-reg (framework-validity on validate era) could isolate the era-specificity question at framework-validity layer; but that is downstream of HA-C4c's substantive scope and would not directly close bar (b) on HA-C4c.

4. **NO CLOSURE — PARTIAL with bar (b) FAIL is a legitimate completion state for the substantive Wiggers C4 question at bout resolution.** Per pre-reg §9.2 second PARTIAL configuration explicitly pre-registered as a legitimate outcome: "the signal exists and is statistically real but is small in magnitude — the substantive Wiggers C4 reading is a weak-effect-but-real positive pattern." The gate fired; direction confirmed; statistical distinguishability confirmed; effect-size characterised at exactly this magnitude with exactly this CI. **The substantive Wiggers C4 claim at bout resolution on this corpus's cross-phase-pooled operationalisation is characterised at δ = +0.120 with 95% CI [+0.064, +0.178] — that is the substantive finding, not a stepping stone to a hypothetical SUPPORTED verdict.** Reading paths 1-3 as required closures would misread the pre-reg's own PARTIAL discipline (which pre-committed this exact configuration as a legitimate completion state). No closure is required for the substantive question to be answered at Stage I resolution; downstream synthesis + external contextualisation + actionability work take the licensed claim as-shaped, not as-narrowed-to-SUPPORTED-eventually.

**Per pre-reg §9.2 second PARTIAL branch (carried verbatim as part of the closure-path statement)**: HA-C4c PARTIAL adds a PARTIAL pointer to the C4 register row (HA-C4 v2 REJECTED-at-daily-aggregate pointer stays); cross-test pass folds in the PARTIAL configuration; parent MD + sub-MD + pipeline all remain LOCKED; HA-C4c-v2 OPTIONAL per §9.2 (not required).

## 4.8 Follow-up suggestions (own-research + external-research tracks)

### Own-research track

- **Bout-return stringency descriptive companion + personal-baseline sister operand candidate (two-step; Step 1 pre-committed here; Step 2 conditional decisions specified without pre-registration).** *New entry surfaced at Phase C cluster-level lock session 2026-07-08 (user Q B2 briefing on stringent-operand-threshold reading; user follow-up specifically asked for less-stringent variants at 30/60/120 min AND raised whether a personal-baseline SD-anchored operand would better match CONVENTIONS §3.1 discipline; walked jointly at lock and confirmed as scoped side-artefact with full two-step discipline).* The parent MD §3.2 `did_not_return_flag` operationalisation uses a **stringent absolute threshold** (return-window > 180 min = 3 hours) that directly operationalises Wiggers-verbatim "Walls of Stress" but produces an already-rare per-day event (base rate mean 0.6444 bouts/day; median 1.00; p25-p75 [0.00, 1.00]) — the observed HA-C4c δ = +0.120 is a small elevation on this already-rare stringent tail, which is a substantively different reading than the same δ on a common event (per §3 licensed-claim amendment). Two related open questions surface: (Q1) does the substantive contrast size scale with stringency threshold in a way that clarifies where the "stuck stress" signal actually lives? (Q2) does a personal-baseline SD-anchored operand — which is what CONVENTIONS §3.1 discipline actually calls for and which the user's Q B2 reframing explicitly points to ("out of my energy envelope") — better operationalise the felt pattern than the training-load absolute `exertion_class_lagged_lcera` × stringent-absolute-`did_not_return_flag` operand pair?

  **Step 1 — descriptive companion (PRE-COMMITTED HERE; scoped; non-blocking for anything).** Two staged sub-passes, hard-separated:
  - **Step 1 Pass 1 — distributional-only**: on the same LOCKED HA-C4c primary stratum (cross-phase-pooled × recovery-phase 4b + 5 × 21-day device-baseline-lag excluded × April 2024 cluster excluded), compute `bout_return_time_min` (continuous per-bout return-time) distribution across all bouts + per-day count aggregates at four return-window thresholds `bout_n_did_not_return_within_{30,60,120,180}_day`. Report: per-day count distribution (mean, median, p25-p75, range), base-rate per stringency level, per-threshold missingness, per-threshold zero-inflation. **DO NOT compute heavy-vs-non-heavy delta at this pass** — the pass is distributional characterisation only per CONVENTIONS §4.1 descriptive-before-inference. Motivation-visible-in-distribution rule (holds the p-hacking line): any Step 2 threshold refinement decision must be made from Step 1 Pass 1 output ONLY, not from Step 1 Pass 2 δ curve.
  - **Step 1 Pass 2 — δ-vs-stringency curve, descriptive framing**: at the four stringency thresholds, compute heavy-T-vs-non-heavy-T Cliff's δ + block-permutation p (E[L]=7 stationary bootstrap; B=10000; seeds rotated per pass so no seed-shopping surface). Report as a δ-vs-stringency curve + a p-vs-stringency curve. **Descriptive-only framing**: the pass does NOT license a licensed-claim upgrade at any threshold; it does NOT substitute for a pre-registered HA at a refined threshold; it does NOT enter the 4-cell Holm family per pre-reg §5.3 (would blow up multiplicity). Written as scoped side-artefact under Stage I §4.7 closure-path 2 (operand-refinement path) framing.
  - **Home**: `analyses/descriptive/operationalisation_support/bout_return_stringency_sensitivity/` (new sub-folder under `operationalisation_support/`, matching `stress_mean_sleep/` + `bout_level_dose_response_calibration/` precedent for scoped descriptive-companion side-artefacts). Effort: S (same bouts already extracted; add 3 columns to bout table + 3 per-day aggregations to `per_day_master.csv`; test template from `desc-SMS`).
  - **Discipline rules pre-committed here to bind Step 2**:
    - (i) Pass 1 output is read by user + orchestrator BEFORE any Step 2 threshold-refinement decision; the Step 2 decision reason MUST be visible in Pass 1's distribution shape (base rate, common-vs-rare, missingness), NOT in Pass 2's δ curve. Peek-and-adjust on the δ curve is the p-hacking anti-pattern and is expressly ruled out here.
    - (ii) Pass 2 output does NOT license any HA claim; it is exploratory descriptive substrate.
    - (iii) Pre-reg of any Step 2 HA-C4c-v2 threshold refinement OR any HA-C4c-personal-baseline sister-pre-reg MUST be written BEFORE peeking at Pass 2 δ curve at that threshold OR at the personal-baseline operand's discrimination values.

  **Step 2 — conditional decisions on Step 1 Pass 1 read (NOT pre-registered here; timing-discipline preserves fair testing).** Three candidate paths, each fully specified so the pattern is not lost across sessions:
  - **(2a) HA-C4c-v2 at descriptively-motivated absolute threshold**. Fires if Step 1 Pass 1 shows 180-min bouts are structurally too-rare (e.g. mean < 0.1 bouts/day on the primary stratum → too-few-per-day for reliable count; base-rate concentrates at 30 or 60 min instead) OR if 180-min is descriptively an arbitrary cut of a smooth return-time distribution with no natural inflection. Would pre-register HA-C4c-v2 with the new threshold as primary + same two-bar discipline (discrimination + effect-size ≥ +0.20) + same cross-phase-pooled stratum. Requires **parent MD §3.2 revision** (return-window threshold is pre-committed at parent MD lock `c57ff3f` 2026-06-19 — re-derivation cascades a drift-trigger to HA11-bout-redo per its Stage I §10 drift trigger 3). Wiggers-verbatim tradition continues at descriptively-honest threshold. HA-C4c r2 remains LOCKED as-is; v2 is a co-existing test at a different threshold, NOT a re-run of the same claim.
  - **(2b) HA-C4c-personal-baseline sister-pre-reg parallel to HA-C3/HA-C3p pattern**. Fires independently of Step 1 Pass 1 read if the CONVENTIONS §3.1 discipline (personal-baseline, not absolute-threshold) is the substantive framing to pursue. Operand candidates: `bout_return_time_z = (return_time - subject_lagged_mean) / subject_lagged_sd` with `_lagged_lcera` reference discipline to avoid the rolling-baseline-inclusive-of-day inflation caught at HA01b-recomputed (v3.1 → v3.2 correction; CONVENTIONS §3.2); flag operand `did_not_return_1sd_flag = time_to_return > (subject_lagged_mean + 1 SD)`; continuous operand `bout_return_time_z_max_day` = highest z-scored return-time bout that day. Operationalises "atypical-for-me recovery" — direct-mapping to user's Q B2 "out of my energy envelope" articulation. Requires (a) **parent MD §3.2 extension** for the SD-anchored operand definition + lagged reference discipline (drift-trigger to HA11-bout-redo per §10 drift trigger 3); (b) exertion classifier reframing candidate: `exertion_z_lagged_lcera` sister operand or personal-envelope-crossing sub-classifier, TBD at pre-reg drafting; (c) 4-cell agreement matrix reading with HA-C4c primary per HA-C3 v2 / HA-C3p precedent (cell "abs REJECTED / personal PARTIAL": Wiggers' 180-min threshold is wrong-for-this-participant but personal-envelope-crossing pattern is real; cell "abs PARTIAL / personal SUPPORTED": personal-baseline operand more strongly detects the same real signal; cell "abs PARTIAL / personal PARTIAL": convergent partial across operationalisations; cell "abs PARTIAL / personal REJECTED": absolute-threshold captures something the personal-baseline operand doesn't). Wiggers-verbatim (HA-C4c primary) + CONVENTIONS §3.1 personal-baseline (HA-C4c-personal-baseline) become **sister pre-regs** at the same layer HA-C3/HA-C3p established. **Trade-off**: personal-baseline operand is more sophisticated + better inherits project discipline BUT less directly Wiggers-comparable at Stage T translation ("your bouts were 1.5 SD above your typical return-time" harder to land with lay audience than "your stress stayed elevated more than 3 hours"). The trade-off is not "which is better"; it's "each answers a different substantive question, sister-pre-regs let both be answered honestly".
  - **(2c) Both 2a AND 2b, parallel-dispatched**. Fires if Step 1 Pass 1 shows a natural refined threshold AND the personal-baseline sister is separately motivated. 4-cell agreement matrix would be extended to 8 cells (two absolute thresholds × two personal-baseline configurations) OR two separate 4-cell matrices at each threshold layer. Highest-yield path but heaviest lock-cost (two parent MD revisions + two pre-regs + full 4-stage arcs on each).

  **Decision-point timing**: Step 2 decisions are downstream of Step 1 Pass 1 read; do NOT pre-commit them now beyond what is specified here. When Step 1 Pass 1 lands, the read informs (a) whether to do (2a), (b) whether to do (2b) independently, (c) whether to do (2c), (d) whether to defer either indefinitely because the descriptive read shows nothing motivating a Step 2. Pre-registration of any Step 2 HA precedes any peek at Step 1 Pass 2 δ curve OR at the personal-baseline operand's discrimination.

  **Not blocking anything**: HA-C4c r2 primary LOCKED as-is; Phase C cluster-level layer CLOSED at 2026-07-08; Stage S₂ + Stage A + Stage T on this cluster do NOT wait on this follow-up; the two-step study is a scoped side-artefact whose timing is decoupled from the cluster-level cascade.

  **Cross-references**: pre-reg §9.2 OPTIONAL path (parent-MD-revision-required category); CONVENTIONS §3.1 (personal-baseline discipline); §3.2 (lagged-baseline reference); §3.4 (crash-drop sensitivity — would apply at Step 2 pre-reg); §4.1 (descriptive-before-inference); §4.3 (confirmatory vs exploratory framing); parent MD [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) §3.2 (return-window rule pre-commit that Step 2a/2b/2c revise); HA-C3 [`HA-C3.md`](HA-C3.md) + HA-C3p [`HA-C3p.md`](HA-C3p.md) sister-pre-reg precedent structure; HA01b v3.1 → v3.2 lagged-baseline correction (structural warning against baseline-inclusive-of-day inflation for the SD-anchored operand at Step 2b); Stage D `descriptive_audit.md` §11 lock-log drift trigger 5 (this follow-up now registered as a drift trigger at Stage D too).

- **Bout-level operand-refinement pre-reg** (per pre-reg §9.2 OPTIONAL path). If downstream synthesis identifies the below-threshold effect-size as too binding for the substantive claim shape at K-bout-recovery-signal, an operand-refinement v2 would re-tune the parent MD §3.2 thresholds (180-min forward cap + +5 baseline tolerance + ≥10-min return-hold) OR promote `bout_recovery_half_life_median_day` as primary OR construct a 3-channel triad. Pre-reg shape: same cross-phase-pooled stratum × heavy-T contrast × two-bar discipline; new operand-threshold values pre-committed at parent MD §3.2 revision before pre-reg drafting. Routing: parent MD revision per `hypothesis_lock_process.md` §3.2 (parent MD §3.2 thresholds are pre-committed at parent MD lock — a re-derivation requires parent MD revision, which itself has drift-trigger consequences for HA11-bout-redo per its Stage I lock log §10 drift trigger 3). **Note**: substantially overlaps with the two-step bout-return stringency descriptive companion + personal-baseline sister entry above; consider this entry the un-scoped general framing of that follow-up's Step 2a. Not double-counted for planning — the entries share operational content; the stringency-sensitivity entry is the concrete-scoped version.

- **Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return`** (per Stage D `descriptive_audit.md` §4.9 entry 2 = OI-023 cascade-context carry-forward; sister to OI-017 for `bout_n_fast_recovery_day`). Not a sister-HA pre-reg; a descriptive infrastructure run that tightens the L7 reach narrative at the next Stage I re-examination AND jointly with OI-017 covers both bout-level sister operands in one S-effort run. Script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/`; S effort per stocktake §3 Shared gap 3.

- **Intraday HR-pacing trend pre-reg** (Q30 intraday-recovery trend per parent MD §1.4 A4 enabled-on-request candidate — the HR-channel bout-level analysis analogue). Would triangulate the "stuck stress" mechanism from a different Garmin channel (HR-derived trend rather than Firstbeat-stress-derived per-bout events); a SUPPORTED-at-HA-C4c-substantive-Wiggers + SUPPORTED-at-HR-channel-trend shape would strengthen the cross-channel triangulation that HA-C4c's substantive PARTIAL alone cannot; a NOT-SUPPORTED-at-HR-channel-trend would surface whether the operand's below-threshold effect-size is Firstbeat-algorithm-specific vs autonomic-state-specific. Routing: `hypothesis_lock_process.md` §3.2 fresh-session drafting; pre-reg would carry explicit cross-cite to HA-C4c and parent MD §1.4 A4 candidate context.

### External-research track

- **Cross-population framework-validity replication of the bout-level recovery operand on PEM-prone populations** at group level. We cannot run this ourselves because **L1 (single-subject reach)** prevents the N=1 corpus from settling whether the substantive Wiggers C4 finding at bout resolution generalises beyond this subject's specific within-day stress trace. Per Daza 2018 N-of-1 inference reach: the present PARTIAL verdict at δ = +0.120 (CI [+0.064, +0.178]) bounds the substantive claim to one subject's cross-phase-pooled data; cross-subject substantive replication would require a group-level study with the same per-minute Garmin stress instrumentation, the same bout-detection rule (parent MD §3), the same heavy-T contrast operationalisation (`exertion_class_lagged_lcera` analogue), and the same two-bar discipline (discrimination + effect-size ≥ +0.20), evaluated against a group-level heavy-T-vs-non-heavy-T contrast. The study would carry pre-registration of the same two §5 bars with the within-population effect-size discipline threshold potentially adjusted to the group's distribution per CENT 2015 item 22 generalisability statement. Minimum n per Daza 2018 + CENT 2015 bounds.

- **Cross-device-generation comparability study for the bout-detection rule** at framework-validity layer applied to the substantive operand. We cannot run this ourselves because **L3 (device generations)** + the Firstbeat-input amplification caveat (pre-reg §8 caveat 7 operational-vs-mechanistic; HA11-bout-redo pre-reg §8 caveat 5) means the bout-level features derived from the closed-source Firstbeat algorithm may not transfer cleanly across device generations even if the column name `all_day_stress_avg` / per-minute stress trace remains semantically stable. The 99.3% motion-confound corpus property (pre-reg §8 caveat 4) is itself a device × extraction-threshold interaction the study would clarify; a cross-device design (paired-subject within-device-generation transition windows per limitations doc L3 caveat calibration discipline) would test whether `bout_n_did_not_return_day` produces statistically comparable distributions across device generations on matched within-subject windows AND whether the motion-confound rate is device-stable or device-specific. This would clarify whether the substantive Wiggers C4 finding at bout resolution is device-stable or device-specific.

Each entry is one paragraph with the proposed study, the L-ID that prevents us from running it ourselves, and what the study would contribute to the construct the HA tests. Both external-research entries respect locked-plan §3.11 N=1-limit scoping discipline (the L-ID-explicit-naming requirement per guide §4.8).

**Distinct from `open_inputs`** per locked-plan §3.5 vs §3.11: the §4.9 `open_inputs` block below names what is missing to complete *this current interpretation* (placeholder slots needing user articulation; cascade-context entries pending closure). The §4.8 follow-up suggestions above name what *next claims* could be built — for us or for others. Both are required; neither substitutes.

## 4.9 `open_inputs` block

Three slots surfaced (one carry-forward from Stage D dispatch-mode; one Stage-I-specific new dispatch-mode placeholder; one cascade-context inherited from Stage D):

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | ~~**OI-021**: User explicit acceptance of HA-C4c Stage D audit's TRUSTED verdict-trust call~~ **CLOSED-BY-USER-LOCK 2026-07-08** (Q B1 at Phase C cluster-level joint lock session: user accepted TRUSTED as-is, no reaudit; Stage D `descriptive_audit.md` LOCKED r1 2026-07-08). | ~~Stage D → LOCKED~~ resolved. | Interview pass fired at Phase C cluster-level joint lock session. | N/A. |
| 2 | ~~**OI-022**: User articulation of lived-experience prior on whether heavy-T days should carry more per-day "stuck stress" bouts than non-heavy-T days~~ **CLOSED-BY-USER-LOCK 2026-07-08** (Q B2 at Phase C cluster-level joint lock session: user articulated verbatim — directional agreement + scope reframe (envelope-crossing not training-load-heavy) + temporal texture (especially near end of day) + language nuance (drops "SHOULD"). See §4.6 for full articulation + structured reading + Phase-A/B/C precedent traceability update to L4-4× series. Two substantive scope-reframing points logged as follow-up in §4.8 own-research entry 1 (bout-return stringency descriptive companion + personal-baseline sister operand candidate two-step study).). | ~~§4.6 completeness~~ resolved. | Interview question fired at Phase C cluster-level joint lock session; user response verbatim in §4.6. | N/A. |
| 3 | **OI-023 (carry-forward from Stage D; REMAINS OPEN as non-blocking cascade-context)**: Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` (stocktake §3 Shared gap 3 pattern; NOT load-bearing for HA-C4c's TRUSTED call per Stage D §4.5 item 2 + §4.9 entry 2; cascade-context sister to OI-017 for `bout_n_fast_recovery_day` on the same operand class). Status: **OPEN 2026-07-08**, propagated to layer-wide `methodology/_open_inputs.md` queue for closure via sister-operand-consolidation with OI-017 at S-effort per stocktake §3 Shared gap 3. Non-blocking for Phase C cluster-level lock (which fired 2026-07-08). | Tightens §4.5 L7 citation from "gating-discipline-bounded" reading to "fully audit-backed" reading; jointly with OI-017 covers both bout-level sister operands in one S-effort run per §4.5 audit note; not blocking either Stage D's TRUSTED call or this Stage I's §3 licensed claim. | S effort per stocktake §3 Shared gap 3 (per-channel missingness rate × era × phase); jointly with OI-017 becomes a two-operand run at the same S effort per the sister-operand-consolidation observation; script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/`. | TRUSTED (Stage D LOCKED r1 as-is) stands with L7 narrowing caveat at Stage I per Stage D audit §4.5 + this artefact §4.5 L7 row gating-discipline-bounded reading; STOCKTAKE §2d "primary-stratum non-NaN days = 1274" figure provides the sample-anchoring context in the interim. |

OI-022 is a new entry; it propagates to `methodology/_open_inputs.md` at the bottom of the queue at user lock time per skill responsibility #6 (deduplicated against existing entries — checked against OI-001 through OI-020; no overlap on (missing-input × blocked-target) key since OI-022's missing-input is HA-C4c-specific lived-experience prior + blocks Stage I §4.6 specifically, distinct from OI-001/OI-002 which were HA-C3 v2 / HA-C3p-specific and OI-018 which was HA11-bout-redo-specific). OI-021 + OI-023 are Stage D carry-forward entries; OI-021 carries the user-acceptance-only lock event for Stage D; OI-023 carries the descriptive-diagnostic sister to OI-017 (dedup at the layer-wide queue may either keep them as distinct entries or consolidate them into a joint two-operand run at the user's option).

## 4.10 Cross-references

- HA-C4c r2 [`hypothesis.md`](../hypotheses/HA-C4c/hypothesis.md), [`result.md`](../hypotheses/HA-C4c/result.md), [`test.py`](../hypotheses/HA-C4c/test.py), `result-data.json` (machine-readable companion; gitignored).
- Stage D audit [`descriptive/HA-C4c/descriptive_audit.md`](../descriptive/HA-C4c/descriptive_audit.md) — DRAFT r1 2026-07-07 (dispatched-by-orchestrator on user authorisation; user-acceptance lock event PENDING at OI-021).
- Synthesis-structure map [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) cluster row (declared 2026-06-23, lock r3 2026-06-24); single-member cluster (HA-C4c only); **cascade-receiver** from C-bout-framework (HA11-bout-redo).
- **Cascade-source Stage I (upstream)**: [`HA11-bout-redo.md`](HA11-bout-redo.md) — LOCKED r1 2026-07-07; Phase B; framework-validity PARTIAL verdict propagates as caveat-class precondition to this cluster's Stage S₁, NOT as co-equal verdict; INVERSE failure-mode contrast (magnitude PASS + p FAIL) vs HA-C4c's p PASS + magnitude FAIL — the two clusters answer structurally different questions with structurally different failure modes.
- **Downstream Stage S₁** (single-member-cluster reading): [`synthesis/cluster-bout-substance.md`](../synthesis/cluster-bout-substance.md) (forward pointer; not yet drafted per Phase C sub-phases D → I → S₁ in `_rollout_order_2026-06-25.md` §2 Phase C).
- Parent methodology MD: [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) — §3 bout-detection rule; §3.2 `did_not_return_flag` semantics + 180-min forward cap + +5 baseline tolerance + ≥10-min return-hold thresholds; §3.4 day-validity gate + motion-confound handling; §4 per-bout feature set + `bout_n_did_not_return_day` per-day aggregation; §5.1 stationary-bootstrap E[L]=7 inheritance; §5.3 Approach A cross-phase headline discipline; §5.5 framework-validity restriction to unmedicated (binds HA11-bout-redo; HA-C4c is the downstream substantive test); §6 framework-validity discipline / MD's own falsifier. Parent MD LOCKED r2 2026-06-19 `c57ff3f`.
- Sub-methodology MD: [`bout_level_dose_response_calibration.md`](../../methodology/bout_level_dose_response_calibration.md) — §6 architectural-implications paragraph (0/7 features CONFIRMED → Approach A NOT load-bearing → cross-phase pooling permitted); §7 caveat 5 underpowered-NULL framing (inheritance to Approach A sensitivity arm at HA-C4c §4.9). Sub-MD LOCKED r4 2026-06-23 `fb97d1c`.
- Cross-phase pooling permission MD: [`citalopram_phase_stratification.md`](../../methodology/citalopram_phase_stratification.md) — §3 the five-phase `citalopram_phase` axis (`unmedicated / buildup / consolidation / afbouw / post_afbouw`); April 2024 cluster exclusion; §4 CONFIRMED-channel inheritance discipline (NOT invoked at HA-C4c primary because 0/7 CONFIRMED per sub-MD §6).
- Approach A methodology MD: [`bout_level_dose_response_calibration.md`](../../methodology/bout_level_dose_response_calibration.md) — §6 inheritance-by-analogue framing; underpowered-NULL discipline; five-surface framing precedent from [`reviews/bout_level_dose_response_calibration-r3-2026-06-22.md §3`](../../reviews/bout_level_dose_response_calibration-r3-2026-06-22.md).
- Layer limitations doc: [`research_line_limitations.md`](../../methodology/research_line_limitations.md) LOCKED r3; §3 seven L-IDs; §5 citation requirements row for `interpretation.md`; §8 downstream-citation-count table (Stage I increments per L-ID cited — L1, L2, L3, L4, L7 for this HA; L5 + L6 NA with reasons documented).
- **Cross-test cross-references** (per pre-reg §4.10 sister-test companion + `result.md` §7 sister-test table): [HA-C4 v2](../hypotheses/HA-C4/) REJECTED (daily-aggregate triad sum 0.0/3.0; sister to HA-C4c at coarser resolution — the two verdicts coexist per pre-reg §8 caveat 9; resolution-of-scale story is Stage S₁ / S₂ territory NOT Stage I); [HA-C4b v3](../hypotheses/HA-C4b/) NOT-SUPPORTED (motion-filter crash-precursor per-episode; sister test on a different question — per-episode crash precursor rather than per-day heavy-T contrast); [HA11 v1](../hypotheses/HA11-stress-udip/) SUPPORTED-on-train (U-dip count +22.8 pp; calm-day sister channel; opposite pole of the same C4 mechanism — U-dip is what fast-recovery looks like, `did_not_return` is what failure-to-recover looks like); [HA11-bout-redo](../hypotheses/HA11-bout-redo/) PARTIAL (framework-validity 2-of-3 bars met; cascade-source verdict per map §3 cluster cell cascade arrow; INVERSE failure-mode from HA-C4c).
- [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) — §3.10 hard predictive gate (Stage I does not cross; predictive claims live at Stage A; HA-C4c's cluster C-bout-substance HAS an A row in map §5 K-bout-recovery-signal per map §5 cluster→construct row); §3.11 follow-up tracks (§4.8 implements); §3.12 commentary boundary (Stage I does not carry; commentary lives at Stage A / T if K-bout-recovery-signal reaches tier-1/tier-2).
- [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) §2 Phase C (single-member cluster C-bout-substance; cascade-receiver from Phase B C-bout-framework).
- CONVENTIONS [§1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode) reviewer-mode-with-authorization discipline + Authorship block; [§3.6](../../CONVENTIONS.md) named counts (pre-reg §4.11 effective-n pinning); [§4.1](../../CONVENTIONS.md#41-no-interpretive-marks-on-raw-or-descriptive-layers) descriptive-before-inference (§2 respects); [§4.2](../../CONVENTIONS.md#42-caveats-vs-a-priori-claims) caveats vs a-priori; [§4.3](../../CONVENTIONS.md#43-prior-driven-hypotheses-are-confirmatory) confirmatory framing for prior-driven substantive Wiggers C4 retest; [§4.4](../../CONVENTIONS.md) no cross-test pass conclusion at result-emission (Stage I preserves the boundary; cross-test reads are Stage S₁ / S₂ territory).
- Literature methodology anchors: [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) (N-of-1 inference reach, cited at §4.5 L1 + §4.8 external-research scoping); [Shamseer / CENT 2015](../../literature/methodology/shamseer_2015_cent_consort_nof1.pdf) (CENT items 21 + 22 generalisability statement, cited at §4.8 external-research); [Tate / SCRIBE 2016](../../literature/methodology/tate_2016_scribe_single_case_reporting.pdf) (L4 participant-as-researcher transparency, cited at §4.5 L4); [Natesan 2023](../../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf) (defensibility bar for single-case verdict-to-claim translation).
- Pre-reg audit reports: [`reviews/HA-C4c-2026-06-23.md`](../../reviews/HA-C4c-2026-06-23.md) (r1 audit REVISION RECOMMENDED; three fires closed at r2); [`reviews/HA-C4c-2026-06-23-v2-reaudit.md`](../../reviews/HA-C4c-2026-06-23-v2-reaudit.md) (r2 re-audit PASS-with-caveats; NO L1-L4 fires; two optional precision items NOT gating).
- Cascade-source Stage S₁ (upstream, downstream from C-bout-framework Stage I): [`synthesis/cluster-bout-framework.md`](../synthesis/cluster-bout-framework.md) LOCKED r1 2026-07-07 (structurally-determined trivial-ORTHOGONAL per single-member-cluster convention); the S₁ verdict on C-bout-framework enters Stage S₁ on C-bout-substance as caveat-class precondition per `internal_synthesis.md` §5.5.

---

## §9.6 refuse-to-lock gate walk

Per guide §9.6 walk (item-by-item):

1. **A §4.5 L-ID is missing from the citation block that the synthesis-structure map's cluster row lists**: NO — every L-ID from the C-bout-substance cluster row (L1, L2, L3, L4, L7; L5 NA; L6 NA) is cited with one-sentence project-specific application. L5 NA is explicitly cited with one-sentence reason (no v24 primary signals; same as Stage D audit A5). L6 NA is explicitly cited with one-sentence reason (gevoelscore not in primary cell; same as Stage D audit A6 + map §3 cluster row L-IDs column annotation).

2. **§4.4 does not address the four predictable overclaim shapes explicitly**: NO — the four-bullet list is present (below-threshold-as-noise; 2-bar-PARTIAL-to-weak-SUPPORTED; PARTIAL-to-CONFIRMED-or-REJECTED-polar-collapse; PARTIAL-on-cross-phase-pooled-to-per-phase-generalisation). Plus an explicit sensitivity-arm overclaim address (motion-clean-only + transient-excluded + Approach A cannot promote primary; corpus-property reaffirmation vs primary fragility distinction preserved).

3. **§4.8 carries an external-research suggestion without explicit N=1-limit scoping**: NO — both external-research entries explicitly name the L-ID that prevents us from running them ourselves (cross-population substantive replication cites L1; cross-device-generation comparability cites L3 + the Firstbeat-input amplification caveat from pre-reg §8 caveat 7). Both respect locked-plan §3.11 binding.

4. **§4.6 records a divergence without recording both readings or without naming the resolution path**: NOT-APPLICABLE-IN-THE-STANDARD-SENSE per the DEFAULTED-PENDING-USER-INPUT placeholder. The §4.6 status is the dispatch-mode placeholder with dispatcher one-sentence default-reading + Phase-A + Phase-B precedent traceability block per the LOCKED-r4 skill §"Dispatch-mode placeholders" convention; the user's separate non-dry-run lock session resolves the placeholder to USER-ARTICULATED status. **Per the placeholder discipline, this §9.6 item does NOT fire on the placeholder itself** — the placeholder IS the legitimate-but-incomplete §4.6 state per skill r4 (artefact is structurally complete + locked-in-shell, awaiting user-articulation-only step). The §9.6 item WOULD fire at production-lock attempt without user articulation; the dispatch-mode placeholder defers production-lock per OI-022 in §4.9 entry 2.

5. **§3 / §2 contain verdict-overclaim language that violates §7 anti-patterns**: NO — §2 reports per-cell descriptive numbers with no verdict-label use (the verdict label appears in §1 header + §3 licenses, not §2 paraphrase). §3 reports the operationalisation-bound PARTIAL claim with the per-bar pattern preserved + qualifier carried verbatim per guide §5.5 (no qualifier collapse; the "direction-correct + (a) discrimination PASS + (b) effect-size FAIL" per-bar pattern stays in the §3 sentence). §3 does NOT extend to "the substantive Wiggers C4 claim is confirmed at bout resolution" (§7.1 anti-pattern applied at PARTIAL-with-below-threshold — covered explicitly at §4.4 bullet 3), does NOT extend to "the underlying autonomic mechanism is validated" (§7.2 anti-pattern — mechanism claims are separate question), does NOT reframe the hypothesis (§7.3 anti-pattern — pre-reg §1 substantive Wiggers C4 retest at bout resolution language is preserved), does NOT smuggle a lived-experience prior (§7.4 anti-pattern, automatically respected since §4.6 is DEFAULTED-PENDING-USER-INPUT and the §3 claim was derived from verdict + audit-trust + L-IDs, never from lived experience), does NOT invent caveats post-hoc (§7.5 anti-pattern — all caveats in §4.5 come from pre-reg §8 + Stage D audit §3 + §4.5 cascade-context notes; INVERSE failure-mode distinction is per-verdict framing not a new caveat), does NOT collapse PARTIAL into weak SUPPORTED (§7.6 anti-pattern — covered explicitly at §4.4 bullet 2), does NOT carry §3.12 subject-narrative commentary (§7.7 anti-pattern — Stage I MUST NOT; this artefact does not carry §3.12; commentary reserved for Stage A K-bout-recovery-signal if it reaches tier-1/tier-2), does NOT compute or cite predictive-quality measures (§7.8 anti-pattern — no PPV, no base rate, no sensitivity / specificity), does NOT invent new verdict labels (§7.9 anti-pattern — the verdict label is `PARTIAL — direction=+, (a) discrimination PASS at p=0.0001, (b) effect-size FAIL at δ=+0.120` carried verbatim from `result.md` §1 throughout).

**Items that would fire on production-lock attempt**:

- **TWO substantive items** would fire on production-lock (LOCKED transition):
  1. **OI-021 (Stage D user-acceptance pending; via §4.9 entry 1)** — Stage D `descriptive_audit.md` must lock first (currently DRAFT r1; user-acceptance lock event pending); the DRAFT-on-DRAFT dispatch-mode placeholder cannot promote to LOCKED at Stage I until upstream Stage D locks. This is the DRAFT-on-DRAFT dispatch-mode constraint per skill r4 §"DRAFT-on-DRAFT dry-run discipline" (production-mode dispatches revert to guide #2 §9.2 refusal-path: source MUST be LOCKED).
  2. **OI-022 (§4.6 user-articulation pending; via §4.9 entry 2)** — §4.6 DEFAULTED-PENDING-USER-INPUT placeholder per LOCKED-r4 skill §"Dispatch-mode placeholders" must transition to USER-ARTICULATED before production-lock; the placeholder IS a legitimate-lock-state per skill r4 (legitimate-but-incomplete), so the artefact can lock in dispatch-mode-shell-state pending non-dry-run user articulation. Per the Phase-A + Phase-B precedent (HA-C3.md §4.6 + HA-C3p.md §4.6 + HA11-bout-redo.md §4.6 lock pattern), one user-articulation session resolves both gates jointly.

- **ZERO substantive §9.6 fires** on the current draft content: all five §9.6 substantive checks (L-ID completeness, §4.4 four-bullet completeness, §4.8 N=1-scoping, §4.6 divergence-recording-with-resolution-path, §3-§2 verdict-overclaim) pass cleanly on the dispatch-mode draft. The two items above are dispatch-mode + user-articulation gates, not substantive content fires.

The artefact is structurally complete on every §9.6 substantive check; the lock event itself awaits OI-021 + OI-022 resolution in the user's separate non-dry-run lock session per Phase-A + Phase-B precedent (one session resolves both jointly).

## §10 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-07-07 | Drafted r1 (DISPATCHED-BY-ORCHESTRATOR ON USER AUTHORISATION; DRAFT-ON-DRAFT-DRY-RUN dispatch mode) | Reviewer-mode-with-authorization by `/research-interpret interpret HA-C4c` skill on first invocation for this target (skill LOCKED r4). Phase C of `_rollout_order_2026-06-25.md` (single-member cluster C-bout-substance; cascade-receiver from Phase B C-bout-framework per synthesis_structure_map r3 §3). Stage D upstream `descriptive_audit.md` (DRAFT r1 dispatched-by-orchestrator 2026-07-07; TRUSTED verdict-trust call; 11 BACKSTOPPED + 2 NOT APPLICABLE; zero anti-pattern fires; refuse-to-lock-clean on every §9.6 substantive check; user-acceptance lock event PENDING at OI-021) consumed as TRUSTED-PROVISIONAL-by-orchestrator-authorisation per skill r4 §"DRAFT-on-DRAFT dry-run discipline". §4.6 lived-experience-prior DEFAULTED-PENDING-USER-INPUT per Phase-A + Phase-B precedent (HA-C3.md + HA-C3p.md + HA11-bout-redo.md §4.6 dry-run pattern); dispatcher one-sentence default-reading + Phase-A + Phase-B traceability block recorded. **INVERSE failure-mode distinction preserved** vs cascade-source HA11-bout-redo: HA-C4c has p PASS + magnitude FAIL (δ=+0.120 CI [+0.064, +0.178] fully bracketed below +0.20 threshold). **Cross-phase pooling load-bearing** empirically confirmed via unmedicated-only stratum-fragility. **99.3% motion-confound corpus property** re-affirmed. **Approach A** honored as descriptive companion under underpowered-NULL frame. **Cascade-source verdict from C-bout-framework** (LOCKED r1 2026-07-07) noted for traceability in §4.5 cascade-context note 1. |
| 2026-07-08 | **LOCKED r1** by user acceptance at Phase C cluster-level joint lock session | Q A "yes accept all three" + Q B1 "TRUSTED as-is" (no reaudit; contrast with Bundle B Phase B) + Q B2 **substantive articulation with stringent-operand-threshold interpretation nuance surfaced by user** (embedded verbatim in §3 as amendment to licensed-claim reading: "modest elevation on already-rare stringent event is substantively different from same δ on common event") **+ §4.6 lived-experience prior USER-ARTICULATED verbatim** ("My felt pattern is that on days that I go out of my energy envelope (so maybe, heavy exertion above baseline?) and especially near the end of the day, I can get 'stuck' longer. So dispatcher's default is good, except I don't know if I would use the word SHOULD here.") — **FIRST-TIME directional agreement in L4-inflation-4× series** with substantive scope reframe (envelope-crossing not training-load-heavy) + temporal texture (end-of-day emphasis) + language nuance (drops "SHOULD"). L4 pattern updated (§4.6): dispatcher-side systematically produces plausible-per-parent-MD-framing prior guesses; subject-side responds with either null-on-lived-salience OR agreement-with-substantive-reframing — subject's felt prior lives at personal-envelope / temporal-profile / actual-felt-quality layer, NOT at parent-MD's operationalisation layer. **+ Q B3 accepted Stage S₁ cascade-receiver §4.5 5c LOAD-BEARING framing as-drafted** after more-detail request walked (first-ever cascade-receiver §4.5 5c LOAD-BEARING sub-paragraph in the corpus; caveat-class-not-co-equal-evidence discipline explained). **+ §4.8 own-research NEW entry added at user's own request**: two-step bout-return-stringency descriptive companion (Step 1 Pass 1 distributional-only + Pass 2 δ-vs-stringency descriptive) + personal-baseline sister operand candidate (Step 2a/2b/2c conditional decisions) with full context so pattern is not lost across sessions; discipline rules pre-committed (motivation-visible-in-distribution rule; Pass 2 does not license any claim; Step 2 pre-reg precedes any peek at Pass 2 δ curve). OI-021 + OI-022 CLOSED. OI-023 remains OPEN (non-blocking cascade-context sister to OI-017; propagated to layer-wide `_open_inputs.md`). Fresh-session `/research-review` per plan §4 NOT dispatched pre-lock — user-authorized deviation matching Phase-B precedent (rationale documented in Status header). Retained as retrospective drift-trigger per plan §3.7. **Drift triggers registered** (updated): (1) HA-C4c `result.md` re-runs (e.g. operand-refinement v2 per pre-reg §9.2 OPTIONAL; pipeline `extract_stress_bouts.py` re-LOCK; parent MD §3.2 return-window threshold revision); (2) Stage D `descriptive_audit.md` re-examined; (3) cited methodology MD changes lock-version (esp. `research_line_limitations.md`; parent MD `bout_level_recovery_dynamics.md` `c57ff3f`; sub-MD `bout_level_dose_response_calibration.md` `fb97d1c`; `citalopram_phase_stratification.md`; `lc_recovery_phase_axis.md`; `synthesis_structure_map.md` r3; `_plan_results_analysis_layer.md` r5); (4) cascade-source C-bout-framework re-LOCK or HA11-bout-redo re-run at operand-refinement v2 changes framework-validity verdict (enters Stage S₁ as caveat-class precondition; needs re-consumption); (5) **bout-return stringency descriptive companion Step 1 Pass 1 lands** (may motivate Step 2a HA-C4c-v2 threshold-refined pre-reg OR Step 2b HA-C4c-personal-baseline sister-pre-reg OR Step 2c both; Step 2 pre-reg pre-committed to precede any peek at Pass 2 δ curve per §4.8 discipline rules); (6) ≥6 months elapse since lock. |
