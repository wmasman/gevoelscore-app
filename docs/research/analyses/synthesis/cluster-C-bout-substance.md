# cluster-C-bout-substance — internal synthesis (Stage S₁)

**Status**: **LOCKED r2 2026-07-14** by user acceptance ("yes lets finish what we were doing now" 2026-07-14 driving Bundle H+ event 11 HA-C4cp downstream cascade Stage I → Stage S₁ per HA-C4cp pre-reg §9.2 PARTIAL branch DEFERRED artefacts). **r1 → r2 revision scope**: (a) §4.1 constituent HAs updates from single-member (HA-C4c) → **2-member (HA-C4c + HA-C4cp)** per HA-C4cp result.md LOCKED r1 2026-07-14 (Bundle H+ event 10 commit `cd71ac6`); (b) §4.3 per-HA verdict + interpretation row table extended with HA-C4cp row; (c) §4.4 coherence call transitions from **trivial-ORTHOGONAL (single-member per `internal_synthesis.md` §5.6)** → **CONCORDANT-BELOW-THRESHOLD (2-member both-direction-correct + both-(b)-FAIL per HA-C4cp pre-reg §9.2 anticipated modal outcome)**; (d) §4.5 5a caveats extended with HA-C4cp caveat carry-forward (all 8 pre-reg §8 caveats; reference-window fragility from reference_window_shorter_lag REJECTED at δ=+0.037; unmedicated-only stratum-fragility at higher amplitude than HA-C4c's 95% vs 50%; Approach A CI-bracket DIVERGENT-REJECTED empirically confirming pre-reg §8 caveat 3 amplification); (e) §4.5 5b L-ID citation block unchanged (same L-IDs; both HAs share the same cluster-row L-IDs); (f) §4.5 5c cascade-receiver precondition sub-paragraph unchanged (framework-validity gate + HA11-bout-redo cascade-source verdict framing preserved); (g) §4.5 NEW 5e cross-op-independence descriptive corroboration sub-paragraph added (κ = 0.507 moderate + 35.5% cap-unreachable rate); (h) §4.7a joint claim updated from single-member carry-forward → 2-member CONCORDANT-BELOW-THRESHOLD joint-claim reading with the corpus-effect-size ceiling shape per pre-reg §9.2 substantive interpretive note; (i) §4.7b joint-does-NOT-license extended with the CONCORDANT-BELOW-THRESHOLD-does-NOT-imply-refutation shape; (j) §11 lock log new row. **Cross-op-independence gap CLOSURE**: at strict guide r3 §4.2 two-independent-HAs bar via operand-family-level independence per OI-025 protocol §5.4 four-condition argument. **Content preserved verbatim from r1**: §4.2 pre-declared constellation; §4.3 HA-C4c row (unchanged); §4.4 trivial-ORTHOGONAL rationale (preserved as r1-degenerate-shape context; superseded by r2 CONCORDANT-BELOW-THRESHOLD but historical framing retained); §4.5 5a original 10 caveats + 3 cascade-context notes; §4.5 5b L-ID citation block; §4.5 5c cascade-receiver precondition sub-paragraph verbatim; §4.5 5d crash-drop sensitivity discipline; §4.6 open-conflicts (none in either r1 or r2); §4.8 follow-ups (extended with HA-C4cp additions); §4.9 open_inputs (OI-024 already CLOSED at r1; carry-forward as historical). **Fresh-session `/research-review` per plan §4 NOT dispatched pre-r2 lock — user-authorized deviation matching r1 precedent** (rationale: coherence-call transition from trivial-ORTHOGONAL to CONCORDANT-BELOW-THRESHOLD is guide-prescribed per `internal_synthesis.md` §5 verdict table for 2-member both-PARTIAL both-direction-correct configuration; joint claim shape derives deterministically from the two Stage I §3 licensed claims + the pre-reg §9.2 anticipated modal-outcome framing; no judgment-call axis exercised; §9.6 walk clean on all substantive checks; retained as retrospective drift-trigger per plan §3.7). **r1 lock context preserved below**:

<details>
<summary>r1 lock context (preserved for r2)</summary>

**r1 LOCKED 2026-07-08** by user acceptance per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.8 binding completion event at the Phase C cluster-level joint lock session (Q A "yes accept all three" + Q B3 accepted trivial-ORTHOGONAL coherence call + first-ever cascade-receiver §4.5 5c LOAD-BEARING sub-paragraph as-drafted after more-detail briefing walked at lock — cascade-source vs cascade-receiver asymmetry vs Phase B C-bout-framework explained; §5.5 rule that cascade-source verdict is caveat-class precondition NOT co-equal evidence explained; agreement-does-not-strengthen discipline reaffirmed via Stage I §4.6 directional-agreement-with-scope-reframe outcome).

</details> Drafted 2026-07-07 by the `/research-interpret synthesise C-bout-substance` engine (skill LOCKED r4) under Phase C of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) — single-member cluster **C-bout-substance** (HA-C4c); **cascade-receiver** from Phase B **C-bout-framework** LOCKED r1 2026-07-07 per [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md) §3. Upstream Stage D `descriptive_audit.md` LOCKED r1 2026-07-08 (OI-021 CLOSED). Upstream Stage I `interpretation/HA-C4c.md` LOCKED r1 2026-07-08 (OI-022 CLOSED; first-time directional-agreement-with-scope-reframe in L4-inflation-4× series). **Closes Phase C of the locked rollout at cluster-level user acceptance; unblocks Stage S₂ dispatch on T-within-day-recovery** per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §9.2 cluster→topic dependency. **Fresh-session `/research-review` per plan §4 row for `cluster-*.md` NOT dispatched pre-lock — user-authorized deviation matching Phase-B precedent** (rationale: trivial-ORTHOGONAL structurally-determined per `internal_synthesis.md` §5.6, NOT a judgment call; joint claim verbatim carry-forward of Stage I §3; cascade-receiver §4.5 5c precondition consumption guide-prescribed per §5.5; user briefing on substantive content at lock confirmed structural discipline; §9.6 walk clean on all 9 substantive checks; retained as retrospective drift-trigger per plan §3.7).

## Authorship

Drafted 2026-07-07 by Claude (Opus 4.7, 1M context) in reviewer-mode-with-authorization per [CONVENTIONS §1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode), via the `/research-interpret synthesise C-bout-substance` skill invocation dispatched by the orchestrator on user authorisation under Phase C of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md). Authorising user: Willem (dispatch framing — user reviews artefact in a separate non-dry-run lock session). Fresh-session `/research-review` peer-check required before LOCK per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §4 row for `cluster-*.md`; user-authorized deviation anticipated at joint lock session per Phase-B precedent (documented in §11 lock log at lock time).

---

## 4.1 Cluster name + constituent HAs

- **Cluster ID + name** (verbatim from [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) r3 §3): **C-bout-substance** — Bout-level recovery substance (heavy-T failure-to-return).
- **Constituent HAs** (r2 update; verbatim from map's "Constituent HAs" cell + Bundle H+ event 11 addition per HA-C4cp result.md LOCKED r1 2026-07-14): **HA-C4c + HA-C4cp** (2-member cluster; r1 was single-member HA-C4c only).
- **Member verdicts** (verbatim from each member's Stage I §1, which itself carries verbatim from `result.md`):
  - **HA-C4c** (revision r2): **PARTIAL** — direction = +, (a) discrimination PASS at block-permutation p = 0.0001, (b) effect-size FAIL at Cliff's δ = +0.120 vs pre-committed threshold ≥ +0.20 (95% CI [+0.064, +0.178] by paired-bootstrap B=2000). Per-bar table: direction PASS (δ = +0.120 > 0); bar (a) PASS (p = 0.0001 < 0.05); bar (b) FAIL (δ = +0.120 < +0.20). Stage D verdict-trust: **TRUSTED**. Fixed-absolute-threshold operand family (`bout_n_did_not_return_day` = per-day count of bouts hitting 180-min return-window cap per parent MD §3.2).
  - **HA-C4cp** (revision r2; ADDED IN r2): **PARTIAL** — direction = +, (a) discrimination PASS at block-permutation p = 0.0090, (b) effect-size FAIL at Cliff's δ = +0.079 vs pre-committed threshold ≥ +0.20 (95% CI [+0.024, +0.132] by paired-bootstrap B=2000). Per-bar table: direction PASS (δ = +0.079 > 0); bar (a) PASS (p = 0.0090 < 0.05); bar (b) FAIL (δ = +0.079 < +0.20). Stage D verdict-trust: **TRUSTED**. Personal-baseline SD-anchored operand family (`bout_n_did_not_return_2sd_day` = per-day Z=2 tail count against personal `[d-90, d-30]` LC-era lagged reference median + `1.4826 × MAD` per parent MD §3.2.2). **SAME failure-mode configuration as HA-C4c** (both direction PASS + (a) PASS + (b) FAIL); ~2× smaller magnitude on the personal-baseline operand family, as pre-reg §7 + §8 caveat 2 operand-family compression argument anticipated.
- **Shared construct** (verbatim from map's "Shared construct" cell): **Within-day stress recovery / failure-to-return at bout resolution on heavy-T days**.
- **Operationalisation-overlap note** (verbatim from map's "Operationalisation overlap note" cell): **Substantive Wiggers C4 retest at bout resolution.** Uses `bout_n_did_not_return_day` per-day count vs `exertion_class_lagged_lcera` heavy-T classifier. **Cross-phase pooled** on citalopram_phase axis (unmedicated + buildup + consolidation + afbouw + post_afbouw), §4.10 unmedicated-only sensitivity arm. S₁ on this cluster MUST consume the C-bout-framework verdict as caveat-class precondition (per cascade arrow) before reading the C-bout-substance verdict on its own merits. **Era-scope mismatch** with C-bout-framework: C-bout-framework runs unmedicated × train; C-bout-substance runs cross-phase pooled — the asymmetry is structural to each cluster's question, not a confound to reconcile away.
- **Cascade-arrow language** (verbatim from map's §3 row): this cluster is the **cascade-receiver** from C-bout-framework (upstream). The upstream cluster's joint claim (LOCKED r1 2026-07-07 per [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md)) propagates INTO this cluster's §4.5 5c cascade-precondition sub-paragraph as caveat-class precondition per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5; NOT co-equal verdict per §5.5 hard rule. See §4.5 5c below for the load-bearing sub-paragraph and §5.5 for the cascade-handling rule in the coherence-call mapping.

**Asymmetry vs Phase-A + Phase-B precedents.** The Phase-A precedent at [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) §4.1 was a **2-member stand-alone cluster** (no cascade-arrow language in either direction — neither upstream nor downstream), exercising the PARTIALLY-CONCORDANT-vs-CONCORDANT judgment-call axis. The Phase-B precedent at [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) §4.1 was a **1-member cascade-source cluster** (downstream-bearing; no upstream precondition), exercising the degenerate-single-member axis + the cascade-source designation for downstream framing. This Phase C cluster is a **1-member cascade-RECEIVER** (upstream-bearing; no downstream cascade obligation, though downstream Stage S₂ on T-within-day-recovery consumes this cluster as its primary constituent per map §4). The load-bearing structural difference from the Phase-B precedent surfaces at §4.5 sub-block 5c — Phase B's 5c was OMITTED (cascade-source has no upstream precondition); this cluster's 5c is LOAD-BEARING (carries C-bout-framework's joint claim verbatim as caveat-class precondition per guide §4.5 5c template). The trivial-ORTHOGONAL coherence-call structure is inherited from Phase-B precedent per guide §5.6 single-member prescription; the cascade-receiver framing at §4.5 5c is inherited from guide §5.5 hard rule directly (no in-corpus precedent — this is the first-ever cascade-receiver Stage S₁ artefact).

## 4.2 Pre-declared constellation

> *Pre-declared constellation* (per [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) §3 row `C-bout-substance`, declared 2026-06-23, lock version r3): the cluster groups **HA-C4c** alone on the shared construct **within-day stress recovery / failure-to-return at bout resolution on heavy-T days** as **substantive Wiggers C4 retest at bout resolution**. The cluster's evidence-strength rationale is "substantive Wiggers C4 retest at bout resolution using `bout_n_did_not_return_day` per-day count vs `exertion_class_lagged_lcera` heavy-T classifier, cross-phase pooled on citalopram_phase axis" — a substantive claim about within-day recovery in PAIS at the bout-resolution scale, in contrast to C-bout-framework's methodology-validation-scope framing. The constellation was locked at the layer-wide map's §7 lock-log entry 2026-06-24 (r3) and has not changed since. This Stage S₁ session reads the constellation from the map row; it does not propose a constellation change.

> *Cascade-arrow precondition* (per the map's §3 row for C-bout-substance): C-bout-framework's [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) verdict (LOCKED r1 2026-07-07; trivial-ORTHOGONAL single-member-collapse to HA11-bout-redo PARTIAL, 2-of-3 framework-validity bars met) propagates as caveat-class precondition into this cluster's coherence call — NOT as a co-equal verdict S₁ reconciles against. See §4.5 5c below for the load-bearing cascade-precondition sub-paragraph and §5.5 of [`internal_synthesis.md`](../../methodology/internal_synthesis.md) for the cascade-handling rule in the coherence-call mapping.

## 4.3 Per-HA verdict + interpretation row

One row per cluster member HA (here: one row only, per single-member cluster). Per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.3 note on cascade-arrow language for cascade-downstream clusters: "the §4.3 row table contains ONLY C-bout-substance's members (HA-C4c); the C-bout-framework verdict propagates via the §4.5 caveats + the §5.5 cascade-handling rule in the coherence-call mapping, NOT as a co-equal row in §4.3." Applied here as the LOAD-BEARING discipline check for this cascade-receiver cluster.

| HA | Verdict | Operationalisation | "What the verdict licenses" (from Stage I §3) | "What the verdict does NOT license" | Effect-size summary | L-IDs cited (Stage I §4.5) | Closure-path (Stage I §4.7) |
|---|---|---|---|---|---|---|---|
| HA-C4c (r2) | PARTIAL — direction = +, (a) discrimination PASS at p = 0.0001, (b) effect-size FAIL at Cliff's δ = +0.120 vs pre-committed threshold ≥ +0.20 | substantive Wiggers C4 retest at bout resolution on operand `bout_n_did_not_return_day` (per-day count of bouts with `did_not_return_flag == True` per parent MD §3.2 — bouts whose stress did not return to within +5 of `pre_bout_baseline` for ≥ 10 consecutive minutes within 180 minutes after peak) tested as heavy-T-vs-non-heavy-T contrast (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T) on the **cross-phase-pooled stratum** on the `citalopram_phase` axis (unmedicated + buildup + consolidation + afbouw + post_afbouw; LC era `>= 2022-11-17`; April 2024 cluster excluded) at n_heavy = 465 / n_non_heavy = 809 | The bout-level operand `bout_n_did_not_return_day` evaluated under the locked cross-phase-pooled substantive operationalisation **produces a direction-correct + statistically-distinguishable + effect-size-below-threshold contrast** on this corpus's substantive primary cell — Cliff's δ = +0.120 (95% CI [+0.064, +0.178]) fully bracketed below the pre-committed +0.20 discipline threshold, with block-permutation p = 0.0001 well below α = 0.05 (E[L] = 7, B = 10000, seed `20260623`). The licensed claim is **direction-of-signal-at-magnitude**; below-threshold effect-size is part of the signal's shape at this corpus's operationalisation, NOT noise to bracket away. | PARTIAL does NOT license Cliff's δ = +0.120 as a "small-but-real substantive within-day recovery signal" without the below-threshold pattern preserved; PARTIAL does NOT collapse to weak SUPPORTED; PARTIAL does NOT license "Wiggers C4 is REJECTED at bout level" NOR "Wiggers C4 is CONFIRMED at bout level" (the licensed shape is distinct from either polar reading); PARTIAL on cross-phase pooled stratum does NOT license per-phase claims (unmedicated-only sensitivity REJECTED at δ = +0.059 p = 0.1145 is a stratum-fragility finding per pre-reg §9.5). | Direction (heavy-T > non-heavy-T; Cliff's δ > 0): **PASS** at δ = +0.120. Bar (a) discrimination (block-perm p < 0.05): **PASS** at p = 0.0001. Bar (b) effect-size (Cliff's δ ≥ +0.20): **FAIL** at δ = +0.120 (95% CI [+0.064, +0.178] fully bracketed below threshold). Mann-Whitney U = 210614 (descriptive companion). Block-perm null δ median = −0.000 with 95% CI [−0.061, +0.060]; observed +0.120 well outside null upper bound. E[L]\* data-driven = 7.0 days exactly (factor-of-2 flag NOT FIRED). Crash-drop sensitivity \|Δ Cliff's δ\| = 0.015 < 0.10 threshold → §3.4 flag NOT FIRED → CLEAN. Sensitivity arms: unmedicated-only REJECTED (δ = +0.059, p = 0.1145; stratum-fragility fired per pre-reg §9.5); motion-clean-only REJECTED at δ = +0.002 (corpus-property reaffirmation per pre-reg §8 caveat 4; only 32/4317 = 0.7% motion-clean bouts); transient-excluded PARTIAL consistent (δ = +0.116, p = 0.0001); baseline-invalid-excluded PARTIAL consistent (δ = +0.123). Holm step-down: primary + transient_excluded survive at adjusted p = 0.0004; unmedicated_only + motion_clean fail. | L1, L2, L3, L4, L7 (L5 NA — no v24 primary signals; L6 NA — gevoelscore not in primary cell). | Operational per [`verdict_to_inference.md`](../../methodology/verdict_to_inference.md) §4.7 for "one-of-two-bars MET + direction-correct" framing: bar (b) FAIL closure paths surveyed in Stage I §4.7 — (1) larger heavy-T pool tightens CI but does not shift point estimate (no closure via corpus accrual at this operand + stratum; magnitude-shortfall not power-shortfall); (2) operand-refinement v2 (OPTIONAL per pre-reg §9.2; not required); (3) framework-validity reference-frame extension (downstream of HA-C4c's substantive scope; would not directly close bar (b)); (4) **NO CLOSURE — PARTIAL with bar (b) FAIL is a legitimate completion state for the substantive Wiggers C4 question at bout resolution per pre-reg §9.2 second PARTIAL branch (weak-effect-but-real positive pattern)**. |

## 4.4 Coherence call

**r2 STATUS (2026-07-14): CONCORDANT-BELOW-THRESHOLD (2-member cluster; both HAs direction-correct + both (a) PASS + both (b) FAIL per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5 verdict table applied to CONCORDANT-BELOW-THRESHOLD sub-shape per HA-C4cp pre-reg §9.2 anticipated modal outcome).**

**r2 rationale paragraph** (2-member cluster reading; supersedes r1 trivial-ORTHOGONAL rationale which is preserved as r1-degenerate-shape context below):

> *CONCORDANT-BELOW-THRESHOLD coherence call* (2-member cluster per Bundle H+ event 10 addition of HA-C4cp to the synthesis-structure map's §3 row C-bout-substance; joint verdict shape per HA-C4cp pre-reg §9.2 anticipated modal outcome): both HA-C4c and HA-C4cp land at direction PASS + (a) discrimination PASS + (b) effect-size FAIL on their respective operand families (fixed-absolute-threshold vs personal-baseline-rolling-reference). The two operationalisations are cross-operationalisation-independent at the operand-family level per OI-025 protocol §5.4 four-condition argument (different reference frame; different construct alignment; different distributional-drift sensitivity; different low-n failure modes) — the same substantive Wiggers-C4 question tested at two independent operand-family levels, both producing direction-correct + statistically-real + effect-size-below-threshold contrasts. **Cross-operationalisation independence at operand-family level, NOT raw-substrate level** per pre-reg §8 caveat 7: both HAs derive from the same Firstbeat-per-minute stress signal via the same bout-detection pipeline; independence binds at operand-family level, and a shared-substrate correlated failure mode (e.g. per parent MD §2.4 minute-resolution amplification) would affect both HAs. Cross-op concordance descriptive on n=1274 primary stratum: 33.5% concordant firings, 20.3% HA-C4c-only, 4.8% SD-anchored-only, 41.4% neither; Cohen's κ = 0.507 (moderate per Landis-Koch); 452 cap-unreachable days (35.5%) where Z=2 threshold exceeds 180-min `tail_length` cap by construction — direct descriptive corroboration of the operand-family-level independence property. **The joint claim shape is: two operand-family-independent operationalisations of the same substantive Wiggers-C4 question both produce direction-correct + statistically-distinguishable + effect-size-below-threshold contrasts on this corpus** — a claim shape structurally different from either "both operationalisations reject Wiggers-C4" or "both operationalisations confirm Wiggers-C4". This is the corpus-effect-size ceiling shape per HA-C4cp pre-reg §9.2 substantive interpretive note — honest sample-limitation reading, NOT a Wiggers-C4 disproof.

**Guide §5 verdict-table mapping**: per `internal_synthesis.md` §5, the 4-way coherence-call taxonomy (CONCORDANT / PARTIALLY CONCORDANT / CONFLICT / ORTHOGONAL) applies to the joint reading. Both HAs share the same substantive direction (heavy-T > non-heavy-T on within-day recovery-failure); both share the same statistical significance status (bar (a) PASS at similar α levels); both share the same effect-size-below-threshold shape (bar (b) FAIL with fully-bracketed-below CIs) — the mapping fires **CONCORDANT** on direction + statistical-real + effect-size-shape at each per-bar level. The **BELOW-THRESHOLD** qualifier per pre-reg §9.2 preserves that CONCORDANT here is NOT "both at SUPPORTED" but "both at PARTIAL with the effect-size failure being part of the joint claim shape". This is the guide-prescribed label for the 2-member both-direction-correct + both-(b)-FAIL configuration when the pre-reg pre-committed the shape as an anticipated modal outcome (which HA-C4cp §9.2 explicitly did).

**Alternative coherence calls considered and NOT selected** (per `internal_synthesis.md` §5.4-§5.6):

- **PARTIALLY CONCORDANT** — would apply if the two HAs had different failure-modes (e.g., one direction PASS + (a) FAIL + (b) FAIL vs the other direction PASS + (a) PASS + (b) FAIL). Both HAs have the same failure-mode (direction PASS + (a) PASS + (b) FAIL); NOT applicable.
- **CONFLICT** — would apply if the two HAs had opposite directions or if one at SUPPORTED and the other at REJECTED. Both PARTIAL + both direction-correct; NOT applicable.
- **ORTHOGONAL** — would apply if the two HAs tested substantively different questions and neither directly informed the other's claim shape. The two HAs test the same substantive Wiggers-C4 question at operand-family-independent levels (per OI-025 protocol §5.4 argument); the claims directly inform each other's shape (each strengthens the other's cross-op-independence gap closure at guide r3 §4.2); NOT applicable.
- **CONCORDANT** without BELOW-THRESHOLD qualifier — would apply if both HAs at SUPPORTED. Both at PARTIAL with (b) FAIL; the BELOW-THRESHOLD qualifier per pre-reg §9.2 pre-committed shape is load-bearing.

**Hard rule observance** (per `internal_synthesis.md` §4.4 + §7.10): the label `CONCORDANT-BELOW-THRESHOLD` is drawn from the four-label set (CONCORDANT / PARTIALLY CONCORDANT / CONFLICT / ORTHOGONAL) with the `BELOW-THRESHOLD` sub-shape qualifier from HA-C4cp pre-reg §9.2 anticipated modal outcome pre-committed at pre-reg drafting time (BEFORE any test execution). This is NOT label invention per §7.10 anti-pattern — the sub-shape qualifier is derived from the pre-reg's own pre-committed §9.2 branch text, not from post-hoc drafting.

**r1 trivial-ORTHOGONAL context preserved** (for r2 reference; superseded by 2-member CONCORDANT-BELOW-THRESHOLD at r2):

<details>
<summary>r1 trivial-ORTHOGONAL rationale (single-member per `internal_synthesis.md` §5.6; preserved for r2)</summary>

**r1 STATUS: trivial-ORTHOGONAL (single-member cluster per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.6).**

**One-sentence default-reading (degeneracy):** the cluster contains a single constituent HA (HA-C4c), so there is no cross-HA coherence to assess; per guide §5.6 the coherence call collapses to **trivial-ORTHOGONAL** with the cluster's joint claim collapsing to the single member's Stage I §3 licensed claim, and the cluster-level reading adds only the §4.5 L-ID citation block at the cluster level + the **cascade-receiver precondition consumption at §4.5 5c** (LOAD-BEARING — the key structural difference from the Phase-B cascade-source cluster whose 5c was OMITTED).

**Rationale paragraph** (drawn from §4.3 the single per-HA row + the map's §3 operationalisation-overlap-note + [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.6 single-member case + §5.5 cascade-handling rule):

> *Trivial ORTHOGONAL coherence call* (single-member cluster per the synthesis-structure map's §3 row C-bout-substance, declared 2026-06-23, lock version r3): the cluster contains a single constituent HA (HA-C4c r2), so there is no cross-HA coherence to assess. The joint claim collapses to HA-C4c's Stage I §3 licensed claim verbatim (carry-forward in §4.7a below): the bout-level operand `bout_n_did_not_return_day` evaluated under the locked cross-phase-pooled substantive operationalisation produces a direction-correct + statistically-distinguishable + effect-size-below-threshold contrast on this corpus's substantive primary cell (Cliff's δ = +0.120 [+0.064, +0.178]; block-perm p = 0.0001; n_heavy = 465 / n_non_heavy = 809). The cluster-level reading adds (a) the §4.5 L-ID citation block at the cluster level (per the limitations-doc §5 binding's cluster-level citation rule) and (b) the **§4.5 5c cascade-receiver precondition sub-paragraph** consuming C-bout-framework's joint claim as caveat-class precondition per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 cascade-handling rule (LOAD-BEARING at this cluster; NOT a co-equal verdict entering §4.4 as a "third vote" per §5.5 hard rule + §7.3 anti-pattern). The cluster-level reading does NOT add cross-HA coherence the single-member structure cannot support.

**Phase-A + Phase-B precedent and degeneracy traceability** (per Phase-A precedent at [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) §4.4 + Phase-B precedent at [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) §4.4): Phase-A was 2-member (HA-C3 v2 + HA-C3p) and exercised the PARTIALLY CONCORDANT vs CONCORDANT judgment-call axis; landed at PARTIALLY CONCORDANT (DEFAULTED-TO-PRESERVE, then user-confirmed). Phase-B was 1-member cascade-source and exercised the degenerate-single-member axis (trivial-ORTHOGONAL per §5.6); cascade-source designation lived at §4.7a + §4.10 with §4.5 5c OMITTED. This Phase C cluster is 1-member cascade-RECEIVER and exercises the same degenerate-single-member axis for §4.4 (trivial-ORTHOGONAL per §5.6, structurally identical shape to Phase B) BUT with the load-bearing §4.5 5c cascade-precondition sub-paragraph as the structural additional-content that Phase B omitted. The user-acceptance question for THIS cluster is whether the trivial-ORTHOGONAL coherence-call + §4.7a single-member joint-claim carry-forward + §4.5 5c cascade-receiver precondition consumption are accepted as drafted (OI-024 below) — not whether the §4.4 label is correct (the label is structurally determined per guide §5.6).

**Dispatch-mode note on the guide-prescribed label.** The dispatch task confirmed that guide §5.6 is explicit on the single-member-cluster case: `trivial-ORTHOGONAL` is the guide-prescribed degenerate coherence-call label, NOT a generic dispatch-task fallback placeholder. Per LOCKED-r4 skill §"Always-read-LOCKED-current-revision rule" — the guide's prescribed label wins over any generic dispatch-task fallback when both are available. The dispatch-mode lock pending at OI-024 is a procedural lock-event step (user-acceptance), not a label-judgment-call step. This matches the Phase-B precedent at [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) §4.4 dispatch-mode paragraph exactly.

**Hard rule observance** (per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.4 + §7.10): the label `trivial-ORTHOGONAL` is drawn from the four-label set (CONCORDANT / PARTIALLY CONCORDANT / CONFLICT / ORTHOGONAL) — specifically the ORTHOGONAL label per §5.4 with the `trivial-` modifier per §5.6 for the single-member case. This is NOT label invention (per §7.10 anti-pattern) — the `trivial-` qualifier is the guide-prescribed degenerate modifier (one of the four labels), not a new fifth label. Guide §5.6 covers exactly this case.

</details>

## 4.5 Cluster-level caveats narrowing the joint claim (with L-ID citation block)

### 5a. Cluster-level caveats from member interpretations

**r2 note**: with HA-C4cp added at Bundle H+ event 11, this sub-block now inherits from BOTH members' §4.5 5a caveats. Stage S₁ inherits without inventing new caveats per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §7.7 anti-pattern. Where a caveat applies to both HAs (e.g., n=1 subject reach, 99.3% motion-confound corpus property, framework-validity calibration from HA11-bout-redo cascade-source), it is stated once at cluster level. Where a caveat is HA-specific (e.g., HA-C4c's fixed-absolute-threshold-operand-specific framing vs HA-C4cp's personal-baseline-reference-frame-specific framing), the cluster-level entry names both.

**HA-C4cp additions to §5a caveat inheritance** (r2; new items surfacing at HA-C4cp result.md LOCK 2026-07-14):

- **Reference-window fragility flag** (HA-C4cp pre-reg §8 caveat 6 LOAD-BEARING; empirically LIVE): the `reference_window_shorter_lag` sensitivity arm (reference window `[d-60, d-30]` instead of primary `[d-90, d-30]`) REJECTED at δ = +0.037 (p = 0.1327; Holm not-rejected at adj_p = 0.398) — the primary's signal materially attenuates and loses statistical distinguishability when the reference window is halved. **Cluster-level consequence**: any downstream S₂ / A framing of the SD-anchored operand's signal is bounded to the `[d-90, d-30]` reference-window construction; the joint-cluster claim's SD-anchored half does NOT project to shorter-lag reference windows on this corpus.
- **HA-C4cp unmedicated-only stratum-fragility at higher amplitude than HA-C4c** (HA-C4cp Stage I §4.5 L2 + pre-reg §8 caveat 8): HA-C4c unmedicated-only sensitivity produced δ = +0.059 vs primary +0.120 (50% attenuation, REJECTED). HA-C4cp unmedicated-only produces δ = +0.004 vs primary +0.079 (95% attenuation, essentially zero-signal, REJECTED). The signal on the personal-baseline operand vanishes almost entirely in the unmedicated sub-arm. **Cluster-level consequence**: the cross-phase pooling is MORE load-bearing on the SD-anchored operand than on HA-C4c's fixed-absolute-threshold operand; medicated phases contribute disproportionately to the SD-anchored signal. A substantive stratum-fragility observation Stage S₂ contextualises (what does it mean about the underlying autonomic mechanism that the medicated phases contribute more of the SD-anchored contrast?).
- **Approach A CI-brackets DIVERGENT REJECTED** (HA-C4cp Stage I §2 + §4.5 5a caveat 3 AMPLIFICATION empirically confirmed): the Approach A dose-adjusted primary template (β = +0.056/mg) shows PARTIAL consistent with primary; but the CI-lower (β = +0.145/mg) and CI-upper (β = −0.034/mg) sub-arms REJECT — the pre-reg §8 caveat 3 amplification anticipation (SD-anchored operand family FURTHER removed from `bout_n_fast_recovery_day` analogue than HA-C4c's fixed-absolute operand) is empirically confirmed on the SD-anchored operand family. **Cluster-level consequence**: the Approach A inheritance-by-analogue descriptive-companion is more fragile on the SD-anchored operand than on the fixed-threshold operand; both operands share the underlying underpowered-NULL frame from the sub-MD recalibration, but the SD-anchored operand's CI-bracket divergence surfaces the amplification empirically.

**Original 10 pre-reg §8 caveats + 3 cascade-context notes from HA-C4c inherit as r1 (preserved for r2)** — items 1-10 below apply to both HAs jointly (or are HA-C4c-specific with HA-C4cp analogue noted where distinct):

- Power-calc dispatch inapplicable per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) within-subject design (pre-reg §8 caveat 1); block-permutation null at E[L] = 7 is the within-subject inferential machinery.
- n = 1 single-subject + observational + multi-source per [CONVENTIONS §3.1](../../CONVENTIONS.md#31-personal-baseline-not-absolute-thresholds); personal-baseline thresholded; cross-subject generalisation out of scope (pre-reg §8 caveat 6).
- **Framework-validity-vs-substantive scope distinction** (LOAD-BEARING for this cluster's substantive framing): unlike the Phase-B cluster C-bout-framework whose PARTIAL was framework-validity operand-fitness, HA-C4c's PARTIAL IS a substantive claim about Wiggers C4 at bout resolution. The below-threshold effect-size is part of the signal's SHAPE at this corpus's operationalisation (per pre-reg §9.2 second PARTIAL configuration: "the signal exists and is statistically real but is small in magnitude — the substantive Wiggers C4 reading is a weak-effect-but-real positive pattern"). Stage S₁ preserves this substantive framing at cluster level; the cascade-receiver consumption at 5c below imports the framework-validity CALIBRATION context, not the framework-validity substantive-scope framing.
- **Calibration caveat naming HA11-bout-redo's bar-3 power-shortfall** (pre-reg §8 caveat 2 LOCKED verbatim): HA11-bout-redo's framework-validity gate cleared bars 1+2 at +20.26 pp / median signed z 2.410 but bar 3 FAILED at p = 0.2609 at n_calm = 70 / n_crash = 11. HA-C4c's verdict MUST be read with this calibration context visible. **Empirically confirmed at HA-C4c**: the cross-phase pooling cleared the power-shortfall as anticipated (bar (a) PASS at p = 0.0001 at n_heavy = 465 / n_non_heavy = 809), so the calibration caveat propagates with the empirical observation that the substantive verdict fails on the OTHER bar (effect-size, bar (b)), not the same bar HA11-bout-redo failed on — a **distinct-failure-mode finding across the two clusters** (Phase-B PARTIAL = magnitude PASS + p FAIL at framework-validity layer; Phase-C PARTIAL = p PASS + magnitude FAIL at substantive layer). This inversion is the empirical shape of the cross-phase-pooling architectural implication per parent MD §5.3 + sub-MD §6.
- **Underpowered-NULL discipline for Approach A inheritance-by-analogue** (pre-reg §4.9 descriptive-companion-only frame; pre-reg §8 caveat 3): the Approach A dose-adjusted sensitivity arm (three sub-arms all PARTIAL consistent with primary) is descriptive companion under the underpowered-NULL frame, NOT a load-bearing dose-correction of the primary claim per sub-MD §6 architectural-implications (0/7 features CONFIRMED → Approach A NOT load-bearing → cross-phase pooling permitted).
- **99.3% motion-confound corpus property** (pre-reg §8 caveat 4): 4285/4317 bouts carry `motion_confound_flag = True`; the entire HA-C4c operand inherits this by construction. Motion-clean-only sensitivity arm REJECTED via near-zero signal (δ = +0.002; p = 0.1841; only 32 motion-clean bouts across corpus) is **corpus-property reaffirmation**, NOT primary fragility. Wiggers' "during rest periods" language is operationalised across the motion-tagged bout pool by construction. Cluster-level consequence: any downstream S₂ or A "rest-stress" semantic claim inherits this 99.3% structural constraint.
- **Transient-fragility flag NOT FIRED at HA-C4c**: transient-excluded arm δ = +0.116 vs primary δ = +0.120 (consistent); transient bouts contribute minimally to the signal at HA-C4c (unlike HA11-bout-redo where transient exclusion dropped disc from +20.26 pp to +11.69 pp) — a distinct-fragility-profile observation between the two clusters (per pre-reg §8 caveat 5).
- **Era restriction: cross-phase pooled by design** (pre-reg §8 caveat 8 + §9.5): HA-C4c runs cross-phase pooled by design per parent MD §5.3 substantive-layer permission (0/7 features CONFIRMED → cross-phase pooling permitted). The unmedicated-only sensitivity arm REJECTED at δ = +0.059 p = 0.1145 IS the descriptive signature of the pooling being load-bearing for the substantive signal — a **stratum-fragility finding**, empirically confirming that medicated phases contribute to the headline contrast. **Unmedicated-only sensitivity arm REJECTED per stratum-fragility per pre-reg §9.5**: the primary cross-phase-pooled PARTIAL cannot be silently generalised to the unmedicated sub-stratum; per-phase reach is out of scope at cluster level.
- **Crash-drop sensitivity CLEAN** (CONVENTIONS §3.4; HA-C4c result.md §5): |Δ Cliff's δ| = 0.015 < 0.10 threshold → §3.4 flag NOT FIRED → CLEAN. Cluster-level reading per §5.7 of guide #3 inherited at §5d below.
- **Pipeline-extraction trust assumption**: `extract_stress_bouts.py` LOCKED `d5b394c` with 6 inline smoke tests + 5-bout spot-check verification (pre-reg §8 caveat inherited from HA11-bout-redo pre-reg §8 caveat 9; Stage D A10 BACKSTOPPED).
- **Multi-comparison via single-cell headline lock; sensitivity arms cannot promote** (pre-reg §5.0 + §4.10): primary-cell verdict is the headline; the four sensitivity arms + Approach A dose-adjusted companion cannot promote the primary to SUPPORTED (they can only fail or reaffirm the primary via §4.10 discipline).

From Stage I §4.5 5a cascade-context notes (carry-forward from Stage D `descriptive_audit.md` §4.5 items 1-3):

- **Cascade-context note 1** (per Stage I §4.5 5a item 1): cascade-source verdict from C-bout-framework (HA11-bout-redo Stage D + I + S₁ all LOCKED r1 2026-07-07) is caveat-class precondition per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5. **This is operationalised at §4.5 5c LOAD-BEARING sub-paragraph below** — Stage I noted for traceability with "cascade-consumption obligation is at Stage S₁ downstream, NOT here"; this Stage S₁ artefact IS that downstream. Empirical observation now confirmed: the cross-phase pooling cleared the power-shortfall as the cascade-context expected (see 5c below for the operationalised precondition consumption).
- **Cascade-context note 2** (per Stage I §4.5 5a item 2): per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` does not yet exist as a standalone descriptive artefact; A2 BACKSTOPPED via the §3.4 day-validity gate + primary-stratum non-NaN days figure (1274 exactly = n_heavy + n_non_heavy); a future per-channel rate audit would tighten the L7 survivorship reach narrative. Logged as OI-023 (sister to OI-017 for `bout_n_fast_recovery_day`; joint two-operand run available at same S effort per §4.5 audit note). **Cluster-level consequence**: closing OI-023 (jointly with OI-017) tightens this cluster's §4.5 5b L7 from "gating-discipline-bounded" to "fully audit-backed"; does not change the §4.4 coherence call or the §4.7a joint claim.
- **Cascade-context note 3** (per Stage I §4.5 5a item 3): stratum-fragility observation — unmedicated-only sensitivity arm REJECTED at δ = +0.059 vs primary PARTIAL at δ = +0.120 (empirically live per pre-reg §9.5). Cross-phase pooling is empirically load-bearing for the substantive signal; medicated phases contribute to the headline contrast, likely via dose-shaped baseline shifts the operand picks up (per pre-reg §8 caveat 10 pacing-behaviour narrative). No descriptive closure path at Stage D; substantive property of operand × stratum interaction on this corpus that downstream Stage S₂ + A contextualise (what does it mean about the underlying autonomic mechanism that the medicated phases contribute more of the contrast?).

### 5b. Cluster-level L-ID citation block (binding per limitations doc §5 row for `cluster-*.md`)

Per the synthesis-structure map's §3 [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) row L-IDs column: **L1, L2, L3, L4, L7 (L5 NA — no v24; L6 NA — gevoelscore not in primary cell)**. Per guide §3 worked-example paragraph for `cluster-bout-substance.md`: "the cluster's L2 citation flows through HA-C4c's interpretation.md cite — no extra L2-era-strata-mismatch cluster-level citation is triggered ... C-bout-substance is single-member; the cross-phase pooling is HA-C4c's own L2-era-confound surface (Stage I already cites it per the per-HA L-ID block), not a cluster-level L2 extension." Applied verbatim: the cluster has only one member (HA-C4c) and the cross-phase pooling is a within-member L2 surface that the L2 citation below names as the era-scope-note at cluster level.

> *Cites L1 (single-subject reach)*: this cluster's joint claim about substantive Wiggers C4 at bout resolution is **single-subject** (L1 inference reach per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) bounds it). The cluster's substantive PARTIAL verdict at δ = +0.120 (CI [+0.064, +0.178]) adds one within-subject data point about the operand's substantive contrast on cross-phase-pooled n; it is NOT a group-level substantive claim about Wiggers C4 at bout resolution. Cross-operationalisation reading does not apply at cluster level (single-member cluster; no sister HA exists at substantive layer at bout resolution); cross-test cross-reference to HA-C4 v2 REJECTED (daily-aggregate; per pre-reg §8 caveat 9 the two verdicts coexist as resolution-of-scale pair) is Stage S₂ / cross-test-interpretation-pass territory, NOT a cluster-level licensing extension. The cascade-arrow propagation from C-bout-framework at §4.5 5c below is a precondition-scope-bound narrowing, NOT a cross-subject reach extension.

> *Cites L2 (era confounds)*: this cluster's single member HA-C4c runs on the LC era cross-phase-pooled stratum (2022-11-17 → present; April 2024 cluster excluded; 21-day device-baseline-lag excluded per parent MD §3.4; primary spans `citalopram_phase ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}`). The PARTIAL substantive verdict at cluster level does NOT project to pre-LC era or to the unmedicated-only sub-stratum on the citalopram axis (where the sensitivity arm REJECTED at δ = +0.059 p = 0.1145 — a stratum-fragility finding making the cross-phase pooling load-bearing, per pre-reg §8 caveat 8 conditional-nature framing + §9.5). **Cluster-level cross-phase-pooling note**: the within-member cross-phase pooling IS the L2 era-scope surface at this cluster (as opposed to the between-member era-strata-mismatch that the limitations-doc §5 "also cite L2 if cluster members are from different era strata" clause would trigger with plural members). Per guide §3 worked-example paragraph for `cluster-bout-substance.md`: no extra L2-era-strata-mismatch cluster-level binding fires; the cluster's L2 citation carries the within-member cross-phase pooling as its era-scope-note by construction. Per-phase reach is out of scope at cluster level and is the stratum-fragility direction downstream Stage S₂ contextualises.

> *Cites L3 (device generations)*: cluster-level dependency on FR245 Elevate-V3 device generation via `extract_stress_bouts.py` LOCKED `d5b394c`. The Firstbeat-input amplification caveat (pre-reg §8 caveat 7 operational-vs-mechanistic + inherited from HA11-bout-redo pre-reg §8 caveat 5) compounds with L3: the per-minute Garmin stress signal is closed-source upstream, and bout-level features may reflect Firstbeat-algorithm-specific artefacts that would not transfer cleanly to a future device generation. The 99.3% motion-confound corpus property (pre-reg §8 caveat 4) is itself a device × extraction-threshold interaction that a different device generation might show at a different rate. Cross-device-generation projection requires explicit calibration evidence per [`research_line_limitations.md`](../../methodology/research_line_limitations.md) §3 L3. **Cluster-level cascade consequence**: the device-generation reach-bound is INHERITED from the cascade-source cluster (C-bout-framework §4.5 5b L3 row: "the device-generation reach-bound propagates to C-bout-substance as caveat-class precondition") — the two clusters share the same `extract_stress_bouts.py` pipeline at the same device generation, so this L3 citation is doubly binding at this cluster level.

> *Cites L4 (analyst-is-subject)*: HA-C4c is prior-driven by the Wiggers C4 verbatim source-verified reading (pre-reg §2 prior 1; PDF lines 1112-1119, 1140-1143, 1223-1231, 1306-1314 — five passages directly supporting the within-day "failure to recover" framing) + the parent MD `bout_level_recovery_dynamics.md` §1.3 substantive-retest-at-bout-resolution enablement; per CONVENTIONS §4.3 the test is confirmatory of the Wiggers C4 substantive claim at bout resolution on this corpus, not exploratory. The L4 mitigation reach per limitations doc r3 §3 L4 caveat: fresh-session `/research-review` peer-check (expected user-authorized-deviated at joint lock session per Phase-B precedent, documented at lock time); producer-vs-reviewer split honored via this artefact's reviewer-mode-with-authorization status. **The member-level §4.6 lived-experience-prior reconciliation is DEFAULTED-PENDING-USER-INPUT at Stage I (OI-022; HA-C4c §4.6)** — Stage S₁ does NOT carry a §4.6-equivalent per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §6.4 (cluster-level prior reconciliation is inherited from member interpretations, not re-litigated); the cluster inherits the member's DEFAULTED-PENDING-USER-INPUT state as caveat-class context per L4 + OI-022 carry-forward. The L4-inflation-verified-three-times pattern (HA-C3 v2 + HA-C3p + HA11-bout-redo dispatcher-default all diverged from user's actual prior; per HA-C4c §4.6 Phase-A + Phase-B precedent traceability block) is inherited as the specific L4 manifestation shape at this cluster.

> *L5 (presence-conditioned data layer) NA*: cluster's single member HA-C4c uses no v24 primary signals; `bout_n_did_not_return_day` is derived from per-minute Garmin stress trace via `extract_stress_bouts.py` (non-v24); `exertion_class_lagged_lcera` is Garmin training-load-derived (non-v24); `crash_v1` labels used only in §4.10 crash-drop sensitivity arm are HA11 v1's primary outcome from Garmin-derived Firstbeat signals (non-v24). The map's §3 row marks L5 NA for the C-bout-substance cluster; this cluster does not trigger L5 independently. NA reason: no v24-derived columns anywhere in the cluster's primary or sensitivity-arm chain per Stage D audit A5 (carried via Stage I §4.5 5b L5 NA).

> *L6 (self-reporting) NA*: cluster's single member HA-C4c does NOT use `gevoelscore` in the primary cell — the primary outcome is `bout_n_did_not_return_day` (Garmin-derived per-day count via the bout-extraction pipeline), and the heavy-T classifier is `exertion_class_lagged_lcera` (Garmin training-load-derived). Gevoelscore does not enter the primary cell nor any sensitivity arm nor the crash-drop sensitivity's `crash_v1` labels (which are HA11 v1's primary outcome from Garmin-derived signals, not gevoelscore-derived). NA reason: gevoelscore not in primary cell; no self-report primary signal (map §3 row explicitly marks L6 NA for C-bout-substance). Distinct from the cascade-source cluster C-bout-framework where L6 applied at the upstream methodology layer via `crash_v1` participant-defined-triage inheritance; at this cluster the crash-drop use of `crash_v1` is a §4.10 sensitivity arm only + it did not fire (|Δ δ| = 0.015 < 0.10 threshold), so no L6 upstream-methodology-layer inheritance carries forward to a live caveat.

> *Cites L7 (survivorship)*: the primary-cell effective coverage n_heavy = 465 / n_non_heavy = 809 (total 1274 primary-stratum non-NaN days) represents the gated subset surviving the §4.4 day-validity gate (≥ 600 valid per-minute stress samples + LC era + 21-day device-baseline-lag exclusion + April 2024 cluster exclusion) + §4.4 exertion-classification non-NaN gate + §4.2 cross-phase-pooled stratum restriction (days `>= 2022-11-17`). The cluster's joint claim does not project to days outside the gated subset. Stage D `descriptive_audit.md` A2 BACKSTOPPED reading via §3.4 day-validity gate + 1274 primary-stratum non-NaN days figure (matches n_heavy + n_non_heavy exactly per `result.md` §9) supplies path (a) of guide #1 §5.2 operational test; the cascade-context note (Stage I §4.5 5a item 2 = OI-023, sister to OI-017 for `bout_n_fast_recovery_day`) flags that a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` would tighten the L7 reach narrative from "gating-discipline-bounded" to "fully audit-backed", and could be jointly closed with OI-017 as a two-operand run at the same S effort. Until that diagnostic lands, the cluster's L7 citation remains gating-discipline-bounded. **Cluster-level cascade consequence**: closing OI-023 at cluster level tightens both bout-level sister operands' L7 reach narratives (this cluster's `bout_n_did_not_return_day` + the cascade-source cluster's `bout_n_fast_recovery_day` per OI-017) — a cross-cluster efficiency observation, not a cluster-level claim extension.

### 5c. Cascade-arrow precondition sub-paragraph (LOAD-BEARING; NOT OMITTED)

Per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.5 5c rule for clusters with cascade-arrow language in the map's §3 row: THIS cluster is **cascade-receiver**; the sub-paragraph MUST quote the cascade-source cluster's §4.7a joint claim verbatim as caveat-class precondition. This is the LOAD-BEARING structural difference from the Phase-B cluster C-bout-framework whose 5c was OMITTED (cascade-source with no upstream precondition).

**Verbatim cascade-source claim from [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) §4.7a "Cascade-source designation" block** (the exact text the framework cluster's §4.7a wrote for this cluster to inherit at 5c per guide §4.5 5c paragraph template):

> *Cascade-arrow source claim for C-bout-substance to consume at its Stage S₁ §4.5 5c*: C-bout-framework's cluster-bout-framework.md reached **trivial-ORTHOGONAL** coherence call (single-member cluster), with HA11-bout-redo's licensed claim of **operand `bout_n_fast_recovery_day` partially fit-for-purpose** — bars 1+2 PASSED indicate direction + effect-size reproduction at +20.26 pp; bar 3 FAILED leaves the block-permutation discrimination at corpus detection limits per the inheritance-risk caveat (pre-reg §8 caveat 4). This propagates into the C-bout-substance cluster's joint claim as caveat-class precondition: the bout-extraction operand's framework fitness is partially established (bars 1+2 PASSED; bar 3 FAILED at corpus detection limits). C-bout-substance's substantive coherence call inherits the 2-of-3 framework caveat: the substantive claim carries the "operand fitness partially established at framework-validity layer" qualifier explicitly. Per `internal_synthesis.md` §5.5 hard rule, this is NOT a co-equal verdict reconciliation; the upstream verdict does not enter C-bout-substance's §4.4 coherence call.

**Caveat-class semantics** (per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 hard rule): this cluster's joint claim (§4.7a below) inherits the C-bout-framework verdict as **CAVEAT-CLASS PRECONDITION, NOT co-equal verdict**. The upstream verdict does NOT enter §4.4 as a "third vote" or as a member of the cluster; the upstream verdict bounds what THIS cluster's substantive verdict may claim about the construct. Per §5.5 for a PARTIALLY-CONCORDANT-analogue upstream (the framework cluster's PARTIAL 2-of-3-bars-met at single-member-collapse to trivial-ORTHOGONAL with joint claim of "operand fitness partially established"): the downstream coherence call MUST narrow its substantive claim accordingly — the §4.7a joint claim carries the "subject to the upstream cluster's operand-fitness-partially-established caveat on framework fitness" qualifier explicitly. The downstream call does NOT auto-downgrade to PARTIALLY CONCORDANT or CONFLICT itself — the downstream member's PARTIAL substantive verdict is independent of the upstream framework's fitness call. But the joint claim's strength is bounded by the upstream's caveat.

**Caveat-class propagation** — specifically **the calibration-discount naming HA11-bout-redo's bar 3 as the failing bar**: HA-C4c pre-reg §8 caveat 2 already inherits this load-bearingly per pre-reg §9.2 cascade-implication (the calibration caveat naming HA11-bout-redo's bar-3 power-shortfall at n = 70/11 stratum); this cluster's §4.5 5c makes that inheritance explicit at cluster-level. **Empirically resolved on HA-C4c's own primary cell**: the cross-phase pooling per parent MD §5.3 + sub-MD §6 architectural-implications cleared the bar-3 power-shortfall constraint at exactly the substantive layer the cascade-context anticipated — bar (a) discrimination PASS at p = 0.0001 at n_heavy = 465 / n_non_heavy = 809. The substantive contrast, once statistically distinguishable at the larger n, sits at a genuinely smaller magnitude than the pre-committed +0.20 discipline threshold. This is the **distinct-failure-mode finding across the two clusters** the cascade-propagation makes explicit: framework-validity layer's failure is on statistical distinguishability at low n (bar 3); substantive layer's failure is on effect-size magnitude at high n (bar (b)) — the two failure modes are structurally different questions and different failure signatures, not different measurements of the same failure.

**Era-scope mismatch note** (per map §3 row for C-bout-substance verbatim): C-bout-framework runs unmedicated × train; C-bout-substance runs cross-phase pooled — **"the asymmetry is structural to each cluster's question, not a confound to reconcile away."** This does NOT create a coherence-call conflict at §4.4 (which is trivial-ORTHOGONAL per §5.6 single-member prescription — determined by structure, not by the era-scope mismatch). The eras were sampled to answer different questions: framework-validity discipline per parent MD §5.5 + §6 requires HA11 v1's reference frame (unmedicated × train × calm-day-pool by construction); substantive discipline per parent MD §5.3 requires cross-phase pool for statistical power (0/7 features CONFIRMED at sub-MD §6 → cross-phase pooling permitted). Reading the era-scope mismatch as a coherence-call conflict would violate the map §3 explicit structural framing + would collapse the cluster-question-scope distinction the map's r1 → r2 split (per the map's §7 lock-log entry 2026-06-23) was designed to preserve.

**§7.3 anti-pattern discipline observance**: this sub-paragraph explicitly declares the framework cluster's PARTIAL verdict as **caveat-class precondition**, NOT a co-equal verdict entering §4.4 as a "third vote" — that would violate [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 hard rule + §7.3 anti-pattern (treating cascade-arrow caveat-class precondition as co-equal verdict). The §4.4 coherence call above carries ONLY HA-C4c's verdict (structurally-determined trivial-ORTHOGONAL per §5.6 single-member); the framework cluster's verdict is at §4.5 5c (this sub-paragraph) as precondition + at §4.7a (below) as bounding qualifier — NOT at §4.4. This is the LOAD-BEARING discipline check for cascade-receiver clusters and it is the primary structural difference between this Phase C cluster and the Phase B cascade-source cluster.

### 5d. Cluster-level crash-drop sensitivity discipline (per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.7)

The single member HA-C4c's `result.md` §5 reports crash-drop sensitivity at |Δ Cliff's δ| = 0.015 < 0.10 threshold → §3.4 flag NOT FIRED → CLEAN. Primary → crash-dropped: n_heavy 465 → 441; n_non_heavy 809 → 745; Cliff's δ +0.120 → +0.135; block-perm p 0.0001 → 0.0001; verdict PARTIAL unchanged. |Δ pp on discrimination| = 1.51 pp (threshold 5.0 pp): NOT FIRED. Route-to-REJECTED (|Δ δ| > 0.20 AND sign-flip): NO. The cluster-level reading per guide #3 §5.7 inherits the CLEAN reading: no cluster-level crash-drop divergence surfaces (single member; no inter-HA crash-drop divergence to assess). Cluster-level surfacing: recorded here for §3.4 audit-hook discipline. The substantive heavy-T signal is robust across the broader pool with crash-days dropped; it is not crash-driven. No cluster-level coherence-call shift; no cluster-level open-conflict triggered.

## 4.6 Open conflicts preserved with both readings

**STATUS: trivial-ORTHOGONAL coherence call per §4.4 (single-member degeneracy).**

Per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.6 for ORTHOGONAL calls: "Cluster members address different aspects of the shared construct; no conflict at the cluster level because the members do not speak to each other directly."

For the trivial-ORTHOGONAL single-member case specifically: **there is no inter-HA agreement-disagreement to assess** (single member). No cluster-internal open conflicts exist by structural construction. The cluster-level reading is the carry-forward of HA-C4c's licensed-claim shape per §4.7a below.

**CRITICAL cascade-receiver discipline**: any tension between C-bout-framework's PARTIAL cascade-source verdict (framework-validity layer, magnitude PASS + p FAIL) and HA-C4c's PARTIAL substantive verdict (substantive layer, p PASS + magnitude FAIL) LIVES in **§4.5 5c as caveat-class precondition**, NOT in §4.6 as open conflict — per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 hard rule. The **distinct-failure-mode finding across the two clusters** (framework-validity's bar-3 p-shortfall at n = 70/11 vs substantive's bar-(b) magnitude-shortfall at n = 465/809) is a **structural feature of the two clusters answering different questions in different eras**, not a cluster-internal open conflict. Reading it as §4.6 open conflict would violate §5.5 hard rule + §7.3 anti-pattern (treating cascade-arrow caveat-class precondition as co-equal verdict).

**Resolution paths (NOT applicable at the cluster-internal level; carried forward to §4.8 follow-up suggestions instead)**:

- A second substantive HA (e.g., HA-C4c-v2 with operand-threshold refinement per pre-reg §9.2 OPTIONAL path, OR HA-C4c-per-phase sister testing per-phase stratification separately) would convert this single-member cluster into a 2-member cluster at the next Stage S₁ re-examination; the coherence-call would then exit the trivial-ORTHOGONAL degeneracy and exercise the standard four-label mapping. Logged in §4.8 own-research track.
- The intraday HR-pacing trend HA (Q30 per parent MD §1.4 A4 candidate) would triangulate the "stuck stress" mechanism from a different Garmin channel; a SUPPORTED-at-HR-channel-trend shape would strengthen the cross-channel triangulation this cluster's substantive PARTIAL alone cannot; would land at a different cluster per constellation implications at map re-lock time. Logged in §4.8 own-research track.

**Inherited member-level placeholder (OI-022) acknowledged**: HA-C4c's Stage I §4.6 lived-experience-prior reconciliation is DEFAULTED-PENDING-USER-INPUT (OI-022). Per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §6.4, Stage S₁ does NOT carry a §4.6-equivalent lived-experience prior reconciliation at the cluster level — that reconciliation lives at the per-HA Stage I level and the cluster inherits it as caveat (per §4.5 5a + 5b L4 above). The placeholder's eventual user-articulation per OI-022 may trigger a re-examination of this cluster's joint claim per the §3.7 drift policy (Stage I revision → cluster re-examination trigger), but does not open a cluster-level §4.6 open-conflict at this draft.

## 4.7 What the cluster jointly licenses + does NOT license

### 7a. What the cluster jointly licenses

**r2 joint claim (2-member CONCORDANT-BELOW-THRESHOLD)** — supersedes r1 single-member carry-forward; r1 shape preserved in `<details>` at end of this sub-block:

> The cluster of bout-level substantive Wiggers C4 retest (2 members HA-C4c + HA-C4cp) tested on this corpus's LC-era cross-phase-pooled stratum on the citalopram_phase axis (`unmedicated + buildup + consolidation + afbouw + post_afbouw`; 2022-11-17 → present; April 2024 cluster excluded; n_heavy = 465, n_non_heavy = 809 on both HAs after HA-C4cp's additive reference-window validity gate trimmed 15 non-heavy days at sub-phase-4b left edge to match HA-C4c primary stratum byte-identically) **establishes a CONCORDANT-BELOW-THRESHOLD weak-effect-but-real positive substantive pattern across two operand-family-independent operationalisations** at cross-phase-pooled resolution: both HA-C4c's fixed-absolute-threshold operand `bout_n_did_not_return_day` (per-day count of bouts hitting 180-min return-window cap per parent MD §3.2; Cliff's δ = +0.120 CI [+0.064, +0.178]; block-permutation p = 0.0001; E[L]=7, B=10000, seed `20260623`) AND HA-C4cp's personal-baseline SD-anchored operand `bout_n_did_not_return_2sd_day` (per-day Z=2 tail count against personal `[d-90, d-30]` LC-era lagged reference median + `1.4826 × MAD` per parent MD §3.2.2; Cliff's δ = +0.079 CI [+0.024, +0.132]; block-permutation p = 0.0090; E[L]=7, B=10000, seed `20260709`) evaluated as heavy-T-vs-non-heavy-T contrast (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T inherited verbatim across both HAs) produce **direction-correct + statistically-real + effect-size-below-discipline-threshold** contrasts, with both effect-size CIs fully bracketed below the pre-committed +0.20 discipline threshold. **The cross-operationalisation-independent evidence** per OI-025 protocol §5.4 four-condition argument (fixed-absolute-threshold vs personal-baseline-rolling-reference differ in reference frame, construct alignment, distributional-drift sensitivity, low-n failure modes) **provides operand-family-level replication of the same substantive Wiggers-C4 finding at bout resolution on this corpus**. **The joint claim is the corpus-effect-size ceiling reading** per HA-C4cp pre-reg §9.2 substantive interpretive note: the Wiggers-C4 stuck-sympathetic signal fires directionally on this corpus at bout-level across both operand families but the effect-size is bounded below the +0.20 SUPPORTED threshold on both operationalisations. Honest sample-limitation, NOT a Wiggers-C4 disproof; NOT a Wiggers-C4 confirmation-at-magnitude either.

**Cross-op-independence gap CLOSURE at strict guide r3 §4.2 two-independent-HAs bar** (per Stage A `construct-bout-recovery-signal.md` r4 §5.2 evidence-layer paragraph update landed at Bundle H+ event 10): the two HAs now populate the C-bout-substance cluster at operand-family-level cross-op-independence per OI-025 protocol §5.4 four-condition argument. The gap named at Stage A §5.2 that was future-closeable-via-OI-033 at r1-r3 is now CLOSED at direction-consistent-below-threshold reading at r4. Descriptive corroboration at operand-family level: Cohen's κ = 0.507 (moderate per Landis-Koch 1977) between the two HAs' primary firings; 452 cap-unreachable days (35.5%) where Z=2 threshold exceeds 180-min `tail_length` cap by construction (SD-anchored operand cannot echo fixed-threshold operand on those days) — direct empirical demonstration of operand-family-level reference-frame difference.

**Bounded by**:
- The cluster's L1 single-subject reach (per §4.5 5b L1 citation applied to both HAs).
- The L2 within-LC-era + within-cross-phase-pooled scope (per §4.5 5b L2 citation; pre-LC + per-phase reach OUT OF SCOPE at substantive layer per both HAs' pre-reg §8 caveat 8; note HA-C4cp's unmedicated-only stratum-fragility is HIGHER amplitude than HA-C4c's (95% vs 50% attenuation) — the SD-anchored operand's cross-phase pooling is more load-bearing than HA-C4c's fixed-threshold operand's).
- The L3 within-device-generation reach (per §4.5 5b L3 citation; Firstbeat-input amplification compounding + shared pipeline).
- The L4 analyst-is-subject prior coupling (per §4.5 5b L4 citation; both HAs prior-driven — HA-C4c by Wiggers C4 verbatim source-reading; HA-C4cp by CONVENTIONS §3.1 personal-baseline discipline + operational realisation of HA-C4c §4.6 user-articulated envelope-crossing reframing).
- The L7 survivorship gating-discipline reach (per §4.5 5b L7 citation; OI-023 cascade-context observation carry-forward).
- The L5 NA + L6 NA confirmations (per §4.5 5b; no v24-derived columns; gevoelscore not in primary cell on either HA).
- **The cascade-receiver precondition from C-bout-framework's joint claim** (per §4.5 5c; unchanged from r1): framework-validity operand-fitness partially established at HA11-bout-redo layer.
- **The reference-window construction dependency (LOAD-BEARING per HA-C4cp §8 caveat 6)** (r2 addition): the SD-anchored half of the joint claim is bounded to the `[d-90, d-30]` reference-window construction; the reference_window_shorter_lag REJECTED at δ = +0.037 is a live fragility flag; the joint claim does NOT project to shorter-lag reference windows.
- **The operand-family-level cross-op-independence at NOT-raw-substrate-level** (per HA-C4cp §8 caveat 7): both HAs derive from the same Firstbeat-per-minute stress signal via the same bout-detection pipeline; shared-substrate correlated failure modes (e.g. parent MD §2.4 minute-resolution amplification) would affect both HAs; the guide r3 §4.2 closure is at operand-family level only.

**Cascade-source framing for downstream Stage S₂** (this cluster is cascade-source for Stage S₂ on T-within-day-recovery per map §4 topic row): the CONCORDANT-BELOW-THRESHOLD joint claim propagates to the Stage S₂ topic-within-day-recovery artefact as its primary constituent-cluster input per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 mirror-application at the S₁-to-S₂ boundary. The downstream Stage S₂ artefact re-consumes this cluster's joint claim on drift-triggered re-consumption per §3.7 discipline; landing scheduled at Bundle H+ event 11 downstream sub-arc per user tempo.

**r1 single-member carry-forward joint claim preserved** (for r2 reference; superseded by 2-member CONCORDANT-BELOW-THRESHOLD at r2):

<details>
<summary>r1 single-member joint claim (preserved for r2)</summary>

> The cluster of bout-level substantive Wiggers C4 retest (single member HA-C4c r2), tested on this corpus's LC-era cross-phase-pooled stratum on the citalopram_phase axis (`unmedicated + buildup + consolidation + afbouw + post_afbouw`; 2022-11-17 → present; April 2024 cluster excluded; n_heavy = 465, n_non_heavy = 809), **establishes a weak-effect-but-real positive substantive pattern** at cross-phase-pooled resolution for the operand `bout_n_did_not_return_day` (per-day count of bouts with `did_not_return_flag == True` per parent MD §3.2) evaluated as heavy-T-vs-non-heavy-T contrast (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T): **direction-correct + statistically-real + effect-size-below-discipline-threshold** — Cliff's δ = +0.120 (95% CI [+0.064, +0.178]) fully bracketed below the pre-committed +0.20 discipline threshold, with block-permutation p = 0.0001 well below α = 0.05 (E[L] = 7, B = 10000, seed `20260623`). The joint claim is bounded by the C-bout-framework cascade-source calibration discount (HA11-bout-redo bar-3 power-shortfall at framework-validity layer as caveat-class precondition per §4.5 5c above); the substantive contrast, once statistically distinguishable at the cross-phase-pooled n that cleared the framework-validity power-shortfall, sits at a genuinely smaller magnitude than the +0.20 discipline threshold. The joint claim is, by single-member-cluster construction, the carry-forward of HA-C4c's Stage I §3 licensed claim with the cluster-level L-ID block applied per §4.5 5b and the cluster-level cascade-receiver precondition applied per §4.5 5c; no width expansion beyond the single member's claim is licensed at this cluster (no cross-operationalisation convergence available at single-member-cluster level).

</details>

**Cascade-source framing for downstream Stage S₂** (this cluster is now cascade-source for Stage S₂ on T-within-day-recovery per map §4 topic row): this cluster's joint claim propagates to the Stage S₂ topic-within-day-recovery artefact as its primary constituent-cluster input per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 mirror-application at the S₁-to-S₂ boundary (analogous to how C-bout-framework §4.7a framed cascade-source designation to THIS cluster). The downstream Stage S₂ artefact will consume this cluster's §4.7a joint claim as constituent-cluster verdict on the T-within-day-recovery topic (per map §4 row: "C-bout-substance (primary); C-bout-framework as upstream calibration precondition"). Stage S₂ is NOT dispatched at this Phase C (rollout §2 Phase C scope closes at Stage S₁); the framing is recorded here for downstream consumption when Stage S₂ dispatches.

The joint claim is bounded by:
- The cluster's L1 single-subject reach (per §4.5 5b L1 citation).
- The L2 within-LC-era + within-cross-phase-pooled scope (per §4.5 5b L2 citation; pre-LC + per-phase reach OUT OF SCOPE at substantive layer per pre-reg §8 caveat 8 + §9.5).
- The L3 within-device-generation reach (per §4.5 5b L3 citation; Firstbeat-input amplification compounding + shared pipeline with cascade-source cluster).
- The L4 analyst-is-subject prior coupling (per §4.5 5b L4 citation; Wiggers C4 verbatim source-reading + parent MD §1.3 substantive-retest enablement; user-articulation pending per OI-022 carry-forward with L4-inflation-verified-three-times pattern inherited).
- The L7 survivorship gating-discipline reach (per §4.5 5b L7 citation; OI-023 cascade-context as L7 narrowing observation sister to OI-017).
- The L5 NA confirmation (per §4.5 5b L5 NA; no v24-derived columns).
- The L6 NA confirmation (per §4.5 5b L6 NA; gevoelscore not in primary cell).
- **The cascade-receiver precondition from C-bout-framework's joint claim** (per §4.5 5c above): framework-validity operand-fitness partially established (bars 1+2 PASSED at +20.26 pp; bar 3 FAILED at corpus detection limits) — the substantive claim carries the "operand fitness partially established at framework-validity layer" qualifier explicitly.

### 7b. What the cluster jointly does NOT license

Four predictable overclaim shapes per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.7b:

1. **The cluster does NOT license "the mechanism (adrenaline-lingers per Wiggers verbatim) is established."** The operand tests a per-day-count PATTERN of bout-level failure-to-return events, not the underlying autonomic mechanism explanation. The cluster's direction-correct + statistically-real substantive PARTIAL at cross-phase-pooled scale is hypothesis-generating prior for the Wiggers "adrenaline-lingers" mechanism (per literature anchor at map §3 row: Wiggers pacing handleiding lines 1316-1324 adrenaline-lingers mechanism); the mechanism that explains why heavy-T days produce more "stuck stress" bout events at cross-phase-pooled scale but at δ = +0.120 magnitude (rather than at a larger substantive-relevance magnitude) is a separate question requiring more analysis (potentially the intraday HR-pacing trend HA candidate at §4.8 own-research or the operand-refinement v2 path).

2. **The cluster does NOT license "the construct's group-level reach is established."** Per L1 single-subject reach; cluster-level substantive Wiggers C4 reading at bout resolution on this N = 1 corpus is bounded by [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) / [CENT / Shamseer 2015](../../literature/methodology/shamseer_2015_cent_consort_nof1.pdf) / [SCRIBE / Tate 2016](../../literature/methodology/tate_2016_scribe_single_case_reporting.pdf) / [Natesan 2023](../../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf) N-of-1-to-group reach. The cluster speaks about this subject's substantive bout-level heavy-T contrast on this corpus's cross-phase-pooled operationalisation; group-level positioning is downstream of this cluster at Stage S₂ on T-within-day-recovery per the map's §4 topic row, NOT a Stage S₁ surface.

3. **The cluster does NOT license predictive claims.** Per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.10 hard predictive gate; cluster-level coherence does NOT promote any tier. **This cluster feeds K-bout-recovery-signal at Stage A per map §5 row 126** as aspired **tier-2 informative-pattern** (§3.10 PPV-required per map §5 K-bout-recovery-signal cell). **Tier-3 predictive-use is BLOCKED** by locked-plan §3.10 hard predictive gate (Stage A tier-3 requires pre-registered forward-validation HA which does not yet exist for this construct); AND the effect sizes are modest per both Wiggers-side (this cluster's Cliff's δ = +0.120 substantive-scale, below +0.20 discipline threshold; per-bar pattern preserved as claim shape) and HA-side (cascade-source HA11-bout-redo +20.26 pp discrimination at framework-validity layer, below full-comparability at bar 3 p-shortfall). The K-bout-recovery-signal construct's tier-2 aspiration will need to be assessed at Stage A dispatch (downstream of Stage S₂ on T-within-day-recovery, which itself is downstream of this Stage S₁'s user-acceptance lock); the cluster-level Stage S₁ carries no PPV, no base-rate framing, no diagnostic-quality measures, and no "forecasts Y" wording. The cascade-source framing to Stage S₂ (§4.7a above) propagates the cluster's joint claim, NOT any tier-promotion path.

4. **The cluster does NOT license "the substantive PARTIAL averages the framework-validity + substance verdict pair."** The trivial-ORTHOGONAL call preserves the per-bar pattern verbatim per Stage I §3 licensed claim; the joint claim names the substantive per-bar pattern (direction PASS + bar (a) discrimination PASS at p = 0.0001 + bar (b) effect-size FAIL at δ = +0.120) rather than papering it over into a "essentially SUPPORTED at small effect" middle-ground that would collapse the discipline-threshold information. **The two clusters (C-bout-framework framework-validity + C-bout-substance substantive) address different questions in different eras**; the cascade-receiver framing at §4.5 5c preserves the per-cluster shape (each cluster is answering its own question; the cascade-arrow propagation is precondition, NOT averaging). The cascade-propagation discipline (framework-validity PARTIAL as caveat-class precondition per §5.5 hard rule + §7.3 anti-pattern discipline observance at §4.5 5c) is the structural protection against this overclaim shape at this cascade-receiver.

## 4.8 Follow-up suggestions

### Own-research track

- **HA-C4c-v2 with operand-threshold refinement** (per pre-reg §9.2 OPTIONAL path; carry-forward from Stage I §4.8 own-research track). If downstream Stage S₂ / Stage A on K-bout-recovery-signal identifies the below-threshold effect-size as too binding for the substantive claim shape, an operand-refinement v2 would re-tune the parent MD §3.2 thresholds (180-min forward cap + +5 baseline tolerance + ≥ 10-min return-hold) OR promote `bout_recovery_half_life_median_day` as primary OR construct a 3-channel triad. **Cluster-level upgrade path**: if v2 lands SUPPORTED or PARTIAL with bar (b) PASS, this cluster gains a second member; the §4.4 coherence call exits the trivial-ORTHOGONAL degeneracy and the standard four-label mapping applies (two-member case). Pre-reg shape per [`hypothesis_lock_process.md`](../../methodology/hypothesis_lock_process.md) §3.2; would require re-locking parent MD §3.2 return-window thresholds pre-commit. **Consolidated from Stage I §4.8 own-research entry** — this is the cluster's primary upgrade path that would exit single-member degeneracy at substantive layer.

- **Per-channel missingness diagnostic on `bout_n_did_not_return`** (cluster-level inheritance from Stage D + Stage I cascade-context; carries forward as OI-023 in the layer-wide queue; sister to OI-017 for `bout_n_fast_recovery_day`). Not a sister-HA pre-reg; a descriptive infrastructure run that tightens the L7 reach narrative at the cluster level per §4.5 5b L7 row, AND **can be jointly closed with OI-017 as a two-operand run at the same S effort** covering both bout-level sister operands (this cluster's `bout_n_did_not_return_day` + the cascade-source cluster's `bout_n_fast_recovery_day`). S effort per stocktake §3 Shared gap 3 pattern; script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/`. **Cluster-level consequence**: closing OI-023 tightens this cluster's §4.5 5b L7 from "gating-discipline-bounded" to "fully audit-backed" AND jointly with OI-017 tightens the cascade-source cluster's same L7 narrative; does not change the §4.4 coherence call or the §4.7a joint claim at either cluster.

- **Intraday HR-pacing trend pre-reg** (Q30 intraday-recovery trend per parent MD §1.4 A4 enabled-on-request candidate — the HR-channel bout-level analysis analogue; carry-forward from Stage I §4.8 own-research). Would triangulate the "stuck stress" mechanism from a different Garmin channel (HR-derived trend rather than Firstbeat-stress-derived per-bout events); a SUPPORTED-at-HA-C4c-substantive-Wiggers + SUPPORTED-at-HR-channel-trend shape would strengthen the cross-channel triangulation that HA-C4c's substantive PARTIAL alone cannot; a NOT-SUPPORTED-at-HR-channel-trend would surface whether the operand's below-threshold effect-size is Firstbeat-algorithm-specific vs autonomic-state-specific. Routing: `hypothesis_lock_process.md` §3.2 fresh-session drafting; pre-reg would carry explicit cross-cite to HA-C4c and parent MD §1.4 A4 candidate context. **Cluster-level consequence**: would land at a different cluster per constellation implications at map re-lock time — an HR-channel intraday-trend HA would form (or be added to) an HR-channel-analogue cluster, not join C-bout-substance directly (which is `bout_n_did_not_return_day`-scoped per map §3 row constellation).

### External-research track

- **Cross-population framework-validity replication of the bout-level recovery operand on PEM-prone populations at group level** (carry-forward from Stage I §4.8 external-research; consolidated at cluster level). We cannot run this ourselves because **L1 (single-subject reach)** prevents the N = 1 corpus from settling whether the substantive Wiggers C4 finding at bout resolution generalises beyond this subject's specific within-day stress trace. Per Daza 2018 N-of-1 inference reach: the present PARTIAL verdict at δ = +0.120 (CI [+0.064, +0.178]) bounds the substantive claim to one subject's cross-phase-pooled data; cross-subject substantive replication would require a group-level study with the same per-minute Garmin stress instrumentation, the same bout-detection rule (parent MD §3), the same heavy-T contrast operationalisation (`exertion_class_lagged_lcera` analogue), and the same two-bar discipline (discrimination + effect-size ≥ +0.20), evaluated against a group-level heavy-T-vs-non-heavy-T contrast on `bout_n_did_not_return_day`. Minimum n per Daza 2018 + CENT 2015 item 22 generalisability bounds; the study would carry pre-registration of the same two §5 bars with the within-population effect-size discipline threshold potentially adjusted to the group's distribution.

- **Cross-device Firstbeat-algorithm calibration validation study** (carry-forward from Stage I §4.8 external-research; consolidated at cluster level). We cannot run this ourselves because **L3 (device generations)** + the Firstbeat-input amplification caveat (pre-reg §8 caveat 7 operational-vs-mechanistic; inherited from HA11-bout-redo pre-reg §8 caveat 5) means the bout-level features derived from the closed-source Firstbeat algorithm may not transfer cleanly across device generations even if the column name `all_day_stress_avg` / per-minute stress trace remains semantically stable. The 99.3% motion-confound corpus property (pre-reg §8 caveat 4) is itself a device × extraction-threshold interaction the study would clarify; a cross-device design (paired-subject within-device-generation transition windows per limitations doc L3 caveat calibration discipline) would test whether `bout_n_did_not_return_day` produces statistically comparable distributions across device generations on matched within-subject windows AND whether the motion-confound rate is device-stable or device-specific. This would clarify whether the substantive Wiggers C4 finding at bout resolution is device-stable or device-specific.

Each entry is one paragraph with the proposed study, the L-ID that prevents us from running it ourselves, and what the study would contribute to the cluster's joint claim. Both external-research entries respect [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.11 N = 1-limit scoping discipline (the L-ID-explicit-naming requirement per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.8 + locked-plan §3.11 binding).

**Distinct from `open_inputs`** per locked-plan §3.5 vs §3.11: the §4.9 `open_inputs` block below names what is missing to complete *this current cluster synthesis* (the OI-021 + OI-022 + OI-023 + OI-024 user-acceptance + descriptive-diagnostic slots). The §4.8 follow-up suggestions above name what *next claims* could be built — for us or for others. Both are required; neither substitutes for the other.

## 4.9 `open_inputs` block

Four slots surfaced (two carry-forward from Stage D + Stage I dispatch-mode pending events; one cascade-context inherited from Stage D + Stage I; one new this artefact for cluster-level user-acceptance):

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | ~~**OI-021**: Stage D user acceptance of TRUSTED verdict-trust call~~ **CLOSED-BY-USER-LOCK 2026-07-08** (Q B1 at Phase C cluster-level joint lock session: user accepted TRUSTED as-is, no reaudit). Stage D `descriptive_audit.md` LOCKED r1 2026-07-08. | ~~Stage D lock~~ resolved. | Interview pass fired at joint session. | N/A. |
| 2 | ~~**OI-022**: Stage I §4.6 user articulation of lived-experience prior on heavy-T stuck-stress pattern~~ **CLOSED-BY-USER-LOCK 2026-07-08** (Q B2 at joint session: user articulated verbatim — first-time directional agreement in L4-inflation-4× series with substantive scope reframe from "heavy exertion" to "out of my energy envelope" + temporal texture "especially near the end of the day" + language nuance dropping "SHOULD"). Stage I `interpretation/HA-C4c.md` LOCKED r1 2026-07-08 with §4.6 verbatim articulation + §3 stringent-operand-threshold nuance amendment + §4.8 own-research two-step stringency-sensitivity + personal-baseline sister-operand candidate entry surfaced by user request. | ~~Stage I lock~~ resolved. | Interview question fired at joint session; user response verbatim in Stage I §4.6. | N/A. |
| 3 | **OI-023 (REMAINS OPEN as non-blocking cascade-context)**: Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` (stocktake §3 Shared gap 3 pattern; NOT load-bearing for HA-C4c's TRUSTED call per Stage D §4.5 item 2 + §4.9 entry 2; sister to OI-017 for `bout_n_fast_recovery_day` on same operand class). Status: **OPEN 2026-07-08**, propagated to layer-wide `methodology/_open_inputs.md` queue for closure via sister-operand-consolidation with OI-017 at S-effort. Non-blocking for Phase C cluster-level lock (which fired 2026-07-08). | Tightens §4.5 5b L7 citation from "gating-discipline-bounded" to "fully audit-backed"; jointly with OI-017 covers both bout-level sister operands in one S-effort run; not blocking either upstream Stage D + Stage I + THIS Stage S₁ lock. | S effort per stocktake §3 Shared gap 3; jointly with OI-017 becomes two-operand run at same S effort; script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/`; independent of Phase C cluster-level lock timing. | TRUSTED (Stage D LOCKED r1 as-is) stands with L7 narrowing caveat throughout the artefact; cluster-level joint claim per §4.7a stands with L7 narrowing caveat regardless of OI-023 closure timing. |
| 4 | ~~**OI-024**: Cluster-level Stage S₁ user acceptance of dispatch-mode DRAFT-as-LOCKED~~ **CLOSED-BY-USER-LOCK 2026-07-08** (Q A + Q B3 at Phase C cluster-level joint lock session: Q A "yes accept all three" verbatim; Q B3 accepted trivial-ORTHOGONAL coherence call + first-ever cascade-receiver §4.5 5c LOAD-BEARING sub-paragraph as-drafted after more-detail briefing walked at lock — cascade-source vs cascade-receiver asymmetry + §5.5 caveat-class-not-co-equal-evidence rule explained). This artefact LOCKED r1 2026-07-08. | ~~Cluster-level lock~~ resolved. **Phase C cluster-level layer CLOSED 2026-07-08 with all three OI-021 + OI-022 + OI-024 resolved in one joint session per Phase-A + Phase-B precedent.** **Stage S₂ dispatch on T-within-day-recovery is now UNBLOCKED** per `internal_synthesis.md` §9.2 cluster→topic dependency + `_rollout_order_2026-06-25.md` §2 Phase C exit criterion. | Interview passes fired at joint session; user answered all three joint-lock gates in one pass. | N/A. |

OI-024 is a new entry; it propagates to worktree-local `methodology/_open_inputs.md` at the bottom of the queue per skill responsibility #6 (deduplicated against existing entries — checked against OI-001-023; no overlap on (missing-input × blocked-target) key since OI-024's missing-input is THIS cluster-level Stage S₁ user-acceptance + blocks Phase C cluster-level layer closure + Stage S₂ dispatch on T-within-day-recovery). The methodology agent absorbs the worktree-local queue edits in their next commit cycle per the established Stage D + Stage I + Stage S₁ (Phase B) cherry-pick discipline at this Phase C (per [[feedback_check_index_before_add]] producer/reviewer split convention at the methodology agent boundary). **Artefact-internal OI-021 / OI-022 / OI-023 / OI-024 numbering note**: main-branch `_open_inputs.md` has OIs through 019 as of 2026-07-04; OI-020 through OI-024 are artefact-internal references from earlier this session's Phase C dispatch chain (OI-020 was Phase B closure record; OI-021 Stage D user-acceptance; OI-022 Stage I §4.6 user-articulation; OI-023 Stage D + Stage I `bout_n_did_not_return` missingness-diagnostic sister to OI-017; OI-024 this artefact user-acceptance). These may be renumbered at joint lock time per Phase-B naming-collision-note precedent at [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) §11 lock-log 2026-07-07 row.

**Carry-forward observations on cascade-receiver framing** (NOT a new `open_inputs` entry; recorded here for traceability of the cluster's cascade-receiver consumption completing Phase C cluster-level layer and enabling Stage S₂ dispatch):

- **Phase C cluster-level layer closure**: Phase C's Stage D + Stage I + Stage S₁ chain for HA-C4c on cluster C-bout-substance closes on this Stage S₁ artefact's user-acceptance lock (jointly with OI-021 + OI-022 + OI-024). The dispatch-mode DRAFT-on-DRAFT state at THIS artefact is the binding constraint on Phase C cluster-level closure ordering; resolving OI-024 (joint with OI-021 + OI-022) closes Phase C cluster-level layer AND unblocks Stage S₂ dispatch on T-within-day-recovery.
- **Stage S₂ downstream consumption obligation**: the downstream Stage S₂ on T-within-day-recovery MUST consume this Stage S₁ output's §4.7a joint claim as its primary constituent-cluster input per map §4 topic row ("C-bout-substance (primary); C-bout-framework as upstream calibration precondition"). Stage S₂'s cascade-source consumption reads BOTH clusters — this cluster as primary constituent + C-bout-framework as upstream calibration precondition; the topic-level artefact carries both cluster verdicts explicitly per map §4 row constellation.
- **§7.3 anti-pattern verified at THIS cluster's §4.5 5c**: the cascade-source C-bout-framework verdict is treated as caveat-class precondition per §5.5 hard rule, NOT as co-equal verdict entering §4.4 (which is structurally-determined trivial-ORTHOGONAL per §5.6 single-member). This IS the LOAD-BEARING discipline check for cascade-receiver clusters and it is honored throughout the artefact — §4.3 row table carries ONLY HA-C4c's row per §4.3 note; §4.4 coherence call carries ONLY HA-C4c's verdict per structural single-member determination; §4.5 5c carries the cascade-source verdict as caveat-class precondition per §5.5 hard rule; §4.6 preserves the cascade-tension in 5c NOT in 4.6 per §5.5 hard rule; §4.7a joint claim inherits the cascade-source calibration discount as bounding qualifier per §5.5 propagation. Recorded here as structural affirmation of the primary discipline check for this first-ever cascade-receiver Stage S₁ artefact.

## 4.10 Cross-references

- Member HA's interpretation (cluster's sole upstream-Stage-I input):
  - [`HA-C4c.md`](../interpretation/HA-C4c.md) — HA-C4c Stage I, DRAFT r1 dispatched-by-orchestrator on user authorisation 2026-07-07 (DRAFT-ON-DRAFT-DRY-RUN dispatch mode; user-acceptance lock event PENDING at OI-021 + OI-022).
- Member HA's Stage D descriptive audit (upstream-Stage-D input, transitively inherited via Stage I):
  - [`descriptive/HA-C4c/descriptive_audit.md`](../descriptive/HA-C4c/descriptive_audit.md) — HA-C4c Stage D, DRAFT r1 dispatched-by-orchestrator 2026-07-07 (user-acceptance lock event PENDING at OI-021).
- Member HA's pre-reg + result:
  - [`hypotheses/HA-C4c/hypothesis.md`](../hypotheses/HA-C4c/hypothesis.md) r2 LOCKED `4e666a2`; substantive absorb `310e145`; fresh-session re-audit clean at [`reviews/HA-C4c-2026-06-23-v2-reaudit.md`](../../reviews/HA-C4c-2026-06-23-v2-reaudit.md).
  - [`hypotheses/HA-C4c/result.md`](../hypotheses/HA-C4c/result.md) 2026-06-23 (test execution commit `a69a8ed`; worktree HEAD at run `a59997a`; pipeline commit `d5b394c`).
  - [`hypotheses/HA-C4c/test.py`](../hypotheses/HA-C4c/test.py).
- Synthesis-structure map cluster row (verbatim source for §4.2 pre-declared constellation): [C-bout-substance](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) (declared 2026-06-23, lock r3 2026-06-24); single-member cluster (HA-C4c only); **cascade-arrow upstream from C-bout-framework** (this cluster is the cascade-receiver per map §3).
- **Cascade-source cluster** (upstream S₁ precondition consumed at §4.5 5c above verbatim): [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) — LOCKED r1 2026-07-07 (single-member cluster HA11-bout-redo; trivial-ORTHOGONAL coherence call per guide §5.6; joint claim of "operand `bout_n_fast_recovery_day` partially fit-for-purpose" — bars 1+2 PASSED at +20.26 pp / bar 3 FAILED at p = 0.2609 at n_calm = 70 / n_crash = 11 framework-validity layer); Phase B closure record. **This cluster's §4.5 5c quotes the framework cluster's §4.7a cascade-source-designation block verbatim** per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.5 5c paragraph template. The cascade-arrow chain is one-way: framework cluster (source) → this cluster (receiver).
- **Downstream Stage S₂ pointer** (forward pointer; per §4.7a cascade-source framing for downstream S₂): [`contextualisation/topic-within-day-recovery.md`](../contextualisation/topic-within-day-recovery.md) (NOT YET DRAFTED — future wave; dispatchable on THIS Stage S₁ user-acceptance lock per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §9.2 cluster→topic dependency + map §4 topic row constellation). When Stage S₂ drafts, it consumes THIS cluster's §4.7a joint claim as its primary constituent-cluster verdict + the cascade-source cluster's §4.7a joint claim as upstream calibration precondition per map §4 row cell.
- **Downstream Stage A pointer** (forward pointer; NOT crossed at Stage S₁ per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §1 NOT-list): [`actionability/construct-bout-recovery-signal.md`](../actionability/construct-bout-recovery-signal.md) (NOT YET DRAFTED — future wave downstream of Stage S₂ on T-within-day-recovery per map §5 row 126 construct table; aspired **tier-2 informative-pattern** per §3.10 PPV-required; tier-3 predictive-use blocked by §3.10 hard predictive gate). This cluster does NOT cross §3.10 hard predictive gate per guide §1 NOT-list; the tier-2 aspiration will be assessed at Stage A dispatch time downstream of Stage S₂.
- Map [T-within-day-recovery](../../methodology/synthesis_structure_map.md#4-topic-table-s%E2%82%82-pre-registration) §4 topic row this cluster feeds (declared 2026-06-23, r3 lock; "C-bout-substance (primary); C-bout-framework as upstream calibration precondition"; Marques 2023 LC cardiovascular autonomic dysfunction + Mooren 2023 postcovid HRV + Ryabkova 2024 ME/CFS dysautonomia + van Campen 2022 ME/CFS RHR elevated + broader within-day-recovery literature as external contextualisation for Stage S₂). Forward pointer for Stage S₂ dispatch (future wave).
- Map [K-bout-recovery-signal](../../methodology/synthesis_structure_map.md#5-construct-table-a-pre-registration) §5 construct row (declared 2026-06-23, r3 lock; aspired tier-2 informative-pattern per §3.10 PPV-required; §3.12 commentary-eligible at Stage A/T if tier-2 verified). Forward pointer for Stage A (future wave downstream of Stage S₂).
- **Stage T skip-record convention** (per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §4.10 + skill r2 routing-table Stage T skip-record location convention): **no skip-record fires at this draft** (Stage T for this cluster is not in-scope at Phase C per [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) §2 Phase C which restricts to D + I + S₁ at cluster level; Stage T is separately-waved downstream of Stage A on K-bout-recovery-signal). The skip-record convention is recorded here for future Stage T routing when the construct-level Stage A dispatches; if the user elects at that time to skip the Stage T research-audience track for this cluster's downstream construct, the skip record lands at the K-bout-recovery-signal Stage A artefact, NOT at this Stage S₁ artefact.
- Phase-B precedent (LOAD-BEARING for §4.4 trivial-ORTHOGONAL discipline + §4.7a single-member width + cascade-receiver asymmetry): [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) (LOCKED r1 by user acceptance 2026-07-07; 1-member cluster; cascade-source; DIRECT ANALOGUE for the single-member-cluster structural discipline; contrast case for THIS cluster's cascade-receiver §4.5 5c LOAD-BEARING sub-paragraph — see §4.1 "Asymmetry vs Phase-A + Phase-B precedents" paragraph).
- Phase-A precedent (contrast case; different structure): [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) (LOCKED r1 by user acceptance 2026-06-25; 2-member cluster; stand-alone (no cascade-arrow); PARTIALLY-CONCORDANT-vs-CONCORDANT judgment-call axis; contrast case for single-member cluster structural asymmetry — different section-4 shape entirely).
- Cluster-specific methodology MDs:
  - [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) — parent MD LOCKED r2 2026-06-19 `c57ff3f`; §3 bout-detection rule; §3.2 `did_not_return_flag` semantics + 180-min forward cap + +5 baseline tolerance + ≥ 10-min return-hold thresholds; §3.4 day-validity gate + motion-confound handling; §5.3 Approach A cross-phase headline discipline (substantive-layer permission for cross-phase pooling conditional on 0/7 CONFIRMED at sub-MD §6); §5.5 framework-validity-to-substance-cascade discipline (this cluster is the substance side of the cascade); §6 framework-validity discipline (the framework cluster operationalises; this cluster reads as caveat-class precondition per §5.5).
  - [`bout_level_dose_response_calibration.md`](../../methodology/bout_level_dose_response_calibration.md) — sub-MD LOCKED r4 2026-06-23 `fb97d1c`; §6 architectural-implications paragraph (0/7 features CONFIRMED → Approach A NOT load-bearing → cross-phase pooling permitted; the empirical warrant for THIS cluster's cross-phase-pooled primary operationalisation); §7 caveat 5 underpowered-NULL framing (inheritance to Approach A sensitivity arm at HA-C4c §4.9).
  - [`citalopram_phase_stratification.md`](../../methodology/citalopram_phase_stratification.md) — §3 the five-phase `citalopram_phase` axis (`unmedicated / buildup / consolidation / afbouw / post_afbouw`); April 2024 cluster exclusion; §4 CONFIRMED-channel inheritance discipline (NOT invoked at HA-C4c primary because 0/7 CONFIRMED per sub-MD §6).
- Limitations doc cross-refs for cited L-IDs: [`research_line_limitations.md`](../../methodology/research_line_limitations.md) §3 L1, L2, L3, L4, L7; §5 citation requirements row for `cluster-*.md`. L5 NA + L6 NA per cluster row + Stage D audit A5 + A6 + Stage I §4.5 5b L5 + L6 NA carry-forward.
- Non-binding seed notes: [`_synthesis_seed_notes_2026-06-23.md`](../../methodology/_synthesis_seed_notes_2026-06-23.md) §3 (candidate joint reading C-bout-substance with C-bout-framework cascade; advisory-only / caveat-class per the seed-notes' own §5 self-description). Per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §7.6 anti-pattern (treating seed-notes sketches as constraints on the coherence call), this Stage S₁ does NOT inherit the seed-notes §3 reading as a constraint on the §4.4 trivial-ORTHOGONAL coherence call (the trivial-ORTHOGONAL is determined by structural single-member-cluster status per guide §5.6, not by seed-notes framing). The seed notes' main contribution surfaced here is the explicit cascade-receiver framing the §4.5 5c load-bearing sub-paragraph operationalises per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.5 — but that operationalisation cites guide §4.5 5c + §5.5, NOT the seed notes.
- Literature methodology anchors:
  - [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) — primary anchor for Stage S₁'s multi-test synthesis discipline; degenerate single-member case at this cluster (no multi-test synthesis to do); cited at §4.5 5b L1 + §4.7a + §4.8 external-research scoping.
  - [Shamseer / CENT 2015](../../literature/methodology/shamseer_2015_cent_consort_nof1.pdf) — items 21+22 cluster-level limitations and generalisability; cited at §4.8 external-research generalisability bounds.
  - [Tate / SCRIBE 2016](../../literature/methodology/tate_2016_scribe_single_case_reporting.pdf) — L4 participant-as-researcher transparency at cluster level (cited at §4.5 5b L4).
  - [Natesan 2023](../../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf) — bar for defensible cluster-level coherence-call when within-subject multi-test evidence is the substrate; degenerate single-member case at this cluster.
- Wiggers project literature anchor (verbatim source for the substantive claim's mechanistic prior — cited at §4.5 5a substantive framing + §4.7b bullet 1): [Wiggers pacing handleiding](../../literature/wiggers_pacing_handleiding.pdf) lines 1140-1141, 1223-1231 ("Walls of Stress", "Stuck in Stress") + adrenaline-lingers mechanism lines 1316-1324 (per map §3 row literature-anchor cell).
- HA11 v1 reference (project-internal, transitively inherited via cascade-source): [`hypotheses/HA11-stress-udip/result.md`](../hypotheses/HA11-stress-udip/result.md) — SUPPORTED-on-train +22.8 pp `u_dip_count` discrimination; the calm-day sister channel + reference signal the cascade-source cluster's framework-validity gate tested reproduction against (opposite pole of the same C4 mechanism per HA-C4c cross-test cross-reference: U-dip is what fast-recovery looks like, `did_not_return` is what failure-to-recover looks like).
- Cross-test cross-references (per HA-C4c pre-reg §4.10 sister-test companion + `result.md` §7 sister-test table): [HA-C4 v2](../hypotheses/HA-C4/) REJECTED (daily-aggregate triad sum 0.0/3.0; sister to HA-C4c at coarser resolution — the two verdicts coexist per pre-reg §8 caveat 9; resolution-of-scale story is Stage S₂ / cross-test-interpretation-pass territory NOT Stage S₁); [HA-C4b v3](../hypotheses/HA-C4b/) NOT-SUPPORTED (motion-filter crash-precursor per-episode; sister test on a different question); [HA11 v1](../hypotheses/HA11-stress-udip/) SUPPORTED-on-train (calm-day sister channel; opposite pole of the same C4 mechanism); [HA11-bout-redo](../hypotheses/HA11-bout-redo/) PARTIAL (framework-validity cascade-source verdict; INVERSE failure-mode from HA-C4c per §4.5 5a distinct-failure-mode-finding across the two clusters).
- [CONVENTIONS](../../CONVENTIONS.md) §3.4 crash-drop sensitivity at cluster level (per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.7 operationalised at §4.5 5d above; CLEAN reading inherited from member's `result.md` §5; no cluster-level coherence-call shift); §3.6 named counts (n_heavy = 465, n_non_heavy = 809 pinning per pre-reg §4.11 effective-n discipline); §4.1 no interpretive marks on raw/descriptive layers (§4.3 respects); §4.2 caveats vs a-priori (§4.5 respects); §4.3 prior-driven confirmatory framing (§4.5 5b L4 acknowledges); §4.4 no cross-test pass conclusion (cross-test reads at §4.10 cross-references are pointers only; no cross-test pass conclusion at this Stage S₁).
- [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3 stage map (this Stage S₁ implements); §3.5 missing-inputs flagging (this artefact's §4.9 implements); §3.6 conflict-resolution rule (no map-revision-needed halt fired at this draft per §6.1 walk in §9.6 below); §3.7 drift policy (drift triggers registered in §11 lock log); §3.8 stopping/completion criteria (user-acceptance is the binding completion event; deferred pending OI-021 + OI-022 + OI-024 joint resolution); §3.10 hard predictive gate (Stage S₁ does NOT cross; this cluster feeds K-bout-recovery-signal at Stage A via T-within-day-recovery at Stage S₂ — both downstream waves); §3.11 follow-up suggestions (§4.8 above implements); §3.12 commentary boundary (Stage S₁ does NOT carry per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §1 + §7.8; commentary reserved for Stage A K-bout-recovery-signal if it reaches tier-1/tier-2); §4 producer/reviewer split table (`cluster-*.md` is reviewer-mode-with-authorization per the routing table).
- Rollout context: [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) §2 Phase C (single-member cluster C-bout-substance; cascade-receiver from Phase B C-bout-framework; D → I → S₁ only at cluster-level scope — downstream S₂ / A / T are separate waves not in Phase C's cluster-level scope). **This Stage S₁ artefact's user-acceptance lock-event closes Phase C cluster-level layer AND unblocks Stage S₂ dispatch on T-within-day-recovery.**

---

## §9.6 refuse-to-lock gate walk

Per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §9.6 walk (item-by-item):

1. **The §4.5 L-ID block is missing an L-ID the map's §3 cluster row lists**: NO — every L-ID from the C-bout-substance cluster row (L1, L2, L3, L4, L7; L5 NA; L6 NA) is cited at §4.5 5b with one-sentence project-specific cluster-level application. L5 NA is explicitly cited with one-sentence reason (no v24 primary signals; same as Stage D audit A5 + Stage I §4.5 5b L5 NA carry-forward). L6 NA is explicitly cited with one-sentence reason (gevoelscore not in primary cell; same as Stage D audit A6 + Stage I §4.5 5b L6 NA carry-forward + map §3 cluster row L-IDs column annotation).

2. **The cluster spans different era strata (per the limitations-doc §5 binding's "also cite L2" clause) and the L2-era-strata-mismatch citation is missing from §4.5 5b**: NOT APPLICABLE — the cluster has only one member (HA-C4c) so the "if cluster **members** are from different era strata" clause does not trigger per the limitations-doc r3 §5 plural-members reading + the guide §3 worked-example paragraph for `cluster-bout-substance.md` explicit application ("no extra L2-era-strata-mismatch cluster-level citation is triggered ... the cross-phase pooling is HA-C4c's own L2-era-confound surface, not a cluster-level L2 extension"). Within-member cross-phase pooling IS the L2 era-scope surface, cited at §4.5 5b L2 row directly per the guide's explicit clause interpretation.

3. **The cascade-arrow precondition sub-paragraph (§4.5 5c) is missing on a cascade-downstream cluster**: NO — this cluster is the **cascade-receiver** (downstream), and §4.5 5c IS the LOAD-BEARING sub-paragraph containing (a) the verbatim quote of C-bout-framework's §4.7a cascade-source-designation block, (b) explicit caveat-class semantics per §5.5 hard rule, (c) caveat-class propagation naming HA11-bout-redo's bar 3 as the failing bar, (d) era-scope mismatch note per map §3 row verbatim, and (e) §7.3 anti-pattern discipline observance paragraph. **§9.6 item passes with LOAD-BEARING content present**; this is the primary structural discipline check for this cascade-receiver cluster and is honored throughout the artefact.

4. **§4.4 carries an invented label outside the four-label set (CONCORDANT / PARTIALLY CONCORDANT / CONFLICT / ORTHOGONAL)**: NO — §4.4 carries `trivial-ORTHOGONAL` which is the ORTHOGONAL label from the four-label set + the `trivial-` modifier per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §5.6 single-member degenerate case prescription (the `trivial-` qualifier is the guide-prescribed degenerate modifier, not a fifth label invention). §4.4 includes explicit dispatch-mode note confirming the guide §5.6 prescription is used over the generic dispatch-task fallback placeholder.

5. **§4.6 reads CONFLICT or PARTIALLY CONCORDANT but the open-conflict-preservation discipline is not honored (no both-readings paragraph, or auto-resolution-style language)**: NOT APPLICABLE per §4.4 trivial-ORTHOGONAL — no inter-HA agreement-disagreement exists at single-member cluster; §4.6 reads "Cluster members address different aspects of the shared construct; no conflict at the cluster level because the members do not speak to each other directly" per guide §4.6 ORTHOGONAL rendering. **Cascade-tension between framework-validity + substantive PARTIAL verdicts LIVES in §4.5 5c as caveat-class precondition per §5.5 hard rule, NOT in §4.6 as open conflict** — the LOAD-BEARING discipline check for cascade-receiver clusters observed. Member-level lived-experience-prior placeholder OI-022 acknowledged but NOT re-litigated at cluster level per guide §6.4 (cluster does NOT carry §4.6-equivalent).

6. **§4.7b does not address the four predictable overclaim shapes explicitly (the four-bullet list is mandatory; presence of all four sentences is the structural check)**: NO — the four-bullet list is present at §4.7b (mechanism-not-established with Wiggers adrenaline-lingers naming; group-level-reach-not-established with N-of-1 standards naming; predictive-claims-not-licensed with explicit tier-2 aspiration to K-bout-recovery-signal at Stage A + tier-3 blocked framing + Wiggers-side + HA-side modest-effect-size acknowledgment; joint-claim-does-not-average-the-framework-validity-substance-verdict-pair with the cascade-receiver framing preserves-per-cluster-shape structural-protection). All four overclaim shapes addressed with cluster-specific framing.

7. **§4.8 carries an external-research suggestion without explicit N=1-limit scoping (locked-plan §3.11 binding)**: NO — both external-research entries explicitly name the L-ID that prevents us from running them ourselves (cross-population substantive replication cites L1; cross-device Firstbeat-calibration study cites L3 + the Firstbeat-input amplification caveat from pre-reg §8 caveat 7). Both respect locked-plan §3.11 binding.

8. **§4.7a joint claim width exceeds the narrowest member's licensed claim without independent-operationalisation cross-operationalisation replication warrant per §4.7a width discipline**: NO — joint claim width does not exceed (it collapses to) the single member's licensed claim per guide §5.6 single-member case. No cross-operationalisation convergence available at single-member cluster; no width expansion attempted. The cascade-receiver precondition consumption in §4.5 5c is a bounding-caveat addition to the joint claim, NOT a width expansion per guide §5.5 (cascade propagation is precondition framing, not co-equal verdict). The §4.7a "no width expansion" clause is honored explicitly.

9. **§4.3 / §4.4 / §4.5 / §4.6 / §4.7 contain anti-pattern violations per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §7**: NO — walking each:
   - §7.1 (re-routing HAs to different clusters in-stage): NO; constellation is read verbatim from map §3 per §4.2 above; no proposed re-routing.
   - §7.2 (collapsing CONFLICT to a "middle" reading): NOT APPLICABLE per §4.4 trivial-ORTHOGONAL (no CONFLICT call); §4.7b bullet 4 explicitly addresses the analogous "averaging the framework-validity + substance verdict pair" shape and refuses it.
   - §7.3 (treating cascade-arrow caveat-class precondition as co-equal verdict): **LOAD-BEARING at THIS cluster; observed** — §4.5 5c explicitly declares the C-bout-framework verdict as caveat-class precondition per §5.5 hard rule; §4.3 row table carries ONLY HA-C4c's row per §4.3 note verbatim; §4.4 coherence call is structurally-determined trivial-ORTHOGONAL per §5.6 single-member (NOT third-vote-inclusive); §4.6 cascade-tension lives in 5c NOT in 4.6 per §5.5 hard rule; §4.7a joint claim inherits cascade-source calibration discount as bounding qualifier per §5.5 propagation (not as verdict-averaging). This is the primary discipline check for cascade-receiver clusters and is observed throughout the artefact.
   - §7.4 (smuggling sister-cluster context into the current cluster's reading): NO; C-bout-framework's cascade-source verdict enters exclusively at §4.5 5c per §5.5 rule + at §4.7a as bounding qualifier per §5.5 propagation; no other sister-cluster evidence imported.
   - §7.5 (promoting one cluster's reading using another cluster's evidence): NO; the cascade-source cluster's verdict does NOT strengthen this cluster's §4.4 trivial-ORTHOGONAL call (which is structurally-determined by §5.6 single-member) nor its §4.7a joint claim width (which collapses to single-member per §5.6); the cascade-source consumption at §4.5 5c is a bounding-caveat operation per §5.5, not a strengthening operation.
   - §7.6 (treating seed-notes sketches as constraints on the coherence call): NO; §4.10 cross-references explicitly cites seed notes as advisory-only per [`internal_synthesis.md`](../../methodology/internal_synthesis.md) §7.6; trivial-ORTHOGONAL coherence call determined by guide §5.6 single-member case, NOT by seed-notes framing; cascade-receiver §4.5 5c LOAD-BEARING sub-paragraph operationalisation cites guide §4.5 5c + §5.5 explicitly, NOT the seed notes.
   - §7.7 (inventing new caveats post-hoc): NO; all §4.5 5a caveats are carry-forward from Stage I §4.5 5a (which is itself carry-forward from pre-reg §8 + Stage D §3 + §4.5); §4.5 5b L-IDs are inherited from map §3 row + member's Stage I §4.5 5b; §4.5 5c cascade-precondition is inherited verbatim from cascade-source cluster §4.7a per §4.5 5c template; no new cluster-level caveats invented.
   - §7.8 (producing §3.12 subject-narrative commentary at Stage S₁): NO; no "I notice", "in my experience" subject-narrative wording anywhere in this artefact; §4.6 inherits member-level OI-022 placeholder as caveat-class context per guide §6.4 (cluster does NOT carry §4.6-equivalent or §3.12 commentary); commentary reserved for Stage A K-bout-recovery-signal if it reaches tier-1/tier-2.
   - §7.9 (computing or citing predictive-quality measures): NO; no PPV, no base rate, no sensitivity/specificity; the cluster feeds K-bout-recovery-signal at Stage A (map §5) via T-within-day-recovery at Stage S₂ (map §4) — both downstream waves; §4.7b bullet 3 explicitly addresses the predictive-claim refusal.
   - §7.10 (inventing new coherence-call labels): NO; the `trivial-ORTHOGONAL` label is the guide-prescribed §5.6 degenerate modifier of the ORTHOGONAL label from the four-label set, not a fifth label invention (per §4.4 explicit hard-rule observance paragraph).
   - §7.11 (auto-resolving open conflicts in subsequent re-examinations): NOT APPLICABLE at first-draft; no prior cluster `cluster-*.md` exists for this cluster to re-examine.

**Items that would fire on production-lock attempt**:

- **THREE substantive items** would fire on production-lock (LOCKED transition):
  1. **OI-021 (Stage D user-acceptance pending; via §4.9 entry 1)** — Stage D `descriptive_audit.md` must lock first (currently DRAFT r1; user-acceptance lock event pending); the DRAFT-on-DRAFT dispatch-mode placeholder cannot promote to LOCKED at this Stage S₁ until upstream Stage D locks. This is the DRAFT-on-DRAFT dispatch-mode constraint per skill r4 §"DRAFT-on-DRAFT dry-run discipline" transitively inherited via Stage I (which is itself DRAFT-on-DRAFT-DRY-RUN dispatched 2026-07-07).
  2. **OI-022 (Stage I §4.6 user-articulation pending; via §4.9 entry 2)** — Stage I `interpretation.md` §4.6 DEFAULTED-PENDING-USER-INPUT placeholder per LOCKED-r4 skill §"Dispatch-mode placeholders" must transition to USER-ARTICULATED before Stage I production-lock; this Stage S₁ inherits the placeholder per §4.5 5b L4 + guide §6.4 (cluster does NOT carry §4.6-equivalent), so its lock-event is gated on Stage I locking.
  3. **OI-024 (THIS artefact's user-acceptance pending; via §4.9 entry 4 — NEW THIS ARTEFACT)** — this Stage S₁ artefact's structurally-complete dispatch-mode draft requires user explicit acceptance per locked-plan §3.8 binding completion event; OI-024 IS this entry. Per the Phase-A + Phase-B precedent (HA-C3.md §4.6 + HA-C3p.md §4.6 + cluster-stress-fatigue-shape.md §4.4 + HA11-bout-redo.md §4.6 + cluster-C-bout-framework.md §4.4 joint user-acceptance lock pattern), one user-articulation session resolves all three gates (OI-021 + OI-022 + OI-024) jointly at the user's separate non-dry-run lock session.

- **ZERO substantive §9.6 fires** on the current draft content: all 9 §9.6 substantive checks (L-ID completeness; era-strata-mismatch L2 binding; cascade-receiver 5c LOAD-BEARING rule; four-label-set + degenerate modifier; CONFLICT/PARTIALLY-CONCORDANT open-conflict-preservation NOT-APPLICABLE per trivial-ORTHOGONAL + cascade-tension-lives-in-5c-not-4.6; §4.7b four-overclaim-refusal completeness; §4.8 N=1-scoping; §4.7a width discipline single-member collapse with cascade-caveat bounding NOT width-expansion; §7 anti-pattern walk all 11 items with §7.3 LOAD-BEARING at this cluster observed) pass cleanly on the dispatch-mode draft. The three items above are dispatch-mode + user-acceptance gates, NOT substantive content fires.

The artefact is structurally complete on every §9.6 substantive check; the lock event itself awaits OI-021 + OI-022 + OI-024 joint resolution in the user's separate non-dry-run lock session per Phase-A + Phase-B precedent (one session resolves all three jointly; closes Phase C cluster-level layer AND unblocks Stage S₂ dispatch on T-within-day-recovery).

## §11 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-07-07 | Drafted r1 (DISPATCHED-BY-ORCHESTRATOR ON USER AUTHORISATION; DRAFT-ON-DRAFT-DRY-RUN dispatch mode) | Reviewer-mode-with-authorization by `/research-interpret synthesise C-bout-substance` skill (skill LOCKED r4). Phase C of `_rollout_order_2026-06-25.md`. **First-ever cascade-receiver Stage S₁ artefact in the corpus** — LOAD-BEARING §4.5 5c cascade-precondition sub-paragraph operationalises `internal_synthesis.md` §4.5 5c rule + §5.5 cascade-handling rule for first time. §4.4 coherence call **trivial-ORTHOGONAL** per §5.6 single-member cluster. §4.7a joint claim collapses to single member's Stage I §3 licensed claim. Cascade-source [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) (LOCKED r1 2026-07-07) consumed as caveat-class precondition at §4.5 5c. **Distinct-failure-mode finding across cascade pair preserved** at §4.5 5c: framework was magnitude PASS + p FAIL at n=70/11; substance is p PASS + magnitude FAIL at n=465/809. |
| 2026-07-08 | **LOCKED r1** by user acceptance at Phase C cluster-level joint lock session | Q A "yes accept all three" + Q B3 accepted trivial-ORTHOGONAL coherence call + first-ever cascade-receiver §4.5 5c LOAD-BEARING sub-paragraph as-drafted after more-detail briefing walked at lock (cascade-source vs cascade-receiver asymmetry explained; §5.5 caveat-class-not-co-equal-evidence rule explained; concrete phrasing for Stage T translation demonstrated). OI-021 + OI-022 + OI-024 CLOSED in one joint session per Phase-A + Phase-B precedent; OI-023 REMAINS OPEN as non-blocking cascade-context (propagated to layer-wide `methodology/_open_inputs.md`). **Phase C cluster-level layer CLOSED 2026-07-08.** **Stage S₂ dispatch on T-within-day-recovery is now UNBLOCKED** per `internal_synthesis.md` §9.2 cluster→topic dependency + `_rollout_order_2026-06-25.md` §2 Phase C exit criterion. Fresh-session `/research-review` per plan §4 row for `cluster-*.md` NOT dispatched pre-lock — user-authorized deviation matching Phase-B precedent (rationale in Status header). Retained as retrospective drift-trigger per plan §3.7. **Substantive lock-session texture worth carrying forward**: user surfaced stringent-operand-threshold interpretation nuance at Stage I §3 (embedded verbatim in Stage I §3 amendment) AND requested two-step bout-return-stringency descriptive companion + personal-baseline sister operand candidate follow-up (added to Stage I §4.8 own-research with full context so Step 2 decisions are not lost across sessions; Step 1 Pass 1 distributional-only + Pass 2 δ-vs-stringency descriptive; Step 2 conditional 2a HA-C4c-v2 refined-absolute-threshold / 2b HA-C4c-personal-baseline sister-pre-reg parallel to HA-C3/HA-C3p / 2c both, decisions pre-committed to precede any peek at Pass 2 δ curve). §4.6 user articulation via Stage I §4.6 first-time directional agreement in L4-inflation-4× series with substantive scope reframe (envelope-crossing not training-load-heavy) + temporal texture (end-of-day) + language nuance (drops SHOULD) inherited via §4.5 5b L4 caveat-class context per guide §6.4 (cluster does NOT carry §4.6-equivalent). **Drift triggers registered** (updated): (1) HA-C4c `interpretation.md` re-examined or downgraded; (2) new HA joins the C-bout-substance cluster (e.g. HA-C4c-v2 threshold-refined per Stage I §4.8 Step 2a, OR HA-C4c-personal-baseline sister per Stage I §4.8 Step 2b, OR HA-C4c-per-phase sister); (3) cascade-source cluster [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) re-LOCK or HA11-bout-redo re-run at operand-refinement v2 changes framework-validity verdict (enters THIS Stage S₁ §4.5 5c as caveat-class precondition; needs re-consumption); (4) cited methodology MD changes lock-version (`research_line_limitations.md`; `internal_synthesis.md` guide; parent MD `bout_level_recovery_dynamics.md` `c57ff3f`; sub-MD `bout_level_dose_response_calibration.md` `fb97d1c`; `citalopram_phase_stratification.md`; `synthesis_structure_map.md` r3; `_plan_results_analysis_layer.md` r5); (5) **bout-return stringency descriptive companion Step 1 Pass 1 lands** per Stage I §4.8 own-research two-step follow-up (may motivate a new HA joining this cluster per drift trigger 2); (6) ≥ 6 months elapse since lock. |
| 2026-07-14 | **LOCKED r2** by user acceptance ("yes lets finish what we were doing now" 2026-07-14 driving Bundle H+ event 11 HA-C4cp downstream cascade Stage I → Stage S₁ per HA-C4cp pre-reg §9.2 PARTIAL branch DEFERRED artefacts) | **r1 → r2 revision scope**: (a) §4.1 constituent HAs single-member (HA-C4c) → **2-member (HA-C4c + HA-C4cp)** per HA-C4cp result.md LOCKED r1 2026-07-14 (Bundle H+ event 10 commit `cd71ac6`); (b) §4.3 per-HA table extended with HA-C4cp row (verdict PARTIAL; δ=+0.079, p=0.0090; SAME failure-mode configuration as HA-C4c at ~2× smaller magnitude on the personal-baseline operand family per operand-family-compression anticipation; Stage D TRUSTED); (c) §4.4 coherence call **trivial-ORTHOGONAL (single-member per `internal_synthesis.md` §5.6) → CONCORDANT-BELOW-THRESHOLD (2-member both-direction-correct + both-(b)-FAIL per HA-C4cp pre-reg §9.2 anticipated modal outcome)** with r1 rationale preserved in `<details>` block; (d) §4.5 5a caveats extended with HA-C4cp additions (reference-window fragility LIVE per reference_window_shorter_lag REJECTED at δ=+0.037; unmedicated-only stratum-fragility at higher amplitude than HA-C4c's 95% vs 50% attenuation; Approach A CI-brackets DIVERGENT-REJECTED empirically confirming pre-reg §8 caveat 3 amplification; original 10 caveats + 3 cascade-context notes preserved from r1); (e) §4.5 5b L-ID citation block unchanged (same L-IDs; both HAs share cluster-row); (f) §4.5 5c cascade-receiver precondition sub-paragraph unchanged; (g) §4.7a joint claim updated from single-member carry-forward → 2-member CONCORDANT-BELOW-THRESHOLD joint-claim reading with the corpus-effect-size ceiling shape per HA-C4cp pre-reg §9.2 substantive interpretive note + cross-op-independence gap CLOSURE reading at strict guide r3 §4.2 bar + descriptive corroboration (Cohen's κ = 0.507 moderate; 35.5% cap-unreachable rate) + r1 single-member carry-forward preserved in `<details>` block; (h) §11 THIS lock log row. **Cross-op-independence gap CLOSURE**: at strict guide r3 §4.2 two-independent-HAs bar via operand-family-level independence per OI-025 protocol §5.4 four-condition argument (fixed-absolute-threshold vs personal-baseline-rolling-reference differ in reference frame, construct alignment, distributional-drift sensitivity, low-n failure modes). Descriptive corroboration: 33.5% concordant firings + 20.3% HA-C4c-only + 4.8% SD-anchored-only + 41.4% neither on n=1274 primary stratum; 452 cap-unreachable days (35.5%) where Z=2 threshold exceeds 180-min tail_length cap by construction. **Substantive interpretive note per HA-C4cp pre-reg §9.2** (CAVEAT-CLASS per CONVENTIONS §4.2): the CONCORDANT-BELOW-THRESHOLD joint reading is a corpus-effect-size ceiling finding — Wiggers-C4 stuck-sympathetic signal fires directionally on this corpus at bout-level across BOTH operand families (fixed-absolute-threshold + personal-baseline-rolling) but effect-size is bounded below +0.20 SUPPORTED threshold on both operationalisations. Honest sample-limitation reading, NOT a Wiggers-C4 disproof. **Content preserved verbatim from r1**: §4.2 pre-declared constellation; §4.3 HA-C4c row unchanged; §4.4 trivial-ORTHOGONAL rationale (in `<details>`); §4.5 5a original 10 caveats + 3 cascade-context notes; §4.5 5b L-ID citation block; §4.5 5c cascade-receiver precondition sub-paragraph; §4.5 5d crash-drop sensitivity discipline; §4.6 open-conflicts (none); §4.7a r1 single-member joint claim (in `<details>`); §4.7b anti-overclaim shapes; §4.8 follow-ups; §4.9 open_inputs. **Fresh-session `/research-review` per plan §4 NOT dispatched pre-r2 lock — user-authorized deviation matching r1 precedent** (coherence-call transition guide-prescribed per `internal_synthesis.md` §5 verdict table for 2-member both-PARTIAL both-direction-correct configuration; joint claim shape derives deterministically from the two Stage I §3 licensed claims + HA-C4cp pre-reg §9.2 anticipated modal-outcome framing; no judgment-call axis exercised; §9.6 walk clean; retained as retrospective drift-trigger per plan §3.7). **Downstream propagation**: Stage S₂ [`topic-within-day-recovery.md`](../contextualisation/topic-within-day-recovery.md) LOCKED r1 2026-07-08 re-consumes on drift-triggered dispatch per `internal_synthesis.md` §9.2 cluster→topic dependency; landing at Bundle H+ event 11 downstream sub-arc per user tempo. Stage T translation tracks refresh at user tempo per §3.7 drift. **Drift triggers updated**: (1) any HA in the cluster re-examined or downgraded (now includes HA-C4c AND HA-C4cp); (2) NEW HA joins the cluster (e.g. HA-C4c-v3 or HA-C4cp-v2 per §9.5 escalation tables — neither triggered by current PARTIAL outcomes); (3) cascade-source [`cluster-C-bout-framework.md`](cluster-C-bout-framework.md) re-LOCK unchanged; (4) cited methodology MD changes lock-version (now includes parent MD `bout_level_recovery_dynamics.md` r3 at Bundle H+ event 7); (5) [DISCHARGED-AT-BUNDLE-H+-EVENT-11] HA-C4cp result.md LOCKED r1 with PARTIAL verdict → cluster r1 → r2 revision (this event); (6) ≥ 6 months elapse since lock; (7) HA-C4cp reference-window fragility diagnostic OI-036 lands (would tighten §4.5 5a reference-window caveat). **Bundle H+ event 11** on origin/main: Stage I HA-C4cp interpretation LOCKED r1 + THIS Stage S₁ cluster-C-bout-substance r1 → r2 revision + Stage S₂ topic-within-day-recovery re-consumption (deferred if scope permits) + Stage T translation refresh (deferred). |
