# cluster-C-bout-framework — internal synthesis (Stage S₁)

**Status**: **DRAFT r1 — dispatched-by-orchestrator on user authorisation; dispatch mode DRAFT-ON-DRAFT-DRY-RUN per skill r4 §11 (Stage I is DRAFT r1; user-acceptance lock event pending at OI-016 + OI-018 + OI-019)**. Drafted 2026-06-27 by `/research-interpret synthesise C-bout-framework` engine (skill LOCKED r4) under Phase B of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) — single-member cluster C-bout-framework (HA11-bout-redo); cascade-source for Phase C C-bout-substance per [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md) §3. Closes Phase B of the locked rollout. Per skill r4 §"DRAFT-on-DRAFT dry-run discipline": upstream Stage I `interpretation.md` is DRAFT r1 at commit `06146ce` (user-acceptance lock event PENDING at OI-016 Stage D + OI-018 Stage I §4.6); the licensed claim is read as **PROVISIONAL-by-orchestrator-authorisation** for the purpose of this Stage S₁ draft only. Stage D upstream `descriptive_audit.md` is DRAFT r1 at commit `980a541` with TRUSTED verdict-trust call read as **TRUSTED-PROVISIONAL-by-orchestrator-authorisation** per the same dispatch-mode discipline (inherited transitively via Stage I). Production-lock cannot fire until all three (a) Stage D lock-event resolves OI-016 AND (b) Stage I §4.6 user-articulation resolves OI-018 AND (c) this Stage S₁ user-acceptance lock event resolves OI-019 below. The user's separate non-dry-run lock session resolves all three gates in one pass per Phase-A precedent.

## Authorship

Drafted 2026-06-27 by Claude (Opus 4.7, 1M context) in reviewer-mode-with-authorization per [CONVENTIONS §1.2](../../CONVENTIONS.md#12-producer-vs-reviewer-mode), via the `/research-interpret synthesise C-bout-framework` skill invocation dispatched by the orchestrator on user authorisation under Phase B of [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md). Authorising user: Willem (dispatch framing — user reviews artefact in a separate non-dry-run lock session). Fresh-session `/research-review` peer-check required before LOCK per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §4 row for `cluster-*.md`.

---

## 4.1 Cluster name + constituent HAs

- **Cluster ID + name** (verbatim from [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) r3 §3): **C-bout-framework** — Bout-level framework-validity.
- **Constituent HAs** (verbatim from map's "Constituent HAs" cell): **HA11-bout-redo** (single member).
- **Member verdicts** (verbatim from [`HA11-bout-redo.md`](../interpretation/HA11-bout-redo.md) §1, which itself carries verbatim from `result.md` §6):
  - **HA11-bout-redo** (revision r2): **PARTIAL (2 of 3 framework-validity bars met; bars 1+2 met cleanly, bar 3 fails)**. Stage D verdict-trust: **TRUSTED** (read as **TRUSTED-PROVISIONAL-by-orchestrator-authorisation** per DRAFT-on-DRAFT dispatch mode transitively inherited via Stage I).
- **Shared construct** (verbatim from map's "Shared construct" cell): Whether the bout-extraction operand re-detects HA11 v1's discrimination signal at bout resolution.
- **Operationalisation-overlap note** (verbatim from map's "Operationalisation overlap note" cell): **NOT a substantive Wiggers claim.** Per HA11-bout-redo §1, this is a methodology-validation check (framework-validity discipline per parent MD §6). Validates that `bout_n_fast_recovery_day` reproduces HA11 v1's +22.8 pp discrimination at bout resolution. S₁ on this cluster reads the verdict as **operand fitness-for-purpose**, NOT as a substantive finding about within-day recovery in PAIS. **Cascade arrow**: this cluster's verdict propagates as a **caveat-class precondition** to C-bout-substance (downstream); it is not a co-equal verdict that S₁ reconciles against C-bout-substance's findings.
- **Cascade-arrow language** (verbatim from map's §3 row): this cluster is the **cascade-source** for C-bout-substance (cascade-arrow downstream; not upstream). The §4.5 5c cascade-precondition sub-paragraph below is OMITTED at this cluster because C-bout-framework has no upstream cluster precondition; it IS the upstream. See §4.10 cross-references for the downstream-cascade pointer to C-bout-substance.

**Asymmetry vs Phase A precedent.** The Phase-A precedent at [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) §4.1 was a **stand-alone cluster** (no cascade-arrow language in either direction — neither upstream nor downstream). This Phase B cluster is a **cascade-source** (downstream-bearing). The asymmetry surfaces at §4.10 cross-references (forward pointer to the cascade-downstream cluster) and at §4.7a joint claim (must explicitly name the cascade-propagation framing). The cluster's §4.5 cascade-precondition sub-block (5c) remains OMITTED — the cascade-source designation does not import an upstream precondition into THIS cluster's caveats; it only obligates the downstream cluster (C-bout-substance) to consume this cluster's joint claim as its own §4.5 5c precondition.

## 4.2 Pre-declared constellation

> *Pre-declared constellation* (per [`synthesis_structure_map.md`](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) §3 row `C-bout-framework`, declared 2026-06-23, lock version r3): the cluster groups **HA11-bout-redo** alone on the shared construct **whether the bout-extraction operand re-detects HA11 v1's discrimination signal at bout resolution** because the cluster is a methodology-validation gate per parent MD [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) §6 framework-validity discipline — the cluster's evidence-strength rationale is "the operand's framework-validity reproduction of HA11 v1 SUPPORTED-on-train signal at three pre-committed comparability bars", not "two independent operationalisations" (which would be the C-stress-fatigue-shape pattern) nor "same-signal multi-takes" (which would be a future C-h05-successor pattern if multiple operationalisations were registered). The constellation was locked at the layer-wide map's §7 lock-log entry 2026-06-24 (r3) and has not changed since. This Stage S₁ session reads the constellation from the map row; it does not propose a constellation change.

> *Cascade-arrow source designation* (per the map's §3 row for C-bout-framework): this cluster is the **cascade-source** for C-bout-substance — the downstream cluster's Stage S₁ will read THIS cluster's joint claim (§4.7a below) as caveat-class precondition into its own §4.5 5c sub-paragraph per `internal_synthesis.md` §5.5 cascade-handling rule. See §4.10 cross-references for the downstream-cascade pointer. No upstream precondition obligates this cluster (cluster has no §4.5 5c cascade-precondition sub-paragraph; OMITTED per `internal_synthesis.md` §4.5).

## 4.3 Per-HA verdict + interpretation row

One row per cluster member HA (here: one row only, per single-member cluster).

| HA | Verdict | Operationalisation | "What the verdict licenses" (from Stage I §3) | "What the verdict does NOT license" | Effect-size summary | L-IDs cited (Stage I §4.5) | Closure-path (Stage I §4.7) |
|---|---|---|---|---|---|---|---|
| HA11-bout-redo (r2) | PARTIAL (2 of 3 framework-validity bars met; bars 1+2 met cleanly, bar 3 fails) | framework-validity reproduction check on `bout_n_fast_recovery_day` (per-day count of bouts with `recovery_half_life ≤ 15 min` AND `tail_length ≤ 45 min` per [parent MD](../../methodology/bout_level_recovery_dynamics.md) §6.1) on the unmedicated × train era × HA11 v1 calm-day reference-date pool (seed `20260605`); 4-day max-signed-z windows against participant's lagged personal baseline; three framework-validity comparability bars verbatim from parent MD §6.2 (directional sign + ≥ +12.8 pp + p < 0.05 under block-permutation at E[L]=7, B=10000, seed `20260622`) | The bout-level operand `bout_n_fast_recovery_day` reproduces HA11 v1's SUPPORTED-on-train `u_dip_count` signal at directional + effect-size comparability bars (bars 1+2 MET cleanly: disc = +20.26 pp positive, within 2.6 pp of HA11 v1's +22.8 pp reference) but fails the block-permutation p-value comparability bar (bar 3 FAIL: p = 0.2609 vs threshold < 0.05) on this corpus's unmedicated × train × calm-day-pool primary cell. | PARTIAL does NOT license "the within-day recovery construct is partially supported" as a substantive claim (per `verdict_to_inference.md` §5.3 worked example: "this PARTIAL is operand fitness, NOT a substantive within-day recovery claim"). PARTIAL does NOT license collapse to weak SUPPORTED (bar-3 failure is part of the licensed-claim shape, not noise to bracket). PARTIAL does NOT license cross-era / cross-phase generalisation (validate-era OUT OF SCOPE per pre-reg §4.3 + §8 caveat 7; cross-phase pooling OUT OF SCOPE at framework-validity layer per parent MD §5.5 + §6). | Bar 1 (directional sign positive) PASS at disc = +20.26 pp; Bar 2 (effect-size comparability ≥ +12.8 pp, within ±10 pp of HA11 v1's +22.8 pp) PASS at +20.26 pp; Bar 3 (block-permutation p < 0.05 at E[L]=7) FAIL at p = 0.2609. Median signed z (triggering crash anchors) = +2.410 vs HA11 v1's 2.168; Cliff's δ = +0.306 with 95% CI [−0.021, +0.626]. Block-perm null disc median = +1.96 pp with 95% CI [−14.6, +18.2]; observed +20.26 pp sits at 74th percentile of null distribution. E[L]\* = 7.0 days (factor-of-2 flag NOT FIRED). Crash-drop sensitivity |Δ pp| = 0.90 < 5.0 threshold → §3.4 flag NOT FIRED → CLEAN. | L1, L2, L3, L4, L6, L7 (L5 NA — no v24 primary signals). | Operational per `verdict_to_inference.md` §4.7 for "k-of-N conditions MET" framing: bar-3 closure paths surveyed in Stage I §4.7 — (1) larger crash-arm n breaks framework-validity reference-frame discipline (no operationally-feasible path); (2) operand-refinement v2 (OPTIONAL per pre-reg §9.2; not required); (3) different framework-validity reference frame (sister-HA territory; tests different operationalisation rather than closing bar 3); (4) **PARTIAL with bar 3 FAIL is a legitimate completion state for the framework-validity question** — the gate fired, operand reproduced HA11 v1's magnitude but not statistical significance at locked n, cascade unblocks with calibration caveat per pre-reg §9.2. |

**Note on cascade-arrow language at §4.3.** Per `internal_synthesis.md` §4.3: "For clusters with cascade-arrow language (C-bout-substance reading C-bout-framework as caveat-class precondition), the §4.3 row table contains ONLY C-bout-substance's members (HA-C4c); the C-bout-framework verdict propagates via the §4.5 caveats + the §5.5 cascade-handling rule in the coherence-call mapping, NOT as a co-equal row in §4.3." That rule applies at the **cascade-downstream** cluster's §4.3. At THIS cluster (the cascade-source), §4.3 carries only this cluster's own member HAs — i.e., the single row above for HA11-bout-redo.

## 4.4 Coherence call

**STATUS: trivial-ORTHOGONAL (single-member cluster per `internal_synthesis.md` §5.6).**

**One-sentence default-reading (degeneracy):** the cluster contains a single constituent HA (HA11-bout-redo), so there is no cross-HA coherence to assess; per guide §5.6 the coherence call collapses to **trivial-ORTHOGONAL** with the cluster's joint claim collapsing to the single member's Stage I §3 licensed claim, and the cluster-level reading adds only the §4.5 L-ID citation block at the cluster level + the cascade-source designation in §4.7a (no §5c cascade-precondition sub-paragraph since this cluster has no upstream precondition).

**Rationale paragraph** (drawn from §4.3 the single per-HA row + the map's §3 operationalisation-overlap-note + `internal_synthesis.md` §5.6 single-member case):

> *Trivial ORTHOGONAL coherence call* (single-member cluster per the synthesis-structure map's §3 row C-bout-framework, declared 2026-06-23, lock version r3): the cluster contains a single constituent HA (HA11-bout-redo r2), so there is no cross-HA coherence to assess. The joint claim collapses to HA11-bout-redo's Stage I §3 licensed claim verbatim (carry-forward in §4.7a below): the bout-level operand `bout_n_fast_recovery_day` reproduces HA11 v1's SUPPORTED-on-train `u_dip_count` signal at directional + effect-size comparability bars (bars 1+2 PASS) but fails the block-permutation p-value comparability bar (bar 3 FAIL) on this corpus's unmedicated × train × calm-day-pool primary cell. The cluster-level reading adds the §4.5 L-ID citation block at the cluster level (per the limitations-doc §5 binding's cluster-level citation rule) and the cascade-source designation in §4.7a (this cluster's joint claim feeds the downstream C-bout-substance cluster's Stage S₁ as caveat-class precondition per `internal_synthesis.md` §5.5 cascade-handling rule), but does NOT add cross-HA coherence the single-member structure cannot support.

**Phase-A precedent and degeneracy traceability** (per Phase-A precedent at [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) §4.4): the Phase-A cluster was 2-member (HA-C3 v2 + HA-C3p) and exercised the PARTIALLY CONCORDANT vs CONCORDANT judgment-call axis. This Phase B cluster is 1-member and exercises the degenerate-single-member axis. The two cases are structurally different: Phase-A's coherence-call landed at PARTIALLY CONCORDANT (DEFAULTED-TO-PRESERVE, then user-confirmed at OI-003 closure); this cluster's coherence-call lands at **trivial-ORTHOGONAL** per guide §5.6 — there is no analogous user judgment-call axis on the §4.4 label for single-member clusters per guide §5.6 prescription (the degenerate label is determined by structure, not by interview). The user-acceptance question for THIS cluster is whether the trivial-ORTHOGONAL coherence-call + §4.7a single-member joint-claim carry-forward + §4.10 cascade-source designation are accepted as drafted (OI-019 below) — not whether the §4.4 label is correct (the label is structurally determined).

**Dispatch-mode note on the dispatch-task placeholder convention.** The dispatch task gave the agent the option to use `DEFAULTED-TO-PRESERVE SINGLE-MEMBER-NA` as a fallback placeholder if guide §5.2 / §4.4 was silent on the single-member case. **Guide §5.6 is NOT silent**: it explicitly prescribes **trivial-ORTHOGONAL** as the coherence-call label for single-member clusters with one-sentence default-reading template (cited verbatim above). The guide-prescribed label is therefore used (per LOCKED-r4 skill §"Always-read-LOCKED-current-revision rule" — the guide's prescribed label wins over a generic dispatch-task fallback when both are available). The dispatch-mode lock pending at OI-019 is a procedural lock-event step (user-acceptance), not a label-judgment-call step.

**Hard rule observance** (per `internal_synthesis.md` §4.4 + §7.10): the label `trivial-ORTHOGONAL` is drawn from the four-label set (CONCORDANT / PARTIALLY CONCORDANT / CONFLICT / ORTHOGONAL) — specifically the ORTHOGONAL label per §5.4 with the `trivial-` modifier per §5.6 for the single-member case. This is NOT label invention (per §7.10 anti-pattern) — the `trivial-` qualifier is the guide-prescribed degenerate modifier (one of the four labels), not a new fifth label. Guide §5.6 covers exactly this case.

## 4.5 Cluster-level caveats narrowing the joint claim (with L-ID citation block)

### 5a. Cluster-level caveats from member interpretations

The single member HA11-bout-redo §4.5 5a carries the cluster's caveat content; Stage S₁ inherits without inventing new caveats (per `internal_synthesis.md` §7.7 anti-pattern). Synthesised carry-forward:

- Power-calc dispatch inapplicable per Daza 2018 within-subject design (pre-reg §8 caveat 1).
- n=1 single-subject + observational + multi-source per [CONVENTIONS §3.1](../../CONVENTIONS.md#31-personal-baseline-not-absolute-thresholds); personal-baseline thresholded; cross-subject generalisation out of scope (pre-reg §8 caveat 2).
- **Framework-validity-NOT-substantive scope** (pre-reg §8 caveat 3, LOCKED verbatim): the PARTIAL verdict is a framework-validity finding, NOT a substantive claim about Wiggers C4. The bout-level operand is *partially fit* for HA-C4c; it does NOT validate the operand globally and does NOT prejudge HA-C4c's substantive verdict. **This is load-bearing for the cluster's cascade-source designation** — the cascade-propagation to C-bout-substance is exactly "operand partially fit for purpose with explicit calibration caveat naming bar 3 as the failing bar", not "operand validates the within-day recovery construct".
- **Inheritance-risk caveat** (pre-reg §8 caveat 4): bar 3's failure cannot fully distinguish between "operand under-power" and "the signal HA11 v1 detected was U-dip-specific, not within-day-recovery-shape-general"; the +20.26 pp directional + magnitude reproduction is strong evidence the construct generalises; the bootstrap-null insignificance at present n is the open question (Stage I §4.5 5a carries forward).
- Firstbeat-input amplification at minute resolution caveat (pre-reg §8 caveat 5); pacing-behaviour mask risk caveat (pre-reg §8 caveat 6); both compound with L3 device-generation reach at the cluster level per §5b below.
- **Era restriction non-negotiable** (pre-reg §8 caveat 7): validate-era reproduction OUT OF SCOPE; cross-phase pooling OUT OF SCOPE at the framework-validity layer per parent MD §5.5 + §6. **Cluster-level consequence**: this cluster's joint claim is bounded to unmedicated × train × calm-day pool by construction; the cascade-source designation propagates this scope-bound to C-bout-substance as a calibration caveat (NOT a scope-extension).
- Crash-drop sensitivity reported, NOT gating (|Δ pp| = 0.90 < 5.0; primary verdict unchanged; cluster-level crash-drop reading per §5.7 of guide #3 inherited at §5d below).
- Pipeline-extraction trust assumption (pre-reg §8 caveat 9; `extract_stress_bouts.py` LOCKED `d5b394c` with 6 inline smoke tests + 5-bout spot-check verification).
- Multi-comparison via single-cell headline lock; sensitivity arms cannot promote (pre-reg §8 caveat 10).

From Stage I §4.5 5a cascade-context notes (carry-forward from Stage D `descriptive_audit.md` §4.5 items 1-3):

- **Cascade-context note 1** (per Stage I §4.5 5a item 1 + Stage D §4.5 item 1 = OI-017): per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` does not yet exist as a standalone descriptive artefact; A2 BACKSTOPPED via the §3.4 day-validity gate + operand-presence figure (1486/1755 = 84.7%); a future per-channel rate audit would tighten the L7 reach narrative at the cluster level per §5b below. Logged at the layer-wide queue as OI-017 (carry-forward; cluster-level inheritance is observation-level, not load-bearing for the joint claim).
- **Cascade-context note 2** (per Stage I §4.5 5a item 2): the 99.3% motion-confound corpus property (4285/4317 bouts carry `motion_confound_flag = True`) is a corpus-level structural finding about the per-minute trace; the cluster's joint claim inherits the motion-tagged-bout-pool restriction by construction (the framework-validity gate uses motion-confounded-inclusive default per parent MD §3.4 + handoff §5 decision #6). **Load-bearing for the cascade-source designation**: any C-bout-substance claim about "rest-stress" semantics on this same operand inherits the 99.3% structural constraint from this cluster as caveat-class precondition.
- **Cascade-context note 3** (per Stage I §4.5 5a item 3): bar 3 power-shortfall observation (+20.26 pp at 74th percentile of null distribution; with 3× larger crash-arm the same effect size would clear bar 3 by a wide margin per `result.md` §7 caveat 10); no descriptive closure path identifiable for bar 3 in the framework-validity reference frame (enlarging n_crash would either expand the HA11 v1 reference-date pool beyond 200 changing HA11 v1's calibration, relax the §4.4 day-validity gate compromising data quality, or extend into validate era changing the reference frame). **Load-bearing for the cascade-source designation**: this is the specific failing bar the cascade-propagation must name in C-bout-substance's §8 caveats per HA11-bout-redo pre-reg §9.2 cascade implication.

### 5b. Cluster-level L-ID citation block (binding per limitations doc §5 row for `cluster-*.md`)

Per the synthesis-structure map's §3 [C-bout-framework](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) row L-IDs column: **L1, L2, L3, L4, L6, L7 (L5 NA)**. The cluster does NOT span different era strata (single-member cluster running on Stratum 4 unmedicated × train only) so no extra L2-era-strata-mismatch citation is triggered beyond the per-HA L2 the member interpretation already cited.

> *Cites L1 (single-subject reach)*: this cluster's joint claim about bout-level framework-validity is **single-subject** (L1 inference reach per [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) bounds it). The cluster's framework-validity reproduction of HA11 v1's signal at bout resolution adds one within-subject data point about the operand's fitness-for-purpose at this corpus; it is NOT a group-level claim about the bout-level recovery construct. Cross-operationalisation reading does not apply at the cluster level (single-member cluster; no sister HA exists at framework-validity layer); the cascade-arrow propagation to C-bout-substance is the reach pathway (cluster-level cascade-source, not Stage I licensing extension).

> *Cites L2 (era confounds)*: this cluster's single member HA11-bout-redo runs on Stratum 4 unmedicated × HA11 v1 train era (2022-09-03 → 2023-12-31; April 2024 cluster excluded; 21-day device-baseline-lag excluded per parent MD §3.4). The PARTIAL framework-validity verdict at cluster level does NOT project to medicated Stratum 4 sub-phases or to HA11 v1's validate era (out of scope per pre-reg §4.3 + §8 caveat 7). Cross-phase pooling is OUT OF SCOPE at the framework-validity layer per parent MD §5.5 + §6. **The cluster's L2 citation does NOT trigger the extra L2-era-strata-mismatch cluster-level binding** (per limitations doc r3 §5 "also cite L2 if cluster members are from different era strata") because the cluster has only one member and that member runs a single stratum (unmedicated × train only). No within-member cross-phase pooling exists at this cluster level (HA11-bout-redo does not pool; HA-C4c is the cross-phase HA, and HA-C4c sits in the cascade-downstream C-bout-substance cluster, NOT here).

> *Cites L3 (device generations)*: cluster-level dependency on FR245 Elevate-V3 device generation via `extract_stress_bouts.py` LOCKED `d5b394c`. The Firstbeat-input amplification caveat (pre-reg §8 caveat 5) + the L3 caveat together bound the cluster's joint claim to within-device-generation reach. Cross-device-generation projection requires explicit calibration evidence per [`research_line_limitations.md`](../../methodology/research_line_limitations.md) §3 L3. **Cluster-level cascade consequence**: the device-generation reach-bound propagates to C-bout-substance as caveat-class precondition (any C-bout-substance claim using the same bout-extraction pipeline at the same device generation inherits this bound).

> *Cites L4 (analyst-is-subject)*: the cluster's framework-validity verdict is prior-driven by the parent MD `bout_level_recovery_dynamics.md` §6 framework-validity discipline (analyst-designed methodology-validation gate); per CONVENTIONS §4.3 the test is confirmatory of operand fitness-for-purpose, not exploratory. The L4 mitigation reach per limitations doc r3 §3 L4 caveat: fresh-session `/research-review` peer-check (queued for the user's separate non-dry-run lock session per Phase-A precedent); producer-vs-reviewer split honored via this artefact's reviewer-mode-with-authorization status. **The §4.6-equivalent lived-experience prior reconciliation is DEFAULTED-PENDING-USER-INPUT at the member-HA level (OI-018; Stage I §4.6)** — Stage S₁ does NOT carry a §4.6-equivalent per `internal_synthesis.md` §6.4 (cluster-level prior reconciliation is inherited from member interpretations, not re-litigated); the cluster inherits the member's DEFAULTED-PENDING-USER-INPUT state as caveat-class context per L4 + OI-018 carry-forward.

> *L5 (presence-conditioned data layer) NA*: cluster's single member HA11-bout-redo uses no v24 primary signals; `bout_n_fast_recovery_day` is derived from per-minute Garmin stress trace via `extract_stress_bouts.py`, and `crash_v1` is HA11 v1's primary outcome from `labels_crash_v2.csv` (both non-v24). The map's §3 row marks L5 NA for the C-bout-framework cluster; this cluster does not trigger L5 independently. NA reason: no v24-derived columns anywhere in the cluster's primary or sensitivity-arm chain per Stage D audit A5 (carried via Stage I §4.5 5b L5 NA).

> *Cites L6 (self-reporting)*: cluster's single member HA11-bout-redo does NOT use `gevoelscore` in the primary cell (the primary outcome is `crash_v1` from HA11 v1's `labels_crash_v2.csv`, a curated label series, not the daily 1-10 subjective entry). However, L6 applies at the **upstream methodology layer** because `crash_v1` labels are themselves derived in part from the participant's lived experience of crash episodes (the `crash_v2-definition` triage rules are participant-defined per Stage I §4.5 5b L6 row). The cluster's joint claim inherits the upstream self-attribution caveat at the label-definition layer: the 11 crash-anchor windows the cluster's framework-validity gate runs against are anchored on participant-defined crash episodes, with the L6 noise-floor caveat applying at the label-definition layer rather than at the per-day signal layer. Per limitations doc L6 forbids: not used as biomarker; not used in cross-subject comparison.

> *Cites L7 (survivorship)*: the n_calm = 70 (of 200 HA11 v1 reference dates) + n_crash = 11 (of 13 unmedicated × train crash-episode anchors) effective coverage on the cluster's primary cell represents the gated subset surviving the §4.4 day-validity gate + §4.5 baseline-validity gate + §4.7 window-coverage gate. The cluster's joint claim does not project to days outside the gated subset. Stage D `descriptive_audit.md` A2 BACKSTOPPED reading via §3.4 day-validity gate + 84.7% operand-presence figure (1486/1755 non-NaN days) supplies path (a) of guide #1 §5.2 operational test; the cascade-context note (Stage I §4.5 5a item 1 = OI-017) flags that a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` would tighten the L7 reach narrative from "gating-discipline-bounded" to "fully audit-backed". Until that diagnostic lands, the cluster's L7 citation remains gating-discipline-bounded. **Cluster-level cascade consequence**: closing OI-017 at the cluster level (one descriptive run) closes the cascade-downstream HA-C4c's Stage D A2 in the same step (same operand on cross-phase pool) per Stage I §4.5 5a item 1 — a cross-cluster efficiency observation, not a cluster-level claim extension.

### 5c. Cascade-arrow precondition sub-paragraph

OMITTED: C-bout-framework carries no upstream cascade-arrow precondition in the map's §3 row. The cluster is itself the cascade-source for C-bout-substance (per §4.1 above); no upstream cluster's `cluster-*.md` is consumed as precondition. Per `internal_synthesis.md` §4.5 5c rule: "For clusters without cascade-arrow language (C-stress-fatigue-shape; C-bout-framework as itself the upstream), this sub-paragraph is omitted." OMITTED here accordingly.

### 5d. Cluster-level crash-drop sensitivity discipline (per `internal_synthesis.md` §5.7)

The single member HA11-bout-redo's `result.md` §5 reports crash-drop sensitivity at |Δ pp| = 0.90 < 5.0 threshold → §3.4 flag NOT FIRED → CLEAN. The cluster-level reading per guide #3 §5.7 inherits the CLEAN reading: no cluster-level crash-drop divergence surfaces (single member; no inter-HA crash-drop divergence to assess). Cluster-level surfacing: recorded here for §3.4 audit-hook discipline. The framework-validity signal is robust across the broader pool with crash-days dropped; it is not crash-driven. No cluster-level coherence-call shift; no cluster-level open-conflict triggered.

## 4.6 Open conflicts preserved with both readings

**STATUS: trivial-ORTHOGONAL coherence call per §4.4 (single-member degeneracy).**

Per `internal_synthesis.md` §4.6 for ORTHOGONAL calls: "Cluster members address different aspects of the shared construct; no conflict at the cluster level because the members do not speak to each other directly."

For the trivial-ORTHOGONAL single-member case specifically: **there is no inter-HA agreement-disagreement to assess** (single member). No cluster-internal open conflicts exist by structural construction. The cluster-level reading is the carry-forward of HA11-bout-redo's licensed-claim shape per §4.7a below.

**Resolution paths (NOT applicable at the cluster-internal level; carried forward to §4.8 follow-up suggestions instead)**:

- A second framework-validity HA (e.g., HA11-bout-redo-v2 with operand-threshold refinement per pre-reg §9.2 OPTIONAL path, OR HA11-bout-redo-validate testing reproduction on HA11 v1's validate era) would convert this single-member cluster into a 2-member cluster at the next Stage S₁ re-examination; the coherence-call would then exit the trivial-ORTHOGONAL degeneracy and exercise the standard four-label mapping. Logged in §4.8 own-research track.
- Cascade-downstream Stage S₁ on C-bout-substance (Phase C of the locked rollout) will surface any tension between this cluster's caveat-class precondition and C-bout-substance's substantive coherence call as the downstream cluster's §4.6 + §5.5 cascade-handling, NOT as this cluster's §4.6 open conflict. Per `internal_synthesis.md` §5.5 hard rule: the cascade-upstream cluster's verdict propagates as caveat-class precondition into the cascade-downstream cluster's §4.5 + §5, NEVER as a co-equal verdict reconciled in the upstream cluster's §4.6.

**Inherited member-level placeholder (OI-018) acknowledged**: HA11-bout-redo's Stage I §4.6 lived-experience-prior reconciliation is DEFAULTED-PENDING-USER-INPUT (OI-018). Per `internal_synthesis.md` §6.4, Stage S₁ does NOT carry a §4.6-equivalent lived-experience prior reconciliation at the cluster level — that reconciliation lives at the per-HA Stage I level and the cluster inherits it as caveat (per §4.5 5a + 5b L4 above). The placeholder's eventual user-articulation per OI-018 may trigger a re-examination of this cluster's joint claim per the §3.7 drift policy (Stage I revision → cluster re-examination trigger), but does not open a cluster-level §4.6 open-conflict at this draft.

## 4.7 What the cluster jointly licenses + does NOT license

### 7a. What the cluster jointly licenses

**The cluster's joint claim** (per `internal_synthesis.md` §5.6 single-member case + §4.7a width discipline — joint claim collapses to single member's Stage I §3 licensed claim, no width expansion possible since no cross-operationalisation independent evidence exists at this cluster):

> The cluster of bout-level framework-validity (single member HA11-bout-redo r2), tested on this corpus's Stratum 4 unmedicated × train era × HA11 v1 calm-day reference-date pool under the locked framework-validity discipline of parent MD [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) §6, **establishes operand fitness-for-purpose for `bout_n_fast_recovery_day` at the directional + effect-size comparability bars (bars 1+2 MET cleanly: disc = +20.26 pp positive, within 2.6 pp of HA11 v1's +22.8 pp reference signal at the same train-era reference frame) but NOT at the block-permutation p-value comparability bar (bar 3 FAIL: p = 0.2609 vs threshold < 0.05 under stationary-bootstrap E[L] = 7, B = 10000, seed `20260622`) at this corpus's locked sample sizes (n_calm = 70, n_crash = 11)**. The framework-validity claim is operand-fitness-for-purpose, NOT a substantive within-day recovery claim about PAIS / LC autonomic recovery dynamics. The joint claim is, by single-member-cluster construction, the carry-forward of HA11-bout-redo's Stage I §3 licensed claim with the cluster-level L-ID block applied per §4.5 5b above; no width expansion beyond the single member's claim is licensed at this cluster (no cross-operationalisation convergence available at single-member-cluster level).

**Cascade-source designation** (load-bearing cluster-level framing for downstream C-bout-substance Stage S₁): this cluster's joint claim propagates to C-bout-substance as **caveat-class precondition** per `internal_synthesis.md` §5.5 cascade-handling rule, NOT as co-equal verdict. The cascade-propagation reads:

> *Cascade-arrow source claim for C-bout-substance to consume at its Stage S₁ §4.5 5c*: C-bout-framework's cluster-bout-framework.md reached **trivial-ORTHOGONAL** coherence call (single-member cluster), with HA11-bout-redo's licensed claim of **operand `bout_n_fast_recovery_day` partially fit-for-purpose** — bars 1+2 PASSED indicate direction + effect-size reproduction at +20.26 pp; bar 3 FAILED leaves the block-permutation discrimination at corpus detection limits per the inheritance-risk caveat (pre-reg §8 caveat 4). This propagates into the C-bout-substance cluster's joint claim as caveat-class precondition: the bout-extraction operand's framework fitness is partially established (bars 1+2 PASSED; bar 3 FAILED at corpus detection limits). C-bout-substance's substantive coherence call inherits the 2-of-3 framework caveat: the substantive claim carries the "operand fitness partially established at framework-validity layer" qualifier explicitly. Per `internal_synthesis.md` §5.5 hard rule, this is NOT a co-equal verdict reconciliation; the upstream verdict does not enter C-bout-substance's §4.4 coherence call.

The joint claim is bounded by:
- The cluster's L1 single-subject reach (per §4.5 5b L1 citation).
- The L2 within-Stratum-4-unmedicated-train phase + era scope (per §4.5 5b L2 citation; cross-phase + cross-era OUT OF SCOPE at framework-validity layer per pre-reg §8 caveat 7).
- The L3 within-device-generation reach (per §4.5 5b L3 citation; Firstbeat-input amplification compounding).
- The L4 analyst-is-subject prior coupling (per §4.5 5b L4 citation; parent MD §6 framework-validity discipline as analyst-designed methodology gate; user-articulation pending per OI-018 carry-forward).
- The L6 + L7 self-report-at-label-layer + survivorship-gating discipline (per §4.5 5b L6 + L7 citations; OI-017 cascade-context as L7 narrowing observation).
- The L5 NA confirmation (per §4.5 5b L5 NA; no v24-derived columns).

### 7b. What the cluster jointly does NOT license

Four predictable overclaim shapes per `internal_synthesis.md` §4.7b:

1. **The cluster does NOT license "the construct's mechanism is established."** The framework-validity reproduction of HA11 v1's signal at bout resolution is operand fitness-for-purpose, NOT a mechanism claim. The mechanism the operand reproduces (HA11 v1's U-dip-count detected as within-day fast-recovery-bout-count proxy) is a hypothesis-generating prior at most per Daza 2018; the mechanism that explains why the bout-level fast-recovery operand reproduces the U-dip signal at directional + magnitude bars but not at the bootstrap-null discrimination bar (under-power vs operand-narrowness vs corpus detection limits) is a separate question requiring more analysis (Stage I §4.7 closure-path bullet 4: bar 3 FAIL is a legitimate completion state for the framework-validity question; no mechanism inference is licensed by this completion).

2. **The cluster does NOT license "the construct's group-level reach is established."** Per L1 single-subject reach; cluster-level framework-validity reading on this N=1 corpus is bounded by Daza 2018 / CENT / SCRIBE / Natesan 2023 N-of-1-to-group reach. The cluster speaks about this subject's bout-level operand fitness-for-purpose at this corpus's specific stratum × era × reference-date pool; group-level positioning is downstream of this cluster (and downstream of C-bout-substance) at Stage S₂ on the T-within-day-recovery topic per the map's §4 topic table, NOT a Stage S₁ surface.

3. **The cluster does NOT license predictive claims.** Per [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.10 hard predictive gate; cluster-level coherence does NOT promote any tier. **This cluster has no Stage A construct in the map §5** (the construct table's K-bout-recovery-signal feeds from T-within-day-recovery via C-bout-substance, NOT from C-bout-framework — see map §4 + §5; C-bout-framework is methodology-validation-only per map §3 cluster cell). No tier-1 / tier-2 / tier-3 claims live at this cluster; the cascade-source designation is the cluster's only downstream framing, and it propagates as caveat-class precondition (NOT as tier promotion path).

4. **The cluster does NOT license "the joint claim averages the framework-validity asymmetry."** The trivial-ORTHOGONAL call preserves the bar-1+2 PASS + bar-3 FAIL pattern verbatim per Stage I §3 licensed claim; the joint claim names the per-bar pattern (bars 1+2 PASSED at +20.26 pp; bar 3 FAILED at p=0.2609 at corpus detection limits) rather than papering it over into a "weakly framework-validated" or "essentially-passed" middle-ground that would collapse the discipline-mechanism information. The cascade-propagation discipline (named bar 3 as the failing bar in cascade-downstream caveats per HA11-bout-redo pre-reg §9.2) is the structural protection against this overclaim shape at the cascade-receiver.

## 4.8 Follow-up suggestions

### Own-research track

- **HA11-bout-redo-v2 with operand-threshold refinement** (per pre-reg §9.2 OPTIONAL path; carry-forward from Stage I §4.8 own-research track). If HA-C4c's substantive verdict (Phase C cascade-receiver) finds the calibration caveat too binding, an operand-refinement v2 would re-tune the 15-min half-life + 45-min tail thresholds in parent MD §6.1 with explicit pre-commitment to new threshold values and re-run the framework-validity gate against HA11 v1's reference frame. **Cluster-level upgrade path**: if v2 lands SUPPORTED or PARTIAL with bar 3 PASS, this cluster gains a second member; the §4.4 coherence call exits the trivial-ORTHOGONAL degeneracy and the standard four-label mapping applies (two-member case). Pre-reg shape per [`hypothesis_lock_process.md`](../../methodology/hypothesis_lock_process.md) §3.2; would require re-locking parent MD §6.1 operand-threshold pre-commit. **Consolidated from Stage I §4.8 own-research entry** — this is the cluster's primary upgrade path that would exit single-member degeneracy.

- **HA11-bout-redo-validate sister pre-reg** (testing bout-level reproduction on HA11 v1's validate era rather than train; carry-forward from Stage I §4.8 own-research). Pre-reg §8 caveat 7 explicitly marks validate-era reproduction OUT OF SCOPE for the current framework-validity gate. A separate HA11-bout-redo-validate pre-reg would isolate the era-specificity question; framework-validity-validate would have its own bar set + reference frame. **Cluster-level upgrade path**: would add a second member at this cluster (cross-era framework-validity sister); coherence call would assess train vs validate operand-fitness convergence at the standard four-label mapping. Routing: `hypothesis_lock_process.md` §3.2 fresh-session drafting; pre-reg would carry explicit cross-cite to HA11-bout-redo (this cluster's existing member) + HA11 v1 validate-era REFUTED context.

- **Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day`** (cluster-level inheritance from Stage D + Stage I cascade-context; carries forward as OI-017 in the layer-wide queue). Not a sister-HA pre-reg; a descriptive infrastructure run that tightens the L7 reach narrative at the cluster level per §4.5 5b L7 row, AND closes the cascade-downstream HA-C4c's Stage D A2 in the same step (same operand on cross-phase pool). S effort per stocktake §3 Shared gap 3 pattern; script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/`. **Cluster-level consequence**: closing OI-017 tightens this cluster's §4.5 5b L7 from "gating-discipline-bounded" to "fully audit-backed"; does not change the §4.4 coherence call or the §4.7a joint claim.

### External-research track

- **Cross-population framework-validity replication of the bout-level recovery operand on PEM-prone populations at group level** (carry-forward from Stage I §4.8 external-research; consolidated at cluster level). We cannot run this ourselves because **L1 (single-subject reach)** prevents the N=1 corpus from settling whether the bout-level operand `bout_n_fast_recovery_day` generalises beyond this subject's specific within-day stress trace. Per Daza 2018 N-of-1 inference reach: the present cluster's PARTIAL joint claim bounds the framework-validity claim to one subject's data; cross-subject framework-validity would require a group-level study with the same per-minute Garmin stress instrumentation, the same bout-detection rule (parent MD §3), and the same comparability bars (parent MD §6.2), evaluated against a group-level crash-precursor reference signal analogous to HA11 v1's `u_dip_count`. Minimum n per Daza 2018 + CENT 2015 item 22 generalisability bounds; the study would carry pre-registration of the same three §5 bars with the within-population effect-size comparability anchor adjusted to the group's distribution.

- **Cross-device-generation framework-validity comparability study** (carry-forward from Stage I §4.8 external-research; consolidated at cluster level). We cannot run this ourselves because **L3 (device generations)** + the Firstbeat-input amplification caveat (pre-reg §8 caveat 5) means the bout-level features derived from the closed-source Firstbeat algorithm may not transfer cleanly across device generations even if the column name remains semantically stable. A cross-device study would test whether the bout-detection rule (parent MD §3) produces statistically comparable `bout_n_fast_recovery_day` distributions across device generations on matched within-subject windows; would clarify whether the framework-validity gate is device-stable or device-specific. Minimum design per L3 caveat: paired-subject within-device-generation transition windows (same subject wearing two devices in parallel), with explicit calibration discipline per [`research_line_limitations.md`](../../methodology/research_line_limitations.md) §3 L3.

Each entry is one paragraph with the proposed study, the L-ID that prevents us from running it ourselves, and what the study would contribute to the cluster's joint claim. Both external-research entries respect [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3.11 N=1-limit scoping discipline (the L-ID-explicit-naming requirement per `internal_synthesis.md` §4.8 + locked-plan §3.11 binding).

**Distinct from `open_inputs`** per locked-plan §3.5 vs §3.11: the §4.9 `open_inputs` block below names what is missing to complete *this current cluster synthesis* (the OI-016 + OI-018 + OI-019 user-acceptance lock-event slots + OI-017 cluster-level cascade-context inheritance). The §4.8 follow-up suggestions name what *next claims* could be built — for us or for others. Both are required; neither substitutes for the other.

## 4.9 `open_inputs` block

Three slots surfaced (two inherited from upstream Stage D + Stage I dispatch-mode pending events; one new this artefact for cluster-level user-acceptance):

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | **OI-016 carry-forward from Stage D** (DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill #7 dispatch-mode convention): User explicit acceptance of HA11-bout-redo Stage D audit's TRUSTED verdict-trust call per guide #1 §8.4 optional seed | DRAFT r1 → LOCKED transition for Stage D `descriptive_audit.md`; downstream Stage I on HA11-bout-redo gateable on TRUSTED per guide #2 §9.2; cluster-level Stage S₁ inherits via DRAFT-on-DRAFT dispatch mode per skill r4 §"DRAFT-on-DRAFT dry-run discipline"; production-lock of THIS Stage S₁ requires Stage D lock-event resolution | One interview pass per guide #1 §8.4 optional seed wording at the user's separate non-dry-run lock session: "Given the eleven checklist rows, do you accept TRUSTED as the audit's verdict-trust call?" — confirmation seed, expected unambiguous YES given Stage D §3 mechanical ticks (resolves jointly with OI-018 + OI-019 in one session per Phase-A precedent) | TRUSTED-PROVISIONAL-by-orchestrator-authorisation reading stands per dispatch-mode discipline at all three upstream layers (Stage D + Stage I + this Stage S₁); cluster-level joint claim is structurally complete and ready for joint user-acceptance-only lock step (no claim degradation needed; the fallback IS the existing TRUSTED-PROVISIONAL label held DRAFT pending the joint user lock-event per locked-plan §3.5 "Hard rule") |
| 2 | **OI-018 carry-forward from Stage I** (DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill §"Dispatch-mode placeholders"): User articulation of lived-experience prior on whether the bout-level operand `bout_n_fast_recovery_day` should reproduce HA11 v1's SUPPORTED-on-train signal at the three §5 framework-validity comparability bars; mirrors Phase-A precedent at OI-001 + OI-002 for HA-C3 v2 + HA-C3p | Stage I §4.6 lived-experience prior reconciliation completeness for HA11-bout-redo; production-lock of Stage I requires §4.6 user-articulation; cluster-level Stage S₁ inherits the placeholder per §4.5 5b L4 + §4.6 dispatch-mode rule (`internal_synthesis.md` §6.4 — cluster does NOT carry §4.6-equivalent; inherits member's placeholder as caveat-class); production-lock of THIS Stage S₁ requires Stage I §4.6 user-articulation resolution | One interview question per the §8.3 seed in `verdict_to_inference.md` at the user's separate non-dry-run lock session (resolves jointly with OI-016 + OI-019 in one session per Phase-A precedent at HA-C3 v2 + HA-C3p Phase A.1 + A.2) | DEFAULTED-PENDING-USER-INPUT placeholder with one-sentence default-reading + Phase-A precedent traceability block per Stage I §4.6 (no narrowing of §3 licensed claim per agreement-does-not-strengthen rule per guide #2 §4.6 r2); cluster-level joint claim per §4.7a stands per single-member trivial-ORTHOGONAL collapse regardless of lived-experience-prior articulation outcome (the cluster's joint claim mechanics do not depend on the member's §4.6 reconciliation per `internal_synthesis.md` §6.4) |
| 3 | **OI-019 (NEW this artefact)** (DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill #7 dispatch-mode convention; mirrors OI-016 + OI-018 pattern): User explicit acceptance of THIS cluster-level Stage S₁ artefact's structurally-complete dispatch-mode draft per `internal_synthesis.md` §9.6 refuse-to-lock gate exit + locked-plan §3.8 binding completion event | DRAFT r1 → LOCKED transition for `analyses/synthesis/cluster-C-bout-framework.md`; downstream Phase C Stage S₁ on C-bout-substance gateable on this cluster's LOCKED state per `internal_synthesis.md` §9.2 cascade-arrow precondition refusal-path; Phase B of `_rollout_order_2026-06-25.md` closes only on this Stage S₁ user-acceptance | One interview pass at the user's separate non-dry-run lock session reading this artefact's status header + §4.4 trivial-ORTHOGONAL coherence call + §4.7a single-member joint-claim carry-forward + §4.10 cascade-source designation + §9.6 walk + this entry — accept-DRAFT-as-LOCKED, revise-then-accept, or reject-and-redraft (resolves jointly with OI-016 + OI-018 in one session per Phase-A precedent; one user session resolves all three Phase B lock-event gates per the established multi-stage lock pattern) | DRAFT r1 status persists per LOCKED-r4 skill #7 dispatch-mode convention; substantive content is locked-in-shell + ready for user-acceptance-only step; no claim degradation needed per locked-plan §3.5 "Hard rule" since the fallback IS the existing trivial-ORTHOGONAL + single-member joint-claim held DRAFT pending the joint user lock-event resolving all three OIs |

OI-019 is a new entry; it propagates to worktree-local `methodology/_open_inputs.md` at the bottom of the queue per skill responsibility #6 (deduplicated against existing entries — checked against OI-001-018; no overlap on (missing-input × blocked-target) key since OI-019's missing-input is THIS cluster-level Stage S₁ user-acceptance + blocks Phase B closure + cascade-arrow precondition for Phase C). The methodology agent absorbs the worktree-local queue edits in their next commit cycle per the established Stage D + Stage I cherry-pick discipline at this Phase B (per [[feedback_check_index_before_add]] producer/reviewer split convention at the methodology agent boundary).

**Carry-forward observations on cascade-arrow downstream framing** (NOT a new `open_inputs` entry; recorded here for traceability of the cluster's cascade-source designation completing Phase B and enabling Phase C):

- **Phase C Stage S₁ on C-bout-substance precondition status**: Phase C cannot start until this Stage S₁ locks (per `internal_synthesis.md` §9.2 cascade-arrow upstream precondition refusal-path 3). The dispatch-mode DRAFT-on-DRAFT state at THIS artefact is the binding constraint on Phase C dispatch ordering; resolving OI-019 (joint with OI-016 + OI-018) closes Phase B AND unblocks Phase C dispatch.
- **Phase C cascade-arrow consumption obligation**: Phase C Stage S₁ on C-bout-substance MUST consume this Stage S₁ output's §4.7a joint claim as caveat-class precondition into its own §4.5 5c cascade-precondition sub-paragraph per `internal_synthesis.md` §5.5 cascade-handling rule. The Phase C draft cannot enter §4.4 with this cluster's verdict as a "third vote" — that would violate `internal_synthesis.md` §5.5 hard rule + §7.3 anti-pattern (treating cascade-arrow caveat-class precondition as co-equal verdict). This Stage S₁ artefact's §4.7a cascade-source claim above is the exact text C-bout-substance Stage S₁ §4.5 5c will quote verbatim (template per guide §4.5 5c paragraph).

## 4.10 Cross-references

- Member HA's interpretation (cluster's sole upstream-Stage-I input):
  - [`HA11-bout-redo.md`](../interpretation/HA11-bout-redo.md) — HA11-bout-redo Stage I, DRAFT r1 dispatched-by-orchestrator on user authorisation 2026-06-26 at commit `06146ce` (DRAFT-ON-DRAFT-DRY-RUN dispatch mode; user-acceptance lock event PENDING at OI-016 + OI-018).
- Member HA's Stage D descriptive audit (upstream-Stage-D input, transitively inherited via Stage I):
  - [`descriptive/HA11-bout-redo/descriptive_audit.md`](../descriptive/HA11-bout-redo/descriptive_audit.md) — HA11-bout-redo Stage D, DRAFT r1 dispatched-by-orchestrator 2026-06-26 at commit `980a541` (user-acceptance lock event PENDING at OI-016).
- Synthesis-structure map cluster row (verbatim source for §4.2 pre-declared constellation): [C-bout-framework](../../methodology/synthesis_structure_map.md#3-cluster-table-s%E2%82%81-pre-registration) (declared 2026-06-23, lock r3 2026-06-24); single-member cluster (HA11-bout-redo only); **cascade-arrow downstream to C-bout-substance** (this cluster is the cascade-source per map §3).
- **Cascade-downstream Stage S₁ pointer** (forward pointer for Phase C consumption; per the dispatch-task §4.10 cascade-arrow downstream framing): [`cluster-C-bout-substance.md`](cluster-C-bout-substance.md) (NOT YET DRAFTED — Phase C of `_rollout_order_2026-06-25.md`; dispatchable on this Stage S₁ user-acceptance lock per `internal_synthesis.md` §9.2 cascade-arrow precondition refusal-path). When Phase C Stage S₁ drafts, it consumes THIS Stage S₁'s §4.7a joint claim as caveat-class precondition per `internal_synthesis.md` §5.5; the cascade-arrow chain is one-way: this cluster (source) → C-bout-substance (receiver).
- Map [T-within-day-recovery](../../methodology/synthesis_structure_map.md#4-topic-table-s%E2%82%82-pre-registration) §4 topic row the downstream cluster C-bout-substance feeds (NOT consumed by this cluster directly; C-bout-framework is methodology-validation-only per map §3 cluster cell and has no direct topic feeder). Forward pointer for Stage S₂ on T-within-day-recovery (Phase C of the rollout; requires C-bout-substance Stage S₁ locked first).
- Map [K-bout-recovery-signal](../../methodology/synthesis_structure_map.md#5-construct-table-a-pre-registration) §5 construct row (NOT consumed by this cluster — C-bout-framework has no Stage A construct per map §5; the construct feeds from T-within-day-recovery via C-bout-substance, not from C-bout-framework). Forward pointer for Stage A (Phase C of the rollout; requires Stage S₂ first; tier-2 PPV-with-base-rate per locked-plan §3.10 hard predictive gate; §3.12 commentary-eligible). This cluster does NOT cross §3.10 hard predictive gate (per `internal_synthesis.md` §1 NOT-list); has no Stage A construct surface.
- **Stage T skip-record convention** (per `internal_synthesis.md` §4.10 + skill r2 routing-table Stage T skip-record location convention): if the user elects to skip the Stage T research-audience track for THIS Stage S₁ artefact (i.e., treat this cluster-level synthesis as research-internal-only with no downstream translation), the skip record lands here per syntax: `Stage T research-audience track: SKIPPED-AS-RESEARCH-INTERNAL -- rationale: <one-sentence>` appended at this section. **No skip-record fires at this draft** (Stage T for this cluster is not in-scope at Phase B per `_rollout_order_2026-06-25.md` §2 Phase B which restricts to D + I + S₁ — no Stage T dispatched for C-bout-framework cluster per the rollout's "No S₂ / A / T per map r3 §3 (C-bout-framework is methodology-validation-only; not a substantive Wiggers claim)" Phase B note); the skip-record convention is recorded here for future Stage T routing if the user adds a research-audience translation track at this cluster in a future rollout extension. The methodology-validation scope-bound at this cluster makes Stage T routing structurally minimal — there is no patient-audience downstream from a framework-validity cluster (commentary lives at Stage A construct per §3.12, and this cluster has no Stage A construct per map §5).
- Limitations doc cross-refs for cited L-IDs: [`research_line_limitations.md`](../../methodology/research_line_limitations.md) §3 L1, L2, L3, L4, L6, L7; §5 citation requirements row for `cluster-*.md`. L5 NA per cluster row + Stage D audit A5 + Stage I §4.5 5b L5 NA carry-forward.
- Non-binding seed notes: [`_synthesis_seed_notes_2026-06-23.md`](../../methodology/_synthesis_seed_notes_2026-06-23.md) §3 (candidate joint reading C-bout-substance with C-bout-framework cascade; advisory-only / caveat-class per the seed-notes' own §5 self-description). The seed notes touch the cascade-arrow framing of this cluster as cascade-source to C-bout-substance; per `internal_synthesis.md` §7.6 anti-pattern (treating seed-notes sketches as constraints on the coherence call), this Stage S₁ does NOT inherit the seed-notes §3 reading as a constraint on the §4.4 trivial-ORTHOGONAL coherence call (the trivial-ORTHOGONAL is determined by structural single-member-cluster status per guide §5.6, not by seed-notes framing). The seed notes' main contribution surfaced here is the explicit cascade-source framing the §4.1 + §4.2 + §4.7a + §4.10 cascade-arrow downstream framing operationalises per `internal_synthesis.md` §5.5.
- Literature methodology anchors:
  - [Daza 2018](../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) — primary anchor for Stage S₁'s multi-test synthesis discipline; degenerate single-member case at this cluster (no multi-test synthesis to do); cited at §4.5 5b L1 + §4.7a + §4.8 external-research scoping.
  - [Shamseer / CENT 2015](../../literature/methodology/shamseer_2015_cent_consort_nof1.pdf) — items 21+22 cluster-level limitations and generalisability; framework-validity-as-self-test discipline (cited indirectly via parent MD §6 framing).
  - [Tate / SCRIBE 2016](../../literature/methodology/tate_2016_scribe_single_case_reporting.pdf) — L4 participant-as-researcher transparency at cluster level (cited at §4.5 5b L4).
  - [Natesan 2023](../../literature/methodology/natesan_2023_nof1_evidence_reporting_systematic_review.pdf) — bar for defensible cluster-level coherence-call when within-subject multi-test evidence is the substrate; degenerate single-member case at this cluster.
- Phase-A precedent (load-bearing for §4.4 trivial-ORTHOGONAL discipline + §4.7a single-member width + cascade-source asymmetry): [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) (LOCKED r1 by user acceptance 2026-06-25; 2-member cluster; stand-alone (no cascade-arrow); contrast case for this single-member cascade-source cluster's structural asymmetry — see §4.1 "Asymmetry vs Phase A precedent" paragraph).
- Cluster-specific methodology MD (the framework-validity discipline this cluster's gate operationalises): [`bout_level_recovery_dynamics.md`](../../methodology/bout_level_recovery_dynamics.md) — LOCKED r2 2026-06-19 `c57ff3f`; §6 framework-validity discipline (the MD's own falsifier; this cluster's joint claim cites the framework-validity scope-bound from this MD per §4.5 5a caveat 3); §6.1 operand definition `bout_n_fast_recovery_day` (15/45 thresholds pre-committed); §6.2 three comparability bars (bars 1+2+3 verbatim inherited at this cluster's §4.3).
- HA11 v1 reference (project-internal): [`hypotheses/HA11-stress-udip/result.md`](../hypotheses/HA11-stress-udip/result.md) — SUPPORTED-on-train +22.8 pp `u_dip_count` discrimination; the reference signal this cluster's framework-validity gate tests reproduction against.
- [CONVENTIONS](../../CONVENTIONS.md) §3.4 crash-drop sensitivity at cluster level (per `internal_synthesis.md` §5.7 operationalised at §4.5 5d above; CLEAN reading inherited from member's `result.md` §5; no cluster-level coherence-call shift).
- [`_plan_results_analysis_layer.md`](../../methodology/_plan_results_analysis_layer.md) §3 stage map (this Stage S₁ implements); §3.5 missing-inputs flagging (this artefact's §4.9 implements); §3.6 conflict-resolution rule (no map-revision-needed halt fired at this draft per §6.1 walk in §9.6 below); §3.7 drift policy (drift triggers registered in §11 lock log); §3.8 stopping/completion criteria (user-acceptance is the binding completion event; deferred pending OI-016 + OI-018 + OI-019 joint resolution); §3.10 hard predictive gate (Stage S₁ does NOT cross; this cluster has no Stage A construct in map §5); §3.11 follow-up suggestions (§4.8 above implements); §3.12 commentary boundary (Stage S₁ does NOT carry per `internal_synthesis.md` §1 + §7.8); §4 producer/reviewer split table (`cluster-*.md` is reviewer-mode-with-authorization per the routing table).
- Rollout context: [`_rollout_order_2026-06-25.md`](../../methodology/_rollout_order_2026-06-25.md) §2 Phase B (single-member cluster C-bout-framework; D → I → S₁ only — no S₂ / A / T per map §3 methodology-validation scope; cascade-source for Phase C). **This Stage S₁ artefact's user-acceptance lock-event closes Phase B AND unblocks Phase C dispatch on C-bout-substance.**

---

## §9.6 refuse-to-lock gate walk

Per `internal_synthesis.md` §9.6 walk (item-by-item):

1. **The §4.5 L-ID block is missing an L-ID the map's §3 cluster row lists**: NO — every L-ID from the C-bout-framework cluster row (L1, L2, L3, L4, L6, L7; L5 NA) is cited at §4.5 5b with one-sentence project-specific cluster-level application. L5 NA is explicitly cited with one-sentence reason (no v24 primary signals; same as Stage D audit A5 + Stage I §4.5 5b L5 NA carry-forward).

2. **The cluster spans different era strata (per the limitations-doc §5 binding's "also cite L2" clause) and the L2-era-strata-mismatch citation is missing from §4.5 5b**: NOT APPLICABLE — the cluster has only one member (HA11-bout-redo) running on a single stratum (unmedicated × train only); the "also cite L2 if cluster members are from different era strata" clause does not trigger (no plural members in different strata). §4.5 5b L2 row records this explicitly per the limitations-doc r3 §5 "applies to cluster members in plural" reading + the Phase-A precedent's analogous handling at [`cluster-stress-fatigue-shape.md`](cluster-stress-fatigue-shape.md) §4.5 5b L2 row (both members on same stratum → no extra L2-era-strata-mismatch citation).

3. **The cascade-arrow precondition sub-paragraph (§4.5 5c) is missing on a cascade-downstream cluster**: NOT APPLICABLE — this cluster is the **cascade-source**, NOT cascade-downstream. Per `internal_synthesis.md` §4.5 5c rule for clusters without upstream cascade-arrow language ("C-stress-fatigue-shape; C-bout-framework as itself the upstream"), the §4.5 5c sub-paragraph is OMITTED at this cluster. §4.5 5c explicitly records the OMITTED state with one-sentence rationale. **§9.6 item passes**; the cascade-source designation lives at §4.7a + §4.10 cross-references (forward pointer to C-bout-substance) instead.

4. **§4.4 carries an invented label outside the four-label set (CONCORDANT / PARTIALLY CONCORDANT / CONFLICT / ORTHOGONAL)**: NO — §4.4 carries `trivial-ORTHOGONAL` which is the ORTHOGONAL label from the four-label set + the `trivial-` modifier per `internal_synthesis.md` §5.6 single-member degenerate case prescription (the `trivial-` qualifier is the guide-prescribed degenerate modifier, not a fifth label invention). §4.4 includes explicit dispatch-mode note confirming the guide §5.6 prescription is used over the generic dispatch-task fallback placeholder.

5. **§4.6 reads CONFLICT or PARTIALLY CONCORDANT but the open-conflict-preservation discipline is not honored (no both-readings paragraph, or auto-resolution-style language)**: NOT APPLICABLE per §4.4 trivial-ORTHOGONAL — no inter-HA agreement-disagreement exists at single-member cluster; §4.6 reads "Cluster members address different aspects of the shared construct; no conflict at the cluster level because the members do not speak to each other directly" per `internal_synthesis.md` §4.6 ORTHOGONAL rendering. Member-level lived-experience-prior placeholder OI-018 acknowledged but NOT re-litigated at cluster level per `internal_synthesis.md` §6.4 (cluster does NOT carry §4.6-equivalent).

6. **§4.7b does not address the four predictable overclaim shapes explicitly (the four-bullet list is mandatory; presence of all four sentences is the structural check)**: NO — the four-bullet list is present at §4.7b (mechanism-not-established; group-level-reach-not-established; predictive-claims-not-licensed including the explicit "this cluster has no Stage A construct per map §5" framing; joint-claim-does-not-average-the-framework-validity-asymmetry with the cascade-propagation-discipline structural-protection framing). All four overclaim shapes addressed with cluster-specific framing.

7. **§4.8 carries an external-research suggestion without explicit N=1-limit scoping (locked-plan §3.11 binding)**: NO — both external-research entries explicitly name the L-ID that prevents us from running them ourselves (cross-population framework-validity replication cites L1; cross-device-generation comparability cites L3 + the Firstbeat-input amplification caveat from pre-reg §8 caveat 5). Both respect locked-plan §3.11 binding.

8. **§4.7a joint claim width exceeds the narrowest member's licensed claim without independent-operationalisation cross-operationalisation replication warrant per §4.7a width discipline**: NO — joint claim width does not exceed (it collapses to) the single member's licensed claim per `internal_synthesis.md` §5.6 single-member case. No cross-operationalisation convergence available at single-member cluster; no width expansion attempted. The cascade-source designation in §4.7a is a downstream-framing addition, NOT a width expansion of the cluster's own joint claim per `internal_synthesis.md` §5.5 (cascade propagation is precondition framing, not co-equal verdict).

9. **§4.3 / §4.4 / §4.5 / §4.6 / §4.7 contain anti-pattern violations per `internal_synthesis.md` §7**: NO — walking each:
   - §7.1 (re-routing HAs to different clusters in-stage): NO; constellation is read verbatim from map §3 per §4.2 above; no proposed re-routing.
   - §7.2 (collapsing CONFLICT to a "middle" reading): NOT APPLICABLE per §4.4 trivial-ORTHOGONAL (no CONFLICT call); §4.7b bullet 4 explicitly addresses the analogous "averaging the framework-validity asymmetry" shape and refuses it.
   - §7.3 (treating cascade-arrow caveat-class precondition as co-equal verdict): NOT APPLICABLE at THIS cluster's coherence call (no upstream cascade-arrow precondition consumed here); recorded at §4.9 entry 3 cascade-arrow framing as the structural-discipline obligation imposed on Phase C C-bout-substance to NOT violate this anti-pattern when consuming this cluster's joint claim.
   - §7.4 (smuggling sister-cluster context into the current cluster's reading): NO; no sister-cluster evidence imported (the cluster has no sister-HA at the framework-validity layer per single-member status; cross-cluster context relegated to §4.10 cross-references + §4.7b bullet 2 group-level-reach refusal).
   - §7.5 (promoting one cluster's reading using another cluster's evidence): NO; no cross-cluster evidence used to strengthen or weaken this cluster's §4.4 trivial-ORTHOGONAL call.
   - §7.6 (treating seed-notes sketches as constraints on the coherence call): NO; §4.10 cross-references explicitly cites seed notes as advisory-only per `internal_synthesis.md` §7.6; trivial-ORTHOGONAL coherence call determined by guide §5.6 single-member case, NOT by seed-notes framing.
   - §7.7 (inventing new caveats post-hoc): NO; all §4.5 5a caveats are carry-forward from Stage I §4.5 5a (which is itself carry-forward from pre-reg §8 + Stage D §3 + §4.5); §4.5 5b L-IDs are inherited from map §3 row + member's Stage I §4.5 5b; no new cluster-level caveats invented.
   - §7.8 (producing §3.12 subject-narrative commentary at Stage S₁): NO; no "I notice", "in my experience" subject-narrative wording anywhere in this artefact; §4.6 inherits member-level OI-018 placeholder as caveat-class context per `internal_synthesis.md` §6.4 (cluster does NOT carry §4.6-equivalent or §3.12 commentary).
   - §7.9 (computing or citing predictive-quality measures): NO; no PPV, no base rate, no sensitivity/specificity; this cluster has no Stage A construct per map §5 (framework-validity-only); §4.7b bullet 3 explicitly addresses the predictive-claim refusal.
   - §7.10 (inventing new coherence-call labels): NO; the `trivial-ORTHOGONAL` label is the guide-prescribed §5.6 degenerate modifier of the ORTHOGONAL label from the four-label set, not a fifth label invention (per §4.4 explicit hard-rule observance paragraph).
   - §7.11 (auto-resolving open conflicts in subsequent re-examinations): NOT APPLICABLE at first-draft; no prior cluster `cluster-*.md` exists for this cluster to re-examine.

**Items that would fire on production-lock attempt**:

- **THREE substantive items** would fire on production-lock (LOCKED transition):
  1. **OI-016 (Stage D user-acceptance pending; via §4.9 entry 1)** — Stage D `descriptive_audit.md` must lock first (currently DRAFT r1; user-acceptance lock event pending); the DRAFT-on-DRAFT dispatch-mode placeholder cannot promote to LOCKED at this Stage S₁ until upstream Stage D locks. This is the DRAFT-on-DRAFT dispatch-mode constraint per skill r4 §"DRAFT-on-DRAFT dry-run discipline" transitively inherited via Stage I (which is itself DRAFT-on-DRAFT-DRY-RUN at commit `06146ce`).
  2. **OI-018 (Stage I §4.6 user-articulation pending; via §4.9 entry 2)** — Stage I `interpretation.md` §4.6 DEFAULTED-PENDING-USER-INPUT placeholder per LOCKED-r4 skill §"Dispatch-mode placeholders" must transition to USER-ARTICULATED before Stage I production-lock; this Stage S₁ inherits the placeholder per §4.5 5b L4 + `internal_synthesis.md` §6.4 (cluster does NOT carry §4.6-equivalent), so its lock-event is gated on Stage I locking.
  3. **OI-019 (THIS artefact's user-acceptance pending; via §4.9 entry 3 — NEW THIS ARTEFACT)** — this Stage S₁ artefact's structurally-complete dispatch-mode draft requires user explicit acceptance per locked-plan §3.8 binding completion event; OI-019 IS this entry. Per the Phase-A precedent (HA-C3.md §4.6 + HA-C3p.md §4.6 + cluster-stress-fatigue-shape.md §4.4 joint user-acceptance lock pattern), one user-articulation session resolves all three gates (OI-016 + OI-018 + OI-019) jointly at the user's separate non-dry-run lock session.

- **ZERO substantive §9.6 fires** on the current draft content: all 9 §9.6 substantive checks (L-ID completeness; era-strata-mismatch L2 binding; cascade-source 5c OMITTED rule; four-label-set + degenerate modifier; CONFLICT/PARTIALLY-CONCORDANT open-conflict-preservation NOT-APPLICABLE per trivial-ORTHOGONAL; §4.7b four-overclaim-refusal completeness; §4.8 N=1-scoping; §4.7a width discipline single-member collapse; §7 anti-pattern walk all 11 items) pass cleanly on the dispatch-mode draft. The three items above are dispatch-mode + user-acceptance gates, NOT substantive content fires.

The artefact is structurally complete on every §9.6 substantive check; the lock event itself awaits OI-016 + OI-018 + OI-019 joint resolution in the user's separate non-dry-run lock session per Phase-A precedent (one session resolves all three jointly; closes Phase B AND unblocks Phase C dispatch on C-bout-substance).

## §11 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-06-27 | Drafted r1 (DISPATCHED-BY-ORCHESTRATOR ON USER AUTHORISATION; DRAFT-ON-DRAFT-DRY-RUN dispatch mode) | Reviewer-mode-with-authorization by `/research-interpret synthesise C-bout-framework` skill on first invocation for this target (skill LOCKED r4). Phase B of `_rollout_order_2026-06-25.md` (single-member cluster C-bout-framework; cascade-source for Phase C C-bout-substance per `synthesis_structure_map.md` r3 §3). **Closes Phase B of the locked rollout** (D → I → S₁ chain for HA11-bout-redo complete; no S₂ / A / T for this methodology-validation-only cluster per map §3 cluster cell). Stage I upstream `interpretation.md` (DRAFT r1 dispatched-by-orchestrator 2026-06-26 at commit `06146ce`; DRAFT-ON-DRAFT-DRY-RUN dispatch mode; user-acceptance lock event PENDING at OI-016 + OI-018) consumed as PROVISIONAL-by-orchestrator-authorisation per skill r4 §"DRAFT-on-DRAFT dry-run discipline" — the placeholder enables Stage S₁ draft on DRAFT-r1 source in dry-run mode (otherwise guide #3 §9.2 refusal-path would halt). Stage D upstream `descriptive_audit.md` (DRAFT r1 at commit `980a541`; TRUSTED verdict-trust call) transitively inherited as TRUSTED-PROVISIONAL-by-orchestrator-authorisation via Stage I. §4.4 coherence call **trivial-ORTHOGONAL** per `internal_synthesis.md` §5.6 single-member cluster prescription (guide-prescribed degenerate label, not a generic dispatch-task fallback placeholder; the guide is explicit on this case). §4.7a joint claim collapses to single member's Stage I §3 licensed claim (no width expansion possible at single-member cluster); cascade-source designation propagates as caveat-class precondition to C-bout-substance for Phase C consumption per `internal_synthesis.md` §5.5. OI-019 appended to worktree-local `_open_inputs.md` per skill responsibility #6 (mirrors OI-016 + OI-018 user-acceptance dispatch-mode pattern); methodology agent absorbs in their next commit cycle per established Stage D + Stage I cherry-pick discipline at this Phase B. **Drift triggers registered** (manual-pending-skill per skill responsibility #10): (1) HA11-bout-redo `interpretation.md` re-examined or downgraded (per `_plan_results_analysis_layer.md` §3.7 drift policy — e.g., user-acceptance lock event resolving OI-016 + OI-018 may surface a Stage I revision); (2) a new HA joins the C-bout-framework cluster (per `synthesis_structure_map.md` update — e.g., HA11-bout-redo-v2 operand-refinement per pre-reg §9.2 OPTIONAL path, OR HA11-bout-redo-validate sister, OR a new framework-validity sister per §4.8 own-research track); (3) cited methodology MD changes lock-version (especially `research_line_limitations.md` since L-ID citations propagate; `internal_synthesis.md` guide; `bout_level_recovery_dynamics.md` parent MD `c57ff3f`; `synthesis_structure_map.md` r3 cluster-cell cascade-arrow language; `_plan_results_analysis_layer.md` r5 §3.6 / §3.7 / §3.10 / §3.11 / §3.12 rule changes); (4) ≥6 months elapse since lock (cadence check per locked-plan §3.7). **STATUS: DRAFT r1 — dispatched-by-orchestrator on user authorisation; NOT LOCKED — awaiting (a) user acceptance per `_plan_results_analysis_layer.md` §3.8 binding completion event, resolving OI-016 (Stage D upstream lock) + OI-018 (Stage I §4.6 user-articulation) + OI-019 (THIS Stage S₁ user-acceptance) in one session per Phase-A precedent (one session resolves all three jointly per the established multi-stage lock pattern); AND (b) fresh-session `/research-review` per `_plan_results_analysis_layer.md` §4 row for `cluster-*.md`. Phase B closes on this lock-event; Phase C dispatch on C-bout-substance unblocks on this lock-event per `internal_synthesis.md` §9.2 cascade-arrow precondition refusal-path.** |
