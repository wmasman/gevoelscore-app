# HA-C4c — descriptive precondition audit

**Status**: **LOCKED r1 2026-07-08** by user acceptance per [`_plan_results_analysis_layer.md`](../../../methodology/_plan_results_analysis_layer.md) §3.8 binding completion event at the Phase C cluster-level joint lock session (Q B1 accepted TRUSTED as-is, no reaudit requested). Producer-mode Stage D audit per [`descriptive_precondition_audit.md`](../../../methodology/descriptive_precondition_audit.md) (LOCKED r2 2026-06-24). Stage D producer-mode artefacts do NOT receive a fresh-session `/research-review` pass per plan §4 (the audit is itself the reviewer-mode-equivalent of Stage D). Phase C of [`_rollout_order_2026-06-25.md`](../../../methodology/_rollout_order_2026-06-25.md) (single-member cluster **C-bout-substance**; cascade-receiver from Phase B **C-bout-framework** which LOCKED r1 2026-07-07 per the [`synthesis_structure_map.md`](../../../methodology/synthesis_structure_map.md) §3 cluster table). First-ever invocation of Stage D on HA-C4c.

Per §3 of guide #1, producer-mode audits do NOT carry an `## Authorship` block. User explicit acceptance per §3.8 of the locked plan is the binding completion event.

---

## 1. Target HA + result reference

- **HA ID**: HA-C4c (revision r2)
- **Pre-reg lock date**: 2026-06-23 (r2 LOCKED at [`hypothesis.md`](../../hypotheses/HA-C4c/hypothesis.md) commit `4e666a2`; substantive absorb `310e145`; fresh-session re-audit clean at [`reviews/HA-C4c-2026-06-23-v2-reaudit.md`](../../../reviews/HA-C4c-2026-06-23-v2-reaudit.md) under Option γ canonical 4-stage)
- **Result lock date**: 2026-06-23 (test execution commit `a69a8ed`; worktree HEAD at run `a59997a`; pipeline commit `d5b394c`; per [`result.md`](../../hypotheses/HA-C4c/result.md) Authorship)
- **Headline verdict from `result.md`** (verbatim §1 + §2): **PARTIAL** — direction=+, (a) discrimination PASS at block-permutation p = 0.0001, (b) effect-size FAIL at Cliff's δ = +0.120 vs pre-committed threshold ≥ +0.20 (95% CI [+0.064, +0.178]). Per-bar table: direction PASS (δ = +0.120 > 0); bar (a) PASS (p = 0.0001 < 0.05); bar (b) FAIL (δ = +0.120 < +0.20). Inverse failure-mode configuration vs HA11-bout-redo's PARTIAL (which had bars 1+2 PASS + bar 3 p FAIL at n_calm=70/n_crash=11); HA-C4c at n_heavy=465/n_non_heavy=809 has p PASS + effect-size FAIL — the cross-phase pooling per parent MD §5.3 + recalibration §6 architectural-implications cleared the power-shortfall constraint the cascade-context anticipated, and the substantive finding is a **weak-effect-but-real positive pattern** per pre-reg §9.2 second PARTIAL configuration.
- **Effective n on the primary cell** (verbatim from `result.md` §3): n_heavy = **465** days; n_non_heavy = **809** days (total primary-stratum non-NaN days = **1274** per §9 verification log — matches n_heavy + n_non_heavy exactly, no exclusion-gate residual). Heavy-T mean `bout_n_did_not_return` = **0.742** bouts/day (median 1.00); non-heavy-T mean = **0.588** bouts/day (median 1.00). Per-day-pool `n_did_not_return` bouts in heavy-T pool = **821** (sum across days); analogous n reported per-arm per pre-reg §4.11 named-count discipline.
- **Operationalisation summary** (one sentence, paraphrasing pre-reg §1 + §4): substantive Wiggers C4 retest at bout resolution on operand `bout_n_did_not_return_day` (per-day count of bouts with `did_not_return_flag == True` per parent MD §3.2 — bouts whose stress did not return to within +5 of `pre_bout_baseline` for ≥ 10 consecutive minutes within 180 minutes after peak) tested as heavy-T-vs-non-heavy-T contrast (`exertion_class_lagged_lcera ∈ {heavy, very_heavy}` on T per HA-C4 v2 §4.1) on the **cross-phase-pooled stratum** on the `citalopram_phase` axis (`citalopram_phase(d) ∈ {unmedicated, buildup, consolidation, afbouw, post_afbouw}`; equivalently `recovery_phase(d) ∈ {sub-phase 4b (pacing_habit_established), phase 5 (citalopram_modulated)}` per the cross-axis layering; days `>= 2022-11-17` LC era; April 2024 cluster excluded), evaluated via Mann-Whitney U + Cliff's δ + block-permutation null at E[L]=7 (B=10,000; seed `20260623`) against pre-committed verdict bands (SUPPORTED requires both bars; PARTIAL is direction-correct + exactly one bar; REJECTED is direction-wrong or both-bars-fail or crash-drop sign-flip; INCONCLUSIVE is walk-forward-gate or dry-run-sanity failure).
- **Methodology MDs cited by the pre-reg** (link list, per pre-reg Authorship "verification log" + §3 + §4 + §10):
  - [`bout_level_recovery_dynamics.md`](../../../methodology/bout_level_recovery_dynamics.md) — parent MD LOCKED r2 `c57ff3f` 2026-06-19; §3 bout-detection rule (peak + return-window + baseline); §3.2 `did_not_return_flag` semantics; §3.4 day-validity gate + motion-confound handling; §4 per-bout feature set including `bout_n_did_not_return_day` per-day aggregation; §5.1 stationary-bootstrap E[L]=7 inheritance; §5.3 Approach A cross-phase headline discipline; §5.5 framework-validity restriction to unmedicated (binds HA11-bout-redo; HA-C4c is the downstream substantive test); §6 framework-validity discipline / MD's own falsifier.
  - [`bout_level_dose_response_calibration.md`](../../../methodology/bout_level_dose_response_calibration.md) — sub-MD LOCKED r4 `fb97d1c` 2026-06-23; §6 inheritance table populated to **0/7 features CONFIRMED at discriminative bar** at this corpus's per-window n → Approach A NOT load-bearing → HA-C4c primary is dose-naive with cross-phase pooling permitted at bout-level per the §6 architectural-implications paragraph.
  - [`permutation_null_block_length.md`](../../../methodology/permutation_null_block_length.md) — §4.7 stationary-bootstrap E[L]=7 inheritance via parent MD §5.1; data-driven E[L]\* companion + factor-of-2 flag per CONVENTIONS §3.6.
  - [`lc_era_temporal_segmentation.md`](../../../methodology/lc_era_temporal_segmentation.md) — Stratum 4 binding (2022-04-04 → 2024-04-08 boundary; LC era gating); M1/M2/M3 sub-segmentation warrant framework.
  - [`citalopram_phase_stratification.md`](../../../methodology/citalopram_phase_stratification.md) — §3 the five-phase `citalopram_phase` axis (`unmedicated / buildup / consolidation / afbouw / post_afbouw`); April 2024 cluster exclusion; §4 CONFIRMED-channel inheritance discipline (NOT invoked at HA-C4c primary because 0/7 CONFIRMED per sub-MD §6).
  - [`lc_recovery_phase_axis.md`](../../../methodology/lc_recovery_phase_axis.md) — LOCKED r1 `d47e0d3` 2026-06-19; canonical 1/2/3/4a/4b/5 recovery phase numbering; §1.3 relationship-to-existing-axes layering; §3.4b sub-phase 4b left edge at 2022-11-17 (8-week-post-ergotherapie M1 lived-experience boundary); §3.5 phase 5 = citalopram_modulated; the cross-axis layering that HA-C4c invokes at §4.2.
  - [`phase_axis_collapsibility_conventions.md`](../../../methodology/phase_axis_collapsibility_conventions.md) — §3.4 Tier B/C collapsibility conventions (NOT invoked for HA-C4c bout-level operand at this corpus per recalibration §6; the cross-phase pooling permission at bout-level bypasses the daily-aggregate Tier B binding).
  - [`hypothesis_lock_process.md`](../../../methodology/hypothesis_lock_process.md) — §3.2 step 4 mandatory dispatches; §3.5 + §3.6 r2 compression-acceptability + Option γ canonical 4-stage; §3.8 four lock-blocking gates; §3.9 dry-run halt discipline.
  - [`symptom_mention_asymmetry.md`](../../../methodology/symptom_mention_asymmetry.md) — variable-class taxonomy (cited via the Stage D §5.5 A5 binding; HA-C4c uses only Garmin-derived bout-level operands + `exertion_class_lagged_lcera` classifier + `crash_v1` labels, no v24 columns).
  - [`nightly_attribution.md`](../../../methodology/nightly_attribution.md) — wake-up-date convention (cited via Stage D §5.6 A6 binding; HA-C4c is daytime bout-level, no nightly column).
  - [`train_validate_split_fate.md`](../../../methodology/train_validate_split_fate.md) — single-pool primary discipline (per Stage D §5.8 A8 binding; HA-C4c is single-pool cross-phase-pooled with unmedicated-only as a §4.10 sensitivity arm, NOT a train/validate split).
  - [CONVENTIONS.md](../../../CONVENTIONS.md) — §3.1 personal baseline; §3.4 crash-drop sensitivity; §3.6 named counts + factor-of-2 flag discipline; §4.1-§4.3 descriptive-before-inference + caveats + confirmatory; §4.4 no cross-test pass conclusion at result-emission.
  - Literature anchor: [`literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf`](../../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) — power-calc dispatch per §3.2 step 4 mandatory.

## 2. Load-bearing assumptions enumerated

| # | Assumption | Source |
|---|---|---|
| A1 | Sample size on every reported cell ≥ pre-registered floor: n ≥ 30 per arm per §4.7 walk-forward gate (INCONCLUSIVE routing on shortfall); §7 dry-run sanity bounds on per-day mean/median/σ pre-committed | Pre-reg §4.7 (walk-forward gate) + §7 (expected effect size + sanity ranges) + §10.4 (dry-run HALT triggers) + `test.py` (gate evaluation logic) |
| A2 | Missingness pattern is MCAR/MAR-compatible OR missingness-aware operationalisation used + documented; bout-level operand emits 0 on valid days with no `did_not_return` bouts vs blank/NaN on §3.4-invalid days | Pre-reg §3 + §4.4 (day-validity gate inherited from parent MD §3.4) + §6 (exclusion rules) + pipeline README (`bout_n_did_not_return` NaN semantics) |
| A3 | Block-length E[L]=7 is appropriate for autocorrelation structure of `bout_n_did_not_return_day[d]` on the cross-phase-pooled stratum under stationary-bootstrap resampling of the `is_heavy_T[d]` label sequence | Pre-reg §4.6 inherits [`permutation_null_block_length.md`](../../../methodology/permutation_null_block_length.md) via parent MD §5.1; pre-reg §4.6 requires data-driven E[L]\* + factor-of-2 flag per CONVENTIONS §3.6 |
| A4 | Era / stratum binding honored: LC era (`>= 2022-04-04`) × cross-phase pool on `citalopram_phase` axis restricted on `recovery_phase` axis to sub-phase 4b + phase 5 (left edge 2022-11-17 per §3.4b of `lc_recovery_phase_axis`) × April 2024 cluster excluded × 21-day device-baseline-lag excluded | Pre-reg §4.2 + §4.4 + §6 (exclusion rules); cites [`lc_era_temporal_segmentation.md`](../../../methodology/lc_era_temporal_segmentation.md) + [`citalopram_phase_stratification.md`](../../../methodology/citalopram_phase_stratification.md) + [`lc_recovery_phase_axis.md`](../../../methodology/lc_recovery_phase_axis.md) + parent MD §3.4 |
| A5 | v24 presence-conditioned semantics respected — NOT APPLICABLE here (no v24 primary signals; `bout_n_did_not_return_day` is derived from per-minute Garmin stress + `did_not_return_flag`; `exertion_class_lagged_lcera` is Garmin-training-load-derived; `crash_v1` labels used only in §4.10 crash-drop sensitivity arm) | NOT APPLICABLE |
| A6 | Nightly attribution per `nightly_attribution.md` — NOT APPLICABLE (bout-level daytime test on per-minute stress trace; no sleep-derived primary signal; gevoelscore NOT in primary cell per synthesis map §3 cluster row) | NOT APPLICABLE |
| A7 | Effect-size direction reported alongside p-values, never alone | Pre-reg §4.6 + §10.3 (`result.md` template requiring per-bar table + per-arm summary + per-arm named counts per §4.11); `result.md` §2 + §3 report Cliff's δ + 95% CI + Mann-Whitney U + block-perm null summary alongside p |
| A8 | Train/validate split fate per `train_validate_split_fate.md` — HA-C4c primary is **single-pool cross-phase-pooled** (per pre-reg §4.2 + §5.0 single-cell headline lock); unmedicated-only arm is a §4.10 sensitivity arm reporting descriptively; no per-portion confirmatory verdict on any train/validate split | Pre-reg §4.2 + §5.0 (single-cell headline lock) + §4.10 (sensitivity arms cannot promote to SUPPORTED); post-2026-06-13 (split-fate MD lock) → discipline applies in full |
| A9 (HA-specific) | **Cross-phase pooling discipline inheritance from parent MD §5.3 + sub-MD §6** — the pool across the `unmedicated → buildup` boundary (= 4b → 5 boundary on recovery axis) is permissible at bout-level at this corpus's per-window n **because and only because** the sub-MD §6 recalibration returned 0/7 features CONFIRMED at the discriminative bar → Approach A NOT load-bearing → no per-channel inheritance binding at the per-bout layer per [`phase_axis_collapsibility_conventions.md`](../../../methodology/phase_axis_collapsibility_conventions.md) §3.4 Tier B (which binds daily-aggregate CONFIRMED-citalopram channels but not bout-level features at this n). Cross-phase pooling permissibility is conditional on the recalibration reading; a future re-run with CONFIRMED features would re-open the pool. | Pre-reg §4.2 + §8 caveat 8 (cross-phase-pooling-permissibility conditional); parent MD §5.3 Approach A + §5.5 framework-validity restriction; sub-MD [`bout_level_dose_response_calibration.md §6`](../../../methodology/bout_level_dose_response_calibration.md) architectural-implications paragraph; STOCKTAKE §6 cross-section synthesis + 2026-06-23 naming-discipline note at `4b53ea1` |
| A10 (HA-specific) | **Bout-extraction pipeline trust**: `extract_stress_bouts.py` LOCKED `d5b394c` 2026-06-22 correctly implements parent MD §3 (bout-detection rule) + §4 (per-bout feature set); per-day `bout_n_did_not_return` joined into `per_day_master.csv` from `per_bout_aggregations_daily.csv` via `03_consolidate/build_unified_dataset.py` with correct NaN semantics | Pre-reg §3 + §10.1 (pre-flight pipeline-trust check) + §10.4 (smoke tests PASS at run-time); pipeline `extract_stress_bouts.py` LOCKED `d5b394c` 2026-06-22; 9 inline smoke tests re-confirmed PASS at HA-C4c test run per `result.md` §8; 5-bout spot-check verification per `pipeline/02_features/README.md` |
| A11 (HA-specific) | **Approach A inheritance-by-analogue discipline (§4.9)** — the descriptive-companion construction sign-flipping `bout_n_fast_recovery_day`'s buildup-post-CPAP β = −0.056/mg [CI −0.145, +0.034] p=0.223 to +0.056/mg for HA-C4c's +1 prior direction is honestly framed as inheritance-by-analogue under the underpowered-NULL frame (NOT inheritance-by-recalibration on the operand itself); the CI-bracket sub-arm characterises inheritance fragility (structurally a NULL-bracket since the source β's CI crosses zero), NOT a substantive precision check on a CONFIRMED β. Load-bearing that this construction is reported as a descriptive companion, not silently promoted to a load-bearing dose correction. | Pre-reg §4.9 r2 (underpowered-NULL framing leads; five-surface precedent from [`reviews/bout_level_dose_response_calibration-r3-2026-06-22.md §3`](../../../reviews/bout_level_dose_response_calibration-r3-2026-06-22.md)); pre-reg §8 caveat 3 (β-recalibration dose-naive primary framing); sub-MD §6 architectural-implications paragraph; `result.md` §5 Approach A sub-arm table explicit "descriptive companion only" framing |
| A12 (HA-specific) | **Motion-confound corpus property as operand-inherited caveat**: 99.3% of extracted bouts (4285/4317) carry `motion_confound_flag=True` on this corpus × extraction threshold — a structural finding about the per-minute stress trace at HA11-bout-redo lock time that the HA-C4c operand inherits as-is. Load-bearing that the "during rest periods" Wiggers-verbatim semantic is operationalised **across the motion-tagged bout pool** by construction (parent MD §3.4 default includes motion-confounded bouts in primary); the motion-clean-only §4.10 sensitivity arm's near-zero-signal REJECTED outcome is a corpus-property reaffirmation, NOT a fragility of the primary verdict. | Pre-reg §8 caveat 4 (99.3% motion-confound corpus property); parent MD §3.4 (motion-confound handling default = include in primary); HA11-bout-redo `result.md` §4 motion-clean-only arm finding (upstream cascade-context source); HA-C4c `result.md` §5 motion-clean-only arm reaffirmation paragraph |
| A13 (HA-specific) | **Bout-level operand-threshold validity**: the 180-minute forward cap + +5 baseline tolerance + ≥10-minute return-hold thresholds in the `did_not_return_flag` definition (parent MD §3.2) are pre-committed at parent MD lock and inherited verbatim by HA-C4c without re-derivation; the operand IS the test of operand-fitness (via the framework-validity gate at HA11-bout-redo which cleared bars 1+2), so the operand definition cannot be re-derived to fit the HA-C4c result. The dry-run §7 sanity-check bounds on the per-day distribution (mean [0.3, 1.5]; median ≤ 4; σ [0.5, 2.0]) are pre-committed sanity-check bounds, NOT calibration-fit checks. | Parent MD §3.2 return-window rule (LOCKED at parent MD lock `c57ff3f` 2026-06-19); pre-reg §4.1 (LOCKED; verbatim inheritance; NOT re-deriving); pre-reg §7 (pre-committed sanity-check ranges); `result.md` §4 dry-run sanity gates all PASS at run-time (mean 0.6444 in [0.3, 1.5]; median 1.00 ≤ 4; σ 0.6703 in [0.5, 2.0]) |

## 3. Per-assumption status

| # | Status | Citation / reason | Stocktake diff |
|---|---|---|---|
| A1 | **BACKSTOPPED** | Per `result.md` §3 per-arm summary + §9 verification log dry-run gates: primary cell n_heavy = **465** and n_non_heavy = **809** both PASS §4.7 walk-forward gate ≥ 30 per arm with comfortable margin (each arm is ~15× the floor); primary-stratum non-NaN total = **1274** matches n_heavy + n_non_heavy exactly. Per-day dry-run sanity gates (§7 + §10.4) all PASS at run-time: mean = 0.6444 (in expected [0.3, 1.5]); median = 1.00 (≤ 4); σ = 0.6703 (in expected [0.5, 2.0]). The §4.7 walk-forward gate's design-level INCONCLUSIVE routing IS the small-n absorber per guide #1 §5.1 stocktake-precedent ("the routing IS the backstop"); §10.4 HALT-on-dry-run is the sanity-gate absorber. All sensitivity arms with valid n also comfortably clear the ≥30 floor (unmedicated-only n_heavy=183/n_non_heavy=323; motion-clean n_heavy=465/n_non_heavy=809; transient-excluded n_heavy=465/n_non_heavy=809; baseline-invalid-excluded n_heavy=465/n_non_heavy=809; crash-drop n_heavy=441/n_non_heavy=745; all in `result.md` §5). CONVENTIONS §3.6 named-count discipline satisfied per `result.md` §3 explicit per-arm reporting. | No diff — STOCKTAKE §2d row implicitly reads A1 BACKSTOPPED via the "cross-phase pooling cleared HA11-bout-redo's bar-3 power-shortfall constraint as the cascade-context expected" framing. |
| A2 | **BACKSTOPPED** | Pre-reg §4.4 inherits parent MD §3.4 day-validity gate verbatim: `bout_n_did_not_return` emits `0` on valid days with no qualifying `did_not_return` bouts; emits blank/NaN on §3.4-invalid days (< 600 valid per-minute stress samples OR first 21 days of `has_garmin_uds=True` OR April 2024 cluster). This IS the missingness-aware operationalisation per guide #1 §5.2 operational test path (a): the pipeline's NaN semantics are explicitly documented in `result.md` §6 exclusion-rules bullets, and exclusion rules (§6 of pre-reg) name every excluded day-class. Per `result.md` §9 verification log the primary-stratum non-NaN days figure is **1274** (matches n_heavy + n_non_heavy exactly = 465 + 809 — no residual gate-excluded days that were non-NaN but excluded by exertion-classification NaN etc.; the gating is transparent). Per parent MD §3.4 + pre-reg §10.1 the pipeline's 9 inline smoke tests + 5-bout spot-check verification per `pipeline/02_features/README.md` provide audit-level coverage of NaN handling. **L7 (survivorship) attribution**: the §3.4 day-validity gate reduces but does not eliminate survivorship reach (days the device did not capture are absent from the analysis pool by construction); the gating discipline supplies path (a) of guide #1 §5.2 operational test but does not produce a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return`. Retains BACKSTOPPED (not BACKSTOPPED-partial) because (a) the missingness-aware operationalisation IS the §3.4 gate + the NaN-vs-0 semantic distinction is explicitly documented, AND (b) the primary-stratum non-NaN figure (1274 days across the cross-phase-pooled stratum spanning 2022-11-17 through post_afbouw) is per-pipeline named-count reported in `result.md` §9 itself. This mirrors HA11-bout-redo A2 BACKSTOPPED reading exactly on the sister operand `bout_n_fast_recovery_day` per its Stage D audit §3. **Stocktake-diff narrowing consideration**: a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` does not yet exist (stocktake §3 Shared gap 3 pattern); this is exactly the OI-017 cascade-context entry HA11-bout-redo Stage D opened (which named "carry-forward for HA-C4c Stage D audit — same operand-class on cross-phase pool"); the closure would tighten HA-C4c A2 in the same step as HA11-bout-redo A2 (same script template from `desc-SMS`, one S-effort per-channel run covering the two sister operands). Not blocking. | No diff — STOCKTAKE §2d reads the primary-stratum missingness backstop implicitly via the "dry-run sanity gates all PASS" framing. |
| A3 | **BACKSTOPPED** | Per `result.md` §4 E[L]\* data-driven companion (pre-reg §4.6 + parent MD §5.1 inheritance): **E[L]\* on `bout_n_did_not_return_day[d]` series over cross-phase-pooled stratum = 7.00 days** (cutoff_lag = 1); factor-of-2 deviation flag **NOT FLAGGED** (`|E[L]\* − 7| / 7 = 0 ≤ 0.5` per CONVENTIONS §3.6); block-length choice is exactly-calibrated. The data-driven E[L]\* on the test's own predictor channel on the actual primary stratum provides first-hand backstop on the specific cells the test reports on (not a single-cell descriptive per anti-pattern §7.2). The p = 0.0001 discrimination PASS is not attributable to a block-length mis-specification. This exactly mirrors HA11-bout-redo A3 BACKSTOPPED at the same E[L]\* = 7.00 exact figure on the sister operand — an independent replication of the block-length calibration at bout-level resolution. | No diff — STOCKTAKE §2d explicitly cites "E[L]\* data-driven 7.0 exactly (no factor-of-2 flag)". |
| A4 | **BACKSTOPPED** | Pre-reg §4.2 cites [`citalopram_phase_stratification.md §3`](../../../methodology/citalopram_phase_stratification.md) for the 5-phase `citalopram_phase` axis (`unmedicated / buildup / consolidation / afbouw / post_afbouw`); §4.2 cites [`lc_recovery_phase_axis.md §1.3 + §3.4b + §3.5`](../../../methodology/lc_recovery_phase_axis.md) for the cross-axis layering — sub-phase 4b (`pacing_habit_established`; 2022-11-17 → 2024-04-08) UNION phase 5 (`citalopram_modulated`; 2024-04-09 onward). Pre-reg §4.4 enforces 21-day device-baseline-lag per parent MD §3.4 (inherited verbatim) + April 2024 cluster exclusion (2024-04-09 → 2024-04-16). Pre-reg §6 exclusion rules make each boundary explicit. The r2 substantive absorb (`310e145`) fixed a 5-surface column-confusion (r1 named the recovery-phase labels as `citalopram_phase` values; r2 corrected all 5 downstream surfaces + the upstream STOCKTAKE §6 source-of-bug at `4b53ea1` with an inline naming-discipline note) — the fresh-session re-audit at `HA-C4c-2026-06-23-v2-reaudit.md` cleared this cleanly, so the era/stratum binding is not just cited but audited-clean at pre-reg lock. Test-time execution binding confirmed per `result.md` §9 verification log: primary-stratum non-NaN days = 1274 across the layered stratum; matches expected primary-cell shape per pre-reg §4.7 expected-n anchor (~317 heavy-T + ~569 non-heavy-T ≈ 886 pre-eligibility → 1274 after enumerated over cross-phase pool = plausible). **Era/stratum binding is a project-wide operational consequence** binding across all HAs invoking cross-phase pooling at bout level per guide #1 §5.4 "BACKSTOPPED via methodology MD" acceptable pattern. | No diff — STOCKTAKE §2d row implicitly reads A4 as BACKSTOPPED via the layered-stratum framing. |
| A5 | **NOT APPLICABLE** | No v24-derived columns (`cat_*`, `state_*`, `day_dominant_polarity`, `per_day_intensity`) appear in pre-reg §3 data sources or §4 operationalisation. Primary signals are `bout_n_did_not_return_day` (derived from per-minute Garmin stress via `extract_stress_bouts.py` at the pipeline layer) + `exertion_class_lagged_lcera` (Garmin training-load-derived per pre-reg §3 + [`citalopram_phase_stratification.md`](../../../methodology/citalopram_phase_stratification.md) + v3.2 lagged-baseline conventions); the `crash_v1` labels used only in §4.10 crash-drop sensitivity arm are NOT v24-derived (source: `crash_v2-definition/labels_crash_v2.csv`, HA11 v1 primary outcome from Garmin-derived Firstbeat signals). Per `symptom_mention_asymmetry.md` variable-class taxonomy: NOT APPLICABLE with one-sentence reason "no v24 primary signals; bout-level Garmin-derived operand + Garmin-derived heavy-T classifier + Garmin-derived crash-drop labels only". Stage I inherits no L5 citation obligation per guide #1 §5.5 final paragraph. | No diff — synthesis map §3 cluster row explicitly annotates "L5 NA — no v24" for C-bout-substance. |
| A6 | **NOT APPLICABLE** | Daytime bout-level test on per-minute stress trace; the bout-extraction rule operates on the daytime window per parent MD §3 (per-minute stress aggregation excluding sleep window). No sleep-derived primary signal in scope. Gevoelscore is NOT in the primary cell per synthesis map §3 cluster row ("L6 NA — gevoelscore not in primary cell"). Reason: "daytime bout-level test on per-minute stress; no nightly column; gevoelscore not in primary cell". | No diff — synthesis map §3 cluster row explicitly annotates "L6 NA — gevoelscore not in primary cell" for C-bout-substance. |
| A7 | **BACKSTOPPED** | Per `result.md` §2 per-bar table + §3 per-arm summary + §5 sensitivity-arms table + §6 Holm table: every cell with a p-value carries a directional effect-size statistic alongside — Cliff's δ = **+0.120** (signed; positive matches predicted direction) with 95% CI [+0.064, +0.178] (paired-bootstrap B=2000); heavy-T mean 0.742 vs non-heavy-T mean 0.588; Mann-Whitney U = 210614 with one-sided normal-approx p = 0.0002 (descriptive); block-permutation p = 0.0001 (B=10000, E[L]=7, seed `20260623`); block-perm null delta median = −0.000 with 95% CI [−0.061, +0.060]; per-arm named counts (n_did_not_return bouts in heavy pool = 821) reported per pre-reg §4.11 CONVENTIONS §3.6 named-count discipline. Per-bar verdict table (`result.md` §2) reports observed value alongside threshold for every bar (direction / (a) discrimination / (b) effect-size). Every sensitivity arm in §5 reports Cliff's δ + block-perm p + verdict pair — no p-value alone. Every Approach A sub-arm reports β + Cliff's δ + block-perm p + verdict. This is stronger than HA11-bout-redo A7's coverage (which reported disc/median-signed-z/Cliff's δ/M-W U in a similar per-arm table). | No diff — STOCKTAKE §2d explicitly quotes δ = +0.120 alongside p = 0.0001. |
| A8 | **BACKSTOPPED** | Pre-reg drafted 2026-06-23, well after the 2026-06-13 split-fate MD lock — discipline applies in full. HA-C4c's primary is **single-pool cross-phase-pooled** (per pre-reg §4.2 + §5.0 single-cell headline lock: "the headline verdict is the SUPPORTED/PARTIAL/REJECTED/INCONCLUSIVE outcome on the single triple {cross-phase-pooled stratum × `bout_n_did_not_return_day` × heavy-T-vs-non-heavy-T}; all sensitivity arms are diagnostic/sensitivity ONLY"); the unmedicated-only §4.10 sensitivity arm is descriptive M3 sensitivity overlay only — no per-portion confirmatory verdict on primary + unmedicated as separate pools. `result.md` §5 reports the unmedicated-only arm as descriptive with "cannot promote to SUPPORTED" annotation; the REJECTED outcome on that arm is surfaced as stratum-fragility per §9.5 but the primary verdict is unchanged per §5.0 single-cell headline lock. No historical 2023-12-31 train/validate split is used at HA-C4c (that split lives at HA11 v1 / HA11-bout-redo; HA-C4c's cascade-source reference frame is HA11-bout-redo's unmedicated × train, but HA-C4c does not adopt it — cross-phase-pooling on the citalopram axis with the recovery-phase left-edge at 2022-11-17 is the pre-reg's primary). Discipline honored in full per guide #1 §5.8 operational test for post-2026-06-13 HAs. | No diff — STOCKTAKE §2d implicitly reads A8 as BACKSTOPPED via the "single-cell headline lock" + "sensitivity arms cannot promote" framing. |
| A9 (HA-specific) | **BACKSTOPPED** | Per sub-MD [`bout_level_dose_response_calibration.md §6`](../../../methodology/bout_level_dose_response_calibration.md) (LOCKED r4 `fb97d1c` 2026-06-23; PRECEDES HA-C4c r2 lock `4e666a2` 2026-06-23 → PRECEDES test execution `a69a8ed` 2026-06-23; no post-hoc contamination per guide #1 §7.3): the inheritance table returned **0/7 features CONFIRMED at the discriminative bar** at this corpus's per-window n (~49 day-clusters/window in buildup-post-CPAP; ~75-78 in afbouw). Per the §6 architectural-implications paragraph: Approach A is NOT load-bearing for any downstream HA pre-reg at this corpus's bout-level n → cross-phase pooling permitted without §5.A/B/C inheritance violation → HA-C4c primary is dose-naive with cross-phase pool as headline. STOCKTAKE §6 cross-section synthesis (r2 fix at commit `4b53ea1` 2026-06-23 with naming-discipline note; PRECEDES HA-C4c r2 lock) carries the same architectural-implications paragraph verbatim as the pre-registered permission. Pre-reg §4.2 + §8 caveat 8 make the conditional-nature-of-the-permission explicit ("cross-phase pooling permissibility is conditional on the recalibration's 0/7 CONFIRMED reading"); a future re-run producing CONFIRMED features would re-open the pooling permission. The cross-phase pooling permission is therefore backstopped by (a) sub-MD §6 architectural-implications paragraph pre-committed at r4 lock; (b) STOCKTAKE §6 naming-discipline note pre-committed at `4b53ea1`; (c) pre-reg §8 caveat 8 explicit conditional framing. **Observed manifestation in `result.md` §5**: the unmedicated-only sensitivity arm REJECTED at δ=+0.059 p=0.1145 vs primary PARTIAL at δ=+0.120 p=0.0001 IS the stratum-fragility descriptive signature of the cross-phase pooling being load-bearing for the substantive signal (per pre-reg §9.5). This is a **substantive descriptive finding** that Stage I will read as the "cross-phase pooling is doing analytical work" nuance; the audit reads the pooling permission itself as BACKSTOPPED (the recalibration's 0/7 CONFIRMED reading is the citation), and the stratum-fragility finding is a lesson for Stage I NOT a Stage-D-level assumption-backstop failure per guide #1 §6.4 (verdict-trust independent of verdict label). | Stocktake §2d row reads the cross-phase pooling as the substantive framing device but does not enumerate A9 explicitly; this audit makes the HA-specific row explicit per guide #1 §5.9. No conflict. |
| A10 (HA-specific) | **BACKSTOPPED** | Per pre-reg §3 + §10.1 + `result.md` §8: bout-extraction pipeline `extract_stress_bouts.py` LOCKED `d5b394c` 2026-06-22 (2026-06-22 PRECEDES HA-C4c r2 pre-reg lock 2026-06-23 PRECEDES test execution 2026-06-23; no post-hoc contamination per guide #1 §7.3); the pipeline's 9 inline smoke tests re-confirmed PASS at run-time (Test 1: canonical bout detected; Test 2: `did_not_return` fires + tail_length capped at 180; Test 3+3b: cross-midnight peak attribution; Test 4: NaN gaps in pre-window correctly fail hysteresis; Test 5: transient bout flagged; Test 6a/b/c: motion_confound flag logic) per `result.md` §8; 5-bout spot-check verification per `pipeline/02_features/README.md` provides audit coverage of the operand-computation path. Per STOCKTAKE §2d representative-day spot-checks (LANDED 2026-06-22): three per-day verifications specifically on `bout_n_did_not_return` — **crash day 2023-02-04 `bout_n_did_not_return=2`** (two sustained-180min-cap bouts consistent with the "stuck sympathetic" Wiggers reading); **calm day 2022-10-18 `bout_n_fast_recovery_day=2`** (67% fast-recovery dominates, low failure-to-return count implied); **heavy-exertion day 2024-04-18** peak=99 saturation + `did_not_return=1` (heavy-T day producing a non-return bout) — provide spot-check anchoring at pipeline-output level. Additionally backstopped by [`analyses/descriptive/operationalisation_support/bout_level_dose_response_calibration/`](../operationalisation_support/bout_level_dose_response_calibration/) which LANDED 2026-06-22 characterising the sister operand `bout_n_fast_recovery_day` distribution across 3 windows and confirming the pipeline is computable + distributionally well-behaved on the same commit `d5b394c`. Per `result.md` §8: "smoke tests re-confirmed PASS at run-time"; no pipeline-trust issues surfaced. Lock-dates: pipeline `d5b394c` (2026-06-22) → parent MD `c57ff3f` (2026-06-19) → sub-MD `fb97d1c` (2026-06-23) → HA-C4c r2 lock `4e666a2` (2026-06-23) → test execution `a69a8ed` (2026-06-23); pipeline + parent MD both PRECEDE pre-reg lock + test execution. | Stocktake §2 implicitly carries pipeline-trust under A2 + A10-equivalent backstops; this audit makes the HA-specific row explicit per guide #1 §5.9. No conflict. |
| A11 (HA-specific) | **BACKSTOPPED** | Per pre-reg §4.9 r2 underpowered-NULL framing (5-surface precedent from [`reviews/bout_level_dose_response_calibration-r3-2026-06-22.md §3`](../../../reviews/bout_level_dose_response_calibration-r3-2026-06-22.md); r2 absorb at `310e145` reordered §4.9 to lead with underpowered-NULL discipline BEFORE the sign-flip mechanics per the audit-fired L4.7 substantive framing-slip fix) + pre-reg §8 caveat 3: the Approach A construction is explicitly framed as **descriptive companion** using the analogue β as coefficient-of-convenience, NOT a CONFIRMED dose-correction on `bout_n_did_not_return_day`. The CI-bracket sub-arm is explicitly named as inheritance-fragility (NULL-bracket structural property of the underpowered-NULL frame), NOT a substantive precision check. `result.md` §5 Approach A sub-arm table (three sub-arms: primary template +0.056/mg → δ=+0.098 p=0.0075; CI lower bracket +0.145/mg → δ=+0.076 p=0.0352; CI upper bracket −0.034/mg → δ=+0.077 p=0.0259) is prefaced with the exact verbatim framing: "This is a **descriptive companion** under the underpowered-NULL frame; NOT a load-bearing dose-correction. The CI-bracket sub-arm characterises **inheritance fragility**, NOT a substantive precision check." `result.md` §10 caveat 3 restates this load-bearingly. The Holm family per pre-reg §5.3 explicitly excludes the Approach A arms (different inferential machinery; not in the {primary, unmed-only sens, motion-clean sens, transient-excluded sens} 4-cell family) — protecting the primary verdict from Approach A pull-through. This is the strongest anti-silent-promotion discipline in the pre-reg (five-surface precedent + audit-endorsed r2 reorder + inline result.md framing + Holm-exclusion). Backstopped by (a) sub-MD [`bout_level_dose_response_calibration.md §6`](../../../methodology/bout_level_dose_response_calibration.md) architectural-implications paragraph + §7 caveat 5 (underpowered-NULL frame); (b) recalibration audit [`reviews/bout_level_dose_response_calibration-r3-2026-06-22.md §3`](../../../reviews/bout_level_dose_response_calibration-r3-2026-06-22.md) five-surface precedent; (c) HA-C4c fresh-session re-audit [`reviews/HA-C4c-2026-06-23-v2-reaudit.md`](../../../reviews/HA-C4c-2026-06-23-v2-reaudit.md) which cleared L4.7 at r2 absorb. Lock-dates: 5-surface precedent (2026-06-22) → sub-MD r4 (2026-06-23) → pre-reg r2 lock (2026-06-23) → test execution (2026-06-23) → all PRECEDE any peek at the observed sub-arm verdicts. | Stocktake §2d cites "Approach A all 3 sub-arms PARTIAL (no β-precision-fragility)" as descriptive observation; the audit makes the underpowered-NULL discipline BACKSTOPPED-explicit here per guide #1 §5.9. No conflict. |
| A12 (HA-specific) | **BACKSTOPPED** | Per pre-reg §8 caveat 4 (99.3% motion-confound corpus property; load-bearing per cascade-context from HA11-bout-redo `result.md` §4): the 4285/4317 (99.3%) `motion_confound_flag=True` figure is a corpus-level structural finding about the per-minute stress trace at parent MD lock time; parent MD §3.4 pre-committed the "include motion-confounded bouts in primary" default at parent MD lock `c57ff3f` (2026-06-19; PRECEDES HA11-bout-redo test 2026-06-23 which first surfaced the 99.3% figure; PRECEDES HA-C4c r2 lock 2026-06-23; PRECEDES HA-C4c test 2026-06-23). The HA-C4c primary operand inherits the motion-confound-inclusive default; the "during rest periods" Wiggers-verbatim semantic is operationalised across the motion-tagged bout pool by construction. `result.md` §5 motion-clean-only arm outcome (n_heavy=465 n_non_heavy=809; heavy mean 0.002 vs non-heavy mean 0.000; Cliff's δ = +0.002; block-perm p = 0.1841; verdict REJECTED) manifests as **near-zero-signal arm rather than degenerate-n INCONCLUSIVE** (pre-reg §8 caveat 4 anticipated INCONCLUSIVE but observed REJECTED-via-zero-signal; a slightly different manifestation of the same corpus property). The result.md §5 motion-clean reaffirmation paragraph explicitly reads this as "corpus-property reaffirmation NOT fragility of primary" per pre-reg §8 caveat 4 framing. `result.md` §10 caveat 4 restates the corpus-property inheritance load-bearingly. The load-bearing assumption here is not that the operand is motion-clean (it isn't) but that the framing is explicit and honest — the Wiggers "rest-stress" semantic is operationalised against the motion-tagged pool AND the sensitivity arm surfaces the corpus property as a reaffirmation (not silent). Backstopped by (a) parent MD §3.4 motion-confound handling default pre-committed at parent MD lock; (b) HA11-bout-redo `result.md` §4 upstream 99.3% finding (2026-06-23); (c) HA-C4c pre-reg §8 caveat 4 explicit anticipation; (d) HA-C4c `result.md` §5 + §10 caveat 4 reaffirmation. | Stocktake §2d explicitly cites "99.3% motion-confound corpus-property reaffirmation per §8 caveat 4" as substantive context; the audit makes A12 BACKSTOPPED explicit per guide #1 §5.9. No conflict. |
| A13 (HA-specific) | **BACKSTOPPED** | Parent MD §3.2 pre-commits the 180-minute forward cap + +5 baseline tolerance + ≥10-minute return-hold thresholds in `did_not_return_flag` based on (a) 180-min tail as the "recovery observation window" project convention, (b) +5 baseline tolerance mirroring per-bout hysteresis rules, (c) ≥10-minute return-hold as the "stable return" persistence anchor. Pre-reg §4.1 inherits verbatim; NOT re-deriving. Crucially, the operand-threshold choice CANNOT be re-derived to fit the HA-C4c result because the operand IS the test of operand-fitness per parent MD §6 — HA11-bout-redo's PARTIAL framework-validity gate at bars 1+2 PASS already validated the bout-level operand class at HA11 v1 reference frame; re-deriving `did_not_return` thresholds would collapse the framework-validity discipline. Pre-reg §7 pre-commits sanity-check bounds: mean [0.3, 1.5] events/day; median ≤ 4; σ [0.5, 2.0] — with §10.4 HALT-on-dry-run enforcement. Per `result.md` §4 dry-run sanity gates all PASS at run-time (mean 0.6444 in [0.3, 1.5]; median 1.00 ≤ 4; σ 0.6703 in [0.5, 2.0]); the per-day distribution behaves as the pre-committed operand-threshold calibration anchor predicts. Per STOCKTAKE §2d representative-day spot-checks: `bout_n_did_not_return` behaves on 3 anchor days as the parent MD §3.2 calibration anchor predicts (crash day 2023-02-04 = 2 non-return bouts; heavy-exertion day 2024-04-18 = 1 non-return bout). Lock-dates: parent MD `c57ff3f` (2026-06-19) → pipeline `d5b394c` (2026-06-22) → pre-reg `4e666a2` (2026-06-23) → test `a69a8ed` (2026-06-23); thresholds precede pipeline precede pre-reg precede test, no post-hoc per §7.3. | Stocktake §2 implicitly carries operand-threshold validity under A3 + framework-validity discipline backstops (inherited from HA11-bout-redo); this audit makes the HA-specific A13 row explicit per guide #1 §5.9. No conflict. |

## 4. Verdict-trust call

**TRUSTED**.

**Rationale**: 11 of 13 load-bearing assumptions are BACKSTOPPED; 2 are NOT APPLICABLE (A5, A6) with documented one-sentence reasons per synthesis map §3 cluster-row L-ID annotations. Every A1-A8 row is BACKSTOPPED (no BACKSTOPPED-partial, no NOT-BACKSTOPPED). All five HA-specific rows (A9 cross-phase pooling discipline inheritance from parent MD §5.3 + sub-MD §6; A10 pipeline trust; A11 Approach A inheritance-by-analogue discipline under underpowered-NULL frame; A12 motion-confound corpus property; A13 bout-level operand-threshold validity) are BACKSTOPPED with explicit citations to parent MD § + sub-MD § + pre-reg § + `result.md` § + lock-date precedence per guide #1 §7.3.

The verdict-trust call is **TRUSTED**, not DOWNGRADED-INCONCLUSIVE-PROVISIONAL, because:

- Every load-bearing assumption has descriptive evidence behind it on the cells the test actually used (A1: `result.md` §3 per-arm summary + §9 verification + §5 sensitivity-arm n reporting; A2: pre-reg §3 + §4.4 §3.4 gate + `result.md` §9 non-NaN days figure of 1274; A3: E[L]\* = 7.00 exactly + factor-of-2 flag NOT FLAGGED; A4: explicit era + stratum binding + r2 5-surface column-confusion fix + re-audit clean; A7: every p-value carries directional effect-size at bar level + per-arm level + Holm level; A8: single-pool cross-phase-pooled primary + sensitivity-arms cannot-promote discipline; A9: sub-MD §6 architectural-implications + STOCKTAKE §6 naming-discipline note + pre-reg §8 caveat 8 conditional-nature-framing; A10: pipeline lock precedes pre-reg precedes test + 9 smoke tests re-confirmed at run-time + representative-day spot-checks per STOCKTAKE §2d; A11: 5-surface underpowered-NULL precedent + r2 reorder + inline `result.md` framing + Holm-exclusion; A12: parent MD §3.4 default pre-commit + upstream 99.3% finding + pre-reg §8 caveat 4 anticipation + `result.md` §5 + §10 caveat 4 reaffirmation; A13: parent MD §3.2 threshold pre-commit + framework-validity gate discipline + §7 sanity-bound PASS at run-time).
- The pre-reg's r2 substantive absorb cycle under Option γ canonical 4-stage (first project use of the re-audit-after-substantive-absorb pattern per STOCKTAKE §5.5) — r1 audit L1.2 BLOCKING column-confusion at 5 surfaces closed via r2 at `310e145` plus upstream STOCKTAKE §6 source-of-bug fix at `4b53ea1`; r1 L4.7 substantive §4.9 framing-slip closed via r2 reorder to lead with underpowered-NULL framing; r1 L3.3 Holm shrunk-family disclosure added at §5.3 — means the load-bearing assumptions have already been adversarially walked at pre-reg-lock time and closed cleanly before test execution.
- The **PARTIAL verdict is a legitimate completion state** per pre-reg §9.2 second PARTIAL configuration (direction-correct + bar (a) discrimination PASS + bar (b) effect-size FAIL — the "weak-effect-but-real positive pattern" configuration explicitly pre-registered as a possible outcome). It is a substantive finding about the operand's discriminative magnitude at the locked n_heavy=465/n_non_heavy=809 sample, NOT an assumption-backstop failure. The audit reads the verdict as the verdict; per guide #1 §6.4 the verdict-trust call is independent of the verdict label (a PARTIAL whose assumptions are all BACKSTOPPED is TRUSTED for Stage I purposes; the audit does not preferentially trust PASSED over PARTIAL or vice versa; per guide #1 §7.4 the audit does not re-read the verdict).
- The "is the underlying weak-but-real signal magnitude-limited by operand fitness at bout resolution, corpus property, or genuine mechanism attenuation?" question is what Stage I will interpret on this trusted verdict, with the calibration-discount caveat from HA11-bout-redo per pre-reg §8 caveat 2 explicit and the stratum-fragility (unmedicated-only REJECTED at δ=+0.059 diverging from primary PARTIAL at δ=+0.120) explicit. Stage I will also inherit the cascade-source verdict from C-bout-framework (HA11-bout-redo Stage D/I/S₁ LOCKED r1 2026-07-07; framework-validity operand-fitness PARTIAL) as caveat-class precondition per `internal_synthesis.md` §5.5; **the cascade-consumption obligation is at Stage S₁ downstream** per synthesis-map §3 cluster row, NOT here at Stage D (the audit notes it for traceability in §4.5 cascade-context note 1 but does not let it enter §3 as a load-bearing assumption of HA-C4c's primary cell).

Per guide #1 §4.4: TRUSTED applies when every load-bearing assumption is BACKSTOPPED (or NOT APPLICABLE with documented reason). All 13 rows here satisfy this strictly.

Per guide #1 §6.4: the verdict-trust call is independent of the verdict label. HA-C4c's PARTIAL verdict whose assumptions are all BACKSTOPPED (or NOT APPLICABLE) is TRUSTED for Stage I purposes; the audit does not preferentially trust PASSED-equivalent verdicts over PARTIAL-equivalent verdicts.

## 4.5 Lessons + queued descriptive work

No BACKSTOPPED-partial or NOT-BACKSTOPPED rows fired in §3; no descriptive closure work is queued as blocking for HA-C4c specifically. Three observation-level notes that are NOT load-bearing for this audit's verdict-trust call but worth recording for the cascade-context-downstream Stage I / Stage S₁ artefacts on HA-C4c:

1. **Cascade-source verdict from C-bout-framework (HA11-bout-redo)** is caveat-class precondition per `internal_synthesis.md` §5.5. C-bout-framework LOCKED r1 2026-07-07 at the joint Phase B lock session (Stage D + I + S₁ all LOCKED r1; OI-016 + OI-018 + OI-019 CLOSED); the framework-validity verdict is **PARTIAL (bars 1+2 PASS at +20.26pp/z=2.410; bar 3 p FAIL at 0.2609 at n_calm=70/n_crash=11)** — operand fitness-for-purpose validated at magnitude/direction level but power-limited on that gate. Per synthesis map §3 cluster row: this cluster's verdict propagates as a **caveat-class precondition** to C-bout-substance; it is not a co-equal verdict that S₁ reconciles against C-bout-substance's findings; there is a structural **era-scope mismatch** (framework runs unmed×train; substance runs cross-phase pooled) that is structural to each cluster's question, not a confound to reconcile away. **Stage D notes for traceability**; the cascade-consumption obligation is at Stage S₁, NOT here — the audit does NOT let this enter §3 as a load-bearing assumption of HA-C4c's primary cell per guide #1 §5.5 framing (framework-validity gate binds HA11-bout-redo's primary, not HA-C4c's). Carry-forward to Stage I: the calibration-discount caveat per pre-reg §8 caveat 2 is load-bearing for the Stage I reading; the "cross-phase pooling cleared the power-shortfall as anticipated" cascade-context is now empirically confirmed (n_heavy=465, n_non_heavy=809; p PASS at 0.0001).
2. **Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return`** does not yet exist as a standalone descriptive artefact (stocktake §3 Shared gap 3 pattern; sister to the OI-017 cascade-context entry HA11-bout-redo Stage D opened for `bout_n_fast_recovery_day`). A2 is BACKSTOPPED via the §3.4 day-validity gate documentation + the primary-stratum non-NaN days figure (1274 = n_heavy+n_non_heavy exactly per `result.md` §9), which together supply path (a) of guide #1 §5.2 operational test; a future per-channel rate audit would tighten the survivorship reach narrative under L7 but is not blocking. **Closure path** (non-blocking; logged for cascade-context; sister to OI-017 pattern): per-channel missingness-rate audit on `bout_n_did_not_return` × era × phase (S effort per stocktake §3 Shared gap 3; script template from `desc-SMS` exists and would cover both bout-level sister operands (`bout_n_fast_recovery_day` from OI-017 + `bout_n_did_not_return` from this entry) in one S-effort run — cheaper as a joint audit than as two).
3. **Stratum-fragility observation for Stage I** (from `result.md` §5 + §10 caveat 8 + STOCKTAKE §2d): the unmedicated-only sensitivity arm REJECTED at δ=+0.059 p=0.1145 (n_heavy=183 / n_non_heavy=323) diverges materially from primary PARTIAL at δ=+0.120 p=0.0001. Per pre-reg §9.5 + §8 caveat 8: the cross-phase pooling permission is empirically load-bearing for the substantive signal on this corpus; the medicated phases contribute to the headline contrast, likely via dose-shaped baseline shifts the operand picks up (per §8 caveat 10 pacing-behaviour narrative). This is a **substantive descriptive finding** the Stage I `interpretation/HA-C4c.md` must foreground when licensing claims — the primary cross-phase-pooled PARTIAL cannot be silently generalised to the unmedicated stratum where the effect materially attenuates and the p-value fails. **No descriptive closure path** is identifiable at Stage D: the divergence is not a fragility of the assumption backstops but a substantive property of the operand × stratum interaction on this corpus that Stage I must interpret. Cascade-forward: Stage S₁ on the single-member C-bout-substance cluster will read the framework-validity precondition (per note 1) alongside this stratum-fragility; both are caveat-class conditioning for the coherence call per `internal_synthesis.md` §5.5 + §5.6.

## 4.9 `open_inputs` block

Two DEFAULTED-PENDING-USER-INPUT slots surfaced per LOCKED-r4 skill #7 dispatch-mode convention. Entry 1 belongs to the lock-event itself; entry 2 is a non-blocking cascade-context carry-forward.

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | ~~User explicit acceptance of this audit's TRUSTED verdict-trust call per guide #1 §8.4 optional seed~~ **CLOSED-BY-USER-LOCK 2026-07-08** (Q B1 at Phase C cluster-level joint lock session: user accepted TRUSTED as-is, no reaudit requested; audit locked as-drafted). Was formerly OI-021. | ~~DRAFT r1 → LOCKED transition~~ resolved: this audit LOCKED r1 2026-07-08. | Interview pass fired at Phase C cluster-level joint lock session; user answered "TRUSTED as-is" verbatim to Q B1. | N/A — TRUSTED lock landed. |
| 2 | Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_did_not_return` (stocktake §3 Shared gap 3 pattern; sister to OI-017 for `bout_n_fast_recovery_day`; NOT load-bearing for HA-C4c's TRUSTED call but observation-level cascade-context note per §4.5 item 2; carry-forward for HA-C4c Stage I L7 citation) | Tightens HA-C4c's A2 from "BACKSTOPPED via §3.4 gate + 1274 primary-stratum non-NaN days" to "BACKSTOPPED with full per-channel diagnostic"; jointly with OI-017 covers both bout-level sister operands in one S-effort run; not blocking Stage I / S₁ | S effort per stocktake §3 Shared gap 3 (per-channel missingness rate × era × phase); jointly with OI-017 becomes a two-operand run at the same S effort per the sister-operand-consolidation observation in §4.5 item 2; script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/` | TRUSTED stands with A2 BACKSTOPPED reading; Stage I §4.5 L7 citation narrows survivorship reach narrative to the §3.4 gate-discipline-bounded reading (no full audit-backed extension claimable until the diagnostic lands); STOCKTAKE §2d "primary-stratum non-NaN days = 1274" figure provides the sample-anchoring context in the interim |

Both entries propagate to [`methodology/_open_inputs.md`](../../../methodology/_open_inputs.md) at user lock time per skill responsibility #6 (deduplicated against existing entries by missing-input × blocked-target key). Entry 2 explicitly deduplicates against OI-017 (`bout_n_fast_recovery_day` operand) — the two carry-forward entries name distinct sister operands sharing a common closure path; the layer-wide queue may either keep them as distinct entries or consolidate them into a joint two-operand run at the user's option.

---

## §9.6 refuse-to-lock gate walk

Per guide #1 §9.6 walk (item-by-item):

1. **Every §3 row has a status assigned** (BACKSTOPPED / NOT BACKSTOPPED / BACKSTOPPED-partial / NOT APPLICABLE): YES — all 13 rows have explicit status + citation + reason + stocktake-diff column.
2. **Every BACKSTOPPED row cites a specific path** (not "BACKSTOPPED by inspection of result.md" — forbidden per guide #1 §4.3 hard rule): YES — A1 cites `result.md` §3 per-arm summary + §9 verification + §5 sensitivity-arm n reporting; A2 cites pre-reg §3 + §4.4 + parent MD §3.4 gate + pipeline README + `result.md` §9 primary-stratum non-NaN days figure of 1274 (a specific `result.md`-embedded named count reported per pre-reg §4.11 discipline, NOT the test's own verdict; per guide #1 §5.2 operational test path (a) documented gate + (c) test's gating mechanism as the missingness handler are both cited); A3 cites `result.md` §4 E[L]\* companion + pre-reg §4.6; A4 cites pre-reg §4.2-§4.4 + §6 + named methodology MDs + r2 5-surface fix + fresh-session re-audit clean; A7 cites `result.md` §2 per-bar + §3 per-arm + §5 sensitivity + §6 Holm tables; A8 cites pre-reg §4.2 + §5.0 single-cell headline lock + §4.10 sensitivity-cannot-promote discipline; A9 cites sub-MD §6 architectural-implications + STOCKTAKE §6 naming-discipline note + pre-reg §8 caveat 8 conditional-nature framing; A10 cites pipeline lock + 9 smoke tests + representative-day spot-checks per STOCKTAKE §2d + `desc-BOUT` at operationalisation_support; A11 cites 5-surface underpowered-NULL precedent + r2 reorder + inline `result.md` framing + Holm-exclusion + fresh-session re-audit clearing L4.7; A12 cites parent MD §3.4 pre-commit + upstream 99.3% finding + pre-reg §8 caveat 4 + `result.md` §5 + §10 caveat 4 reaffirmation; A13 cites parent MD §3.2 threshold pre-commit + framework-validity gate discipline + `result.md` §4 sanity-bound PASS + STOCKTAKE §2d spot-checks.
3. **Every NOT-BACKSTOPPED row maps to an §4.5 `open_inputs` entry** (per guide #1 §4.3): NOT APPLICABLE — zero NOT-BACKSTOPPED rows fired.
4. **Every NOT-APPLICABLE row carries one-sentence reason** (per guide #1 §4.3): YES — A5 ("no v24 primary signals; bout-level Garmin-derived operand + Garmin-derived heavy-T classifier + Garmin-derived crash-drop labels only"); A6 ("daytime bout-level test on per-minute stress; no nightly column; gevoelscore not in primary cell").
5. **Anti-pattern §7.1 not fired** (no "the test ran, therefore the assumptions held" reading): YES — every BACKSTOPPED row cites a separate descriptive artefact (descriptive run, methodology MD, pipeline smoke tests, representative-day spot-checks per STOCKTAKE §2d, `desc-BOUT` at operationalisation_support, fresh-session re-audit); no row backstops via the test's own verdict alone. Where `result.md` sections are cited, they cite embedded named counts (per-arm n; primary-stratum non-NaN days; per-arm bout counts; dry-run sanity gate values; E[L]\* companion; per-arm Cliff's δ + CI + p) that are pre-committed per-cell descriptive readouts under pre-reg §4.11 discipline, NOT the verdict-derivation itself.
6. **Anti-pattern §7.2 not fired** (no single-cell descriptive cited as global backstop): YES — the E[L]\* companion is computed on the actual cross-phase-pooled stratum the test reports on (not an unmed×train-restricted single cell); the per-arm summary covers heavy + non-heavy on the same stratum + every sensitivity arm the pre-reg pre-committed; per-bar reporting per CONVENTIONS §3.6 covers per-arm + per-cell counts explicitly. Where cross-cited to HA11-bout-redo (as cascade-source), the citation is transparent per guide #1 §5.5 framing (framework-validity is caveat-class precondition, not co-equal backstop).
7. **Anti-pattern §7.3 not fired** (no post-hoc descriptive backstop): YES — all artefact lock-dates precede the test execution: parent MD `c57ff3f` 2026-06-19; pipeline `d5b394c` 2026-06-22; `desc-BOUT` 2026-06-22; sub-MD `fb97d1c` 2026-06-23; HA-C4c r2 lock `4e666a2` 2026-06-23; test execution `a69a8ed` 2026-06-23; fresh-session re-audit at `HA-C4c-2026-06-23-v2-reaudit.md` closed pre-test. All backstops PRECEDE test execution; no post-hoc-and-peeking artefacts.
8. **Anti-pattern §7.4 not fired** (no re-interpreting the verdict in the audit): YES — the audit reads PARTIAL as PARTIAL; no re-reading to "PARTIAL is actually SUPPORTED" or "the bar (b) failure should be narrowed"; the §4 verdict-trust call explicitly cites guide #1 §6.4 verdict-trust-independent-of-verdict-label + §7.4 no-verdict-re-reading rules. The audit reads the stratum-fragility (§4.5 note 3) as a lesson for Stage I NOT a re-interpretation of the primary verdict; the substantive weak-effect-but-real reading is Stage I's job to interpret against literature + prior evidence.
9. **Anti-pattern §7.5 not fired** (no silent assumption-list editing): YES — the audit adds five HA-specific rows (A9-A13) beyond the spine of A1-A8 with explicit source citations (A9: parent MD §5.3 + sub-MD §6 + pre-reg §8 caveat 8; A10: pipeline lock + pre-reg §3 + §10.1; A11: pre-reg §4.9 r2 + §8 caveat 3 + sub-MD §6/§7 + 5-surface precedent; A12: parent MD §3.4 + pre-reg §8 caveat 4 + upstream 99.3% finding; A13: parent MD §3.2 + pre-reg §4.1 + §7 sanity bounds); no row is added without a pre-reg or methodology-MD or `test.py` source per guide #1 §7.5. The cascade-source verdict from C-bout-framework is transparently noted in §4.5 cascade-context note 1 for traceability, NOT silently absorbed into §3.
10. **Anti-pattern §7.6 not fired** (no marking TRUSTED to avoid blocking Stage I): YES — TRUSTED is supported by every §3 row being BACKSTOPPED or NOT APPLICABLE per the §6.4 hard rule; no Stage I forcing pressure. The stratum-fragility observation (§4.5 note 3) is honestly surfaced as substantive downstream reading NOT quietly folded away.
11. **Anti-pattern §7.7 not fired** (no citing the limitations doc instead of producing the backstop): YES — the audit cites the L7 manifestation in §3 A2 reasoning; A5 is NOT APPLICABLE so the §5.5 L5 surface obligation does not fire here (Stage I inherits NO L5 citation obligation per guide #1 §5.5 final paragraph). The §3 BACKSTOPPED rows cite the actual descriptive artefacts (pipeline NaN semantics, day-validity gate, primary-stratum non-NaN figure, E[L]\* companion, `desc-BOUT` at operationalisation_support, fresh-session re-audit), not the limitations doc as the backstop.
12. **L5 + L7 citations** per guide #1 §2: A2 cites L7 (survivorship) in the row's reasoning; A5 is NOT APPLICABLE so the §5.5 L5 surface obligation does not fire here (Stage I inherits NO L5 citation obligation per guide #1 §5.5 final paragraph). The audit does not cite further L-IDs beyond L5 + L7 per guide #1 §2 final paragraph (Stage I will pick up broader L-ID citations on its own per the limitations doc §5 row for `interpretation.md`; synthesis map §3 C-bout-substance cluster row explicitly binds L1, L2, L3, L4, L7 for Stage I / S₁).

**Items that would fire on lock attempt**: ONE (the user-acceptance-only DEFAULTED-PENDING-USER-INPUT slot per §4.9 entry 1 — not a §9.6 substantive fire; this is the binding-completion-event-pending state per locked-plan §3.8). The artefact is structurally complete and refuse-to-lock-clean on every §9.6 substantive check (items 2 through 12 all PASS); the lock-event itself is the only outstanding action.

The second §4.9 entry (per-channel missingness diagnostic on `bout_n_did_not_return`) does NOT fire §9.6 because A2 is BACKSTOPPED on the present checklist reading via the §3.4 day-validity gate + primary-stratum non-NaN days figure of 1274; the entry is observation-level cascade-context for Stage I / joint-consolidation-with-OI-017 potential, not a lock-blocker for HA-C4c's audit.

## §11 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-07-07 | Drafted r1 (DISPATCHED-BY-ORCHESTRATOR ON USER AUTHORISATION) | Producer-mode by dispatched-orchestrator invocation on first-ever Stage D pass for HA-C4c. Phase C of `_rollout_order_2026-06-25.md` (single-member cluster C-bout-substance; cascade-receiver from Phase B C-bout-framework which LOCKED r1 2026-07-07 per synthesis_structure_map §3 cluster table). All audit content drawn from HA-C4c r2 pre-reg (LOCKED 2026-06-23 `4e666a2`; substantive absorb `310e145`; fresh-session re-audit clean at `HA-C4c-2026-06-23-v2-reaudit.md`), `result.md` (LANDED 2026-06-23 `a69a8ed`; worktree HEAD at run `a59997a`; pipeline `d5b394c`), `test.py`, and the cited methodology MDs (parent MD `bout_level_recovery_dynamics.md` LOCKED r2 2026-06-19 `c57ff3f`; sub-MD `bout_level_dose_response_calibration.md` LOCKED r4 2026-06-23 `fb97d1c`; permutation_null_block_length; lc_era_temporal_segmentation; citalopram_phase_stratification; lc_recovery_phase_axis LOCKED r1 2026-06-19 `d47e0d3`; phase_axis_collapsibility_conventions; hypothesis_lock_process; symptom_mention_asymmetry; nightly_attribution; train_validate_split_fate). Cascade-source verdict from C-bout-framework (HA11-bout-redo Stage D + I + S₁ all LOCKED r1 2026-07-07) noted for traceability in §4.5 cascade-context note 1 as caveat-class precondition per `internal_synthesis.md` §5.5; cascade-consumption obligation is at Stage S₁ downstream NOT here at Stage D. |
| 2026-07-08 | **LOCKED r1** by user acceptance at Phase C cluster-level joint lock session | Q B1 at joint session: user accepted TRUSTED verdict-trust call as-is, no reaudit requested (contrast with Bundle B Phase B where user requested + received an independent reaudit on HA11-bout-redo Stage D). Interview walked 13 rows compressed into TRUSTED-rationale block per §4; Q B1 answered unambiguous YES on the confirmation seed. OI-021 CLOSED. Fresh-session `/research-review` per plan §4 NOT dispatched pre-lock — user-authorized deviation matching Phase-A + Phase-B precedent (rationale: producer-mode Stage D artefacts are the reviewer-mode-equivalent of Stage D per plan §4 exclusion; the upstream verification chain is HA-C4c r2 pre-reg + `result.md` + fresh-session re-audit `HA-C4c-2026-06-23-v2-reaudit.md` all clean at test-execution-time). Retained as retrospective drift-trigger per plan §3.7. **Drift triggers registered** (manual-pending-skill carry-forward per skill responsibility #10): (1) HA-C4c `result.md` re-runs (e.g. any operand-refinement v2 per pre-reg §9.2 OPTIONAL path; pipeline `extract_stress_bouts.py` re-LOCK at new commit); (2) any cited methodology MD changes lock-version (especially parent MD `bout_level_recovery_dynamics.md` `c57ff3f` — re-LOCK would re-open §3.2 return-window rule + §3.4 motion-confound default + §5.3 Approach A framing; also sub-MD `bout_level_dose_response_calibration.md` `fb97d1c` — a re-run producing CONFIRMED features would re-open the cross-phase-pooling permission per pre-reg §8 caveat 8; also `permutation_null_block_length.md` + `lc_era_temporal_segmentation.md` + `citalopram_phase_stratification.md` + `lc_recovery_phase_axis.md`); (3) per-channel missingness audit on `bout_n_did_not_return` lands (OI-023 remains open per §4.9 entry 2 cascade-context; may consolidate with OI-017 sister-operand run); (4) C-bout-framework re-LOCK or HA11-bout-redo re-run at operand-refinement v2 changes the framework-validity verdict (which enters Stage S₁ as caveat-class precondition per §4.5 note 1 and would need re-consumption at Stage S₁); (5) **bout-return stringency descriptive companion + personal-baseline sister operand candidate lands** per Stage I §4.8 own-research two-step follow-up (Stage I entry carries full context; landing of Step 1 Pass 1 would inform HA-C4c-v2 threshold refinement decision or HA-C4c-personal-baseline sister-pre-reg parallel to HA-C3/HA-C3p pattern); (6) ≥6 months elapse since lock. |
