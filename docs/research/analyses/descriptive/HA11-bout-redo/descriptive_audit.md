# HA11-bout-redo — descriptive precondition audit

**Status**: **DRAFT r1 — dispatched-by-orchestrator on user authorisation** per Phase B of [`_rollout_order_2026-06-25.md`](../../../methodology/_rollout_order_2026-06-25.md) (single-member cluster C-bout-framework; cascade-source for Phase C C-bout-substance per the [`synthesis_structure_map.md`](../../../methodology/synthesis_structure_map.md) §3 cluster table). First-ever invocation of the [`/research-interpret descriptive HA11-bout-redo`](../../../../../.claude/skills/research-interpret/SKILL.md) skill at LOCKED r4 on this target. The user's separate lock-event in a non-dry-run session will promote DRAFT r1 to LOCKED per [`_plan_results_analysis_layer.md`](../../../methodology/_plan_results_analysis_layer.md) §3.8.

This audit is a Stage D producer-mode artefact per [`descriptive_precondition_audit.md`](../../../methodology/descriptive_precondition_audit.md) (LOCKED r2 2026-06-24). Per §3 of guide #1, producer-mode audits do NOT carry an `## Authorship` block and do NOT receive a fresh-session `/research-review` pass; user explicit acceptance per §3.8 of the locked plan is the binding completion event.

---

## 1. Target HA + result reference

- **HA ID**: HA11-bout-redo (revision r2)
- **Pre-reg lock date**: 2026-06-23 (commit `5c71aa0` + footer-fix `b5bf0f8`; per [`hypothesis.md`](../../hypotheses/HA11-bout-redo/hypothesis.md) Authorship + §11 lock log)
- **Result lock date**: 2026-06-23 (test execution `6e06d12`; worktree HEAD at run `250a466`; pipeline commit `d5b394c`; per [`result.md`](../../hypotheses/HA11-bout-redo/result.md) Authorship)
- **Headline verdict from `result.md`** (verbatim §6): **PARTIAL (2 of 3 framework-validity bars met; bars 1+2 met cleanly, bar 3 fails)**. Bar 1 (directional sign positive) PASS — disc +20.26 pp; Bar 2 (disc ≥ +12.8 pp; effect-size comparability within ±10 pp of HA11 v1's +22.8 pp) PASS at +20.26 pp; Bar 3 (block-permutation p < 0.05 at E[L]=7) FAIL at p = 0.2609 (n_ge = 2608 of B = 10000 draws). Cascade implication: HA-C4c drafting UNBLOCKS with explicit calibration caveat per pre-reg §9.2 (the substantive HA-C4c §8 must carry a caveat naming bar 3 as the failing bar).
- **Effective n on the primary cell** (verbatim from `result.md` §2): n_calm = **70** of 200 HA11 v1 reference dates (73 in unmed × train pre-window-coverage; 70 surviving the §4.7 ≥ 3-of-4 window-coverage gate); n_crash = **11** of 13 unmedicated × train crash-episode anchors after §4.7 window-coverage. Analysis-day pool: 413 days in unmed × train satisfying §4.4 day-validity + §4.6 baseline-validity (zero low-variability skips at σ ≤ 0.5 floor; 67 insufficient-prior-day skips).
- **Operationalisation summary** (one sentence, paraphrasing pre-reg §1 + §4): framework-validity reproduction check on `bout_n_fast_recovery_day` (per-day count of bouts with `recovery_half_life ≤ 15 min` AND `tail_length ≤ 45 min` per [parent MD `bout_level_recovery_dynamics.md`](../../../methodology/bout_level_recovery_dynamics.md) §6.1) on the unmedicated × train era × HA11 v1 calm-day reference-date pool (seed `20260605`), evaluated as 4-day max-signed-z windows against the participant's lagged personal baseline ([d-90, d-30]; 60-day window; σ > 0.5 floor) per HA11 v1 §4.5-§4.7, with the three framework-validity comparability bars verbatim from parent MD §6.2 (directional sign + ≥ +12.8 pp + p < 0.05 under block-permutation at E[L]=7, B=10000, seed `20260622`).
- **Methodology MDs cited by the pre-reg** (link list):
  - [`bout_level_recovery_dynamics.md`](../../../methodology/bout_level_recovery_dynamics.md) — parent MD (LOCKED r2 2026-06-19 `c57ff3f`); §3 bout-detection rule; §4 per-bout feature set + §3.4 day-validity; §5.5 framework-validity restriction to unmedicated stratum; §6 framework-validity discipline (the MD's own falsifier); §6.1 operand definition `bout_n_fast_recovery_day` (15/45 thresholds pre-committed); §6.2 three comparability bars; §6.3 halt rule + per-bout-n reporting discipline.
  - [`permutation_null_block_length.md`](../../../methodology/permutation_null_block_length.md) — §4.7 stationary-bootstrap E[L]=7 inheritance via parent MD §5.1; data-driven E[L]\* companion + factor-of-2 flag.
  - [`lc_era_temporal_segmentation.md`](../../../methodology/lc_era_temporal_segmentation.md) — Stratum 4 binding (2022-04-04 → 2024-04-08); HA11 v1 train era 2022-09-03 → 2023-12-31 (per HA11 v1 §3 inheritance).
  - [`citalopram_phase_stratification.md`](../../../methodology/citalopram_phase_stratification.md) — §3 unmedicated phase boundary (citalopram start 2024-04-09); April 2024 cluster exclusion (2024-04-09 → 2024-04-16).
  - [`hypothesis_lock_process.md`](../../../methodology/hypothesis_lock_process.md) — §3.2 step 4 mandatory dispatches; §3.5 + §3.6 r2 compression-acceptability; §3.8 four lock-blocking gates; §3.9 dry-run halt discipline (structural analogue to parent MD §6 framework-validity halt).
  - [`symptom_mention_asymmetry.md`](../../../methodology/symptom_mention_asymmetry.md) — variable-class taxonomy (cited via the Stage D §5.5 A5 binding; HA11-bout-redo uses only Garmin-derived stress signals + crash labels, no v24 columns).
  - [`nightly_attribution.md`](../../../methodology/nightly_attribution.md) — wake-up-date convention (cited via the Stage D §5.6 A6 binding; HA11-bout-redo is daytime bout-level, no nightly column in scope).
  - [`train_validate_split_fate.md`](../../../methodology/train_validate_split_fate.md) — single-pool primary discipline (per Stage D §5.8 A8 binding; HA11-bout-redo's "train era" inheritance from HA11 v1 is the framework-validity reference frame, not a single/multi-pool primary choice — see A8 reading below).
  - [`CONVENTIONS.md`](../../../CONVENTIONS.md) — §3.1 personal baseline; §3.4 crash-drop sensitivity; §3.6 named counts discipline (per parent MD §6.3 r2 absorb + pre-reg §4.11); §4.1-§4.3 descriptive-before-inference + caveats + confirmatory.
  - Literature anchor: [`literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf`](../../../literature/methodology/daza_2018_self_tracked_n_of_1_counterfactual.pdf) — power-calc dispatch per §3.2 step 4 mandatory.

## 2. Load-bearing assumptions enumerated

| # | Assumption | Source |
|---|---|---|
| A1 | Sample size on every reported cell ≥ pre-registered floor: n_calm ≥ 30 + n_crash ≥ 10 per pre-reg §4.9 walk-forward gate (PARTIAL/PASS/FAIL/INCONCLUSIVE routing) | Pre-reg §4.9 (walk-forward gate) + §10.4 (HALT triggers on §7.5 sanity gates) + `test.py` (gate evaluation logic) |
| A2 | Missingness pattern is MCAR/MAR-compatible OR missingness-aware operationalisation used + documented; bout-level operand emits 0 on valid days with no bouts vs blank/NaN on §3.4-invalid days | Pre-reg §3 (day-validity gate inherited from HA11 v1 §4.4 + parent MD §3.4) + §6 (exclusion rules) + pipeline README (`bout_n_fast_recovery_day` NaN semantics) |
| A3 | Block-length E[L]=7 is appropriate for autocorrelation structure of `bout_n_fast_recovery_day[d]` over train era under stationary-bootstrap-resampling of `is_crash` labels | Pre-reg §4.8 inherits [`permutation_null_block_length.md`](../../../methodology/permutation_null_block_length.md) via parent MD §5.1; pre-reg §4.8 reports data-driven E[L]\* + factor-of-2 flag per CONVENTIONS §3.6 |
| A4 | Era / Stratum 4 binding honored: unmedicated phase × HA11 v1 train era × April 2024 cluster excluded + 21-day device-baseline-lag excluded | Pre-reg §4.2 + §4.3 + §4.4 + §6 (exclusion rules); cites [`lc_era_temporal_segmentation.md`](../../../methodology/lc_era_temporal_segmentation.md) + [`citalopram_phase_stratification.md`](../../../methodology/citalopram_phase_stratification.md) + parent MD §3.4 + §5.5 |
| A5 | v24 presence-conditioned semantics respected — NOT APPLICABLE here (no v24 primary signals; `bout_n_fast_recovery_day` is derived from per-minute Garmin stress + `crash_v1` labels, both non-v24) | NOT APPLICABLE |
| A6 | Nightly attribution per `nightly_attribution.md` — NOT APPLICABLE (bout-level daytime test on per-minute stress trace; no sleep-derived primary signal) | NOT APPLICABLE |
| A7 | Effect-size direction reported alongside p-values, never alone | Pre-reg §4.8 + §10.3 (result.md template); `result.md` §3 reports disc (pp) + median signed z + Cliff's δ + Mann-Whitney U alongside block-permutation p |
| A8 | Train/validate split fate per `train_validate_split_fate.md` — HA11-bout-redo's "train era" is the HA11 v1 reference-frame-preservation choice (framework-validity demands reproducing HA11 v1's signal in HA11 v1's reference frame), NOT a per-portion confirmatory verdict on a single-pool primary; the validate-era out-of-scope per pre-reg §4.3 is the inheritance discipline | Pre-reg §4.3 + §8 caveat 7 (era restriction non-negotiable); cites [`train_validate_split_fate.md`](../../../methodology/train_validate_split_fate.md) via parent MD; the framework-validity question is reference-frame-bounded by construction |
| A9 (HA-specific) | **Framework-validity-as-self-test discipline**: HA11 v1's SUPPORTED-on-train +22.8 pp `u_dip_count` signal is project-trusted independent evidence that the within-day recovery-shape construct carries crash-precursor information on this corpus in this stratum + era; the framework-validity gate is a check that `bout_n_fast_recovery_day` reproduces that signal at directional + effect-size + p-value comparability | Parent MD §6 framework-validity discipline (the MD's own falsifier); pre-reg §1 (what "reproduces" means narrowly) + §2 (priors); HA11 v1 [`result.md`](../../hypotheses/HA11-stress-udip/result.md) headline (+22.8 pp, median signed z = 2.168) |
| A10 (HA-specific) | **Bout-extraction pipeline trust**: `extract_stress_bouts.py` LOCKED `d5b394c` correctly implements parent MD §3 (bout-detection rule) + §4 (per-bout feature set); per-day `bout_n_fast_recovery_day` joined into `per_day_master.csv` via `03_consolidate/build_unified_dataset.py` with correct NaN semantics | Pre-reg §3 + §10.1 (pre-flight pipeline-trust check) + §8 caveat 9 (pipeline-extraction trust assumption); pipeline `extract_stress_bouts.py` LOCKED `d5b394c` 2026-06-22; 6 inline smoke tests + 5-bout spot-check verification per `pipeline/02_features/README.md` |
| A11 (HA-specific) | **σ ≤ 0.5 low-variability skip threshold inheritance**: HA11 v1's σ floor is retained verbatim on a structurally different distribution (bout-count vs U-dip-count); the empirical σ on `bout_n_fast_recovery_day` sits modestly below HA11 v1's `u_dip_count` (median 0.739 vs 0.840) but well above the 0.5 floor; zero low-variability skips fire on the test's pool | Pre-reg §4.6 r2 inheritance-of-σ-floor note + audit-closure single-script count [`scripts/count_HA11_bout_redo_effective_n.py`](../../../../../scripts/count_HA11_bout_redo_effective_n.py); §8 caveat 4 (inheritance-risk caveat); `result.md` §2 dry-run gate 2 PASS at observed median σ = 0.739 |
| A12 (HA-specific) | **Bout-level operand-threshold validity**: the 15-min half-life + 45-min tail thresholds in `bout_n_fast_recovery_day` operand definition (parent MD §6.1) are pre-committed at parent MD lock and inherited verbatim by HA11-bout-redo without re-derivation; the operand IS the test of operand-fitness, so the operand definition cannot be re-derived to fit the result | Parent MD §6.1 operand definition (15/45 pre-committed); pre-reg §4.1 (locked; verbatim from parent MD §6.1; NOT re-deriving); `desc-BOUT` (per-feature recalibration at [`analyses/descriptive/operationalisation_support/bout_level_dose_response_calibration/result.md`](../operationalisation_support/bout_level_dose_response_calibration/result.md), LANDED 2026-06-22) characterises `bout_n_fast_recovery_day` distribution across 3 windows (afbouw_2026 + buildup_post_cpap_2024 + spring_2025_control) |
| A13 (HA-specific) | **Cross-phase pooling NOT in scope at framework-validity layer**: HA11-bout-redo restricts to unmedicated stratum × train era per parent MD §5.5; cross-phase reproduction is a substantive question for HA-C4c, not a framework-validity gate | Parent MD §5.5 + §6 framework-validity restriction; pre-reg §4.2 (stratum: unmedicated phase only; locked per parent MD §5.5 + §6) + §8 caveat 7 (era restriction non-negotiable); [`phase_axis_collapsibility_conventions.md`](../../../methodology/phase_axis_collapsibility_conventions.md) explicitly NOT invoked here |

## 3. Per-assumption status

| # | Status | Citation / reason | Stocktake diff |
|---|---|---|---|
| A1 | **BACKSTOPPED** | Per `result.md` §2 dry-run gates table: Gate 1 (effective n_calm ≥ 30 per §4.9 walk-forward) PASS at **70** (with exact match to the r2 pre-lock pinned count via [`scripts/count_HA11_bout_redo_effective_n.py`](../../../../../scripts/count_HA11_bout_redo_effective_n.py); CONVENTIONS §3.6 named count). AUX gate (n_crash ≥ 10 per §4.9) PASS at **11** of 13 unmed × train crash anchors after §4.7 coverage. The §4.9 walk-forward gate's design-level INCONCLUSIVE routing IS the small-n absorber: had n_calm < 30 OR n_crash < 10, the verdict would route to INCONCLUSIVE per pre-reg §9.4; this is structurally analogous to HA-C4 v2 §5.3 INCONCLUSIVE-aware routing (stocktake-precedent: "the routing IS the backstop" per guide #1 §5.1). Matches stocktake §2 row (B). | No diff |
| A2 | **BACKSTOPPED** | Pre-reg §3 inherits the §3.4 day-validity gate (`bout_n_fast_recovery_day` emits 0 on valid days with no qualifying bouts; emits blank/NaN on §3.4-invalid days: < 600 valid per-minute stress samples OR first 21 days of `has_garmin_uds=True` OR April 2024 cluster). This IS the missingness-aware operationalisation per guide #1 §5.2 operational test path (a): the pipeline's NaN semantics are explicitly documented, and exclusion rules (§6 of pre-reg) name every excluded day-class. `result.md` §2 reports `bout_n_fast_recovery_day` non-NaN on **1486 of 1755** days (operand-presence dry-run gate PASS at ≥ 1479 per pipeline README — a named per-pipeline coverage figure). Per parent MD §3.4 + pre-reg §10.1 the pipeline's 6 inline smoke tests + 5-bout spot-check verification provide audit-level coverage of the NaN-handling. **L7 (survivorship) attribution**: the §3.4 day-validity gate reduces but does not eliminate survivorship reach (days the device did not capture are absent from the analysis pool by construction); the gating discipline supplies path (a) of guide #1 §5.2 operational test but does not produce a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day`. Stocktake row marked **B**; strict reading per guide #1 §6.3 conflict rule retains BACKSTOPPED because (a) the missingness-aware operationalisation IS the §3.4 gate + the NaN-vs-0 semantic distinction is explicitly documented, AND (b) the operand-presence figure (1486/1755 days = 84.7%) is per-pipeline named-count reported in `result.md` §2 itself; this is stronger documentation than HA-C3 v2's A2 BACKSTOPPED-partial reading where no per-channel rate audit existed. **Stocktake-diff narrowing consideration**: a per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` does not yet exist (stocktake §3 Shared gap 3 pattern); it is not load-bearing for the framework-validity gate (the operand-presence figure + the §3.4 gate-documentation suffice for path (a)) but tightening to "BACKSTOPPED with full per-channel diagnostic" would close the open margin. | No diff |
| A3 | **BACKSTOPPED** | Per `result.md` §3 E[L]\* data-driven companion (pre-reg §4.8 + parent MD §5.1 inheritance): E[L]\* on `bout_n_fast_recovery_day` series over train era = **7.0 days** (cutoff_lag = 1); factor-of-2 flag NOT FIRED (|E[L]\* − 7| / 7 = 0 ≤ 0.5 per CONVENTIONS §3.6); block-length choice is well-calibrated. The data-driven E[L]\* on the test's own predictor channel provides first-hand backstop. Per `result.md` §3 caveat 10: "the p=0.2609 failure on bar 3 is not attributable to a block-length mis-specification." Matches stocktake B reading. | No diff |
| A4 | **BACKSTOPPED** | Pre-reg §4.2 cites [`citalopram_phase_stratification.md`](../../../methodology/citalopram_phase_stratification.md) §3 for unmedicated phase boundary (2022-04-04 ≤ date ≤ 2024-04-08); pre-reg §4.3 cites HA11 v1 §3 for train era (2022-09-03 ≤ date ≤ 2023-12-31); pre-reg §4.4 enforces 21-day device-baseline-lag per parent MD §3.4 (inherited verbatim) + April 2024 cluster exclusion per `citalopram_phase_stratification` (trivially redundant with unmedicated phase exclusion). Per parent MD §5.5 framework-validity restriction to unmedicated stratum is explicit; HA11 v1's calibration baseline is preserved by construction. Matches stocktake B. | No diff |
| A5 | **NOT APPLICABLE** | No v24-derived columns (`cat_*`, `state_*`, `day_dominant_polarity`, `per_day_intensity`) appear in pre-reg §3 data sources or §4 operationalisation. Primary signals are `bout_n_fast_recovery_day` (derived from per-minute Garmin stress trace via `extract_stress_bouts.py`) and `crash_v1` labels (HA11 v1's primary outcome from `labels_crash_v2.csv`); both are non-v24. Per `symptom_mention_asymmetry.md` variable-class taxonomy: NOT APPLICABLE with one-sentence reason "no v24 primary signals; bout-level Garmin-derived + crash-label test". | No diff |
| A6 | **NOT APPLICABLE** | Daytime bout-level test on per-minute stress trace; the bout-extraction rule operates on the daytime window per parent MD §3 (per-minute stress aggregation excluding sleep window). No sleep-derived primary signal in scope. Reason: "daytime bout-level test on per-minute stress; no nightly column". | No diff |
| A7 | **BACKSTOPPED** | Per `result.md` §3: every cell with a p-value carries a directional effect-size statistic alongside — discrimination = **+20.26 pp** (signed; positive matches HA11 v1's predicted direction); trigger frequencies 54.5% (crash, 6/11) and 34.3% (calm, 24/70); median signed z (triggering crash anchors) = +2.410; Cliff's δ = +0.306 with 95% CI [−0.021, +0.626] (paired-bootstrap, B=2000); Mann-Whitney U = 503 with one-sided normal-approx p = 0.0519 (descriptive); block-permutation p = 0.2609 (B=10000, E[L]=7, seed `20260622`); block-perm null disc median = +1.96 pp with 95% CI [−14.6, +18.2]. Per-bar verdict table (`result.md` §3) reports observed value alongside threshold for every bar. Per pre-reg §4.5.1 + parent MD §6.2 + §6.3 reporting discipline: per-bout n + per-cell n_bouts × n_days × n_bouts_per_day explicitly required (per CONVENTIONS §3.6 named-counts hook); `result.md` §2 + §3 satisfy. | No diff |
| A8 | **BACKSTOPPED** | Pre-reg drafted 2026-06-22, well after the 2026-06-13 split-fate MD lock — discipline applies. The framework-validity gate's "train era" inheritance from HA11 v1 §3 is the **reference-frame-preservation** choice per parent MD §6 + pre-reg §4.3; reproducing HA11 v1's SUPPORTED-on-train signal requires testing on HA11 v1's train era by construction (the question "does the bout-level operand reproduce HA11 v1's signal?" is well-defined only on the era where the signal lives). This is NOT a single/multi-pool primary choice — there is no per-portion confirmatory verdict on train + validate; the validate era is explicitly OUT OF SCOPE per pre-reg §4.3 + §8 caveat 7. Per stocktake row: "HA11 v1's train-era reference frame is being reproduced; framework-validity gate, not single-pool primary question" — matches B reading with the explicit cross-cite to the framework-validity-specific reading of the split-fate MD's discipline. | No diff |
| A9 (HA-specific) | **BACKSTOPPED** | Per parent MD §6 (the MD's own falsifier) the framework-validity discipline is load-bearing-by-design: HA11 v1's SUPPORTED-on-train signal is the project-trusted independent reference; the gate asks whether `bout_n_fast_recovery_day` reproduces that signal at the three §6.2 comparability bars. The pre-reg §1 + §2 explicitly carry the framework-validity-NOT-substantive scope discipline; `result.md` §6 + §7 caveat 3 carry it verbatim. HA11 v1 [`result.md`](../../hypotheses/HA11-stress-udip/result.md) provides the reference numbers (+22.8 pp; median signed z = 2.168; trigger freq 64.3% / 41.5%; n_crash = 14, n_null = 200); HA11-bout-redo `result.md` §3 reports the bout-level analogues (+20.26 pp; median signed z = 2.410; trigger freq 54.5% / 34.3%; n_crash = 11, n_null = 70) side-by-side in the HA11 v1 reference-frame comparison table. The discipline is itself the test; the BACKSTOPPED reading is that the discipline is correctly framed and the comparison is well-defined (NOT that the verdict is PASSED — that is the verdict, which the audit does not re-read per guide #1 §7.4). | Stocktake §2 marks framework-validity-discipline rows B by aggregation under A1+A3+A4 columns; explicit HA-specific A9 row not separately tracked. No conflict; this audit makes the HA-specific row explicit per guide #1 §5.9. |
| A10 (HA-specific) | **BACKSTOPPED** | Per pre-reg §3 + §10.1: bout-extraction pipeline `extract_stress_bouts.py` LOCKED `d5b394c` 2026-06-22; the pipeline's 6 inline smoke tests + 5-bout spot-check verification per `pipeline/02_features/README.md` provide audit coverage. Per `result.md` §8: "No pipeline-trust issues surfaced during test execution." Per `result.md` §2: operand-presence gate PASS at 1486 non-NaN days ≥ 1479 (pipeline README named threshold). Additionally backstopped by `desc-BOUT` (`analyses/descriptive/operationalisation_support/bout_level_dose_response_calibration/result.md`) which LANDED 2026-06-22 on the same pipeline commit and produced a 189-row CSV across 7 features × 3 windows × 9 specs; the parallel descriptive run on `bout_n_fast_recovery_day` confirms the operand is computable, distributionally well-behaved (per `desc-BOUT` summary), and consumable by downstream consumers. Per [STOCKTAKE](../../../STOCKTAKE.md) §2d: "Three representative-day verifications consistent with the Wiggers C4 substantive reading at bout-level resolution" (crash day 2023-02-04 `bout_n_fast_recovery_day = 0`; calm day 2022-10-18 `bout_n_fast_recovery_day = 2`; heavy-exertion day 2024-04-18 peak = 99 + did_not_return = 1) provide spot-check anchoring at pipeline-output level. Lock-dates: pipeline `d5b394c` (2026-06-22) PRECEDES HA11-bout-redo r2 LOCK (2026-06-23) PRECEDES test execution (2026-06-23); no post-hoc contamination per guide #1 §7.3. | Stocktake §2 row implicitly carries pipeline-trust under A1+A2+A4 backstops; this audit makes the HA-specific A10 row explicit per guide #1 §5.9. No conflict. |
| A11 (HA-specific) | **BACKSTOPPED** | Per pre-reg §4.6 r2 inheritance-of-σ-floor note (closes audit L3 minor on operand-specific distribution differences per audit report [`reviews/HA11-bout-redo-2026-06-22.md`](../../../reviews/HA11-bout-redo-2026-06-22.md)): empirical σ of `bout_n_fast_recovery_day` across the 413 analysis days in unmed × train: median **0.739**, IQR [0.672, 0.826], range [0.589, 1.026]. HA11 v1 `u_dip_count` σ on the equivalent 480-day pool: median **0.840**, IQR [0.773, 0.899], range [0.633, 1.010]. Median σ delta: **−0.10** (bout-count distribution slightly less variable; both well above the 0.5 floor). **Zero** low-variability skips occur on either distribution in this pool. The σ ≤ 0.5 threshold is retained verbatim from HA11 v1 on inheritance-consistency grounds (pre-spec'd; not data-driven calibration). Verified pre-lock by single-script count [`scripts/count_HA11_bout_redo_effective_n.py`](../../../../../scripts/count_HA11_bout_redo_effective_n.py); `result.md` §2 dry-run gate 2 reports observed median σ = 0.7389853116132565 (exact match to r2 pinned value 0.739). The σ-floor inheritance choice IS load-bearing for the framework-validity gate's interpretability (a higher σ floor would re-introduce low-variability skips and shrink the effective n; a lower σ floor would admit days with near-zero variability where the z-score machinery becomes unstable); the empirical confirmation that zero skips fire on the pool removes the risk that the threshold mismatch is silently degrading the test. | Stocktake §2 implicitly inherits this under A3 + framework-validity-discipline backstops; this audit makes the HA-specific A11 row explicit per guide #1 §5.9. No conflict. |
| A12 (HA-specific) | **BACKSTOPPED** | Parent MD §6.1 pre-commits the 15-min half-life + 45-min tail thresholds based on (a) HA11 v1 U-dip event-rate distribution calibration (mean ~0.85 events/day per HA11 §7.B; the bout-level fast-recovery count should fall in a comparable range to be informative) and (b) within-bout recovery shape rationale (sharp recovery + return = structurally same trajectory HA11 v1's U-dip detection captures, read inverted). Pre-reg §4.1 inherits verbatim; NOT re-deriving. Crucially, the operand-threshold choice CANNOT be re-derived to fit the result because the operand IS the test of operand-fitness per parent MD §6 — re-deriving would collapse the framework-validity discipline. Backstopped by `desc-BOUT` ([`analyses/descriptive/operationalisation_support/bout_level_dose_response_calibration/result.md`](../operationalisation_support/bout_level_dose_response_calibration/result.md)) which characterises `bout_n_fast_recovery_day` distribution across 3 windows (afbouw_2026 + buildup_post_cpap_2024 + spring_2025_control; LANDED 2026-06-22 BEFORE HA11-bout-redo test execution 2026-06-23, no post-hoc contamination per guide #1 §7.3). Per `result.md` §2 dry-run sanity gates: per-day `bout_n_fast_recovery_day` mean / median / σ within pre-reg §7 expected ranges ([0.4, 1.7] / [0, 5] / [0.5, 3.0] — pre-committed sanity-check bounds). Per stocktake §2d representative-day spot-checks: bout-counts on 3 anchor days behave as the parent MD §6.1 calibration anchor predicts. Lock-dates: parent MD `c57ff3f` (2026-06-19) → pre-reg `5c71aa0` (2026-06-23) → test `6e06d12` (2026-06-23); thresholds precede pre-reg precede test, no post-hoc per §7.3. | Stocktake §2 row marks the bout-level operand validity under the framework-validity gate's B reading at A3 + custom rows; this audit makes the HA-specific A12 row explicit per guide #1 §5.9. No conflict. |
| A13 (HA-specific) | **BACKSTOPPED** | Per parent MD §5.5 + §6 the framework-validity restriction to unmedicated stratum is pre-committed at parent MD lock; pre-reg §4.2 carries the binding verbatim. Cross-phase pooling is OUT OF SCOPE per pre-reg §4.2 + §8 caveat 7 (era restriction non-negotiable). Approach A dose-adjustment per parent MD §5.3 explicitly applies to HA-C4c (the downstream substantive test), NOT here. Pre-reg §1 + §8 caveat 3 carry the framework-validity-NOT-substantive scope discipline verbatim; `result.md` §6 + §7 caveat 3 carry through. The audit confirms the cross-phase discipline is correctly framed and the framework-validity question is bounded to a single stratum (unmedicated × train × calm-day pool — the construction reproducing HA11 v1's reference frame). | Stocktake §2 row carries this implicitly under A4 + framework-validity-restriction backstops; this audit makes the HA-specific A13 row explicit per guide #1 §5.9. No conflict. |

## 4. Verdict-trust call

**TRUSTED**.

**Rationale**: 11 of 13 load-bearing assumptions are BACKSTOPPED; 2 are NOT APPLICABLE (A5, A6) with documented one-sentence reasons. Every A1-A8 row is BACKSTOPPED (no BACKSTOPPED-partial, no NOT-BACKSTOPPED). All five HA-specific rows (A9 framework-validity-as-self-test; A10 pipeline trust; A11 σ floor inheritance; A12 operand-threshold validity; A13 cross-phase restriction) are BACKSTOPPED with explicit citations to parent MD §6 + pre-reg sections + audit-closure single-script count + lock-date precedence per guide #1 §7.3.

The verdict-trust call is **TRUSTED**, not DOWNGRADED-INCONCLUSIVE-PROVISIONAL, because:

- Every load-bearing assumption has descriptive evidence behind it on the cells the test actually used (A1: dry-run gates table; A2: §3.4 day-validity gate + 1486/1755 operand-presence; A3: E[L]\* = 7.0 exactly + factor-of-2 flag NOT FIRED; A4: explicit era + stratum binding; A7: every p-value carries directional effect-size; A8: framework-validity reference-frame discipline; A9-A13: parent MD §6 + pre-reg explicit pre-commits + `desc-BOUT` + audit-closure single-script count).
- The pre-reg's r2 audit-closure cycle ([`reviews/HA11-bout-redo-2026-06-22.md`](../../../reviews/HA11-bout-redo-2026-06-22.md) PASS-with-caveats verdict; one L3 substantive closed by pinning effective n=70; one L3 minor closed by σ-inheritance documentation) means the load-bearing assumptions have already been adversarially walked at pre-reg-lock time and closed before test execution. The audit-closure-via-§3.6-compression is the project's standard pattern for mechanical pre-lock strengthening per [`hypothesis_lock_process.md`](../../../methodology/hypothesis_lock_process.md) §3.5 + §3.6.
- The PARTIAL verdict on the framework-validity gate (bar 3 FAIL at p = 0.2609) is a substantive finding about the bout-level operand's discriminative power at the locked n_calm=70/n_crash=11 sample, NOT an assumption-backstop failure. The audit reads the verdict as the verdict; per guide #1 §6.4 the verdict-trust call is independent of the verdict label (a PARTIAL whose assumptions are all BACKSTOPPED is TRUSTED for Stage I purposes; the audit does not preferentially trust PASSED over PARTIAL or vice versa).
- The "is the underlying bout-level signal real or under-powered?" question is what Stage I will interpret on this trusted verdict, with the inheritance-risk caveat per pre-reg §8 caveat 4 explicit; the audit does not pre-judge the Stage I reading.

**Dry-run default-reading on the verdict-trust call** (superseded by user acceptance in non-dry-run session; recorded for traceability only per LOCKED-r4 skill #7 dispatch-mode convention): the §8.4 optional seed in guide #1 ("Given the eight checklist rows, do you accept TRUSTED / DOWNGRADED-INCONCLUSIVE-PROVISIONAL / REQUIRES-DESCRIPTIVE-WORK as the audit's verdict-trust call?") is a confirmation seed, not a discovery seed; by construction the §3 ticks mechanically determine the call here (every assumption BACKSTOPPED or NOT APPLICABLE → TRUSTED). The dispatcher reads the §8.4 seed as DEFAULTED-PENDING-USER-INPUT in the strict sense that "user acceptance" is the binding completion event per locked-plan §3.8; in the substantive sense, the §3 ticks force the call to TRUSTED unambiguously (no judgment ambiguity exists at the verdict-trust-label boundary the way one might at e.g. a coherence-call boundary in Stage S₁).

Per guide #1 §4.4: TRUSTED applies when every load-bearing assumption is BACKSTOPPED (or NOT APPLICABLE with documented reason). All 13 rows here satisfy this strictly.

Per guide #1 §6.4: the verdict-trust call is independent of the verdict label. HA11-bout-redo's PARTIAL verdict whose assumptions are all BACKSTOPPED (or NOT APPLICABLE) is TRUSTED for Stage I purposes; the audit does not preferentially trust PASSED-equivalent verdicts over PARTIAL-equivalent verdicts.

## 4.5 Lessons + queued descriptive work

No BACKSTOPPED-partial or NOT-BACKSTOPPED rows fired in §3; no descriptive closure work is queued for HA11-bout-redo specifically. Three observation-level notes that are NOT load-bearing for this audit's verdict-trust call but worth recording for the cascade-context-downstream HA-C4c audit:

1. **Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day`** does not yet exist as a standalone descriptive artefact (stocktake §3 Shared gap 3 pattern). A2 is BACKSTOPPED via the §3.4 day-validity gate documentation + the operand-presence figure (1486/1755 = 84.7% per `result.md` §2), which together supply path (a) of guide #1 §5.2 operational test; a future per-channel rate audit would tighten the survivorship reach narrative under L7 but is not blocking. **Closure path** (non-blocking; logged for cascade-context): per-channel missingness-rate audit on `bout_n_fast_recovery_day` × era × phase (S effort per stocktake §3 Shared gap 3; script template from `desc-SMS` exists). Carry-forward for HA-C4c Stage D audit: HA-C4c uses the same operand on a cross-phase pool, so the same audit closure would tighten HA-C4c's A2 in the same step.
2. **The 99.3% motion-confound corpus property** surfaced in HA11-bout-redo `result.md` §4 (sensitivity arm motion-clean-only INCONCLUSIVE due to 4285/4317 bouts carrying `motion_confound_flag=True`) is a corpus-level structural finding about the per-minute trace, not an assumption-backstop failure on HA11-bout-redo's primary cell. The primary cell uses the motion-confounded-inclusive default per parent MD §3.4 + handoff §5 decision #6; the framework-validity verdict is unaffected by the motion-clean-arm degeneracy per pre-reg §9.5 + §4.10 (sensitivity arms cannot promote to PASSED + INCONCLUSIVE on an arm is informative, not blocking on the primary). **Carry-forward for HA-C4c Stage D audit**: HA-C4c uses the same operand on a cross-phase pool; the same 99.3% structural finding will apply; HA-C4c's audit should explicitly note this as a NOT-load-bearing-for-primary-but-load-bearing-for-motion-clean-claims observation.
3. **Bar 3 power-shortfall observation** (the +20.26 pp observed discrimination sits at the 74th percentile of the block-permutation null distribution; with a 3× larger crash-arm the same effect size would clear bar 3 by a wide margin per `result.md` §7 caveat 10) is the substantive PARTIAL finding; the audit does not re-read it. The Stage I interpretation will need to reconcile the directional + magnitude reproduction with the bootstrap-null insignificance at the present n; the inheritance-risk caveat per pre-reg §8 caveat 4 will be load-bearing for the Stage I reading. **No descriptive closure path** is identifiable for bar 3 in the framework-validity reference frame: enlarging n_crash would require either expanding the HA11 v1 reference-date pool beyond 200 (changing HA11 v1's calibration), relaxing the §4.4 day-validity gate (compromising data quality), or extending the analysis window into validate era (changing the reference frame per pre-reg §4.3 + §8 caveat 7). Per stocktake §4 + pre-reg §9.2: the operand-refinement v2 of HA11-bout-redo (re-tuning the 15 / 45 thresholds in parent MD §6.1) is OPTIONAL but not required; PARTIAL does not halt the cascade.

## 4.9 `open_inputs` block

Two DEFAULTED-PENDING-USER-INPUT slots surfaced per LOCKED-r4 skill #7 dispatch-mode convention. Both belong to the lock-event itself rather than the audit-content:

| # | What is missing | What it blocks | Cheapest acquisition path | Fallback claim available |
|---|---|---|---|---|
| 1 | User explicit acceptance of this audit's TRUSTED verdict-trust call per guide #1 §8.4 optional seed (DEFAULTED-PENDING-USER-INPUT per LOCKED-r4 skill #7 — dispatch-mode placeholder fired because the §8.4 seed is a confirmation seed requiring user explicit acceptance, NOT autonomous skill-filling; binding completion event per locked-plan §3.8 ("user explicitly accepts" is the binding completion event)) | DRAFT r1 → LOCKED transition for this audit; downstream Stage I on HA11-bout-redo gateable on TRUSTED per guide #2 §9.2 refusal-path | One interview pass per guide #1 §8.4 optional seed wording: "Given the eleven checklist rows, do you accept TRUSTED as the audit's verdict-trust call?" — confirmation seed, expected unambiguous YES given §3 mechanical ticks (every row BACKSTOPPED or NOT APPLICABLE per §4 verdict-trust call rationale) | DRAFT r1 status persists per LOCKED-r4 skill #7 convention; substantive content is locked-in-shell + ready for user-acceptance-only step; no claim degradation needed per locked-plan §3.5 "Hard rule" since the fallback IS the existing TRUSTED label held DRAFT pending user lock-event |
| 2 | Per-channel missingness-rate × era × phase MCAR/MAR diagnostic specifically on `bout_n_fast_recovery_day` (stocktake §3 Shared gap 3 pattern; NOT load-bearing for HA11-bout-redo's TRUSTED call but observation-level note per §4.5 item 1; carry-forward for HA-C4c Stage D audit) | Tightens HA11-bout-redo's A2 from "BACKSTOPPED via §3.4 gate + 84.7% operand-presence" to "BACKSTOPPED with full per-channel diagnostic"; tightens cascade-downstream HA-C4c Stage D A2 in the same step (same operand on cross-phase pool); not blocking either Stage D's TRUSTED reading | S effort per stocktake §3 Shared gap 3 (per-channel missingness rate × era × phase); script template from `desc-SMS` exists in `analyses/descriptive/operationalisation_support/stress_mean_sleep/` | TRUSTED stands with A2 BACKSTOPPED reading; Stage I §4.5 L7 citation narrows survivorship reach narrative to the §3.4 gate-discipline-bounded reading (no full audit-backed extension claimable until the diagnostic lands) |

Both entries propagate to [`methodology/_open_inputs.md`](../../../methodology/_open_inputs.md) at lock time per skill responsibility #6 (deduplicated against existing entries by missing-input × blocked-target key). The OI-002 carry-forward entry deduplicates against any pre-existing HA-C3 v2 OI-001-equivalent per-channel missingness queue entries — checked: none exist; the queue carries no per-channel missingness entry on `bout_n_fast_recovery_day` as of read-time (the closest is OI-001-008 cluster on HA-C3 / HA-C3p / synthesis / contextualisation / actionability / translation / Stage T for the dry-run set, none on bout-level).

---

## §9.6 refuse-to-lock gate walk

Per guide #1 §9.6 walk (item-by-item):

1. **Every §3 row has a status assigned** (BACKSTOPPED / NOT BACKSTOPPED / BACKSTOPPED-partial / NOT APPLICABLE): YES — all 13 rows have explicit status + citation + reason + stocktake-diff column.
2. **Every BACKSTOPPED row cites a specific path** (not "BACKSTOPPED by inspection of result.md" — forbidden per guide #1 §4.3 hard rule): YES — A1 cites `result.md` §2 dry-run gates table + `scripts/count_HA11_bout_redo_effective_n.py`; A2 cites pre-reg §3 + pipeline README + `result.md` §2 operand-presence figure; A3 cites `result.md` §3 E[L]\* companion + pre-reg §4.8; A4 cites pre-reg §4.2-§4.4 + named methodology MDs; A7 cites `result.md` §3 table; A8 cites pre-reg §4.3 + §8 caveat 7; A9 cites parent MD §6 + HA11 v1 `result.md`; A10 cites pipeline lock + `desc-BOUT` + stocktake §2d spot-checks; A11 cites pre-reg §4.6 r2 + audit-closure script; A12 cites parent MD §6.1 + `desc-BOUT` + lock-date precedence; A13 cites parent MD §5.5 + §6 + pre-reg §4.2.
3. **Every NOT-BACKSTOPPED row maps to an §4.5 `open_inputs` entry** (per guide #1 §4.3): NOT APPLICABLE — zero NOT-BACKSTOPPED rows fired.
4. **Every NOT-APPLICABLE row carries one-sentence reason** (per guide #1 §4.3): YES — A5 ("no v24 primary signals; bout-level Garmin-derived + crash-label test"); A6 ("daytime bout-level test on per-minute stress; no nightly column").
5. **Anti-pattern §7.1 not fired** (no "the test ran, therefore the assumptions held" reading): YES — every BACKSTOPPED row cites a separate descriptive artefact (descriptive run, methodology MD, garmin-card analysis, single-script count); no row backstops via the test's own result.md alone.
6. **Anti-pattern §7.2 not fired** (no single-cell descriptive cited as global backstop): YES — `desc-BOUT` covers `bout_n_fast_recovery_day` across 3 windows including the unmedicated × train pool; the §3.4 day-validity gate covers all cells the test reports on; per-bar reporting per CONVENTIONS §3.6 covers per-arm + per-cell counts explicitly.
7. **Anti-pattern §7.3 not fired** (no post-hoc descriptive backstop): YES — all artefact lock-dates precede the test execution (parent MD `c57ff3f` 2026-06-19; pipeline `d5b394c` 2026-06-22; `desc-BOUT` LANDED 2026-06-22; HA11-bout-redo pre-reg r2 LOCKED `5c71aa0` 2026-06-23; test execution `6e06d12` 2026-06-23; audit-closure script [`scripts/count_HA11_bout_redo_effective_n.py`](../../../../../scripts/count_HA11_bout_redo_effective_n.py) is a pre-lock single-script count that pins r2 audit-closure values — explicitly pre-test, not post-test peeking).
8. **Anti-pattern §7.4 not fired** (no re-interpreting the verdict in the audit): YES — the audit reads PARTIAL as PARTIAL; no re-reading to "PARTIAL is actually PASSED" or "the bar 3 failure should be narrowed". The §4 verdict-trust call is independent of the verdict label per guide #1 §6.4.
9. **Anti-pattern §7.5 not fired** (no silent assumption-list editing): YES — the audit adds five HA-specific rows (A9-A13) beyond the spine of A1-A8 with explicit source citations (parent MD §6, pre-reg §3 + §10.1, pre-reg §4.6 r2, parent MD §6.1, parent MD §5.5 + §6); no row is added without a pre-reg or methodology-MD or test.py source per guide #1 §7.5.
10. **Anti-pattern §7.6 not fired** (no marking TRUSTED to avoid blocking Stage I): YES — TRUSTED is supported by every §3 row being BACKSTOPPED or NOT APPLICABLE per the §6.4 hard rule; no Stage I forcing pressure.
11. **Anti-pattern §7.7 not fired** (no citing the limitations doc instead of producing the backstop): YES — the audit cites the L5 + L7 manifestations in §3.5 A2 + would cite in §3.5 A5 if A5 were not NOT APPLICABLE; the §3 BACKSTOPPED rows cite the actual descriptive artefacts (pipeline NaN semantics, day-validity gate, operand-presence figure, `desc-BOUT`, audit-closure script), not the limitations doc as the backstop.
12. **L5 + L7 citations** per guide #1 §2: A2 cites L7 (survivorship) in the row's reasoning; A5 is NOT APPLICABLE so the §5.5 L5 surface obligation does not fire here (Stage I inherits NO L5 citation obligation per guide #1 §5.5 final paragraph). The audit does not cite further L-IDs beyond L5 + L7 per guide #1 §2 final paragraph (Stage I will pick up broader L-ID citations on its own per the limitations doc §5 row for `interpretation.md`).

**Items that would fire on lock attempt**: ONE (the user-acceptance-only DEFAULTED-PENDING-USER-INPUT slot per §4.9 entry 1 — not a §9.6 substantive fire; this is the binding-completion-event-pending state per locked-plan §3.8). The artefact is structurally complete and refuse-to-lock-clean on every §9.6 substantive check; the lock-event itself is the only outstanding action.

The second §4.9 entry (per-channel missingness diagnostic on `bout_n_fast_recovery_day`) does NOT fire §9.6 because A2 is BACKSTOPPED on the present checklist reading via the §3.4 day-validity gate + operand-presence figure; the entry is observation-level cascade-context for HA-C4c's audit, not a lock-blocker for HA11-bout-redo's audit.

## §11 Lock log

| Date | Event | Note |
|---|---|---|
| 2026-06-26 | Drafted r1 (DISPATCHED-BY-ORCHESTRATOR ON USER AUTHORISATION) | Producer-mode by `/research-interpret descriptive HA11-bout-redo` skill on first invocation for this target (skill LOCKED r4). Phase B of `_rollout_order_2026-06-25.md` (single-member cluster C-bout-framework; cascade-source for Phase C C-bout-substance per synthesis_structure_map r3 §3). All audit content drawn from HA11-bout-redo r2 pre-reg (LOCKED 2026-06-23 `5c71aa0` + footer-fix `b5bf0f8`), `result.md` (LOCKED 2026-06-23 `6e06d12`; worktree HEAD at run `250a466`; pipeline `d5b394c`), `test.py`, `result-data.json`, and the cited methodology MDs (parent MD `bout_level_recovery_dynamics.md` LOCKED r2 2026-06-19 `c57ff3f`; permutation_null_block_length; lc_era_temporal_segmentation; citalopram_phase_stratification; hypothesis_lock_process; symptom_mention_asymmetry; nightly_attribution; train_validate_split_fate). **Drift triggers registered** (manual-pending-skill carry-forward per skill responsibility #10): (1) HA11-bout-redo `result.md` re-runs (e.g. operand-refinement v2 per pre-reg §9.2 OPTIONAL path; pipeline `extract_stress_bouts.py` re-LOCK at new commit); (2) any cited methodology MD changes lock-version (especially `bout_level_recovery_dynamics.md` parent MD `c57ff3f` — re-LOCK would re-open the §6 framework-validity discipline + §6.1 operand-threshold pre-commit; also `permutation_null_block_length.md` + `lc_era_temporal_segmentation.md` + `citalopram_phase_stratification.md`); (3) per-channel missingness audit on `bout_n_fast_recovery_day` lands (closes the §4.9 cascade-context entry tightening A2 from "BACKSTOPPED via §3.4 gate" to "BACKSTOPPED with full per-channel diagnostic"); (4) HA-C4c (cascade-receiver) Stage D audit lands and surfaces a discrepancy with HA11-bout-redo's A1-A13 readings; (5) ≥6 months elapse since lock. **STATUS: DRAFT r1 — dispatched-by-orchestrator on user authorisation; NOT LOCKED — awaiting user acceptance per `_plan_results_analysis_layer.md` §3.8 binding completion event.** |
