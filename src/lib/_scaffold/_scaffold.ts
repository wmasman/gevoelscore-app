// SCAFFOLD — delete this directory once src/lib/{first-real-module}/ has its first real test.
// Purpose: prove the TDD loop (RED → GREEN → REFACTOR) works end-to-end before any feature lands.
// Walkthrough lives in src/lib/_scaffold/README.md.

export function answerToTheUltimateQuestion(): number {
  // Intentionally wrong so the paired test is RED on first run.
  // Change to `return 42` to demonstrate the RED → GREEN transition, then delete this whole folder.
  return 0;
}
